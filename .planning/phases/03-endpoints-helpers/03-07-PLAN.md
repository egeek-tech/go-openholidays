---
phase: 03-endpoints-helpers
prep_note: "Wave 2 — needs Plan 3 (Subdivisions endpoint + DE fixture) and Plan 6 (Holiday.IsInRegion fast-path); both live in Wave 1 so Plan 7 cannot start until Wave 1 closes."
plan: 07
type: execute
wave: 2
depends_on:
  - 03-03
  - 03-06
files_modified:
  - client_isinregion.go
  - client_isinregion_test.go
autonomous: true
requirements:
  - HELP-02
must_haves:
  truths:
    - "Client.IsInRegion(ctx, h, code) (bool, error) walks the Subdivision tree to detect whether code is a descendant of any subdivision the holiday applies to (D-59, CL-09)"
    - "Same fast-path guards as Holiday.IsInRegion: empty code → (false, nil); h.Nationwide → (true, nil); direct match on h.Subdivisions[].Code → (true, nil)"
    - "When no flat match and h.Subdivisions empty → (false, nil) — no HTTP issued"
    - "When no flat match and h.Subdivisions non-empty → fetches /Subdivisions for the country implied by h.Subdivisions[0].Code prefix; builds parent-index from Subdivision.Children; walks upward from code until match or root"
    - "Defensive cycle bound: iteration cap of len(parentIdx)+1 prevents infinite recursion on malformed upstream data (Pitfall 4)"
    - "splitCountryFromSubdivision helper extracts country prefix from code via strings.IndexByte('-')"
    - "buildParentIndex recursively walks Subdivision.Children and returns child→parent map keyed by uppercase code"
    - "Hierarchical test against DE-BY/Augsburg from testdata/subdivisions_de.json proves the recursive walk works against live-grounded upstream data"
    - "Cycle-defense subtest is an ENFORCEMENT regression: a `time.AfterFunc(2*time.Second, ...)` bounds the IsInRegion upward-walk against a synthetic cyclic tree and fails the test on timeout"
  artifacts:
    - path: client_isinregion.go
      provides: "Client.IsInRegion + splitCountryFromSubdivision + buildParentIndex"
      contains: "func (c *Client) IsInRegion"
    - path: client_isinregion_test.go
      provides: "TestClient_IsInRegion with subtests covering fast-paths, flat match, hierarchical match against DE fixture, cycle defense"
  key_links:
    - from: "client_isinregion.go"
      to: "subdivisions.go Subdivisions endpoint"
      via: "c.Subdivisions(ctx, SubdivisionsRequest{CountryIsoCode: ...}) for the tree fetch"
      pattern: "c.Subdivisions\\(ctx"
    - from: "client_isinregion.go"
      to: "types.go Subdivision.Children"
      via: "recursive walk in buildParentIndex"
      pattern: "n.Children"
---

<objective>
Ship the hierarchical region-membership helper `Client.IsInRegion`. The only Phase 3 method that issues hidden I/O. Fetches `/Subdivisions` and walks `Subdivision.Children` to detect whether a child-level subdivision code is covered by a holiday whose declared `Holiday.Subdivisions` only contain the parent.

Purpose: Adds CL-09 to the locked decision ledger — this is a new public method beyond REQUIREMENTS HELP-02's literal text. Closes the hierarchical case that `Holiday.IsInRegion` (flat-only per D-58) cannot handle.

Output: `client_isinregion.go` + `client_isinregion_test.go`. Test target: testdata/subdivisions_de.json's DE-BY/Augsburg pair (captured by Plan 3 per Assumption A3) — the only live-grounded hierarchical case in the captured fixtures since PL is flat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-endpoints-helpers/03-CONTEXT.md
@.planning/phases/03-endpoints-helpers/03-RESEARCH.md
@.planning/phases/03-endpoints-helpers/03-PATTERNS.md
@subdivisions.go
@holiday.go
@types.go
@client.go
@testdata/subdivisions_de.json

<interfaces>
Key types and contracts.

From Plan 3 (subdivisions.go):
- type SubdivisionsRequest struct { CountryIsoCode string; LanguageIsoCode string }
- func (c *Client) Subdivisions(ctx context.Context, req SubdivisionsRequest) ([]Subdivision, error)

From Plan 6 (holiday.go):
- func (h Holiday) IsInRegion(code string) bool  // flat-only, used as fast-path

From types.go:
- type Subdivision struct { Code string; ShortName string; ...; Children []Subdivision; ... }
- type SubdivisionRef struct { Code string; ShortName string }  // shape on Holiday.Subdivisions

Target shapes:
- func (c *Client) IsInRegion(ctx context.Context, h Holiday, code string) (bool, error)
- func splitCountryFromSubdivision(code string) (string, bool)  // unexported
- func buildParentIndex(tree []Subdivision) map[string]string   // unexported, child→parent map keyed by uppercase code
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write client_isinregion.go with Client.IsInRegion + splitCountryFromSubdivision + buildParentIndex</name>
  <files>client_isinregion.go</files>
  <read_first>
    - holiday.go (Plan 6 — the flat IsInRegion fast-path that Client.IsInRegion mirrors before fetching)
    - subdivisions.go (Plan 3 — the SubdivisionsRequest signature consumed)
    - types.go lines 184-216 (Subdivision struct including the recursive Children field)
    - client.go (the *Client receiver; no new fields needed on Client for Phase 3)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md D-59, CL-09 (hierarchical method scope; godoc must document the hidden I/O cost; Phase 4 cache will mitigate hot-loop cost)
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"Pattern 4: Hierarchical Client.IsInRegion" lines 502-605 (verbatim implementation including splitCountryFromSubdivision, buildParentIndex, cycle defense; PL-flat vs DE-nested live verification at lines 506-513)
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"Common Pitfalls — Pitfall 4" lines 904-915 (cycle defense rationale: visited-set + iteration cap)
    - .planning/phases/03-endpoints-helpers/03-PATTERNS.md §"`client_isinregion.go`" lines 451-518 (verbatim skeleton)
  </read_first>
  <behavior>
    - Client.IsInRegion(ctx, h, "") → (false, nil) no HTTP
    - Client.IsInRegion(ctx, h, code) when h.Nationwide → (true, nil) no HTTP
    - Client.IsInRegion(ctx, h, code) when code matches h.Subdivisions[].Code (case-insensitive) → (true, nil) no HTTP
    - Client.IsInRegion(ctx, h, code) when no flat match and h.Subdivisions empty → (false, nil) no HTTP
    - Client.IsInRegion(ctx, h, "DE-BY-AUG") when h.Subdivisions includes only "DE-BY" → fetches /Subdivisions?countryIsoCode=DE; walks DE-BY's Children to find Augsburg; walks upward from "DE-BY-AUG" through the parent-index to find DE-BY; returns (true, nil)
    - Client.IsInRegion(ctx, h, code) when c.Subdivisions returns an error → (false, err)
    - splitCountryFromSubdivision("PL-SL") → ("PL", true); splitCountryFromSubdivision("X") → ("", false)
    - buildParentIndex walks the tree depth-first; top-level entries have no parent entry in the map; nested entries (children of children) are correctly indexed; cycles in upstream data do not loop forever
  </behavior>
  <action>
    Create new file client_isinregion.go in repo root with `package openholidays`.
    Imports: context, strings.

    Declare func (c *Client) IsInRegion(ctx context.Context, h Holiday, code string) (bool, error).
    Body per RESEARCH.md Pattern 4 lines 534-572 verbatim:
      If code == "" return (false, nil).
      If h.Nationwide return (true, nil).
      Fast path: for each s in h.Subdivisions, if strings.EqualFold(s.Code, code) return (true, nil).
      If len(h.Subdivisions) == 0 return (false, nil) — no country context to fetch a tree for.
      Call splitCountryFromSubdivision on h.Subdivisions[0].Code; if it returns ok=false return (false, nil).
      Call tree, err := c.Subdivisions(ctx, SubdivisionsRequest{CountryIsoCode: countryCode}); if err != nil return (false, err).
      Build parentIdx := buildParentIndex(tree).
      Walk upward from code with a defensive iteration cap of len(parentIdx)+1:
        At each step, iterate h.Subdivisions and return (true, nil) when strings.EqualFold(s.Code, current).
        Look up parent for current in parentIdx (key is strings.ToUpper(current)); if not found return (false, nil) — reached root or unknown code.
        Otherwise advance current to parent.
      After the loop bound is exceeded return (false, nil) — cycle defense, should never trigger in practice.
    godoc on the method explains: hierarchical behavior; hidden I/O note ("issues an HTTP GET to /Subdivisions"); cost note ("repeated calls in a hot loop incur a round-trip per call — Phase 4's cache will mitigate"); behavior when h.Subdivisions is empty AND h.Nationwide is false; reference CL-09 / D-59.

    Declare unexported func splitCountryFromSubdivision(code string) (string, bool).
    Body per RESEARCH.md lines 576-582 verbatim: if i := strings.IndexByte(code, '-'); i > 0 return (code[:i], true); otherwise return ("", false).
    godoc explains: extracts "PL" from "PL-SL", "DE" from "DE-BY", etc. Returns false when the input has no hyphen or the hyphen is at position 0.

    Declare unexported func buildParentIndex(tree []Subdivision) map[string]string.
    Body per RESEARCH.md lines 590-605 verbatim: declare idx := make(map[string]string); declare a recursive walk closure walk(parent string, nodes []Subdivision) that for each n in nodes inserts idx[strings.ToUpper(n.Code)] = parent when parent != "", then recurses into n.Children when non-empty; call walk("", tree); return idx.
    godoc explains: depth-first walk; child→parent map keyed by uppercase subdivision code; top-level subdivisions are not present in the map (they have no parent). Note about cycle defense: this function itself does NOT bound recursion — the cap lives in IsInRegion's upward walk. A truly cyclic Children pointer would loop in buildParentIndex; in practice upstream data is curated and the IsInRegion cap is defense-in-depth (RESEARCH.md Pitfall 4 explicitly accepts this design: "the cycle bound in IsInRegion is a defense-in-depth guard against malformed upstream data").

    Every error string (none here — errors are propagated verbatim from c.Subdivisions) and every godoc starts with the symbol name per Gold Rule 1.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go build ./...</automated>
  </verify>
  <done>
    client_isinregion.go compiles. `grep -c "func (c \*Client) IsInRegion" client_isinregion.go` returns 1. `grep -c "func splitCountryFromSubdivision" client_isinregion.go` returns 1. `grep -c "func buildParentIndex" client_isinregion.go` returns 1. `grep -c "c.Subdivisions(ctx" client_isinregion.go` returns 1. `grep -c "n.Children" client_isinregion.go` returns ≥ 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write client_isinregion_test.go covering fast-paths + flat match + hierarchical match against DE fixture + cycle defense (enforcement)</name>
  <files>client_isinregion_test.go</files>
  <read_first>
    - client_isinregion.go (just-created)
    - holiday.go (Plan 6 — the flat IsInRegion analog; Client.IsInRegion's fast-path duplicates its behavior)
    - subdivisions.go (Plan 3 — and its sibling test for how httptest serves the Subdivisions endpoint)
    - subdivisions_test.go (Plan 3 — the fixture-loading pattern for testdata/subdivisions_de.json)
    - testdata/subdivisions_de.json (Plan 3 captured — contains DE-BY with Children including Augsburg per Assumption A3)
    - types.go (Holiday, SubdivisionRef shapes for the test cases)
    - .planning/phases/03-endpoints-helpers/03-PATTERNS.md §"`client_isinregion_test.go`" lines 30-32 (test pattern)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md §"Specific Ideas" line 220-222 (the canonical hierarchical test plan — but adjusted per RESEARCH.md A3 to use DE-BY/Augsburg instead of PL since PL is flat)
  </read_first>
  <behavior>
    - Test 1 (empty code fast-path): Client.IsInRegion(ctx, h, "") → (false, nil) no HTTP (use WithBaseURL example.invalid so accidental HTTP fails loudly)
    - Test 2 (Nationwide fast-path): h.Nationwide=true; Client.IsInRegion(ctx, h, "PL-SL") → (true, nil); no HTTP
    - Test 3 (flat-match fast-path): h.Subdivisions=[{Code:"PL-SL"}]; Client.IsInRegion(ctx, h, "PL-SL") → (true, nil); no HTTP
    - Test 4 (flat-match case-insensitive): same as 3 but with code "pl-sl"; assert (true, nil)
    - Test 5 (no flat match + empty Subdivisions): h.Subdivisions=nil, Nationwide=false; Client.IsInRegion(ctx, h, "PL-SL") → (false, nil); no HTTP
    - Test 6 (hierarchical DE-BY/Augsburg): httptest server returns testdata/subdivisions_de.json bytes for the /Subdivisions request; construct a Holiday with Subdivisions=[{Code:"DE-BY"}] and Nationwide=false; query Client.IsInRegion(ctx, h, "<augsburg-code>") where augsburg-code is the actual Code value parsed out of the DE-BY's Children entry in testdata/subdivisions_de.json; assert (true, nil)
    - Test 7 (hierarchical miss for non-DE-BY child): same setup as test 6 but query a code that exists under a different Bundesland's tree or doesn't exist at all; assert (false, nil)
    - Test 8 (transport error from Subdivisions surfaces verbatim): httptest server returns 500; assert ok==false AND err is non-nil AND err is *APIError; no panic
    - Test 9 (cycle defense ENFORCEMENT): construct a synthetic Subdivision tree whose child→parent relationship forms a cycle for IsInRegion's upward walk (e.g. a server-supplied tree where two sibling-level Subdivisions form a cycle when treated as parents). Serve that tree via an httptest /Subdivisions endpoint. Bound the IsInRegion call with `time.AfterFunc(2*time.Second, func(){ panic("IsInRegion failed to bound cycle — exceeded 2s") })` (or an equivalent `done` channel + `select` with `case <-time.After(2*time.Second): t.Fatal(...)` pattern). Assert the call returns (false, nil) within the timeout — the test FAILS if the call hangs or panics from the timer.
  </behavior>
  <action>
    Create new file client_isinregion_test.go in repo root with `package openholidays`.
    Imports: context, encoding/json, errors, net/http, net/http/httptest, os, path/filepath, testing, time, plus testify assert and require.

    Declare exactly one func TestClient_IsInRegion(t *testing.T) per Gold Rule 3 (Client.IsInRegion is the production function under test).
    Top-level t.Parallel().

    Helper at file scope: a small helper `findFirstWithChildren(t *testing.T, tree []Subdivision) (parentCode, childCode string)` that walks the parsed DE fixture tree and returns the first (parent, child) Code pair where parent carries a non-empty Children slice and Children[0] is the child. The test then uses these literal codes in the hierarchical assertions. This keeps the test resilient to small changes in the DE fixture: as long as some entry carries Children, the test works.

    Subtests under t.Run:
      "empty code returns (false, nil) without HTTP" — WithBaseURL example.invalid; assert ok==false && err==nil.
      "Nationwide returns (true, nil) without HTTP" — WithBaseURL example.invalid; h.Nationwide=true; assert ok==true && err==nil.
      "flat match on h.Subdivisions returns (true, nil) without HTTP" — same setup; h.Subdivisions=[{Code:"PL-SL"}]; assert (true, nil) for code "PL-SL".
      "flat match is case-insensitive" — same setup; assert (true, nil) for code "pl-sl".
      "no flat match + empty Subdivisions returns (false, nil) without HTTP" — same setup; h.Subdivisions=nil; assert (false, nil).
      "hierarchical match against DE fixture" — load testdata/subdivisions_de.json; parse into []Subdivision via json.Unmarshal; call findFirstWithChildren to extract parentCode (e.g. "DE-BY") and childCode (e.g. the Augsburg code observed in the fixture); httptest server responds to /Subdivisions with the fixture body verbatim; construct Holiday{Subdivisions:[]SubdivisionRef{{Code: parentCode}}}; assert Client.IsInRegion(ctx, h, childCode) returns (true, nil).
      "hierarchical miss returns (false, nil)" — same server setup; same Holiday with parentCode; assert Client.IsInRegion(ctx, h, "ZZ-XX-NEVER") returns (false, nil).
      "transport error from Subdivisions surfaces" — httptest server returns 500 for /Subdivisions; Holiday{Subdivisions:[]SubdivisionRef{{Code:"DE-BY"}}}; assert err != nil and errors.As(err, &apiErr) recovers an *APIError with StatusCode 500.
      "cycle in upstream tree is bounded by IsInRegion's iteration cap (enforcement)" — construct a synthetic Subdivisions tree in Go (`[]Subdivision`) representing a cycle visible to IsInRegion's upward walk. Concretely: declare two top-level subdivisions "DE-A" and "DE-B" where DE-A.Children contains a node {Code:"DE-B"} and DE-B.Children contains a node {Code:"DE-A"} — buildParentIndex will yield parentIdx["DE-A"]="DE-B" and parentIdx["DE-B"]="DE-A". Marshal that tree to JSON; serve it via the httptest handler for /Subdivisions. Construct Holiday{Subdivisions:[]SubdivisionRef{{Code:"DE-X"}}, Nationwide:false} so the upward walk has no flat match. Run the call in a goroutine and gate completion with a `done := make(chan struct{})` channel; in the goroutine call `ok, err := c.IsInRegion(ctx, h, "DE-A")` then `close(done)`. Then `select { case <-done: /* assert ok==false && err==nil */ ; case <-time.After(2*time.Second): t.Fatalf("IsInRegion failed to bound cycle — exceeded 2s") }`. This subtest FAILS if the iteration cap regresses (e.g. if a future change accidentally drops the `len(parentIdx)+1` bound or replaces it with an unbounded loop). The 2-second bound is generous; the actual bounded walk completes in microseconds.

    Every subtest calls t.Parallel().

    Add two separate test functions for the unexported helpers (Gold Rule 3 — one TestXxx per production function applies to unexported helpers too when they have non-trivial logic):
      func TestSplitCountryFromSubdivision(t *testing.T) with subtests: "PL-SL extracts PL"; "DE-BY extracts DE"; "DE-BY-AUG extracts DE" (only first segment); "no hyphen returns false"; "leading hyphen returns false".
      func TestBuildParentIndex(t *testing.T) with subtests: "flat tree produces empty map"; "single-level nesting indexes children to parents"; "two-level nesting indexes deeper children". Do NOT include a buildParentIndex-level cycle subtest here — buildParentIndex itself does not enforce a cap (it walks Subdivision.Children); the cycle ENFORCEMENT regression lives in the TestClient_IsInRegion cycle subtest above (which exercises the IsInRegion upward-walk cap, the actual mitigation surface for T-3-DoS-CycleInChildren).

    File-header godoc explains Gold Rule 3 application, references CL-09 / D-59, and notes Assumption A3 (DE fixture is the live-grounded hierarchical test target because PL is flat) AND notes the cycle-enforcement subtest naming convention (the threat-model row T-3-DoS-CycleInChildren is regression-locked by that subtest).
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go test -race -timeout 30s -run "TestClient_IsInRegion|TestSplitCountryFromSubdivision|TestBuildParentIndex" -count=1 ./...</automated>
  </verify>
  <done>
    `go test -race -timeout 30s -run "TestClient_IsInRegion|TestSplitCountryFromSubdivision|TestBuildParentIndex" -count=1 ./...` exits 0. `grep -c "func TestClient_IsInRegion" client_isinregion_test.go` returns 1. `grep -c "func TestSplitCountryFromSubdivision" client_isinregion_test.go` returns 1. `grep -c "func TestBuildParentIndex" client_isinregion_test.go` returns 1. The hierarchical-match subtest passes against the actual DE fixture content (no synthetic tree shortcut for that case). The cycle-defense subtest enforces the iteration cap via a real 2-second timeout (`grep -c "time.After(2\*time.Second)" client_isinregion_test.go` returns ≥ 1) — manually verify by temporarily removing the `len(parentIdx)+1` cap in client_isinregion.go and confirming the cycle subtest FAILS via the 2s timeout; restore the cap before commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Client.IsInRegion | ctx, Holiday value, and code string cross the boundary; ctx propagates to the inner Subdivisions call. |
| Client.IsInRegion → upstream (via c.Subdivisions) | The /Subdivisions response IS the tree this method walks. A malformed/cyclic tree is the threat surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-DoS-CycleInChildren | DoS | client_isinregion.go (IsInRegion upward walk) | mitigate | IsInRegion's upward walk has an explicit iteration cap of `len(parentIdx) + 1` — cycles produce a bounded walk and (false, nil) return, never an infinite loop. RESEARCH.md Pitfall 4 + ASVS V5.1.4. The cap is REGRESSION-LOCKED by the cycle-enforcement subtest in TestClient_IsInRegion ("cycle in upstream tree is bounded by IsInRegion's iteration cap (enforcement)"), which fails on a 2-second timeout if the cap is accidentally removed. buildParentIndex itself is NOT in scope for this mitigation: it walks Subdivision.Children verbatim, and a truly cyclic Children pointer would loop forever inside buildParentIndex. We accept that residual risk: real upstream data is curated and not cyclic at the JSON level; the realistic threat surface is parent-index-level cycles (e.g. data shapes where DE-A and DE-B each claim the other as a child), which is precisely what the IsInRegion cap defends against. |
| T-3-DoS-OverSize | DoS | inherited via c.Subdivisions → doJSONGet | mitigate | 10 MiB cap inherited. |
| T-3-InfoDisc-APIErrorBody | InfoDisc | inherited via c.Subdivisions → doJSONGet | mitigate | 4 KiB APIError.Body cap inherited. |
| T-3-NoFlatNoSubs-NoHTTP | DoS (caller-induced) | client_isinregion.go | mitigate | When Holiday has no Subdivisions AND is not Nationwide, the method returns (false, nil) WITHOUT issuing HTTP — prevents accidental requests with no country context. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test -race -timeout 30s -run "TestClient_IsInRegion|TestSplitCountryFromSubdivision|TestBuildParentIndex" -count=1 ./...` exits 0.
- `grep -c "func (c \*Client) IsInRegion" client_isinregion.go` returns 1.
- `grep -c "len(parentIdx)" client_isinregion.go` returns ≥ 1 (the iteration cap).
- The hierarchical-match subtest uses the actual DE fixture content (no synthetic shortcut).
- The cycle-defense subtest uses `time.After(2*time.Second)` (or equivalent `time.AfterFunc`) to bound IsInRegion against a synthetic cyclic-parent-index tree, failing on timeout — `grep -c "time.After(2\*time.Second)\|time.AfterFunc(2\*time.Second" client_isinregion_test.go` returns ≥ 1.
</verification>

<success_criteria>
- HELP-02 extended satisfied: Client.IsInRegion exists with the hierarchical behavior locked in D-59.
- CL-09 landed: documented as a public method beyond REQUIREMENTS HELP-02 literal text; hidden I/O cost documented in godoc; cycle defense in place per Pitfall 4 AND regression-locked by the cycle-enforcement subtest.
- Tests are live-grounded via testdata/subdivisions_de.json — the DE-BY/Augsburg pair is exercised end-to-end through Client.Subdivisions → buildParentIndex → upward walk.
- splitCountryFromSubdivision and buildParentIndex unit-tested via their own TestXxx functions per Gold Rule 3.
- T-3-DoS-CycleInChildren mitigation is no longer informational: removing the `len(parentIdx)+1` cap from IsInRegion FAILS the test suite via the 2-second timeout.
</success_criteria>

<output>
After completion, write `.planning/phases/03-endpoints-helpers/03-07-SUMMARY.md` recording: the exact (parent, child) Code pair extracted from testdata/subdivisions_de.json for the hierarchical test (e.g. "DE-BY" + the Augsburg Code observed), the subtest count for each of the three TestXxx functions, the iteration cap value used, confirmation that the cycle-defense subtest is an enforcement regression (verified by removing the cap locally and observing the 2s timeout failure, then restoring the cap), and any deviation from D-59 + CL-09.

This is the plan that adds the CL-09 row to PROJECT.md `Key Decisions` table. The executor's SUMMARY MUST note the line to append:
- "CL-09: Client.IsInRegion(ctx, holiday, code) (bool, error) — hierarchical region-membership beyond REQUIREMENTS HELP-02 literal text; fetches /Subdivisions and walks Subdivision.Children to detect descendant subdivisions; D-59"
</output>
