---
phase: 03-endpoints-helpers
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - holiday.go
  - holiday_test.go
autonomous: true
requirements:
  - HELP-01
  - HELP-02
  - HELP-03
  - HELP-04
must_haves:
  truths:
    - "Holiday.NameFor(lang) returns the case-insensitive localized name with first-entry fallback (D-57, CL-10)"
    - "Holiday.IsInRegion(code) returns true when code is empty? — no, returns false; true when Nationwide; true when code matches any Holiday.Subdivisions[].Code via strings.EqualFold (D-58)"
    - "Holiday.Days() returns the inclusive day count via Date.DaysUntil (D-60); returns 14 for the canonical Polish ferie zimowe Śląskie 2025 (Jan 18 - Jan 31)"
    - "Holiday.Range() returns iter.Seq[Date] (NOT iter.Seq[time.Time]) yielding every Date from StartDate to EndDate inclusive; rebuilds each yielded Date via NewDate to preserve UTC-midnight invariant (D-61, CL-11)"
    - "No I/O in any helper; no Client dependency; pure value methods on Holiday"
  artifacts:
    - path: holiday.go
      provides: "Four Holiday value methods: NameFor, IsInRegion, Days, Range"
      contains: "func (h Holiday) NameFor"
    - path: holiday_test.go
      provides: "TestHoliday_NameFor + TestHoliday_IsInRegion + TestHoliday_Days + TestHoliday_Range — one TestXxx per production function per Gold Rule 3"
  key_links:
    - from: "holiday.go NameFor"
      to: "types.go pickLocalized"
      via: "delegation"
      pattern: "pickLocalized\\(h.Name"
    - from: "holiday.go Days"
      to: "date.go DaysUntil"
      via: "delegation"
      pattern: "h.StartDate.DaysUntil"
    - from: "holiday.go Range"
      to: "date.go NewDate"
      via: "per-step rebuild preserving UTC-midnight invariant"
      pattern: "NewDate\\(next.Year"
---

<objective>
Ship the four pure-value `Holiday` helper methods: `NameFor`, `IsInRegion`, `Days`, `Range`. No HTTP. No Client dependency. Direct delegation to existing Phase 1 primitives (`pickLocalized`, `Date.DaysUntil`, `NewDate`, `strings.EqualFold`).

Purpose: Covers REQUIREMENTS.md HELP-01..04. Independent of Plans 2-5 — can run in parallel since holiday.go has zero dependencies on any other Phase 3 file.

Output: `holiday.go` + `holiday_test.go`. Adds CL-10 (`NameFor` collision-avoiding name) and CL-11 (`iter.Seq[Date]` deviation from ROADMAP literal) to the locked decision ledger.

Note: Per CONTEXT.md D-72, this plan is parallel-eligible with Plans 2-5 because it touches no shared files. The dependency-free posture is intentional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-endpoints-helpers/03-CONTEXT.md
@.planning/phases/03-endpoints-helpers/03-RESEARCH.md
@.planning/phases/03-endpoints-helpers/03-PATTERNS.md
@types.go
@date.go

<interfaces>
Key types and contracts.

From types.go:
- type Holiday struct { ... ID, StartDate Date, EndDate Date, Type HolidayType, Name []LocalizedText, Nationwide bool, ..., Subdivisions []SubdivisionRef, ... }
- type SubdivisionRef struct { Code string; ShortName string }
- func pickLocalized(entries []LocalizedText, lang string) string  // unexported, in types.go — case-insensitive match + first-entry fallback
- func (c Country) NameFor(lang string) string  // analog implementation lines 163-165
- func (l Language) NameFor(lang string) string  // analog lines 180-182
- func (s Subdivision) NameFor(lang string) string  // analog lines 220-222

From date.go:
- type Date struct { time.Time }  // embedded
- func NewDate(year int, month time.Month, day int) Date  // returns UTC-midnight
- func (d Date) IsZero() bool  // inherited via embedded time.Time
- func (d Date) Before(other Date) bool  // normalized comparison
- func (d Date) DaysUntil(other Date) int  // inclusive count, DST-safe

From stdlib:
- iter.Seq[T] = func(yield func(T) bool) — Go 1.23 range-over-func type

Target shape:
- func (h Holiday) NameFor(lang string) string
- func (h Holiday) IsInRegion(code string) bool
- func (h Holiday) Days() int
- func (h Holiday) Range() iter.Seq[Date]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write holiday.go with the four pure Holiday helper methods</name>
  <files>holiday.go</files>
  <read_first>
    - types.go (full file — the three existing NameFor accessors on Country/Language/Subdivision are the verbatim analogs at lines 153-165, 178-182, 218-222; pickLocalized at lines 224-243 is what Holiday.NameFor delegates to)
    - date.go (full file — Date embedding of time.Time; NewDate normalizes UTC-midnight; DaysUntil contract; AddDate inherited from time.Time)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md D-57, D-58, D-59 (D-58 is THIS plan's scope, D-59 is Plan 7), D-60, D-61, CL-10, CL-11
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"Pattern 2: iter.Seq[Date] canonical body for Holiday.Range" lines 334-386 (verbatim canonical body + UTC-midnight rationale + zero-call-after-false-yield contract)
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"Common Pitfalls — Pitfall 3" lines 893-902 (the NewDate-rebuild justification for Range)
    - .planning/phases/03-endpoints-helpers/03-PATTERNS.md §"`holiday.go`" lines 362-447 (verbatim skeletons for all four methods)
  </read_first>
  <behavior>
    - NameFor returns pickLocalized output: case-insensitive match + first-entry fallback + empty on empty Name
    - IsInRegion: empty code → false; Nationwide → true; iterate Holiday.Subdivisions and match Code via strings.EqualFold; otherwise false
    - Days returns h.StartDate.DaysUntil(h.EndDate): same-day → 1; multi-day → inclusive count
    - Range returns iter.Seq[Date]: yields each Date from StartDate to EndDate inclusive; early termination via yield-returning-false; each yielded Date is rebuilt via NewDate(next.Year, next.Month, next.Day) to preserve UTC-midnight; defensive empty-yield when EndDate strictly before StartDate
  </behavior>
  <action>
    Create new file holiday.go in repo root with `package openholidays`.
    Imports: iter, strings.

    Declare four methods on Holiday in this order: NameFor, IsInRegion, Days, Range.

    Method 1 — NameFor: receiver is Holiday value (not pointer — symmetric with the three existing NameFor accessors). Body is a single line returning pickLocalized(h.Name, lang). godoc starts with "NameFor returns ..." and explains the case-insensitive match, first-entry fallback, empty-on-empty contract. The godoc also explains the collision-avoiding name choice (Holiday.Name is the []LocalizedText field, so a method named Name(lang) would collide — same CL-05 rationale as Country.NameFor, now CL-10).

    Method 2 — IsInRegion: receiver is Holiday value. Body per RESEARCH.md §"Pattern 2 — Helper" and PATTERNS.md lines 400-410 verbatim:
      if code == "" return false (defensive, no panic).
      if h.Nationwide return true (REQUIREMENTS HELP-02 literal: "or is nationwide").
      for _, s := range h.Subdivisions: if strings.EqualFold(s.Code, code) return true.
      return false.
    godoc explains the flat-only behavior (no recursion into Subdivision.Children — that lives in Client.IsInRegion per Plan 7 / D-59 / CL-09) and the case-insensitive match.

    Method 3 — Days: receiver is Holiday value. Body is a single line returning h.StartDate.DaysUntil(h.EndDate). godoc explains: inclusive day count; single-day → 1; multi-day → inclusive count; calendar-correct across DST boundaries because Date is UTC-midnight (Phase 1 D-10 / Pitfall TZ-2). Reference the verified test case: Polish ferie zimowe Śląskie 2025 (Jan 18 to Jan 31) → 14.

    Method 4 — Range: receiver is Holiday value. Return type iter.Seq[Date] (NOT iter.Seq[time.Time] — CL-11 deviation from ROADMAP literal). Body per RESEARCH.md §"Pattern 2" lines 362-379 verbatim:
      Return a closure of shape func(yield func(Date) bool).
      Defensive guard: if h.EndDate.Before(h.StartDate) return (empty iteration; documented as the malformed-Holiday case — validateHolidays would have caught it before reaching the caller for endpoint-returned Holidays, but Range is documented as side-effect-free and must not panic on a hand-built Holiday).
      Initialize d := h.StartDate.
      Loop: call yield(d); if it returns false return immediately (single-use contract — yield is never called after returning false, per pkg.go.dev/iter).
      If d is not before h.EndDate (i.e. d == EndDate after the yield) return — last calendar day yielded.
      Otherwise compute next := d.AddDate(0, 0, 1) and rebuild d := NewDate(next.Year(), next.Month(), next.Day()) — the rebuild via NewDate is the UTC-midnight invariant guard (Pitfall 3).
    godoc explains: single-use closure semantics; UTC-midnight invariant preserved; calendar-correct across DST; empty-on-malformed-Holiday behavior; reference CL-11 for the type-parameter deviation.

    Every error string (none in this file) and every godoc starts with "openholidays: " / the symbol name respectively per Gold Rule 1.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go build ./...</automated>
  </verify>
  <done>
    holiday.go compiles. `grep -c "func (h Holiday) NameFor" holiday.go` returns 1. `grep -c "func (h Holiday) IsInRegion" holiday.go` returns 1. `grep -c "func (h Holiday) Days" holiday.go` returns 1. `grep -c "func (h Holiday) Range" holiday.go` returns 1. `grep -c "iter.Seq\[Date\]" holiday.go` returns 1. `grep -c "NewDate(next.Year" holiday.go` returns 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write holiday_test.go covering all four helpers with one TestXxx per method (Gold Rule 3)</name>
  <files>holiday_test.go</files>
  <read_first>
    - holiday.go (just-created — the contracts being exercised)
    - types_test.go (the canonical analog for type-method tests; one TestXxx per exported function; testify + t.Run pattern)
    - date_test.go (the calendar-correctness pattern for Days/Range — DST-boundary handling expected style)
    - .planning/phases/03-endpoints-helpers/03-PATTERNS.md §"`holiday_test.go`" lines 670-713 (verbatim sub-test skeleton for TestHoliday_NameFor; the ferie-zimowe assertions for Days and Range)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md D-70 (the locked sanity values: ferie zimowe Śląskie 2025 is Jan 18 to Jan 31 → Days returns 14, Range yields 14 Dates)
  </read_first>
  <behavior>
    - TestHoliday_NameFor: matches Polish name case-insensitively; falls back to first entry on miss; returns empty on empty Name slice
    - TestHoliday_IsInRegion: empty code → false; Nationwide=true → true; matching Code (case-insensitive) → true; non-matching code with Nationwide=false → false; empty Subdivisions slice with Nationwide=false → false
    - TestHoliday_Days: same-day → 1; 14-day span (Jan 18 to Jan 31) → 14; cross-year span (Dec 31 to Jan 1) → 2
    - TestHoliday_Range: 14 yielded Dates for the Jan 18 to Jan 31 span (first=Jan 18, last=Jan 31, every intermediate yielded); single-day yields exactly 1 Date; EndDate before StartDate yields 0 Dates (empty range); early break via the consumer returning false stops yields; every yielded Date has Location() == time.UTC and zero hour/minute/second/nanosecond
  </behavior>
  <action>
    Create new file holiday_test.go in repo root with `package openholidays`.
    Imports: testing, time, plus testify assert and require.
    Declare exactly FOUR TestXxx functions per Gold Rule 3 (one per Holiday production method):
      func TestHoliday_NameFor(t *testing.T)
      func TestHoliday_IsInRegion(t *testing.T)
      func TestHoliday_Days(t *testing.T)
      func TestHoliday_Range(t *testing.T)
    Each function calls t.Parallel() at the top.

    TestHoliday_NameFor subtests:
      "matches Polish entry case-insensitively" — Holiday with Name = []LocalizedText{{Language:"pl", Text:"Wigilia"}, {Language:"en", Text:"Christmas Eve"}}; assert h.NameFor("pl") == "Wigilia" AND h.NameFor("PL") == "Wigilia"
      "falls back to first entry on miss" — Holiday with same Name; assert h.NameFor("xx") == "Wigilia" (first entry text)
      "returns empty on empty Name" — Holiday with Name nil or empty; assert h.NameFor("pl") == ""

    TestHoliday_IsInRegion subtests:
      "empty code returns false even when Nationwide" — Holiday{Nationwide:true}; assert h.IsInRegion("") == false
      "Nationwide returns true for any non-empty code" — Holiday{Nationwide:true}; assert h.IsInRegion("PL-SL") == true
      "matches Code in Subdivisions case-insensitively" — Holiday{Subdivisions:[]SubdivisionRef{{Code:"PL-SL"}, {Code:"PL-DS"}}}; assert h.IsInRegion("pl-sl") == true AND h.IsInRegion("PL-SL") == true
      "returns false when code is not in Subdivisions and not Nationwide" — Holiday{Subdivisions:[]SubdivisionRef{{Code:"PL-SL"}}}; assert h.IsInRegion("PL-DS") == false
      "returns false when Subdivisions is empty and not Nationwide" — Holiday{Nationwide:false, Subdivisions:nil}; assert h.IsInRegion("PL-SL") == false

    TestHoliday_Days subtests:
      "single-day returns 1" — Holiday{StartDate:NewDate(2025,time.January,1), EndDate:NewDate(2025,time.January,1)}; assert h.Days() == 1
      "14-day ferie zimowe Śląskie returns 14" — Holiday{StartDate:NewDate(2025,time.January,18), EndDate:NewDate(2025,time.January,31)}; assert h.Days() == 14
      "cross-year span returns 2" — Holiday{StartDate:NewDate(2025,time.December,31), EndDate:NewDate(2026,time.January,1)}; assert h.Days() == 2

    TestHoliday_Range subtests:
      "14-day ferie zimowe yields 14 Dates inclusive" — Holiday with StartDate Jan 18 and EndDate Jan 31; collect every yielded Date into a slice via the range loop `for d := range h.Range() { dates = append(dates, d) }`; assert len(dates) == 14; assert dates[0].Equal(NewDate(2025,time.January,18)); assert dates[13].Equal(NewDate(2025,time.January,31))
      "single-day yields exactly one Date" — StartDate == EndDate; assert exactly 1 yield
      "EndDate before StartDate yields zero" — Holiday{StartDate:NewDate(2025,time.January,31), EndDate:NewDate(2025,time.January,18)}; collect via range; assert len(dates) == 0
      "early break stops iteration" — use a counter and break after 3 yields; assert counter == 3
      "every yielded Date is UTC midnight" — for each yielded Date in the 14-day case, assert d.Location() == time.UTC AND d.Hour() == 0 AND d.Minute() == 0 AND d.Second() == 0 AND d.Nanosecond() == 0

    Each subtest calls t.Parallel().
    File-header godoc explains Gold Rule 3 application (4 TestXxx for 4 production methods) and references CL-10/CL-11.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go test -race -run "TestHoliday_NameFor|TestHoliday_IsInRegion|TestHoliday_Days|TestHoliday_Range" -count=1 ./...</automated>
  </verify>
  <done>
    `go test -race -run "TestHoliday_NameFor|TestHoliday_IsInRegion|TestHoliday_Days|TestHoliday_Range" -count=1 ./...` exits 0. holiday_test.go declares exactly four TestXxx functions matching the four production methods. `grep -c "func TestHoliday_" holiday_test.go` returns 4.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Holiday helpers | No I/O. The methods operate purely on the Holiday value the caller already holds. The only inputs that cross are method arguments (lang string, code string). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-Tampering-MalformedRange | Tampering (caller-induced) | holiday.go Range | mitigate | When EndDate strictly before StartDate, Range yields nothing rather than panicking — guards against hand-built malformed Holidays. validateHolidays (Plan 4) catches the endpoint case before it reaches the caller. |
| T-3-DoS-NilSlice | DoS | holiday.go NameFor, IsInRegion | accept | nil Name / nil Subdivisions are normal Go zero values; the for-range over nil slice iterates 0 times. pickLocalized handles empty entries gracefully (returns empty string). No active mitigation needed. |
| T-3-NoExternalIO | n/a | holiday.go | n/a | No network, no file system, no goroutines. No threat surface beyond the caller-controlled input. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test -race -run "TestHoliday_" -count=1 ./...` exits 0.
- `grep -c "func (h Holiday)" holiday.go` returns 4.
- `grep -c "iter.Seq\[Date\]" holiday.go` returns 1.
- `grep -c "pickLocalized" holiday.go` returns 1.
- `grep -c "DaysUntil" holiday.go` returns 1.
</verification>

<success_criteria>
- HELP-01 satisfied: Holiday.NameFor exists, delegates to pickLocalized, matches case-insensitively with first-entry fallback.
- HELP-02 satisfied (flat portion): Holiday.IsInRegion exists, matches per the D-58 rules. The hierarchical extension lands in Plan 7.
- HELP-03 satisfied: Holiday.Days returns 14 for the canonical 14-day ferie zimowe Śląskie 2025 case.
- HELP-04 satisfied: Holiday.Range returns iter.Seq[Date] (CL-11 deviation), yields 14 Dates for the canonical case, preserves UTC-midnight invariant.
- holiday_test.go uses one TestXxx per production method per Gold Rule 3.
</success_criteria>

<output>
After completion, write `.planning/phases/03-endpoints-helpers/03-06-SUMMARY.md` recording: the four method signatures, the subtest count per TestXxx, the exact ferie zimowe Days/Range assertions (14, first=Jan 18, last=Jan 31), and any deviation from D-57..D-61 + CL-10 + CL-11.

This is the plan that adds CL-10 and CL-11 rows to PROJECT.md `Key Decisions` table. The executor's SUMMARY MUST note both lines to append:
- "CL-10: Holiday.NameFor collision-avoiding name (Holiday.Name is the []LocalizedText field) — D-57"
- "CL-11: Holiday.Range yields iter.Seq[Date] not iter.Seq[time.Time] — D-61; deviation from ROADMAP success criterion #4 literal; rationale: composition with Date math helpers without conversion churn"
</output>
