---
phase: 03-endpoints-helpers
plan: 04
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - public_holidays.go
  - public_holidays_test.go
  - request.go
  - errors.go
  - internal_test.go
  - testdata/public_holidays_pl_2025.json
autonomous: true
requirements:
  - ENDPT-04
  - TEST-01
must_haves:
  truths:
    - "PublicHolidays(ctx, PublicHolidaysRequest) ([]Holiday, error) returns the public holidays for a country in a date window"
    - "Required CountryIsoCode + ValidFrom + ValidTo validated before HTTP"
    - "Optional LanguageIsoCode validated only when non-empty (D-55, D-56)"
    - "Optional SubdivisionCode is shape-tolerant (no client-side validator; passed through to upstream)"
    - "Endpoint dispatches through doJSONGet[[]Holiday] then calls validateHolidays for post-decode schema-drift detection"
    - "validateHolidays rejects zero StartDate, zero EndDate, EndDate before StartDate — wraps ErrMalformedResponse (D-65, CL-12)"
    - "ErrMalformedResponse is the seventh exported sentinel in errors.go (D-66, CL-12)"
    - "internal_test.go::allowedVars is extended to include ErrMalformedResponse (Pitfall 6 from RESEARCH.md)"
    - "Happy-path PL 2025 fixture has 14 holidays including Wigilia Bożego Narodzenia on 2025-12-24 (D-70 sanity assertion)"
    - "D-51: PublicHolidays(ctx, PublicHolidaysRequest) follows the uniform (ctx, Request) endpoint shape established in Plan 01"
    - "D-53: PublicHolidaysRequest.CountryIsoCode/LanguageIsoCode/SubdivisionCode field names follow the code-field naming convention (match upstream countryIsoCode/languageIsoCode/subdivisionCode query params)"
    - "D-54: PublicHolidaysRequest{CountryIsoCode, ValidFrom, ValidTo, LanguageIsoCode, SubdivisionCode} exposes every upstream-supported filter for /PublicHolidays (CL-13)"
    - "D-64: public_holidays.go file is scoped to the PublicHolidaysRequest type + PublicHolidays endpoint method (≤30 lines) + endpoint-specific post-decode validateHolidays call; HTTP pipeline lives only in request.go's doJSONGet"
    - "D-69: publicHolidaysPL2025FixtureCapturedAt const recorded in public_holidays_test.go and included in fixture-drift test failure messages"
    - "D-71: testdata/public_holidays_pl_2025.json fixture path matches the D-71 root-level testdata/ convention"
  artifacts:
    - path: public_holidays.go
      provides: "PublicHolidays endpoint method + PublicHolidaysRequest type"
      contains: "type PublicHolidaysRequest struct"
    - path: public_holidays_test.go
      provides: "TestClient_PublicHolidays + TestValidateHolidays"
    - path: request.go
      provides: "validateHolidays added to request.go (placement per RESEARCH.md Pattern 6)"
      contains: "func validateHolidays(hs []Holiday, path string) error"
    - path: errors.go
      provides: "ErrMalformedResponse exported sentinel added to existing var block"
      contains: "ErrMalformedResponse"
    - path: internal_test.go
      provides: "allowedVars extended with ErrMalformedResponse"
      contains: "ErrMalformedResponse"
    - path: testdata/public_holidays_pl_2025.json
      provides: "Live-captured PL 2025 public holidays fixture (14 entries)"
  key_links:
    - from: "public_holidays.go"
      to: "request.go"
      via: "doJSONGet[[]Holiday] + validateHolidays post-decode call"
      pattern: "doJSONGet\\[\\[\\]Holiday\\]"
    - from: "public_holidays.go"
      to: "validate.go"
      via: "validateCountry + validateDateRange + optional validateLanguage"
      pattern: "validateDateRange\\("
    - from: "request.go validateHolidays"
      to: "errors.go ErrMalformedResponse"
      via: "fmt.Errorf %w wrap"
      pattern: "ErrMalformedResponse"
---

<objective>
Ship the `PublicHolidays` endpoint — the most invariant-heavy endpoint in Phase 3. Three required validators (country, date range), two optional fields (language, subdivision), a brand-new sentinel `ErrMalformedResponse` for post-decode Holiday schema-drift detection, the `validateHolidays` helper that enforces the invariant, and the corresponding `allowedVars` extension in `internal_test.go` so the CLIENT-10 AST audit stays green.

Purpose: Covers REQUIREMENTS.md ENDPT-04 + TEST-01 (per-endpoint table-driven 4-error-path coverage). Adds CL-12 AND CL-13 to the locked decision ledger (per D-72 step 4: this plan owns both rows because both rows describe semantics that only become observable once PublicHolidays ships — the `ErrMalformedResponse` sentinel for CL-12, and the "Request structs expose every upstream-supported filter" pattern for CL-13).

Output: `public_holidays.go` + `public_holidays_test.go` + `testdata/public_holidays_pl_2025.json` + modifications to `request.go`, `errors.go`, `internal_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-endpoints-helpers/03-CONTEXT.md
@.planning/phases/03-endpoints-helpers/03-RESEARCH.md
@.planning/phases/03-endpoints-helpers/03-PATTERNS.md
@.planning/phases/03-endpoints-helpers/03-VALIDATION.md
@countries.go
@countries_test.go
@request.go
@errors.go
@internal_test.go
@validate.go
@types.go
@date.go

<interfaces>
Key types and contracts.

From types.go (Holiday struct, lines 94-138):
- Holiday.ID (string), Holiday.StartDate (Date), Holiday.EndDate (Date), Holiday.Type (HolidayType), Holiday.Name []LocalizedText, Holiday.Nationwide (bool), Holiday.RegionalScope/TemporalScope (string), Holiday.Subdivisions, Holiday.Groups, Holiday.Tags, Holiday.Quality.

From date.go:
- Date.IsZero() bool (inherited via embedded time.Time)
- Date.Before(other Date) bool
- Date.String() string (YYYY-MM-DD per dateLayout)

From validate.go:
- validateCountry(code string) (string, error)
- validateLanguage(code string) (string, error)
- validateDateRange(from, to Date) error

From request.go (Plan 1):
- func doJSONGet[T any](ctx context.Context, c *Client, path string, q url.Values) (T, error)

From errors.go (current state — Plan 4 appends to this block):
- ErrInvalidCountry, ErrInvalidLanguage, ErrDateRangeTooLarge, ErrInvalidDateRange, ErrEmptyResponse, ErrResponseTooLarge (6 sentinels). Plan 4 appends ErrMalformedResponse.

From internal_test.go (current allowedVars at lines 54-62): 7 entries (6 exported sentinels + errEmptyDate). Plan 4 extends to 8.

Target type and method:
- type PublicHolidaysRequest struct { CountryIsoCode string; ValidFrom Date; ValidTo Date; LanguageIsoCode string; SubdivisionCode string }
- func (c *Client) PublicHolidays(ctx context.Context, req PublicHolidaysRequest) ([]Holiday, error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ErrMalformedResponse sentinel + extend internal_test.go allowedVars + add validateHolidays to request.go</name>
  <files>errors.go, internal_test.go, request.go</files>
  <read_first>
    - errors.go (full file — append the new sentinel to the existing var block; the surrounding godoc shape is the analog)
    - internal_test.go lines 33-62 (the `allowedVars` map declaration and surrounding godoc explaining the extension protocol)
    - request.go (created by Plan 1 — `validateHolidays` is added here per RESEARCH.md §"Pattern 6 placement" lines 776-817 recommendation, NOT in a new `holiday.go`)
    - types.go (Holiday struct + Date type — fields h.StartDate, h.EndDate, h.ID consumed by the validator)
    - date.go (IsZero, Before — the predicates validateHolidays calls)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md D-65, D-66 (validator scope; ErrMalformedResponse exported)
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"Pattern 6: validateHolidays placement and body" lines 776-817 (verbatim helper body)
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"Common Pitfalls — Pitfall 6" lines 930-939 (the same-commit protocol: sentinel + allowlist extension MUST land together)
    - .planning/phases/03-endpoints-helpers/03-PATTERNS.md §"`internal_test.go::allowedVars` extension protocol" lines 783-801 (the exact one-line map addition)
  </read_first>
  <behavior>
    - errors.go declares a new exported sentinel ErrMalformedResponse with message "openholidays: malformed response"
    - internal_test.go's allowedVars map includes the entry for ErrMalformedResponse so TestNoInitOrGlobalState does not regress
    - request.go declares func validateHolidays(hs []Holiday, path string) error that returns nil on a valid slice and wraps ErrMalformedResponse on the first violation
    - The validator's error message includes the offending Holiday's ID and the failing predicate (per RESEARCH.md §"Specific Ideas")
  </behavior>
  <action>
    Edit errors.go: append the new sentinel declaration to the existing var (...) block (after ErrResponseTooLarge). The declaration uses errors.New with the message string "openholidays: malformed response". Add a godoc comment block above it explaining: returned when the upstream returns a structurally-decodable JSON response that violates the Holiday post-decode invariants (zero StartDate, zero EndDate, EndDate before StartDate). Wrapped via fmt.Errorf with the %w verb from validateHolidays so errors.Is against this sentinel holds through caller wraps. Reference D-65, D-66, CL-12 in the comment.

    Edit internal_test.go: extend the allowedVars map (lines 54-62) by adding the entry for ErrMalformedResponse — place it AFTER the entry for ErrResponseTooLarge for chronological clarity (Plan 4 deliberate choice over alphabetical ordering). Update the godoc above the map to mention the seventh exported sentinel from Phase 3 (D-66 / CL-12). Total allowedVars entries after this change: 8 (7 exported sentinels + errEmptyDate).

    Edit request.go: append the validateHolidays function per RESEARCH.md §"Pattern 6" recommendation that the helper lives in request.go next to doJSONGet. Body comes verbatim from RESEARCH.md lines 791-808.
    Signature: func validateHolidays(hs []Holiday, path string) error.
    godoc explains the three invariants enforced (StartDate non-zero, EndDate non-zero, EndDate not strictly before StartDate); states that path is included in the wrapped error message; references D-65 / CL-12 / Pitfall JSON-4 / Pitfall TZ-2.
    Body uses pointer iteration (for i := range hs followed by `h := &hs[i]`) per the rangeValCopy linter recommendation cited in RESEARCH.md line 811.
    On each predicate failure returns a fmt.Errorf wrap with the %w verb against ErrMalformedResponse, formatted as: `openholidays: malformed holiday %q at %s: <predicate>: %w` — the first %q being h.ID, %s being path, and the trailing %w being the sentinel. For the EndDate-before-StartDate case the message also includes both Date values via Date.String formatting.
    Returns nil after the loop completes.
    No new imports — request.go already has "fmt" from Plan 1.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go build ./... &amp;&amp; go test -run TestNoInitOrGlobalState -count=1 ./...</automated>
  </verify>
  <done>
    `go build ./...` succeeds. `go test -run TestNoInitOrGlobalState -count=1 ./...` exits 0 (the AST audit accepts the new sentinel because allowedVars is extended). `grep -c "ErrMalformedResponse" errors.go` returns at least 1. `grep -c "ErrMalformedResponse" internal_test.go` returns at least 1. `grep -c "func validateHolidays" request.go` returns 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture testdata/public_holidays_pl_2025.json from live API and write public_holidays.go</name>
  <files>public_holidays.go, testdata/public_holidays_pl_2025.json</files>
  <read_first>
    - countries.go (post-Plan-1 analog — the canonical endpoint dispatch shape)
    - subdivisions.go (Plan 3 sibling for the required+optional validator pattern)
    - request.go (the doJSONGet[T] signature and validateHolidays from Task 1)
    - validate.go (validateCountry + validateLanguage + validateDateRange)
    - date.go (Date.String returning YYYY-MM-DD — used for the validFrom/validTo query params)
    - types.go (Holiday struct shape)
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"Endpoint method body — PublicHolidays" lines 991-1033 (verbatim template)
    - .planning/phases/03-endpoints-helpers/03-PATTERNS.md §"`public_holidays.go`" lines 295-336 (verbatim skeleton)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md D-54 (Request field set), D-56 (validator wiring matrix), D-65 (validateHolidays call site), D-70 (sanity assertions: 14 holidays in PL 2025, includes Wigilia 2025-12-24)
  </read_first>
  <behavior>
    - Endpoint signature is (c *Client) PublicHolidays(ctx context.Context, req PublicHolidaysRequest) ([]Holiday, error)
    - Empty CountryIsoCode → ErrInvalidCountry without HTTP
    - validFrom > validTo → ErrInvalidDateRange without HTTP
    - window > 3 years → ErrDateRangeTooLarge without HTTP
    - Empty LanguageIsoCode → no language query param sent
    - Non-empty LanguageIsoCode validated; lowercased canonical form sent
    - Empty SubdivisionCode → no subdivisionCode query param sent
    - Non-empty SubdivisionCode → passed through verbatim (no client-side validator per D-56)
    - 200 + valid JSON array of Holidays → returns []Holiday after validateHolidays passes
    - 200 + JSON Holidays with zero StartDate (or EndDate before StartDate) → returns nil + ErrMalformedResponse wrap
  </behavior>
  <action>
    Capture fixture: issue a GET to https://openholidaysapi.org/PublicHolidays with query params countryIsoCode=PL, validFrom=2025-01-01, validTo=2025-12-31, languageIsoCode=PL. Pretty-print the response with json.Indent (or python -m json.tool) and write to testdata/public_holidays_pl_2025.json. Verify post-capture sanity: exactly 14 entries; at least one entry's name array contains a LocalizedText with language "pl" and text "Wigilia Bozego Narodzenia" (with Polish diacritics — the literal text per D-70); that same entry's startDate is "2025-12-24". If any sanity check fails, abort and surface the upstream issue (RESEARCH.md §"Specific Ideas" guard — do not commit a degenerate fixture from a transient outage).

    Create new file public_holidays.go in repo root with `package openholidays`.
    Imports: context, net/url.
    Declare type PublicHolidaysRequest as a struct with five fields in order: CountryIsoCode string, ValidFrom Date, ValidTo Date, LanguageIsoCode string, SubdivisionCode string. godoc explains: CountryIsoCode required (ISO 3166-1 alpha-2, case-insensitive); ValidFrom and ValidTo required (window ≤ 3 calendar years; from ≤ to); LanguageIsoCode optional (ISO 639-1, case-insensitive); SubdivisionCode optional (passed through verbatim, e.g. "PL-SL").
    Declare func (c *Client) PublicHolidays(ctx context.Context, req PublicHolidaysRequest) ([]Holiday, error).
    Method body (verbatim from RESEARCH.md §"Endpoint method body — PublicHolidays" lines 1008-1033):
      Call validateCountry on req.CountryIsoCode capturing the canonical (uppercased) form; on error return nil with the wrapped error.
      Call validateDateRange on (req.ValidFrom, req.ValidTo); on error return nil with the wrapped error.
      Declare q := url.Values{}.
      q.Set("countryIsoCode", canonicalCountry); q.Set("validFrom", req.ValidFrom.String()); q.Set("validTo", req.ValidTo.String()).
      If req.LanguageIsoCode != "": call validateLanguage capturing the canonical (lowercased) form; on error return nil with the wrapped error; q.Set("languageIsoCode", canonicalLang).
      If req.SubdivisionCode != "": q.Set("subdivisionCode", req.SubdivisionCode) — no validator per D-56.
      Call doJSONGet[[]Holiday](ctx, c, "/PublicHolidays", q); on error return nil with the wrapped error.
      Call validateHolidays(holidays, "/PublicHolidays"); on error return nil with the wrapped error.
      Return holidays, nil.
    godoc on the method mirrors the Phase 2 Countries pattern: first sentence starts with the symbol name; sections for per-request timeout, errors (validateCountry / validateDateRange / validateLanguage failures + ErrMalformedResponse from validateHolidays + the transport-error contract from doJSONGet); concurrency note.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go build ./...</automated>
  </verify>
  <done>
    public_holidays.go compiles. testdata/public_holidays_pl_2025.json is valid JSON with exactly 14 entries; one entry has the Polish name "Wigilia Bozego Narodzenia" (with proper Polish diacritics) and startDate "2025-12-24". `grep -c "type PublicHolidaysRequest" public_holidays.go` returns 1. `grep -c "doJSONGet\[\[\]Holiday\]" public_holidays.go` returns 1. `grep -c "validateHolidays" public_holidays.go` returns 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write public_holidays_test.go covering happy path + ErrMalformedResponse + 4 error paths + validation-error paths</name>
  <files>public_holidays_test.go</files>
  <read_first>
    - countries_test.go (the canonical analog: 7 subtests + file header style + testify usage)
    - subdivisions_test.go (Plan 3 sibling for query-param contract assertion pattern + validation-error subtest pattern)
    - public_holidays.go (just-created — the contract being exercised)
    - request.go (the validateHolidays signature; for the dedicated TestValidateHolidays sub-test entry)
    - testdata/public_holidays_pl_2025.json (just-captured)
    - types.go (Holiday struct + HolidayType constants — VERIFY before committing the raw-JSON literals: the test's malformed-Holiday subtest constructs JSON with `"type":"Public"` or `"type":"School"` strings; confirm those exact string values match `HolidayTypePublic` and `HolidayTypeSchool` constants in types.go before writing the test. The current types.go declares `HolidayTypePublic HolidayType = "Public"` and `HolidayTypeSchool HolidayType = "School"` — both PascalCase, matching the raw literal. If a future types.go change drifts these to other casing/values, this test's JSON literals must be updated accordingly.)
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"Pattern 3: httptest.NewServer table tests" lines 388-495 (verbatim test skeleton; happy path with URL contract assertions; validation-error subtests; ErrMalformedResponse subtest body lines 467-493)
    - .planning/phases/03-endpoints-helpers/03-PATTERNS.md §"ErrMalformedResponse subtest pattern" lines 640-664 (verbatim malformed-holiday JSON example)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md D-69, D-70 (fixture-capture-date const naming + the locked sanity assertions: 14 entries, Wigilia on 2025-12-24)
  </read_first>
  <behavior>
    - Test 1 (happy path): handler asserts URL.Path == /PublicHolidays, query has countryIsoCode=PL, validFrom=2025-01-01, validTo=2025-12-31; client receives 14 Holidays; one of them has NameFor("pl") returning the literal Polish name and StartDate equal to NewDate(2025, time.December, 24)
    - Test 2 (validation: empty CountryIsoCode): WithBaseURL example.invalid; assert errors.Is err ErrInvalidCountry
    - Test 3 (validation: validFrom after validTo): assert errors.Is err ErrInvalidDateRange
    - Test 4 (validation: window > 3 years): from=2020-01-01 to=2025-01-01 (assert errors.Is err ErrDateRangeTooLarge)
    - Test 5 (validation: invalid LanguageIsoCode): assert errors.Is err ErrInvalidLanguage
    - Test 6 (4xx): server returns 404 + ProblemDetails; assert *APIError with Path /PublicHolidays
    - Test 7 (5xx with title fallback)
    - Test 8 (malformed JSON): assert err non-nil and not any sentinel
    - Test 9 (ctx cancel): assert errors.Is err context.Canceled
    - Test 10 (ErrMalformedResponse: EndDate before StartDate): server returns a single Holiday with startDate "2025-12-25", endDate "2025-01-01"; assert errors.Is err ErrMalformedResponse
    - Test 11 (optional SubdivisionCode reaches the wire): handler asserts r.URL.Query().Get("subdivisionCode") == "PL-SL" when caller passes it
    - Separate TestValidateHolidays function exercising the unit-level validator: 3 happy entries → nil; one entry with IsZero StartDate → wraps ErrMalformedResponse; one entry with EndDate before StartDate → wraps ErrMalformedResponse
  </behavior>
  <action>
    Before writing the file, run `grep -n "HolidayType" types.go` and confirm the wire-format values match the raw-JSON literals this task emits (`"Public"`, `"School"`). If they have changed since this plan was written, update the JSON literals in the malformed-Holiday subtest to match the current constants (the test must keep using a real HolidayType wire value so the json.Unmarshal step does not fail at the type level before validateHolidays runs).

    Create new file public_holidays_test.go in repo root with `package openholidays`.
    Imports: context, errors, net/http, net/http/httptest, os, path/filepath, testing, time, plus testify assert and require.
    Declare const publicHolidaysPL2025FixtureCapturedAt set to the Task 2 capture date.
    Declare exactly one func TestClient_PublicHolidays(t *testing.T) per Gold Rule 3. Top-level t.Parallel().
    Subtests under t.Run mirroring the behavior list above. Use the verbatim ErrMalformedResponse subtest body from PATTERNS.md lines 640-664 — server returns a single Holiday with startDate "2025-12-25" and endDate "2025-01-01" (other required fields populated minimally — `"type":"Public"` matching the HolidayTypePublic constant verified above); the assertion is errors.Is(err, ErrMalformedResponse).
    For the happy path subtest: the handler inspects r.URL.Query() per RESEARCH.md Pattern 3 line 406-414 to assert countryIsoCode=PL, validFrom=2025-01-01, validTo=2025-12-31. After receiving 14 Holidays from c.PublicHolidays, iterate to find the entry whose NameFor("pl") matches the literal Polish "Wigilia Bozego Narodzenia" (with Polish diacritics), and assert that entry's StartDate equals NewDate(2025, time.December, 24).
    For validation-only subtests use WithBaseURL("http://example.invalid") (RFC 6761 reserved domain) — no HTTP is dispatched.
    Declare a SEPARATE func TestValidateHolidays(t *testing.T) (Gold Rule 3 dedicated function — validateHolidays is the production function under test). Top-level t.Parallel(). Subtests: valid slice (3 holidays with proper Dates) → nil error; one holiday with default-zero StartDate → errors.Is err ErrMalformedResponse and the err message contains the Holiday ID substring; one holiday with EndDate strictly before StartDate → errors.Is err ErrMalformedResponse.
    Every subtest except those inspecting runtime state calls t.Parallel().
    File-header godoc mirrors countries_test.go style.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go test -race -run "TestClient_PublicHolidays|TestValidateHolidays" -count=1 ./...</automated>
  </verify>
  <done>
    `go test -race -run "TestClient_PublicHolidays|TestValidateHolidays" -count=1 ./...` exits 0. public_holidays_test.go declares exactly one TestClient_PublicHolidays function and exactly one TestValidateHolidays function. At least 11 subtests inside TestClient_PublicHolidays (`grep -c 't.Run(' public_holidays_test.go` returns ≥ 13 — 11 endpoint subtests + at least 2 validateHolidays subtests). The `"type":"<value>"` literals in the malformed-Holiday subtest match an actual HolidayType constant declared in types.go (verified by the executor before write).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Client.PublicHolidays | PublicHolidaysRequest crosses boundary; country/dates required; language/subdivision optional. All shape-validated client-side. |
| Client.PublicHolidays → upstream | /PublicHolidays response treated as untrusted both at the byte cap level (doJSONGet) AND at the post-decode shape level (validateHolidays). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-InputVal-Country | Tampering / DoS | public_holidays.go (validateCountry call) | mitigate | validateCountry runs before HTTP; rejects non-ASCII / Unicode-fold tricks (W-01 hardening from Phase 2). |
| T-3-InputVal-DateRange | Tampering / DoS | public_holidays.go (validateDateRange call) | mitigate | validateDateRange enforces from ≤ to AND window ≤ 3 calendar years (Phase 1 D-22). Defensive client-side cap on upstream's documented limit. |
| T-3-InputVal-Lang | Tampering | public_holidays.go (validateLanguage call) | mitigate | validateLanguage runs when LanguageIsoCode is non-empty. |
| T-3-DoS-EmptyQueryParam | DoS (self-inflicted) | public_holidays.go query builder | mitigate | Empty-string guards for both LanguageIsoCode and SubdivisionCode (D-55). |
| T-3-Tampering-MalformedHoliday | Tampering | request.go validateHolidays | mitigate | Post-decode invariant check (StartDate non-zero, EndDate non-zero, EndDate ≥ StartDate) wraps ErrMalformedResponse so callers can branch on errors.Is. Closes Pitfall JSON-4 (D-65 / CL-12). ASVS V5.5.2 ("verify that data ingested from external services is parsed and validated"). |
| T-3-DoS-OverSize | DoS | doJSONGet (inherited) | mitigate | 10 MiB cap inherited from Plan 1 / Phase 2 D-24. |
| T-3-InfoDisc-APIErrorBody | InfoDisc | doJSONGet (inherited) | mitigate | 4 KiB APIError.Body cap inherited from Plan 1 / Phase 1 D-17. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test -race -run "TestClient_PublicHolidays|TestValidateHolidays|TestNoInitOrGlobalState" -count=1 ./...` exits 0.
- testdata/public_holidays_pl_2025.json contains 14 entries; the Wigilia entry has startDate "2025-12-24".
- errors.go declares the ErrMalformedResponse sentinel; internal_test.go's allowedVars map contains its entry.
- `grep -c "type PublicHolidaysRequest" public_holidays.go` returns 1.
- `grep -c "doJSONGet\[\[\]Holiday\]" public_holidays.go` returns 1.
- `grep -c "func validateHolidays" request.go` returns 1.
- `grep -c "ErrMalformedResponse" errors.go` returns at least 1 (declaration line).
</verification>

<success_criteria>
- ENDPT-04 satisfied: PublicHolidays endpoint exists with all five Request fields.
- TEST-01 satisfied for this endpoint: 4 error paths covered (network/4xx/5xx/malformed/ctx-cancel) plus the new ErrMalformedResponse subtest plus four validation-only subtests.
- CL-12 landed: ErrMalformedResponse exported sentinel + validateHolidays helper + allowedVars extension all in this plan.
- CL-13 landed: PublicHolidaysRequest exposes every upstream-supported filter (CountryIsoCode + ValidFrom + ValidTo + LanguageIsoCode + SubdivisionCode), establishing the pattern that all sibling Request structs follow.
- Pitfall JSON-4 closed: post-decode invariant enforced.
- D-70 sanity assertions verified live: 14 holidays in PL 2025 fixture; Wigilia on 2025-12-24.
</success_criteria>

<output>
After completion, write `.planning/phases/03-endpoints-helpers/03-04-SUMMARY.md` recording: the actual capture date of testdata/public_holidays_pl_2025.json, the literal Polish text of the Wigilia entry's name (for downstream tests to compare against), confirmation that internal_test.go::allowedVars now has 8 entries, the subtest count in public_holidays_test.go, and any deviation from D-51..D-66 + CL-12 + CL-13.

This is the plan that adds BOTH the CL-12 row AND the CL-13 row to PROJECT.md `Key Decisions` table (per D-72 step 4). The executor's SUMMARY MUST note the two lines to append:

- "CL-12: ErrMalformedResponse sentinel for post-decode Holiday schema-drift detection — D-65 / D-66 / public_holidays.go and request.go"
- "CL-13: Request structs expose every upstream-supported filter — CountriesRequest.LanguageIsoCode, LanguagesRequest.LanguageIsoCode, SubdivisionsRequest, PublicHolidaysRequest.SubdivisionCode, SchoolHolidaysRequest.GroupCode — exceeding REQUIREMENTS literal text; D-54"
</output>
