---
phase: 03-endpoints-helpers
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - languages.go
  - languages_test.go
  - testdata/languages.json
autonomous: true
requirements:
  - ENDPT-02
  - TEST-01
must_haves:
  truths:
    - "Languages(ctx, LanguagesRequest) ([]Language, error) returns the supported-language list"
    - "Optional LanguageIsoCode filter is validated only when non-empty (D-55, D-56)"
    - "Endpoint dispatches through doJSONGet[[]Language] — no inline pipeline"
    - "Happy-path test asserts ≥ 14 languages (loose lower bound per D-70 — actual live count was 31 on 2026-05-27)"
    - "Per-endpoint TEST-01 covers the 4 error paths: 4xx, 5xx, malformed JSON, ctx cancel"
    - "D-51: Languages(ctx, LanguagesRequest) follows the uniform (ctx, Request) endpoint shape established in Plan 01"
    - "D-53: LanguagesRequest.LanguageIsoCode field name follows the LanguageIsoCode code-field naming convention (matches upstream languageIsoCode query param)"
    - "D-54: LanguagesRequest{LanguageIsoCode string} exposes every upstream-supported filter for /Languages (CL-13 expose-every-filter pattern)"
    - "D-64: languages.go file is scoped to the LanguagesRequest type + Languages endpoint method (≤30 lines) + endpoint-specific validation; HTTP pipeline lives only in request.go's doJSONGet"
    - "D-69: languagesFixtureCapturedAt const recorded in languages_test.go and included in fixture-drift test failure messages"
    - "D-71: testdata/languages.json fixture path matches the D-71 root-level testdata/ convention"
  artifacts:
    - path: languages.go
      provides: "Languages endpoint method + LanguagesRequest type"
      contains: "type LanguagesRequest struct"
    - path: languages_test.go
      provides: "TestClient_Languages with table-driven subtests"
    - path: testdata/languages.json
      provides: "Live-captured fixture of /Languages response"
  key_links:
    - from: "languages.go"
      to: "request.go"
      via: "doJSONGet[[]Language]"
      pattern: "doJSONGet\\[\\[\\]Language\\]"
    - from: "languages.go"
      to: "validate.go"
      via: "validateLanguage on non-empty LanguageIsoCode"
      pattern: "validateLanguage\\("
---

<objective>
Ship the `Languages` endpoint method. Follows the canonical post-Plan-1 endpoint shape: validate optional filter → build query → `doJSONGet[[]Language]` → return.

Purpose: Covers REQUIREMENTS.md ENDPT-02. Closes one of the four remaining endpoint gaps.

Output: `languages.go` + `languages_test.go` + `testdata/languages.json` (live-captured).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-endpoints-helpers/03-CONTEXT.md
@.planning/phases/03-endpoints-helpers/03-RESEARCH.md
@.planning/phases/03-endpoints-helpers/03-PATTERNS.md
@.planning/phases/03-endpoints-helpers/03-VALIDATION.md
@countries.go
@countries_test.go
@request.go
@validate.go
@types.go
@errors.go

<interfaces>
Key types and contracts the executor needs.

From types.go:
```go
type Language struct {
    IsoCode string          `json:"isoCode"`
    Name    []LocalizedText `json:"name"`
}
func (l Language) NameFor(lang string) string
```

From request.go (created in Plan 1):
```go
func doJSONGet[T any](ctx context.Context, c *Client, path string, q url.Values) (T, error)
```

From validate.go:
```go
func validateLanguage(code string) (string, error)  // returns lowercased canonical form, wraps ErrInvalidLanguage on shape failure
```

From countries.go (created in Plan 1, the analog for this plan):
```go
type CountriesRequest struct { LanguageIsoCode string }
func (c *Client) Countries(ctx context.Context, req CountriesRequest) ([]Country, error)
```

Target shape:
```go
type LanguagesRequest struct { LanguageIsoCode string }
func (c *Client) Languages(ctx context.Context, req LanguagesRequest) ([]Language, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture testdata/languages.json from the live API and write languages.go</name>
  <files>languages.go, testdata/languages.json</files>
  <read_first>
    - countries.go (the post-refactor analog from Plan 1 — the exact shape this endpoint mirrors)
    - request.go (the `doJSONGet[T any]` signature; the `url.Values` argument convention)
    - types.go (Language struct lines 167-182; LocalizedText lines 42-52; pickLocalized lines 224-243)
    - validate.go (validateLanguage lines 53-62 — return value is canonicalized lowercase form)
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"Endpoint method body — Subdivisions" lines 942-987 (the template shape)
    - .planning/phases/03-endpoints-helpers/03-PATTERNS.md §"`languages.go`" lines 209-262 (the exact code skeleton)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md D-51, D-54, D-55, D-56 (the contract: uniform Request shape; LanguageIsoCode optional; validator runs only on non-empty)
  </read_first>
  <behavior>
    - Endpoint method signature is `(c *Client) Languages(ctx context.Context, req LanguagesRequest) ([]Language, error)`
    - Empty LanguageIsoCode → no query param sent, validator does not run
    - Non-empty LanguageIsoCode → validateLanguage runs first; valid codes are lowercased before being put into the query string
    - Invalid LanguageIsoCode → returns nil + ErrInvalidLanguage wrap; no HTTP issued
    - Successful 200 + JSON array body → returns []Language decoded
  </behavior>
  <action>
    Capture fixture: run the shell command `curl -fsS 'https://openholidaysapi.org/Languages' | python3 -m json.tool > testdata/languages.json`. The captured file MUST be valid JSON, an array of ≥ 14 entries (per D-70 loose lower bound; actual was 31 on 2026-05-27), with each entry carrying `isoCode` and `name` fields. If `curl` fails or the result is empty/`[]`, abort the plan and surface the upstream issue rather than committing a degenerate fixture.

    Create new file `languages.go` in repo root with `package openholidays`.
    Imports: `context`, `net/url`.
    Declare `type LanguagesRequest struct { LanguageIsoCode string }` with godoc explaining the optional filter semantics.
    Declare `func (c *Client) Languages(ctx context.Context, req LanguagesRequest) ([]Language, error)`.
    Method body: declare `q := url.Values{}`; if `req.LanguageIsoCode != ""` call `lang, err := validateLanguage(req.LanguageIsoCode)` and return `nil, err` on error, then `q.Set("languageIsoCode", lang)`; finally `return doJSONGet[[]Language](ctx, c, "/Languages", q)`.
    godoc on the method follows the Phase 2 Countries pattern: first sentence starts with the symbol name; sections for per-request timeout, errors (validateLanguage + transport via doJSONGet contract), concurrency. Reference RESEARCH.md §"Endpoint method body" lines 944-987 verbatim — only swap Country for Language and Subdivisions for Languages.
    No call to validateCountry (Languages has no country param).
    No call to validateHolidays (Languages does not return Holiday slices).
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go build ./... &amp;&amp; python3 -c "import json,sys;d=json.load(open('testdata/languages.json'));assert isinstance(d,list) and len(d)&gt;=14, 'fixture invalid'"</automated>
  </verify>
  <done>
    languages.go compiles. testdata/languages.json is valid JSON with ≥ 14 entries. `grep -c "type LanguagesRequest" languages.go` returns 1. `grep -c "doJSONGet\[\[\]Language\]" languages.go` returns 1. `grep -c "validateLanguage" languages.go` returns 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write languages_test.go covering happy path + 4 error paths (TEST-01)</name>
  <files>languages_test.go</files>
  <read_first>
    - countries_test.go (the canonical analog: header, file structure, subtest names, testify patterns; reuse the 4 error-path subtests verbatim with Languages substitutions)
    - languages.go (just-created; the contract being exercised)
    - testdata/languages.json (just-captured; for `os.ReadFile` happy-path body)
    - .planning/phases/03-endpoints-helpers/03-PATTERNS.md §"`*_test.go`" lines 522-666 (test file shape — header pattern, happy-path skeleton, 4xx subtest, validation-error subtest)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md D-69, D-70 (fixture-captured-at const naming; sanity-assertion floor of "≥ 14 languages")
  </read_first>
  <behavior>
    - Test 1 (happy path): server returns testdata/languages.json bytes; assert `len(got) >= 14`; assert at least one entry has `IsoCode == "en"` (live data confirms English is always present); log `languagesFixtureCapturedAt`
    - Test 2 (optional filter): server handler inspects `r.URL.Query().Get("languageIsoCode") == "en"`; assert no error
    - Test 3 (invalid LanguageIsoCode): use `WithBaseURL("http://example.invalid")`; assert `errors.Is(err, ErrInvalidLanguage)`; no HTTP issued
    - Test 4 (4xx): server returns 404 + `{"detail":"…"}`; assert `errors.As(err, &apiErr)` with StatusCode 404 and Path "/Languages"
    - Test 5 (5xx with title fallback): server returns 500 + `{"title":"Internal Server Error"}`; assert apiErr.Message == "Internal Server Error"
    - Test 6 (malformed JSON): server returns 200 + `not valid json`; assert err wraps a JSON decode error (NOT a sentinel — just `require.Error` + `assert.NotErrorIs(err, ErrEmptyResponse)`)
    - Test 7 (ctx cancel): construct cancelled ctx; call returns within ≤ 100 ms with `errors.Is(err, context.Canceled)` true (mirror Phase 2 ConcurrentAccess pattern)
  </behavior>
  <action>
    Create new file `languages_test.go` in repo root with `package openholidays`.
    Imports: `context`, `errors`, `net/http`, `net/http/httptest`, `os`, `path/filepath`, `testing`, `time`, plus testify `assert` and `require`.
    Declare `const languagesFixtureCapturedAt = "YYYY-MM-DD"` set to the actual capture date used in Task 1.
    Declare exactly one `func TestClient_Languages(t *testing.T)` per Gold Rule 3 (one TestXxx per exported production function `Languages`). Top-level `t.Parallel()`.
    Subtests under `t.Run`:
      "happy path returns ≥14 languages from fixture" — read testdata/languages.json bytes; httptest server writes them with Content-Type application/json; `c := NewClient(WithBaseURL(srv.URL))`; `got, err := c.Languages(context.Background(), LanguagesRequest{})`; `require.NoError`; `require.GreaterOrEqual(len(got), 14)`; iterate entries to confirm at least one has `IsoCode == "en"` (or any case-insensitive variant — use `strings.EqualFold` if needed).
      "optional LanguageIsoCode sent as query param when non-empty" — handler asserts `r.URL.Query().Get("languageIsoCode") == "en"`; client calls with `LanguagesRequest{LanguageIsoCode: "EN"}` (uppercase input → validateLanguage lowercases to "en").
      "invalid LanguageIsoCode wraps ErrInvalidLanguage without HTTP" — `c := NewClient(WithBaseURL("http://example.invalid"))`; call with `LanguagesRequest{LanguageIsoCode: "x"}`; assert `errors.Is(err, ErrInvalidLanguage)`.
      "4xx returns *APIError with Path /Languages" — mirror the Countries 4xx subtest, substituting path string.
      "5xx with title fallback" — same shape as Countries.
      "malformed JSON returns decode error (not sentinel)" — server returns 200 + `[not valid json`; assert err is non-nil and `errors.As(err, &apiErr) == false` and `errors.Is(err, ErrEmptyResponse) == false`.
      "ctx cancel returns context.Canceled" — server handler sleeps `time.Sleep(200*time.Millisecond)` before writing; caller passes `ctx` from `context.WithCancel`, calls `cancel()` immediately, calls `c.Languages(ctx, LanguagesRequest{})`; asserts `errors.Is(err, context.Canceled)`.
    Every subtest except those that explicitly inspect runtime state calls `t.Parallel()`.
    Header godoc on the file mirrors `countries_test.go` lines 1-32 with Languages substitutions.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go test -race -run TestClient_Languages -count=1 ./...</automated>
  </verify>
  <done>
    `go test -race -run TestClient_Languages -count=1 ./...` exits 0. languages_test.go declares exactly one `TestClient_Languages` function (`grep -c "func TestClient_Languages" languages_test.go` returns 1). All 7 subtests above exist (`grep -c 't.Run(' languages_test.go` returns ≥ 7).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Client.Languages | LanguagesRequest crosses the boundary; LanguageIsoCode shape must be validated. |
| Client.Languages → upstream | /Languages response treated as untrusted; size-capped + decoded via doJSONGet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-InputVal-Lang | Tampering / DoS | languages.go (validator call) | mitigate | validateLanguage runs before any HTTP when LanguageIsoCode is non-empty; rejects non-ASCII / Unicode-fold tricks (W-01 hardening from Phase 2). |
| T-3-DoS-EmptyQueryParam | DoS (self-inflicted) | languages.go query builder | mitigate | Empty-string guard `if req.LanguageIsoCode != ""` prevents `?languageIsoCode=` empty-value param (D-55). |
| T-3-DoS-OverSize | DoS | doJSONGet (inherited) | mitigate | 10 MiB cap inherited from Plan 1 / Phase 2 D-24. |
| T-3-InfoDisc-APIErrorBody | InfoDisc | doJSONGet (inherited) | mitigate | 4 KiB APIError.Body cap inherited from Plan 1 / Phase 1 D-17. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test -race -run TestClient_Languages -count=1 ./...` exits 0.
- testdata/languages.json is valid JSON with ≥ 14 entries.
- `grep -c "type LanguagesRequest" languages.go` returns 1.
- `grep -c "doJSONGet\[\[\]Language\]" languages.go` returns 1.
- `grep -v '^//' languages.go | grep -c "validateLanguage"` returns ≥ 1.
</verification>

<success_criteria>
- ENDPT-02 satisfied: Languages endpoint exists and is callable.
- TEST-01 partially satisfied: 4 error paths covered in the languages_test.go table.
- Fixture sanity-asserted: ≥ 14 languages in the happy-path test.
- No hidden HTTP for invalid LanguageIsoCode (validator short-circuits).
</success_criteria>

<output>
After completion, write `.planning/phases/03-endpoints-helpers/03-02-SUMMARY.md` recording: capture date for testdata/languages.json, the actual language count observed live, the 7 subtest names, and any deviation from the locked decisions D-51..D-56.
</output>
