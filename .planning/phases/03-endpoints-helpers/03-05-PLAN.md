---
phase: 03-endpoints-helpers
plan: 05
type: execute
wave: 1
depends_on:
  - 03-01
  - 03-04
files_modified:
  - school_holidays.go
  - school_holidays_test.go
  - testdata/school_holidays_pl_2025.json
autonomous: true
requirements:
  - ENDPT-05
  - TEST-01
must_haves:
  truths:
    - "SchoolHolidays(ctx, SchoolHolidaysRequest) ([]Holiday, error) returns school holidays for a country in a date window"
    - "Required CountryIsoCode + ValidFrom + ValidTo validated before HTTP"
    - "Optional LanguageIsoCode validated only when non-empty"
    - "Optional SubdivisionCode and GroupCode pass through verbatim (no client-side validators per D-56)"
    - "Endpoint dispatches through doJSONGet[[]Holiday] then calls validateHolidays for post-decode schema-drift detection"
    - "Happy-path PL 2025 fixture has 7 school periods (D-70 sanity assertion); at least one is the ferie zimowe entry whose Subdivisions include the PL-SL code (drives the Plan 6 Holiday.IsInRegion test)"
    - "Per-endpoint TEST-01 covers 4 error paths"
    - "D-51: SchoolHolidays(ctx, SchoolHolidaysRequest) follows the uniform (ctx, Request) endpoint shape established in Plan 01"
    - "D-53: SchoolHolidaysRequest.CountryIsoCode/LanguageIsoCode/SubdivisionCode/GroupCode field names follow the code-field naming convention (match upstream countryIsoCode/languageIsoCode/subdivisionCode/groupCode query params)"
    - "D-54: SchoolHolidaysRequest{CountryIsoCode, ValidFrom, ValidTo, LanguageIsoCode, SubdivisionCode, GroupCode} exposes every upstream-supported filter for /SchoolHolidays including the GroupCode cohort filter (CL-13)"
    - "D-64: school_holidays.go file is scoped to the SchoolHolidaysRequest type + SchoolHolidays endpoint method (≤30 lines) + endpoint-specific post-decode validateHolidays call (reused from Plan 04); HTTP pipeline lives only in request.go's doJSONGet"
    - "D-69: schoolHolidaysPL2025FixtureCapturedAt const recorded in school_holidays_test.go and included in fixture-drift test failure messages"
    - "D-71: testdata/school_holidays_pl_2025.json fixture path matches the D-71 root-level testdata/ convention"
  artifacts:
    - path: school_holidays.go
      provides: "SchoolHolidays endpoint method + SchoolHolidaysRequest type"
      contains: "type SchoolHolidaysRequest struct"
    - path: school_holidays_test.go
      provides: "TestClient_SchoolHolidays with table-driven subtests"
    - path: testdata/school_holidays_pl_2025.json
      provides: "Live-captured PL 2025 school holidays fixture (7 entries; includes ferie zimowe with PL-SL subdivision)"
  key_links:
    - from: "school_holidays.go"
      to: "request.go"
      via: "doJSONGet[[]Holiday] + validateHolidays post-decode call"
      pattern: "doJSONGet\\[\\[\\]Holiday\\]"
    - from: "school_holidays.go"
      to: "validate.go"
      via: "validateCountry + validateDateRange + optional validateLanguage"
      pattern: "validateDateRange\\("
---

<objective>
Ship the `SchoolHolidays` endpoint — siblings the `PublicHolidays` endpoint with one extra optional field (GroupCode) for Polish ferie cohort filtering. Reuses `validateHolidays` from Plan 4 (already added to request.go).

Purpose: Covers REQUIREMENTS.md ENDPT-05. Final missing endpoint.

Output: `school_holidays.go` + `school_holidays_test.go` + `testdata/school_holidays_pl_2025.json`. Depends on Plan 4 for `validateHolidays` and `ErrMalformedResponse` to exist; depends on Plan 1 for `doJSONGet`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-endpoints-helpers/03-CONTEXT.md
@.planning/phases/03-endpoints-helpers/03-RESEARCH.md
@.planning/phases/03-endpoints-helpers/03-PATTERNS.md
@public_holidays.go
@public_holidays_test.go
@request.go
@countries.go
@validate.go
@types.go
@date.go
@errors.go

<interfaces>
Key types and contracts.

From public_holidays.go (Plan 4 sibling — School mirrors Public with one extra optional field):
- type PublicHolidaysRequest struct { CountryIsoCode string; ValidFrom Date; ValidTo Date; LanguageIsoCode string; SubdivisionCode string }
- func (c *Client) PublicHolidays(ctx context.Context, req PublicHolidaysRequest) ([]Holiday, error)

From request.go (validateHolidays added by Plan 4):
- func validateHolidays(hs []Holiday, path string) error  // wraps ErrMalformedResponse

Target shape:
- type SchoolHolidaysRequest struct { CountryIsoCode string; ValidFrom Date; ValidTo Date; LanguageIsoCode string; SubdivisionCode string; GroupCode string }
- func (c *Client) SchoolHolidays(ctx context.Context, req SchoolHolidaysRequest) ([]Holiday, error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture testdata/school_holidays_pl_2025.json from live API and write school_holidays.go</name>
  <files>school_holidays.go, testdata/school_holidays_pl_2025.json</files>
  <read_first>
    - public_holidays.go (Plan 4 — the exact analog this endpoint mirrors; copy the dispatch shape verbatim and add one optional field)
    - request.go (doJSONGet + validateHolidays signatures)
    - validate.go (validateCountry, validateLanguage, validateDateRange)
    - types.go (Holiday struct + SubdivisionRef — the Subdivisions field is what Plan 6's Holiday.IsInRegion will read from)
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"User Constraints — D-54" lines 36-46 (the Request struct shape for School) + §"Live API verification — SchoolHolidays" + Assumption A2
    - .planning/phases/03-endpoints-helpers/03-PATTERNS.md §"`school_holidays.go`" lines 339-358 (the verbatim skeleton; the only diff from public_holidays.go is the GroupCode optional field and one extra if-block in the query builder)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md D-54 (SchoolHolidaysRequest field set including GroupCode), D-56 (GroupCode is shape-tolerant — pass through to upstream, no client-side validator), D-70 (sanity assertions: 7 periods in PL 2025; at least one is ferie zimowe with PL-SL subdivision)
  </read_first>
  <behavior>
    - Endpoint signature is (c *Client) SchoolHolidays(ctx context.Context, req SchoolHolidaysRequest) ([]Holiday, error)
    - Empty CountryIsoCode → ErrInvalidCountry without HTTP
    - validFrom > validTo → ErrInvalidDateRange without HTTP
    - window > 3 years → ErrDateRangeTooLarge without HTTP
    - Empty LanguageIsoCode → no language query param sent
    - Empty SubdivisionCode → no subdivisionCode query param sent
    - Empty GroupCode → no groupCode query param sent
    - Non-empty GroupCode → passed through verbatim (no client-side validator per D-56)
    - 200 + valid JSON array of Holidays → returns []Holiday after validateHolidays passes
    - Plan 6's Holiday.IsInRegion will be tested against this fixture's ferie zimowe entry whose Subdivisions includes "PL-SL"
  </behavior>
  <action>
    Capture fixture: issue a GET to https://openholidaysapi.org/SchoolHolidays with query params countryIsoCode=PL, validFrom=2025-01-01, validTo=2025-12-31, languageIsoCode=PL. Pretty-print the response and write to testdata/school_holidays_pl_2025.json. Verify post-capture sanity: exactly 7 entries (per D-70); at least one entry has the literal Polish name "ferie zimowe" in its name array (with case-insensitive comparison ok) AND that entry's `subdivisions` array contains an object whose code is "PL-SL". If any sanity check fails, abort and surface the upstream issue.

    Create new file school_holidays.go in repo root with `package openholidays`.
    Imports: context, net/url.
    Declare type SchoolHolidaysRequest as a struct with six fields in order: CountryIsoCode string, ValidFrom Date, ValidTo Date, LanguageIsoCode string, SubdivisionCode string, GroupCode string. godoc explains: same required/optional semantics as PublicHolidaysRequest plus GroupCode (optional; pass-through; expected values are upstream-defined cohort codes like "A", "B", "C", "D" for Polish ferie).
    Declare func (c *Client) SchoolHolidays(ctx context.Context, req SchoolHolidaysRequest) ([]Holiday, error).
    Method body mirrors public_holidays.go exactly, with one extra optional-field block:
      Call validateCountry on req.CountryIsoCode capturing canonical form.
      Call validateDateRange on (req.ValidFrom, req.ValidTo).
      Build q := url.Values{} with required country/validFrom/validTo Set calls.
      If LanguageIsoCode non-empty: validateLanguage + q.Set("languageIsoCode", canonicalLang).
      If SubdivisionCode non-empty: q.Set("subdivisionCode", req.SubdivisionCode).
      If GroupCode non-empty: q.Set("groupCode", req.GroupCode) — no validator per D-56.
      Call doJSONGet[[]Holiday](ctx, c, "/SchoolHolidays", q); on error return nil + wrapped error.
      Call validateHolidays(holidays, "/SchoolHolidays"); on error return nil + wrapped error.
      Return holidays, nil.
    godoc on the method mirrors PublicHolidays godoc with substitutions for School and a note about the optional GroupCode used to filter Polish ferie cohorts (RESEARCH.md Assumption A2 — DE entries carry groups in the response but PL entries do not; the GroupCode field is the query-param filter, not a response-field predicate).
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go build ./...</automated>
  </verify>
  <done>
    school_holidays.go compiles. testdata/school_holidays_pl_2025.json contains exactly 7 entries; at least one entry is named "ferie zimowe" and carries a subdivision whose code is "PL-SL". `grep -c "type SchoolHolidaysRequest" school_holidays.go` returns 1. `grep -c "doJSONGet\[\[\]Holiday\]" school_holidays.go` returns 1. `grep -c "validateHolidays" school_holidays.go` returns 1. `grep -c "GroupCode" school_holidays.go` returns ≥ 2 (field declaration + query builder usage).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write school_holidays_test.go covering happy path + 4 error paths + GroupCode query contract</name>
  <files>school_holidays_test.go</files>
  <read_first>
    - public_holidays_test.go (Plan 4 sibling — copy the structure with substitutions)
    - school_holidays.go (just-created)
    - testdata/school_holidays_pl_2025.json (just-captured)
    - .planning/phases/03-endpoints-helpers/03-RESEARCH.md §"Pattern 3" lines 388-499 (test pattern)
    - .planning/phases/03-endpoints-helpers/03-CONTEXT.md D-69, D-70 (`schoolHolidaysPL2025FixtureCapturedAt` const + sanity assertions: 7 periods; one is ferie zimowe with PL-SL subdivision)
  </read_first>
  <behavior>
    - Test 1 (happy path): handler asserts URL.Path /SchoolHolidays + query countryIsoCode=PL, validFrom=2025-01-01, validTo=2025-12-31; client receives exactly 7 Holidays; iterating finds at least one entry whose NameFor("pl") matches "ferie zimowe" (case-insensitively) and whose Subdivisions slice contains an entry with Code "PL-SL"
    - Test 2 (validation: empty CountryIsoCode): assert ErrInvalidCountry
    - Test 3 (validation: validFrom after validTo): assert ErrInvalidDateRange
    - Test 4 (validation: invalid LanguageIsoCode): assert ErrInvalidLanguage
    - Test 5 (4xx): assert *APIError with Path /SchoolHolidays
    - Test 6 (5xx with title fallback)
    - Test 7 (malformed JSON): assert err non-nil; not any sentinel
    - Test 8 (ctx cancel): assert errors.Is err context.Canceled
    - Test 9 (GroupCode reaches the wire): handler asserts r.URL.Query().Get("groupCode") == "A" when caller passes GroupCode: "A"
    - Test 10 (SubdivisionCode reaches the wire): handler asserts r.URL.Query().Get("subdivisionCode") == "PL-SL" when caller passes it
    - Test 11 (ErrMalformedResponse: zero StartDate): server returns one Holiday with default-zero startDate (use startDate "0001-01-01" — the literal Date zero round-trips to this) — assert errors.Is err ErrMalformedResponse
  </behavior>
  <action>
    Create new file school_holidays_test.go in repo root with `package openholidays`.
    Imports: context, errors, net/http, net/http/httptest, os, path/filepath, strings, testing, time, plus testify assert and require.
    Declare const schoolHolidaysPL2025FixtureCapturedAt set to the Task 1 capture date.
    Declare exactly one func TestClient_SchoolHolidays(t *testing.T) per Gold Rule 3.
    Top-level t.Parallel().
    Subtests under t.Run mirroring the behavior list. For the happy path: read testdata/school_holidays_pl_2025.json; handler inspects r.URL.Query() to verify countryIsoCode=PL, validFrom=2025-01-01, validTo=2025-12-31, URL.Path=/SchoolHolidays; client calls c.SchoolHolidays with the matching request. Assert len(got) == 7. Loop through got to find an entry whose NameFor("pl") equals "ferie zimowe" via strings.EqualFold AND whose Subdivisions contains an entry with Code "PL-SL" (case-insensitive compare). require.NotNil on the found entry with descriptive failure ("Plan 6 Holiday.IsInRegion test depends on this fixture entry").
    For validation-only subtests: use WithBaseURL("http://example.invalid").
    For GroupCode and SubdivisionCode query-contract subtests: handler asserts the specific query value; respond with an empty array (so validateHolidays returns nil — empty slice is valid).
    For the malformed-response subtest: server returns the literal JSON `[{"id":"bad","startDate":"0001-01-01","endDate":"2025-12-31","type":"School","name":[{"language":"en","text":"X"}],"nationwide":true,"regionalScope":"National","temporalScope":"FullDay"}]` and asserts errors.Is err ErrMalformedResponse (the "0001-01-01" round-trips to Date zero — validateHolidays catches IsZero StartDate).
    Every subtest except those inspecting runtime state calls t.Parallel().
    File-header godoc mirrors public_holidays_test.go.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays &amp;&amp; go test -race -run TestClient_SchoolHolidays -count=1 ./...</automated>
  </verify>
  <done>
    `go test -race -run TestClient_SchoolHolidays -count=1 ./...` exits 0. school_holidays_test.go declares exactly one TestClient_SchoolHolidays function. `grep -c 't.Run(' school_holidays_test.go` returns ≥ 11.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Client.SchoolHolidays | SchoolHolidaysRequest crosses boundary; country/dates required; language/subdivision/group optional. All shape-validated client-side. |
| Client.SchoolHolidays → upstream | /SchoolHolidays response treated as untrusted both at byte-cap level (doJSONGet) AND post-decode shape level (validateHolidays). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-InputVal-Country | Tampering / DoS | school_holidays.go (validateCountry) | mitigate | validateCountry runs before HTTP. |
| T-3-InputVal-DateRange | Tampering / DoS | school_holidays.go (validateDateRange) | mitigate | validateDateRange enforces from ≤ to AND window ≤ 3 calendar years. |
| T-3-InputVal-Lang | Tampering | school_holidays.go (validateLanguage) | mitigate | validateLanguage runs when LanguageIsoCode is non-empty. |
| T-3-DoS-EmptyQueryParam | DoS (self-inflicted) | school_holidays.go query builder | mitigate | Empty-string guards for LanguageIsoCode, SubdivisionCode, GroupCode (D-55). |
| T-3-Tampering-MalformedHoliday | Tampering | request.go validateHolidays (inherited from Plan 4) | mitigate | Post-decode invariant check wraps ErrMalformedResponse — same as PublicHolidays. |
| T-3-DoS-OverSize | DoS | doJSONGet (inherited) | mitigate | 10 MiB cap inherited. |
| T-3-InfoDisc-APIErrorBody | InfoDisc | doJSONGet (inherited) | mitigate | 4 KiB APIError.Body cap inherited. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test -race -run TestClient_SchoolHolidays -count=1 ./...` exits 0.
- testdata/school_holidays_pl_2025.json contains 7 entries; one is "ferie zimowe" with a PL-SL subdivision.
- `grep -c "type SchoolHolidaysRequest" school_holidays.go` returns 1.
- `grep -c "GroupCode" school_holidays.go` returns ≥ 2.
- `grep -c "doJSONGet\[\[\]Holiday\]" school_holidays.go` returns 1.
</verification>

<success_criteria>
- ENDPT-05 satisfied: SchoolHolidays endpoint exists with all six Request fields.
- TEST-01 partially satisfied: 4 error paths + 2 query-contract assertions + 4 validation-only subtests + ErrMalformedResponse subtest covered.
- D-70 sanity assertions verified live: 7 school periods in PL 2025 fixture; ferie zimowe entry with PL-SL subdivision (which Plan 6 will exercise).
- The seam between PL school-holiday data and Plan 6's Holiday.IsInRegion test is established by this plan's fixture contract.
</success_criteria>

<output>
After completion, write `.planning/phases/03-endpoints-helpers/03-05-SUMMARY.md` recording: capture date for testdata/school_holidays_pl_2025.json, the literal Polish name string for the ferie zimowe entry (so Plan 6 can match against it), the subdivision codes carried by the ferie zimowe entry, the 11 subtest names, and any deviation from D-51..D-66.
</output>
