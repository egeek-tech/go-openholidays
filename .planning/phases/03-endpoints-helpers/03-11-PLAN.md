---
phase: 03-endpoints-helpers
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - school_holidays_test.go
  - .planning/PROJECT.md
autonomous: true
gap_closure: true
gap_ids:
  - SC2-COMBINED
requirements:
  - HELP-02
  - ENDPT-05
must_haves:
  truths:
    - "A new subtest TestClient_SchoolHolidays_IsInRegion_FerieZimowe in school_holidays_test.go loads testdata/school_holidays_pl_2025.json, decodes it via the SchoolHolidays endpoint via httptest, locates ALL FOUR Ferie zimowe entries, and calls Holiday.IsInRegion(\"PL-SL\") on each one"
    - "The subtest asserts: cohort 1 (StartDate 2025-01-20, EndDate 2025-02-02, subdivisions include PL-SL) returns IsInRegion(\"PL-SL\") == true; cohorts 2 (2025-01-27..2025-02-09), 3 (2025-02-03..2025-02-16), and 4 (2025-02-17..2025-03-02) all return IsInRegion(\"PL-SL\") == false"
    - "The integrated assertion satisfies ROADMAP SC#2 literal wording: 'correctly identifies the Śląskie ferie zimowe cohort while excluding the other three regional cohorts' — a single test scenario exercising both the positive and the three negative paths against the golden fixture (was previously compositional across two unrelated test functions, per VERIFICATION.md SC2-COMBINED gap)"
    - "go test -race -count=1 -run TestClient_SchoolHolidays_IsInRegion_FerieZimowe ./... passes"
    - "The Gold-Rule-3 narrow exception — a second top-level TestXxx tied to Client.SchoolHolidays — is recorded in PROJECT.md Key Decisions as CL-14 so future maintainers reading CLAUDE.md or PROJECT.md do not believe Gold Rule 3 was silently broken; the function godoc additionally references CL-14 + the SC2-COMBINED gap_id"
  artifacts:
    - path: school_holidays_test.go
      provides: "New SC#2-integrated subtest exercising all 4 ferie zimowe cohorts against Holiday.IsInRegion('PL-SL')"
      contains: "IsInRegion_FerieZimowe"
    - path: .planning/PROJECT.md
      provides: "CL-14 entry in Key Decisions documenting the Gold-Rule-3 narrow exception for the SC#2-integrated characterization test"
      contains: "CL-14"
  key_links:
    - from: "school_holidays_test.go SC2-COMBINED subtest"
      to: "testdata/school_holidays_pl_2025.json"
      via: "os.ReadFile + httptest.NewServer serving the fixture, then c.SchoolHolidays decodes"
      pattern: "school_holidays_pl_2025\\.json"
    - from: "school_holidays_test.go SC2-COMBINED subtest"
      to: "holiday.go Holiday.IsInRegion"
      via: "direct method call on each of the 4 ferie zimowe Holiday values"
      pattern: "\\.IsInRegion\\(\"PL-SL\"\\)"
    - from: ".planning/PROJECT.md CL-14 entry"
      to: "school_holidays_test.go TestClient_SchoolHolidays_IsInRegion_FerieZimowe"
      via: "Key Decisions row naming the test function + SC#2 + scope-limit clause"
      pattern: "CL-14"
---

<objective>
Close SC2-COMBINED: add a single integrated subtest that satisfies the literal ROADMAP SC#2 wording — "correctly identifies the Śląskie ferie zimowe cohort while excluding the other three regional cohorts" — in ONE test scenario against the golden fixture, rather than splitting the proof across two unrelated test functions. Also record the narrow Gold-Rule-3 exception in PROJECT.md so the rule does not appear to be silently broken.

Purpose: Closes the medium-severity SC2-COMBINED gap. The behavior is already correct (verified by VERIFICATION.md: existing tests prove fixture has 4 ferie zimowe cohorts, only cohort 1 carries PL-SL; existing tests prove `Holiday.IsInRegion` matches Subdivisions case-insensitively and returns false otherwise). The gap is test-coverage shape: ROADMAP SC#2 implies a single integrated assertion, and the current proof is compositional across `school_holidays_test.go` (finds the PL-SL entry) and `holiday_test.go` (verifies the IsInRegion logic in isolation).

Output: One new top-level test function inside `school_holidays_test.go` exercising all four ferie zimowe cohorts. The function is a "characterization test" — it does not change production behavior; it makes the SC#2 compositional proof an integrated single-scenario proof. A new `CL-14` entry in `.planning/PROJECT.md` Key Decisions captures the Gold-Rule-3 narrow exception with explicit scope-limit wording so the exception cannot generalize.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-endpoints-helpers/03-VERIFICATION.md
@.planning/phases/03-endpoints-helpers/03-CONTEXT.md
@school_holidays.go
@school_holidays_test.go
@holiday.go
@testdata/school_holidays_pl_2025.json

<interfaces>
Fixture contents (verified by inspection of testdata/school_holidays_pl_2025.json — 7 entries total, 4 Ferie zimowe):

| Index | StartDate | EndDate | Name (PL) | Subdivisions include PL-SL? |
|-------|-----------|---------|-----------|---------------------------|
| 0 | 2025-01-20 | 2025-02-02 | Ferie zimowe | YES — subs include PL-SL, PL-LU, PL-WP, PL-MA, PL-KP |
| 1 | 2025-01-27 | 2025-02-09 | Ferie zimowe | NO — subs are PL-WN, PL-PK |
| 2 | 2025-02-03 | 2025-02-16 | Ferie zimowe | NO — subs are PL-ZP, PL-DS, PL-MZ, PL-OP |
| 3 | 2025-02-17 | 2025-03-02 | Ferie zimowe | NO — subs are PL-SK, PL-PD, PL-LB, PL-PM, PL-LD |
| 4 | 2025-04-17 | 2025-04-22 | Wiosenna przerwa świąteczna | (different name; do not match — subtest filters by name "Ferie zimowe") |
| 5 | 2025-06-28 | 2025-08-31 | Ferie letnie | (different name) |
| 6 | 2025-12-22 | 2025-12-31 | Zimowa przerwa świąteczna | (different name) |

Cohort numbering: VERIFICATION.md SC2-COMBINED uses 1-based cohort numbering (cohort1 = index 0 = the one WITH PL-SL; cohorts 2/3/4 = indices 1/2/3 = the three WITHOUT PL-SL).

From school_holidays.go (current signature):
- func (c *Client) SchoolHolidays(ctx context.Context, req SchoolHolidaysRequest) ([]Holiday, error)
- SchoolHolidaysRequest fields used in tests: CountryIsoCode, ValidFrom, ValidTo, LanguageIsoCode (optional), SubdivisionCode (optional), GroupCode (optional)

From holiday.go:
- func (h Holiday) IsInRegion(code string) bool — empty code returns false; Nationwide returns true; otherwise iterates Subdivisions case-insensitively via strings.EqualFold

From school_holidays_test.go (existing happy-path subtest at lines 49-116 already loads the fixture, builds the httptest server, and decodes via c.SchoolHolidays — the new subtest can follow the SAME pattern; in fact it can reuse the same fixture path constant schoolHolidaysPL2025FixtureCapturedAt at line 34).

PROJECT.md Key Decisions ledger (`.planning/PROJECT.md` section `## Key Decisions`, lines 83 onwards):
- Currently contains CL-01 through CL-06 as table rows (verified by `grep -n 'CL-0[1-6]' .planning/PROJECT.md`).
- CL-12 and CL-13 are referenced from `.planning/ROADMAP.md` line 74 and `.planning/phases/03-endpoints-helpers/03-04-SUMMARY.md` lines 49-50 but are NOT YET present as rows in the PROJECT.md table — recording them is outside this plan's scope.
- CL-07 through CL-11 are referenced from prior phase CONTEXT/SUMMARY files but are likewise not yet in PROJECT.md.
- The next available CL number for THIS plan is **CL-14**. Do not reuse CL-12 or CL-13 (they are taken by the ErrMalformedResponse sentinel and the expose-every-filter pattern respectively).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integrated SC#2 subtest exercising all 4 ferie zimowe cohorts against IsInRegion("PL-SL")</name>
  <files>school_holidays_test.go</files>
  <read_first>
    - school_holidays_test.go (full file — note the existing TestClient_SchoolHolidays function structure; the new subtest follows the same httptest+os.ReadFile pattern used by the existing "happy path PL 2025" subtest at lines 49-116)
    - testdata/school_holidays_pl_2025.json (confirm the 4 ferie zimowe entries match the table in the <interfaces> block above; in particular confirm index 0 has PL-SL in its subdivisions array and indices 1, 2, 3 do NOT)
    - holiday.go (confirm Holiday.IsInRegion signature: `func (h Holiday) IsInRegion(code string) bool`)
    - .planning/phases/03-endpoints-helpers/03-VERIFICATION.md SC2-COMBINED entry (expected_after_fix: `go test -race -count=1 -run TestClient_SchoolHolidays_IsInRegion_FerieZimowe ./... passes; subtest exercises all 4 cohorts`)
    - CLAUDE.md Gold Rule 3 (one TestXxx per exported production function; every case under t.Run; testify required)
  </read_first>
  <action>
**Decision on placement (Gold Rule 3 compliance):** The VERIFICATION.md expected_after_fix names the test as `TestClient_SchoolHolidays_IsInRegion_FerieZimowe` and refers to it as a "subtest". Gold Rule 3 says exactly one TestXxx per exported production function. `SchoolHolidays` already has `TestClient_SchoolHolidays`. The new scenario is a SchoolHolidays scenario exercising IsInRegion (already covered by TestHoliday_IsInRegion). The cleanest Gold-Rule-3-compatible placement is therefore a NEW t.Run subtest INSIDE the existing `TestClient_SchoolHolidays` function, named with the descriptor `"IsInRegion('PL-SL') matches only cohort 1 of the 4 ferie zimowe cohorts (SC#2 integrated)"`. The expected_after_fix `-run` target then becomes `TestClient_SchoolHolidays/IsInRegion` (the leading filter matches by TestXxx, the trailing slash filters subtests by substring).

**However**, since VERIFICATION.md explicitly names `TestClient_SchoolHolidays_IsInRegion_FerieZimowe` as a top-level test name, the executor MUST honor that exact target (because the orchestrator's gap-closure verification will use that exact `-run` command). To satisfy BOTH the VERIFICATION.md expected_after_fix AND Gold Rule 3:
- Add the new top-level function `func TestClient_SchoolHolidays_IsInRegion_FerieZimowe(t *testing.T)` in school_holidays_test.go.
- Document in the function's godoc comment that this is an SC#2-integrated CHARACTERIZATION test (not a new production-function test), explicitly referencing the SC2-COMBINED gap_id from 03-VERIFICATION.md and the Gold-Rule-3 narrow exception (CL-14 — recorded in PROJECT.md by Task 2 of this plan).

**Implementation:**

Add a new top-level test function in `school_holidays_test.go`. Place it AFTER the existing `TestClient_SchoolHolidays` function (i.e. append at end of file). Use the same imports already declared at the top of the file (`os`, `path/filepath`, `net/http`, `net/http/httptest`, `context`, `testing`, `time`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`). No new imports needed.

Function body:

```
// TestClient_SchoolHolidays_IsInRegion_FerieZimowe is the SC#2-integrated
// characterization test: it loads testdata/school_holidays_pl_2025.json
// through the SchoolHolidays endpoint (httptest server), locates all four
// "Ferie zimowe" entries, and calls Holiday.IsInRegion("PL-SL") on each.
// Per the golden PL 2025 fixture, exactly cohort 1 (Jan 20 - Feb 2)
// carries PL-SL in its subdivisions; cohorts 2-4 do not.
//
// This is a Gold-Rule-3 narrow exception recorded as CL-14 in
// .planning/PROJECT.md Key Decisions: there is no new exported
// production function being tested here. The function exists to close
// the SC2-COMBINED gap from .planning/phases/03-endpoints-helpers/
// 03-VERIFICATION.md by providing a single integrated test scenario
// that satisfies the literal ROADMAP SC#2 wording — "correctly
// identifies the Śląskie ferie zimowe cohort while excluding the other
// three regional cohorts" — without splitting the proof across two
// unrelated test functions (school_holidays_test.go for "fixture has
// the entry" + holiday_test.go for "IsInRegion logic is correct").
// The CL-14 exception is explicitly scoped to THIS test only; future
// SchoolHolidays-related tests must continue to live inside the single
// TestClient_SchoolHolidays t.Run tree per Gold Rule 3.
func TestClient_SchoolHolidays_IsInRegion_FerieZimowe(t *testing.T) {
    t.Parallel()

    body, err := os.ReadFile(filepath.Join("testdata", "school_holidays_pl_2025.json"))
    require.NoError(t, err, "fixture missing — re-capture from live API per Plan 03-05 Task 1 (captured %s)",
        schoolHolidaysPL2025FixtureCapturedAt)

    srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
        w.Header().Set("Content-Type", "application/json")
        _, _ = w.Write(body)
    }))
    t.Cleanup(srv.Close)

    c := NewClient(WithBaseURL(srv.URL))
    holidays, err := c.SchoolHolidays(context.Background(), SchoolHolidaysRequest{
        CountryIsoCode: "PL",
        ValidFrom:      NewDate(2025, time.January, 1),
        ValidTo:        NewDate(2025, time.December, 31),
    })
    require.NoError(t, err)
    require.Len(t, holidays, 7, "fixture must have 7 entries (captured %s)",
        schoolHolidaysPL2025FixtureCapturedAt)

    // Collect every "Ferie zimowe" entry from the fixture in StartDate
    // order. Per the golden fixture, there are exactly 4 such entries
    // (the four regional cohorts of the Polish school winter break).
    var ferieZimowe []Holiday
    for _, h := range holidays {
        if h.NameFor("pl") == "Ferie zimowe" {
            ferieZimowe = append(ferieZimowe, h)
        }
    }
    require.Len(t, ferieZimowe, 4,
        "fixture must contain exactly 4 Ferie zimowe cohorts (captured %s) — re-capture if upstream drifted",
        schoolHolidaysPL2025FixtureCapturedAt)

    // Cohort 1 (2025-01-20 .. 2025-02-02) is the only cohort carrying
    // PL-SL. The other three cohorts do not. Subtests below give a
    // per-cohort name so a CI failure points at the exact cohort that
    // regressed.
    type cohortCase struct {
        idx      int
        start    Date
        end      Date
        wantPLSL bool
    }
    cohorts := []cohortCase{
        {0, NewDate(2025, time.January, 20), NewDate(2025, time.February, 2), true},
        {1, NewDate(2025, time.January, 27), NewDate(2025, time.February, 9), false},
        {2, NewDate(2025, time.February, 3), NewDate(2025, time.February, 16), false},
        {3, NewDate(2025, time.February, 17), NewDate(2025, time.March, 2), false},
    }

    for _, tc := range cohorts {
        t.Run(formatCohortName(tc.idx, tc.start, tc.end, tc.wantPLSL), func(t *testing.T) {
            t.Parallel()
            require.Less(t, tc.idx, len(ferieZimowe),
                "fixture has fewer Ferie zimowe entries than expected — cohort index %d out of range", tc.idx)
            h := ferieZimowe[tc.idx]
            // Cohort identity check: dates must match the expected
            // window for the named cohort. If the upstream re-ordered
            // entries this fires loudly instead of silently testing
            // the wrong cohort.
            assert.True(t, h.StartDate.Equal(tc.start),
                "cohort %d StartDate mismatch: want %s, got %s — fixture may have re-ordered (captured %s)",
                tc.idx, tc.start, h.StartDate, schoolHolidaysPL2025FixtureCapturedAt)
            assert.True(t, h.EndDate.Equal(tc.end),
                "cohort %d EndDate mismatch: want %s, got %s — fixture may have re-ordered (captured %s)",
                tc.idx, tc.end, h.EndDate, schoolHolidaysPL2025FixtureCapturedAt)
            // The actual SC#2 assertion:
            got := h.IsInRegion("PL-SL")
            assert.Equal(t, tc.wantPLSL, got,
                "cohort %d IsInRegion(\"PL-SL\") = %v, want %v (subdivisions=%v)",
                tc.idx, got, tc.wantPLSL, h.Subdivisions)
        })
    }
}

// formatCohortName builds a stable, human-readable subtest name from the
// cohort index, date window, and expected IsInRegion("PL-SL") result.
// Kept package-private and adjacent to the only call site.
func formatCohortName(idx int, start Date, end Date, wantPLSL bool) string {
    expected := "excludes_PL-SL"
    if wantPLSL {
        expected = "matches_PL-SL"
    }
    return fmt.Sprintf("cohort_%d_%s_to_%s_%s",
        idx+1, start.Time.Format("2006-01-02"), end.Time.Format("2006-01-02"), expected)
}
```

Add `"fmt"` to the existing import block at the top of school_holidays_test.go (the existing block already imports `context`, `errors`, `net/http`, `net/http/httptest`, `os`, `path/filepath`, `strings`, `testing`, `time`, and the testify packages; `fmt` is NOT currently imported by this file — verify by reading the existing import block; if `fmt` IS already imported, do not duplicate).

Caveat — `Date.Time.Format` versus a direct method: `Date` embeds `time.Time` per Phase 1 D-10. Accessing `start.Time.Format(...)` works because of the embedded field. If a `(d Date) Format(layout string) string` method exists on Date itself (check date.go before writing the helper), prefer the named method form `start.Format(...)`. Either compiles; pick the one that matches existing usage in the codebase by grepping `grep -nE '\.Format\("2006-01-02"\)' *.go` before writing.

Do NOT modify the existing `TestClient_SchoolHolidays` function. Do NOT add the new subtest INSIDE TestClient_SchoolHolidays (the planner considered this option and rejected it — see the "Decision on placement" note above; the VERIFICATION.md expected_after_fix names the function explicitly).
  </action>
  <verify>
    <automated>grep -cE 'func TestClient_SchoolHolidays_IsInRegion_FerieZimowe\(t \*testing\.T\)' school_holidays_test.go | grep -qx 1 && awk '/^func TestClient_SchoolHolidays_IsInRegion_FerieZimowe/,/^}/' school_holidays_test.go | grep -cE '\.IsInRegion\("PL-SL"\)' | { read -r n; [ "$n" -ge 1 ]; } && go vet ./... && go build ./... && go test -race -count=1 -v -run TestClient_SchoolHolidays_IsInRegion_FerieZimowe ./... | grep -cE '^    --- PASS: TestClient_SchoolHolidays_IsInRegion_FerieZimowe/cohort_' | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'func TestClient_SchoolHolidays_IsInRegion_FerieZimowe\(t \*testing\.T\)' school_holidays_test.go` returns exactly 1 (the new top-level test function exists with the exact name VERIFICATION.md expected_after_fix references).
    - `grep -cE 'func TestClient_SchoolHolidays\(t \*testing\.T\)' school_holidays_test.go` returns exactly 1 (the existing TestClient_SchoolHolidays remains; not renamed, not deleted).
    - `awk '/^func TestClient_SchoolHolidays_IsInRegion_FerieZimowe/,/^}/' school_holidays_test.go | grep -cE '\.IsInRegion\("PL-SL"\)'` returns at least 1 (the actual SC#2 assertion call lives INSIDE the new function body, not somewhere else in the file). This replaces the previous looser file-wide grep, per checker WARNING #4.
    - `grep -cE '"Ferie zimowe"' school_holidays_test.go` returns at least 1 (the filter to locate the 4 cohorts; the existing TestClient_SchoolHolidays uses case-insensitive matching, so the new test may either match case-insensitively or exact-match — exact-match is fine because the fixture string IS literally "Ferie zimowe" with that exact casing).
    - The new function uses 4 t.Run subtests (one per cohort): `go test -race -count=1 -v -run TestClient_SchoolHolidays_IsInRegion_FerieZimowe ./...` output contains exactly 4 `--- PASS: TestClient_SchoolHolidays_IsInRegion_FerieZimowe/cohort_` lines. This is the authoritative gate that all 4 cohorts are exercised and pass with the expected boolean outcomes (cohort_1 = matches_PL-SL, cohorts 2-4 = excludes_PL-SL).
    - The function godoc references `CL-14` and `SC2-COMBINED` so a future maintainer reading the test file can find the exception's rationale without consulting external docs.
    - `go vet ./...` exits 0.
    - `go build ./...` exits 0.
    - `go test -race -count=1 -run TestClient_SchoolHolidays_IsInRegion_FerieZimowe ./...` exits 0 (the new test passes).
    - `go test -race -count=1 ./...` continues to exit 0 (no regressions; the new test is additive).
  </acceptance_criteria>
  <done>
    A new top-level function `TestClient_SchoolHolidays_IsInRegion_FerieZimowe` exists in `school_holidays_test.go` and exercises all 4 Ferie zimowe cohorts from `testdata/school_holidays_pl_2025.json` against `Holiday.IsInRegion("PL-SL")`. Cohort 1 asserts true; cohorts 2, 3, 4 assert false. The Gold-Rule-3 narrow exception is documented in the function godoc with explicit reference to the SC2-COMBINED gap_id AND to CL-14. The exact `-run` target named in VERIFICATION.md expected_after_fix runs green, and the 4-subtest pass count is asserted by the `<verify>` gate.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the Gold-Rule-3 narrow exception as CL-14 in .planning/PROJECT.md Key Decisions</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full file — locate the `## Key Decisions` section, currently starting at line ~83; confirm the table format `| CL-XX: ... | rationale | outcome |` and that CL-01 through CL-06 are the existing rows; CL-07 through CL-13 are NOT YET recorded as rows even though referenced in phase CONTEXT/SUMMARY files)
    - .planning/phases/03-endpoints-helpers/03-VERIFICATION.md (the SC2-COMBINED gap entry — the new CL-14 row references this as the source)
    - CLAUDE.md (Gold Rule 3 — confirms the rule's literal text so the exception's wording can be precise about what is being excepted)
    - school_holidays_test.go post-Task-1 (so the CL-14 row can reference the exact function name `TestClient_SchoolHolidays_IsInRegion_FerieZimowe`)
  </read_first>
  <action>
Append a new row to the `## Key Decisions` table in `.planning/PROJECT.md`. The row uses **CL-14** (the next available CL number; CL-12 is taken by ErrMalformedResponse and CL-13 by the expose-every-filter pattern, both referenced in ROADMAP.md and 03-04-SUMMARY.md though not yet present as rows in PROJECT.md — recording those is outside this plan's scope; do NOT renumber the new entry to CL-07 or any lower available slot, even though earlier slots are physically empty in the table, because the existing references in CONTEXT.md, SUMMARY files, and ROADMAP.md fix the meaning of CL-07 through CL-13 to other decisions).

Add the row IMMEDIATELY AFTER the existing CL-06 row (line ~103 per the current file), inside the same table — DO NOT create a new table, a new section, or a new subheading. The row must use the same `| Decision | Rationale | Outcome |` three-column format as CL-01 through CL-06.

Row content (use this exact text — the column dividers `|` must align with the existing table; markdown tables are tolerant of variable cell width, so absolute column-edge alignment is not required, but the three `|` separators per row are mandatory):

```
| CL-14: Narrow Gold-Rule-3 exception — a second top-level TestXxx tied to `Client.SchoolHolidays` (`TestClient_SchoolHolidays_IsInRegion_FerieZimowe` in `school_holidays_test.go`) is permitted ALONGSIDE the existing `TestClient_SchoolHolidays` in order to satisfy the literal ROADMAP SC#2 wording — "correctly identifies the Śląskie ferie zimowe cohort while excluding the other three regional cohorts" — as a single integrated test scenario against the golden fixture rather than a compositional proof split across `school_holidays_test.go` (fixture-has-the-entry) and `holiday_test.go` (IsInRegion-logic-is-correct). Scope: THIS test only — future SchoolHolidays-related tests must continue to live inside the single `TestClient_SchoolHolidays` t.Run tree per Gold Rule 3. | Gold Rule 3 ("one TestXxx per exported production function") has no documented exception mechanism in CLAUDE.md or PROJECT.md, so without this entry future maintainers would believe the rule is silently broken when they discover the second test function. Recording the narrow exception keeps Gold Rule 3 enforceable for every other test by making the one allowed exception explicit, named, and scope-limited. Source: `.planning/phases/03-endpoints-helpers/03-VERIFICATION.md` gap `SC2-COMBINED` + `.planning/phases/03-endpoints-helpers/03-11-PLAN.md` Task 1. | Adopted in Phase 3 (gap closure) |
```

**Important formatting constraints:**
- The row is a single line in the markdown source (long lines are fine inside a table cell; markdown does not wrap them).
- Do NOT break the row across multiple lines with `\n` or `<br>` — that would corrupt the table.
- Use backticks `` ` `` around code identifiers (`TestClient_SchoolHolidays_IsInRegion_FerieZimowe`, `Client.SchoolHolidays`, file paths, etc.) inside the table cells.
- The "Outcome" column reads `Adopted in Phase 3 (gap closure)` to match the existing convention (`Adopted in Phase 1` for CL-01 through CL-06).
- Do NOT touch any other line in PROJECT.md. The diff for this file must show exactly ONE inserted line: the new CL-14 row.

After inserting the row, verify by reading the table back: the line count of the `## Key Decisions` table grows by exactly 1, the new line is the 7th data row (after CL-01 through CL-06), and CL-14 is present.
  </action>
  <verify>
    <automated>grep -cE '^\| CL-14:' .planning/PROJECT.md | grep -qx 1 && grep -F 'TestClient_SchoolHolidays_IsInRegion_FerieZimowe' .planning/PROJECT.md | grep -cF 'CL-14' | { read -r n; [ "$n" -ge 1 ]; } && grep -cE '^\| CL-0[1-6]:' .planning/PROJECT.md | grep -qx 6 && grep -cE '^\| CL-' .planning/PROJECT.md | { read -r n; [ "$n" -eq 7 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\| CL-14:' .planning/PROJECT.md` returns exactly 1 (the new row exists at table-row position).
    - The CL-14 row contains the literal substring `TestClient_SchoolHolidays_IsInRegion_FerieZimowe` (the function name being excepted is named explicitly).
    - The CL-14 row contains the literal substring `SC2-COMBINED` OR `SC#2` (so a future reader can trace the exception to its source gap).
    - The CL-14 row contains scope-limit language — at minimum the words "THIS test only" or "this one test only" or "scope: THIS test" — so the exception cannot be cited to justify a third TestXxx later.
    - `grep -cE '^\| CL-0[1-6]:' .planning/PROJECT.md` returns 6 (the existing CL-01 through CL-06 rows are untouched).
    - `grep -cE '^\| CL-' .planning/PROJECT.md` returns 7 (the table now has exactly 7 CL-prefixed data rows: CL-01..CL-06 + CL-14).
    - `git diff .planning/PROJECT.md` shows ONLY the inserted CL-14 row — no other line in PROJECT.md is modified, deleted, or reformatted.
    - The markdown table still renders cleanly — the row has exactly three `|` separators between the four `|` column-edge markers (a 3-column row: Decision | Rationale | Outcome).
  </acceptance_criteria>
  <done>
    `.planning/PROJECT.md` now records the Gold-Rule-3 narrow exception as CL-14 with explicit scope-limit wording. Future maintainers reading PROJECT.md (or grepping CLAUDE.md for an exception mechanism and following the reference) will find this entry instead of believing the rule was silently broken. The diff is a single inserted line.
  </done>
</task>

</tasks>

<verification>
- Task 1 grep + awk gates confirm the new function exists with the exact name VERIFICATION.md mandates AND that the `.IsInRegion("PL-SL")` call lives inside the new function body (not loosely anywhere in the file), per checker WARNING #4.
- Task 1 `-v -run` gate confirms exactly 4 cohort subtests pass — the authoritative gate that all 4 cohorts are exercised with the expected boolean outcomes.
- Task 2 grep gates confirm the CL-14 row is present, sole, and well-formed in PROJECT.md.
- The 4 subtests (one per cohort) cover both the positive (cohort 1) and negative (cohorts 2, 3, 4) paths against the golden fixture in a SINGLE test scenario — satisfying the literal SC#2 wording per the VERIFICATION.md SC2-COMBINED summary.
- The cohort-identity assertions (StartDate / EndDate match the expected windows) make this a regression-stable test: if the upstream re-orders entries the test fails loudly rather than silently testing the wrong cohort.
- The CL-14 entry closes the documentation gap raised by checker WARNING #3: the exception now has a discoverable, scope-limited home in PROJECT.md rather than only living in the test file's godoc.
</verification>

<success_criteria>
- A new function `TestClient_SchoolHolidays_IsInRegion_FerieZimowe` exists in `school_holidays_test.go`.
- It contains exactly 4 t.Run subtests, one per cohort, named by date window and expected match outcome.
- All 4 subtests PASS under `go test -race -count=1 -run TestClient_SchoolHolidays_IsInRegion_FerieZimowe ./...`.
- The full suite `go test -race -count=1 ./...` exits 0.
- `.planning/PROJECT.md` Key Decisions table contains a new CL-14 row recording the Gold-Rule-3 narrow exception with explicit scope-limit wording, naming the exact function and the SC2-COMBINED gap_id.
- `git diff --stat` shows exactly two files: `school_holidays_test.go` and `.planning/PROJECT.md`.
</success_criteria>

<output>
Create `.planning/phases/03-endpoints-helpers/03-11-SUMMARY.md` when done. Summary must include:
- The function name and the 4 cohort-subtest names with their expected outcomes (cohort 1 true, cohorts 2/3/4 false).
- Confirmation that the SC2-COMBINED gap is closed (a single test scenario now exercises both the positive and three negative paths against the golden fixture).
- The exact CL-14 row text inserted into PROJECT.md (for traceability), and confirmation that no other line in PROJECT.md was modified.
- The Gold-Rule-3 narrow exception note (this is a characterization test, not a new-production-function test; recorded in the function godoc AND in PROJECT.md as CL-14).
</output>
</content>
</invoke>