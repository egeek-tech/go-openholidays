---
phase: 04-resilience
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - retry.go
  - retry_test.go
  - request.go
  - options.go
  - options_test.go
  - config.go
autonomous: true
requirements:
  - RESIL-01
  - RESIL-02
  - RESIL-03
  - RESIL-04
  - RESIL-05
  - TEST-05
user_setup: []

must_haves:
  truths:
    - "`c := NewClient(WithRetry(5, 250*time.Millisecond))` against a fake transport sequencing 429 → 500 → 200 issues exactly three round trips; the final `Countries(ctx)` call returns the decoded payload without error."
    - "When the fake transport sends `Retry-After: 2`, the per-attempt sleep computed by `computeBackoff` is at least 2 seconds (and ≤ `maxRetryWait`); when it sends `Retry-After: <RFC 1123 HTTP-date>`, `parseRetryAfter` returns the date-minus-now duration."
    - "When the caller's ctx cancels mid-sleep inside the retry loop, the endpoint method returns `ctx.Err()` (either `context.Canceled` or `context.DeadlineExceeded`) within ≤ 100 ms — verified via `fakeClock.Sleep` returning the ctx error."
    - "`shouldRetry` returns true for {408, 429, 500, 502, 503, 504}, for `net.Error` with `Timeout() == true`, for errors wrapping `syscall.ECONNRESET`; returns false for 400/401/403/404 and for `context.Canceled`/`context.DeadlineExceeded`."
    - "Retry exhaustion wraps the last error via `fmt.Errorf(\"openholidays: retry exhausted (%d attempts): %w\", n, lastErr)` so callers' `errors.Is(err, ErrEmptyResponse)` / `errors.As(err, &apiErr)` still match (D-77 + Phase 1 D-23)."
    - "No file named `transport_retry.go` exists; no `retryTransport` struct exists anywhere in the package (RESIL-05 — retry is in the endpoint layer, NOT a RoundTripper)."
    - "D-73: public retry surface ships exactly two options — `WithRetry(maxAttempts int, baseDelay time.Duration) Option` and `WithMaxRetryWait(d time.Duration) Option`. No `RetryConfig` struct, no per-status overrides, no `WithRetryPolicy(...)`."
    - "D-74: defaults — `WithRetry` not called means retry DISABLED; `maxAttempts <= 0` interpreted as DISABLED; `baseDelay <= 0` falls back to 250ms; `WithMaxRetryWait` not called means `maxRetryWait = 60s`."
    - "D-75: retryable conditions matrix — HTTP statuses {408, 429, 500, 502, 503, 504}; `net.Error` with `Timeout() == true`; errors wrapping `syscall.ECONNRESET` via `errors.Is`; NEVER `context.Canceled` / `context.DeadlineExceeded`."
    - "D-76: `Retry-After` handling — `parseRetryAfter` accepts integer seconds (`strconv.Atoi`) and RFC 7231 HTTP-date (`http.ParseTime`); per-attempt sleep is `max(retryAfter, jitterDelay)` capped at `maxRetryWait`; absent header falls back to full-jitter `time.Duration(rand.Int64N(int64(min(maxRetryWait, baseDelay << attempt))))`."
  artifacts:
    - path: "retry.go"
      provides: "retryConfig (filled-in struct), shouldRetry, parseRetryAfter, computeBackoff helpers + named defaults."
      contains: "func shouldRetry"
      min_lines: 80
    - path: "retry_test.go"
      provides: "TestShouldRetry, TestParseRetryAfter, TestComputeBackoff, TestRetry_E2E_429Then500Then200, TestRetry_HonorsRetryAfterSeconds, TestRetry_HonorsRetryAfterDate, TestRetry_CtxCancel, TestRetry_NeverRetriesCtxErrors, TestRetry_DeterministicClock, TestRetry_NotARoundTripper."
    - path: "request.go"
      provides: "doJSONGet retry loop wrapping c.http.Do + 4xx/5xx error-build (D-77)."
      contains: "for attempt := 0; attempt < maxAttempts"
    - path: "options.go"
      provides: "WithRetry(maxAttempts int, baseDelay time.Duration) Option + WithMaxRetryWait(d time.Duration) Option."
      exports: ["WithRetry", "WithMaxRetryWait"]
    - path: "config.go"
      provides: "retryConfig struct gains maxAttempts, baseDelay, maxWait fields (Plan 02 declared the empty stub; this plan fills it)."
  key_links:
    - from: "request.go::doJSONGet retry loop"
      to: "retry.go::shouldRetry + computeBackoff + parseRetryAfter"
      via: "function calls inside the for-loop wrapping c.http.Do(req)"
      pattern: "shouldRetry\\(resp, httpErr\\)"
    - from: "request.go::doJSONGet ctx-aware sleep"
      to: "client.go::Client.sleepFunc (Plan 02 default = ctxSleep)"
      via: "c.sleepFunc(ctx, delay)"
      pattern: "c\\.sleepFunc\\(ctx, "
    - from: "retry.go::computeBackoff"
      to: "client.go::Client.rand (Plan 02 default = newClientRand via ChaCha8)"
      via: "rnd.Int64N(int64(capped))"
      pattern: "rnd\\.Int64N\\("
---

<objective>
Implement the retry layer per D-77 verbatim. Retry lives INSIDE `doJSONGet` (NOT a RoundTripper — RESIL-05). The retry loop wraps `c.http.Do(req)` + the 4xx/5xx error-build path; the decode path runs ONCE after the loop succeeds. Honors `Retry-After` (integer seconds + RFC 7231 HTTP-date), supports full-jitter exponential backoff via `math/rand/v2` (Pitfall RETRY-4), and is ctx-aware (Pitfall RETRY-3).

**Purpose:** Cover RESIL-01..05 + TEST-05. Six new helpers (retryConfig fields filled, shouldRetry, parseRetryAfter, computeBackoff, plus two named consts `defaultMaxRetryWait`/`defaultBaseDelay`). Two new exported options (`WithRetry`, `WithMaxRetryWait`). Ten new tests in `retry_test.go` (one TestXxx per exported retry-related prod function plus the E2E + ctx-cancel composition tests).

**Output:** Three new helper functions + one struct fill-in + two exported options + one rewritten section of `doJSONGet` (lines 75-88 today). Tests use `fakeClock` (Plan 01) and `roundTripperFunc` (existing in `transport_header_test.go:19`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-resilience/04-CONTEXT.md
@.planning/phases/04-resilience/04-RESEARCH.md
@.planning/phases/04-resilience/04-PATTERNS.md
@.planning/phases/04-resilience/04-VALIDATION.md
@.planning/phases/04-resilience/04-01-SUMMARY.md
@.planning/phases/04-resilience/04-02-SUMMARY.md

<interfaces>
<!-- Existing doJSONGet retry-injection point (request.go lines 75-88 BEFORE Plan 02): -->
```go
resp, err := c.http.Do(req)
if err != nil {
    return zero, fmt.Errorf("openholidays: GET %s: %w", path, err)
}
defer func() {
    _, _ = io.Copy(io.Discard, io.LimitReader(resp.Body, maxResponseBytes+1))
    _ = resp.Body.Close()
}()
if resp.StatusCode >= 400 {
    return zero, buildAPIError(resp, path)
}
```

Replace with the retry-wrapped pipeline per D-77 + RESEARCH.md lines 385-426 (the full block below in Task 3 spells out the literal pseudocode).

<!-- Phase 2 Client struct (after Plan 02) exposes these fields needed by retry: -->
- `c.rand *rand.Rand` (math/rand/v2) — set by newClientRand in NewClient (Plan 02 Task 1)
- `c.nowFunc func() time.Time` — set to time.Now in NewClient (Plan 02 Task 1)
- `c.sleepFunc func(context.Context, time.Duration) error` — set to ctxSleep in NewClient (Plan 02 Task 1)
- `c.retry retryConfig` — populated by WithRetry/WithMaxRetryWait (this plan, Task 2)

<!-- Existing test-helper reuse from transport_header_test.go lines 14-24: -->
```go
type roundTripperFunc func(*http.Request) (*http.Response, error)
func (f roundTripperFunc) RoundTrip(r *http.Request) (*http.Response, error) { return f(r) }
```
Phase 4 retry tests REUSE this — do NOT redeclare.

<!-- fakeClock from Plan 01 (clock_test.go): -->
- `fc := newFakeClock(t)` where `t time.Time`.
- `fc.Now()` returns the current fake time.
- `fc.Advance(d time.Duration)` advances.
- `fc.Sleep(ctx, d) error` synchronously advances by d unless ctx is cancelled.

<!-- RESEARCH.md verified upstream behavior: -->
- `http.ParseTime(h)` accepts RFC 1123, RFC 850, ANSI C asctime per RFC 7231 §7.1.1.1.
- `errors.Is(err, syscall.ECONNRESET)` works on Linux/macOS/Windows because Go's syscall package abstracts the value.
- `math/rand/v2.NewChaCha8(seed)` produces a goroutine-safe source.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create retry.go with retryConfig (filled), shouldRetry, parseRetryAfter, computeBackoff helpers</name>
  <read_first>
    - .planning/phases/04-resilience/04-RESEARCH.md lines 316-382 (Pattern 1 retry helpers — full verbatim implementation)
    - .planning/phases/04-resilience/04-CONTEXT.md D-73 to D-78 (locked semantics for retry config + matrix + Retry-After + per-Client rand)
    - .planning/phases/04-resilience/04-PATTERNS.md lines 30-110 (retry.go file shape; helper-functions-module pattern from request.go)
    - request.go lines 1-33 (named constants pattern to replicate at top of retry.go)
    - config.go (verify retryConfig stub from Plan 02 — must be replaced with the filled struct in this task)
  </read_first>
  <files>retry.go, config.go</files>
  <behavior>
    - `shouldRetry(nil, &net.OpError{Op: "read", Err: syscall.ECONNRESET})` → true (transport reset).
    - `shouldRetry(&http.Response{StatusCode: 429}, nil)` → true; same for 408, 500, 502, 503, 504.
    - `shouldRetry(&http.Response{StatusCode: 400}, nil)` → false; same for 401, 403, 404, 200, 201.
    - `shouldRetry(nil, context.Canceled)` → false; same for `context.DeadlineExceeded`.
    - `shouldRetry(nil, &fakeNetErr{timeout: true})` → true (where fakeNetErr implements net.Error with `Timeout()==true`).
    - `parseRetryAfter("2", time.Now())` → `(2*time.Second, true)`.
    - `parseRetryAfter("Wed, 21 Oct 2026 07:28:00 GMT", time.Date(2026,10,21,7,27,0,0,time.UTC))` → `(time.Minute, true)` approximately (60s difference).
    - `parseRetryAfter("garbage", time.Now())` → `(0, false)`.
    - `parseRetryAfter("Wed, 21 Oct 2020 07:28:00 GMT", time.Date(2026,10,21,7,28,0,0,time.UTC))` → `(0, false)` (past date — Pitfall 9 guard).
    - `parseRetryAfter("", time.Now())` → `(0, false)`.
    - `computeBackoff(0, 0, retryConfig{baseDelay: 100*time.Millisecond, maxWait: time.Second}, rnd)` returns a duration in `[0, 100ms)` (jitter < baseDelay << 0 = baseDelay; capped at maxWait).
    - `computeBackoff(3, 0, retryConfig{baseDelay: 100*time.Millisecond, maxWait: time.Second}, rnd)` returns a duration in `[0, 800ms)` (baseDelay << 3 = 800ms; ≤ maxWait).
    - `computeBackoff(0, 5*time.Second, retryConfig{baseDelay: 100*time.Millisecond, maxWait: 10*time.Second}, rnd)` returns `5*time.Second` (retryAfter beats jitter; ≤ maxWait).
    - `computeBackoff(0, 5*time.Second, retryConfig{baseDelay: 100*time.Millisecond, maxWait: 2*time.Second}, rnd)` returns `2*time.Second` (retryAfter capped at maxWait per D-76).
  </behavior>
  <action>
    **config.go edit:** Update the `retryConfig` stub Plan 02 declared (`type retryConfig struct{}`) to the FULL struct per D-77:
    ```go
    type retryConfig struct {
        maxAttempts int
        baseDelay   time.Duration
        maxWait     time.Duration
    }
    ```
    Move the type declaration FROM `config.go` TO `retry.go` so all retry types live in one file (matches the one-concern-per-file convention from PATTERNS.md). Remove the placeholder from config.go entirely.

    **Create retry.go** at repo root. File-level godoc per PATTERNS.md lines 36-48 (analog request.go lines 1-12): cite D-77, name the four exports/helpers, name the requirements covered (RESIL-01..05, TEST-05).

    Package `openholidays`. Imports (alphabetized stdlib): `errors`, `math/rand/v2` (no alias — package name is `rand`), `net`, `net/http`, `strconv`, `syscall`, `time`. (Do NOT use `math/rand` v1 — use v2 per Pitfall RETRY-4.)

    Declare named constants at the TOP of the file (analog request.go lines 25-33):
    - `const defaultBaseDelay = 250 * time.Millisecond` — D-74 default; cited in godoc.
    - `const defaultMaxRetryWait = 60 * time.Second` — D-74 default; cited in godoc (Pitfall RETRY-2 AWS guidance).

    Declare `retryConfig` (moved from config.go) with three fields per D-77. Godoc starts with "retryConfig is the unexported retry policy carried by Client.retry. Zero-value (maxAttempts == 0) means retry is disabled."

    Declare `shouldRetry(resp *http.Response, err error) bool` — pure function. Godoc cites D-75 matrix verbatim. Body matches RESEARCH.md lines 332-349:
    - if err != nil: check `errors.As(err, &netErr)` then `netErr.Timeout()` for the net.Error path (use `errors.As` because net.Error is the interface — `errors.Is` does not work for interface assertion); check `errors.Is(err, syscall.ECONNRESET)` for the conn-reset path; otherwise return false.
    - if resp != nil: switch on `resp.StatusCode` for {408, 429, 500, 502, 503, 504} → return true; default → false.
    - if both nil (defensive — shouldn't happen but be explicit): return false.
    - **Critical:** the retry loop in Task 3 handles `context.Canceled` / `context.DeadlineExceeded` BEFORE calling shouldRetry (via the loop-top `ctx.Err()` check). shouldRetry sees ctx errors only as raw errors and returns false (no net.Error wrapping, no syscall match) — this is the correct behavior per D-75 ("ctx errors NEVER retried").

    Declare `parseRetryAfter(h string, now time.Time) (time.Duration, bool)` — pure function. Godoc cites D-76 + Pitfall RETRY-2 verbatim formula + Pitfall 9 negative-duration guard. Body matches RESEARCH.md lines 351-364:
    - if `h == ""` return `(0, false)`.
    - try `strconv.Atoi(h)`; if success AND `s >= 0` return `(time.Duration(s)*time.Second, true)`.
    - try `http.ParseTime(h)`; if success AND `d := t.Sub(now); d > 0` return `(d, true)`.
    - else return `(0, false)`.

    Declare `computeBackoff(attempt int, retryAfter time.Duration, cfg retryConfig, rnd *rand.Rand) time.Duration` — pure function. Godoc cites D-76 + RESIL-01 full-jitter (AWS canonical) + D-78 per-Client rnd source. Body matches RESEARCH.md lines 366-381:
    - `capped := cfg.maxWait`
    - `if exp := cfg.baseDelay << attempt; exp < capped { capped = exp }` — exponential ceiling.
    - `if capped <= 0 { capped = time.Millisecond }` — defensive against zero baseDelay.
    - `jitter := time.Duration(rnd.Int64N(int64(capped)))` — full jitter (uniform random in [0, capped)).
    - `if retryAfter > 0 && retryAfter > jitter { return min(retryAfter, cfg.maxWait) }` — Retry-After wins over jitter, capped at maxWait per D-76.
    - else `return jitter`.
    Note: `min` is a Go 1.21+ builtin (Assumption A2 — project floor is Go 1.23, safe).

    All four exports (or three since retryConfig is unexported, plus two named consts) — every godoc starts with the symbol name (Gold Rule 1).
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && go build ./... && go vet ./...</automated>
  </verify>
  <done>
    retry.go compiles; `retryConfig` is fully declared with three fields; the three helper functions are pure (no Client dependency); `defaultBaseDelay` and `defaultMaxRetryWait` are named consts at the top; `go build ./...` and `go vet ./...` exit 0; `gofmt -l retry.go config.go` produces no output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add WithRetry + WithMaxRetryWait options + TestWithRetry/TestWithMaxRetryWait</name>
  <read_first>
    - options.go (whole file after Plan 02 — to find the insertion point after WithStrictDecoding)
    - options_test.go lines 146-172 (TestWithTimeout shape — direct analog for duration-flavored option tests)
    - retry.go (Task 1 output — for the retryConfig field names)
    - .planning/phases/04-resilience/04-CONTEXT.md D-73 + D-74 (retry option semantics)
    - .planning/phases/04-resilience/04-PATTERNS.md lines 541-583 (WithRetry shape)
  </read_first>
  <files>options.go, options_test.go</files>
  <behavior>
    - `NewClient(WithRetry(5, 100*time.Millisecond))` sets `c.retry.maxAttempts == 5` and `c.retry.baseDelay == 100*time.Millisecond`.
    - `NewClient(WithRetry(0, 100*time.Millisecond))` leaves `c.retry.maxAttempts == 0` (retry stays disabled — defensive symmetry per D-74).
    - `NewClient(WithRetry(3, 0))` sets `c.retry.maxAttempts == 3` and `c.retry.baseDelay == defaultBaseDelay` (250ms — fallback per D-74).
    - `NewClient(WithRetry(3, -1*time.Second))` sets `c.retry.baseDelay == defaultBaseDelay` (negative → fallback per D-74).
    - `NewClient()` (no WithRetry) leaves `c.retry.maxAttempts == 0` (disabled per D-74).
    - `NewClient(WithMaxRetryWait(10*time.Second))` sets `c.retry.maxWait == 10*time.Second`.
    - `NewClient(WithMaxRetryWait(0))` sets `c.retry.maxWait == defaultMaxRetryWait` (60s — fallback per D-74).
    - `NewClient()` (no WithMaxRetryWait) leaves `c.retry.maxWait == 0` (the zero value — interpreted in `computeBackoff` correctly because retry is disabled when `maxAttempts == 0`, so `maxWait` is irrelevant; but if a caller passes only `WithRetry` and not `WithMaxRetryWait`, the maxWait field is still 0 and `computeBackoff` reads it as the cap. To avoid a "0 cap means jitter is 0" bug, `WithRetry` MUST also set `maxWait` to `defaultMaxRetryWait` when its own assignment runs and `cfg.retry.maxWait == 0`).
  </behavior>
  <action>
    **options.go edit:** Append two new exported options after `WithStrictDecoding` (Plan 02):

    `WithRetry(maxAttempts int, baseDelay time.Duration) Option`:
    ```go
    func WithRetry(maxAttempts int, baseDelay time.Duration) Option {
        return func(cfg *clientConfig) {
            cfg.retry.maxAttempts = maxAttempts
            if baseDelay > 0 {
                cfg.retry.baseDelay = baseDelay
            } else {
                cfg.retry.baseDelay = defaultBaseDelay
            }
            // Ensure maxWait has a sane default if the caller did not also call WithMaxRetryWait (D-74).
            if cfg.retry.maxWait <= 0 {
                cfg.retry.maxWait = defaultMaxRetryWait
            }
        }
    }
    ```
    Godoc starts with "WithRetry enables retry with exponential backoff + full jitter..." cites D-73/D-74/D-75/D-76/D-77/RESIL-05 + placement rationale (retry in doJSONGet, NOT a RoundTripper). Explicitly documents:
    - `maxAttempts <= 0` disables retry (D-74).
    - `baseDelay <= 0` falls back to 250ms (D-74).
    - Default maxWait = 60s when WithMaxRetryWait not also called.
    - Retryable conditions (D-75) bulleted.
    - Retry-After handling (D-76) one-line summary.
    - Placement (D-77 + RESIL-05): NOT a RoundTripper.

    `WithMaxRetryWait(d time.Duration) Option`:
    ```go
    func WithMaxRetryWait(d time.Duration) Option {
        return func(cfg *clientConfig) {
            if d > 0 {
                cfg.retry.maxWait = d
            } else {
                cfg.retry.maxWait = defaultMaxRetryWait
            }
        }
    }
    ```
    Godoc starts with "WithMaxRetryWait sets the per-attempt sleep ceiling..." cites D-74 + documents the per-attempt-not-cumulative semantics (CONTEXT.md `<specifics>` 5) — "5 attempts with 60s cap can still take ~5 min total; consumers wanting a cumulative cap supply ctx.WithTimeout".

    **options_test.go edit:** Append two new TestXxx functions:

    `TestWithRetry(t *testing.T)` — parallel; three+ subtests per the behavior block above. Match TestWithTimeout shape (table-driven over `[]tc` or three explicit subtests). Use `require.NotNil(t, cli)` precondition; `assert.Equal(t, want, cli.retry.maxAttempts)` and `assert.Equal(t, want, cli.retry.baseDelay)` verifications.

    `TestWithMaxRetryWait(t *testing.T)` — parallel; three subtests:
    1. "positive d stored verbatim": `cli := NewClient(WithMaxRetryWait(10*time.Second))`; `assert.Equal(t, 10*time.Second, cli.retry.maxWait)`.
    2. "zero d falls back to defaultMaxRetryWait": `cli := NewClient(WithMaxRetryWait(0))`; `assert.Equal(t, defaultMaxRetryWait, cli.retry.maxWait)`.
    3. "negative d falls back to defaultMaxRetryWait": `cli := NewClient(WithMaxRetryWait(-1 * time.Second))`; `assert.Equal(t, defaultMaxRetryWait, cli.retry.maxWait)`.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && go test -race -count=1 -run 'TestWithRetry|TestWithMaxRetryWait' ./...</automated>
  </verify>
  <done>
    Both new tests pass; `go build ./...` and `go vet ./...` exit 0; existing `TestNoInitOrGlobalState` remains green (`defaultBaseDelay` and `defaultMaxRetryWait` are package-level CONSTS, not vars — they are exempt from the `allowedVars` audit, which only catches `var` declarations per `internal_test.go::TestNoInitOrGlobalState`); `gofmt -l options.go options_test.go` produces no output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wrap c.http.Do in retry loop inside doJSONGet + comprehensive retry_test.go</name>
  <read_first>
    - request.go lines 75-88 (current `c.http.Do` block — the literal replacement point)
    - .planning/phases/04-resilience/04-RESEARCH.md lines 385-426 (full retry-wrapped pipeline pseudocode)
    - .planning/phases/04-resilience/04-PATTERNS.md lines 689-757 (request.go modification block — verbatim)
    - .planning/phases/04-resilience/04-CONTEXT.md D-77 (loop semantics: ctx.Err() at top, last-attempt-no-sleep, drain-and-close per Pitfall HTTP-3)
    - transport_header_test.go lines 14-24 (roundTripperFunc — reuse via same-package)
    - clock_test.go (Plan 01 — fakeClock + Sleep)
    - .planning/phases/04-resilience/04-VALIDATION.md lines 45-79 (per-task verification map — every test name listed here MUST be created with the listed `-run` target)
  </read_first>
  <files>request.go, retry_test.go</files>
  <behavior>
    - Retry loop runs at most `c.retry.maxAttempts` iterations (or 1 if maxAttempts == 0 — disabled).
    - Loop top checks `ctx.Err()`; if non-nil, return immediately (D-75: ctx errors never retried).
    - After each failed `c.http.Do`, if `shouldRetry(resp, err) == true`, drain+close `resp.Body` per Pitfall HTTP-3 (only when `resp != nil`), compute `delay := computeBackoff(...)`, then `c.sleepFunc(ctx, delay)`; if sleep returns non-nil (ctx cancelled), return that error.
    - If `shouldRetry == false`, break out of the loop (the 200 path falls through to decode; the non-retryable 4xx falls through to buildAPIError).
    - If the last attempt fails AND `maxAttempts > 1`, the error is wrapped with `fmt.Errorf("openholidays: retry exhausted (%d attempts): %w", maxAttempts, httpErr)` — `%w` keeps callers' `errors.Is(err, ErrEmptyResponse)` / `errors.As(err, &apiErr)` working.
    - If `maxAttempts == 1` (or disabled), the error wrap matches the existing Phase 3 format: `fmt.Errorf("openholidays: GET %s: %w", path, httpErr)`.
    - The successful response (200) drain-and-close defer fires exactly once after the loop exits.
    - The 4xx/5xx error-build path (`buildAPIError(resp, path)`) runs only after the loop exits with a non-retryable response.
    - The decode path runs ONCE after the loop, regardless of how many retries occurred.
    - NO file named `transport_retry.go` exists at repo root (RESIL-05 enforcement).
  </behavior>
  <action>
    **request.go edit:** Replace lines 75-88 with the retry-wrapped pipeline per RESEARCH.md lines 385-426 + PATTERNS.md lines 715-758. The replacement block follows D-77 verbatim:

    1. Declare `var resp *http.Response; var httpErr error` ABOVE the loop.
    2. Determine `maxAttempts`: if `c.retry.maxAttempts > 0` use it; else `maxAttempts := 1` (retry disabled — single attempt).
    3. `for attempt := 0; attempt < maxAttempts; attempt++` loop:
       - First: `if ctxErr := ctx.Err(); ctxErr != nil { return zero, ctxErr }` — Pitfall RETRY-3 + D-75.
       - `resp, httpErr = c.http.Do(req)`.
       - `if !shouldRetry(resp, httpErr) { break }` — non-retryable: fall through to current 4xx/decode/etc. path.
       - `if attempt == maxAttempts-1 { break }` — last attempt: surface error without sleeping.
       - If `resp != nil`: parse `retryAfter, _ := parseRetryAfter(resp.Header.Get("Retry-After"), c.nowFunc())`; drain+close per Pitfall HTTP-3 (`io.Copy(io.Discard, io.LimitReader(resp.Body, maxResponseBytes+1))`; `resp.Body.Close()`).
       - `delay := computeBackoff(attempt, retryAfter, c.retry, c.rand)`.
       - `if sleepErr := c.sleepFunc(ctx, delay); sleepErr != nil { return zero, sleepErr }` — ctx-aware sleep.
    4. After the loop:
       - `if httpErr != nil` (final attempt failed):
         - If `maxAttempts > 1`: `return zero, fmt.Errorf("openholidays: retry exhausted (%d attempts): %w", maxAttempts, httpErr)`.
         - Else: `return zero, fmt.Errorf("openholidays: GET %s: %w", path, httpErr)` (existing format preserved).
    5. The existing `defer { drain+close }` block follows AFTER the loop (rendering: deferred drain runs on the final, successful response).
    6. The existing `if resp.StatusCode >= 400 { return zero, buildAPIError(resp, path) }` line stays unchanged BELOW the loop and the defer.
    7. The existing decode block (lines 90+) is unchanged.

    Add a code-block comment at the top of the new retry loop citing D-77 + RESIL-05 (retry lives here, NOT in `transport.go`).

    **Create retry_test.go** at repo root. Package `openholidays`. Imports include: `context`, `errors`, `net`, `net/http`, `net/http/httptest`, `strconv`, `syscall`, `sync/atomic`, `testing`, `time`, plus testify. File-level godoc cites Gold Rule 3 + D-77 + TEST-05.

    Add helper near the top (test-only):
    ```go
    // fakeNetErr is a test-only net.Error implementation used to drive
    // shouldRetry's net.Error.Timeout() branch (D-75).
    type fakeNetErr struct{ timeout bool }
    func (e *fakeNetErr) Error() string   { return "fake net error" }
    func (e *fakeNetErr) Timeout() bool   { return e.timeout }
    func (e *fakeNetErr) Temporary() bool { return false } // deprecated; required by net.Error interface
    ```

    Add ten test functions (one TestXxx per concern; per Gold Rule 3 the "concern" for pure helpers is one function per helper, plus E2E composition tests for the loop):

    `TestShouldRetry(t *testing.T)` — table-driven per the behavior block in Task 1. Eight+ cases covering: 408/429/500/502/503/504 (true), 400/401/403/404 (false), 200 (false), net.Error{Timeout:true} (true), `&net.OpError{Op:"read", Err: syscall.ECONNRESET}` (true), `context.Canceled` raw (false), nil/nil defensive (false).

    `TestParseRetryAfter(t *testing.T)` — table-driven over `[]tc{name, h string, now time.Time, wantD time.Duration, wantOK bool}`. Six cases: "integer seconds", "RFC 1123 future date", "RFC 1123 past date (Pitfall 9 — returns false)", "ANSI C asctime", "empty string", "garbage". Use a fixed `now := time.Date(2026, 10, 21, 7, 27, 0, 0, time.UTC)` so date math is deterministic.

    `TestComputeBackoff(t *testing.T)` — three subtests per Task 1 behavior block. Construct rnd via `rand.New(rand.NewChaCha8([32]byte{1, 2, 3, 4, /* ... */}))` (deterministic seed for test reproducibility — test isolation matters more than production randomness here). Each subtest computes the expected range and asserts `assert.GreaterOrEqual(t, got, lower)` + `assert.Less(t, got, upper)`.

    `TestComputeBackoff_HonorsRetryAfter(t *testing.T)` — two subtests: (a) retryAfter > jitter → retryAfter wins; (b) retryAfter > maxWait → capped at maxWait.

    `TestRetry_E2E_429Then500Then200(t *testing.T)` — the marquee retry test (TEST-05 explicit). One subtest:
    - Construct `var hits atomic.Int32`. httptest.Server handler counts hits and returns:
      - hit 1: `http.StatusTooManyRequests` (429) with `Retry-After: 0` header + non-JSON body (so the body isn't trying to decode).
      - hit 2: `http.StatusInternalServerError` (500), no body.
      - hit 3: `http.StatusOK` with `[{"isoCode":"PL","name":[{"language":"en","text":"Poland"}]}]` (matches `[]Country` shape).
    - `t.Cleanup(srv.Close)`.
    - `fc := newFakeClock(time.Unix(0, 0))`.
    - `c := newClientForTest(fc.Now, fc.Sleep, WithBaseURL(srv.URL), WithRetry(5, 10*time.Millisecond), WithMaxRetryWait(time.Second))`.
    - `cs, err := c.Countries(context.Background(), CountriesRequest{})`.
    - `require.NoError(t, err)`.
    - `assert.Len(t, cs, 1)`.
    - `assert.Equal(t, int32(3), hits.Load(), "expected exactly 3 round trips: 429 → 500 → 200")`.

    `TestRetry_HonorsRetryAfterSeconds(t *testing.T)` — one subtest:
    - Server returns 503 with `Retry-After: 2` on hit 1, then 200 with valid Country JSON on hit 2.
    - `fc := newFakeClock(time.Unix(0, 0))`.
    - `c := newClientForTest(fc.Now, fc.Sleep, WithBaseURL(srv.URL), WithRetry(2, 10*time.Millisecond), WithMaxRetryWait(10*time.Second))`.
    - Before the call, capture `fcStart := fc.Now()`.
    - `_, err := c.Countries(context.Background(), CountriesRequest{})`.
    - `require.NoError(t, err)`.
    - `assert.GreaterOrEqual(t, fc.Now().Sub(fcStart), 2*time.Second, "Retry-After: 2 must produce at least 2s of fake-clock advance")`.

    `TestRetry_HonorsRetryAfterDate(t *testing.T)` — one subtest:
    - Same shape as above but `Retry-After` is `time.Now().UTC().Add(1*time.Minute).Format(http.TimeFormat)` (RFC 1123 future) AND the test injects a fakeClock with a fixed `time.Date(...)`. Actually because real httptest server time is `time.Now`, and `parseRetryAfter` is called with `c.nowFunc()` (which is `fc.Now`), the test would compute `serverTime + 1min - fakeClockStart` which is a giant duration. Instead: have the handler set `Retry-After` based on a FIXED future time, and seed the fakeClock to a time 30s BEFORE that fixed point. Specifically:
      - `fixedFuture := time.Date(2026, 10, 21, 7, 28, 0, 0, time.UTC)`.
      - Handler: `w.Header().Set("Retry-After", fixedFuture.Format(http.TimeFormat))` on hit 1.
      - `fc := newFakeClock(time.Date(2026, 10, 21, 7, 27, 30, 0, time.UTC))` — 30s before fixedFuture.
      - `c := newClientForTest(fc.Now, fc.Sleep, WithBaseURL(srv.URL), WithRetry(2, 10*time.Millisecond), WithMaxRetryWait(10*time.Minute))`.
      - After call: `assert.GreaterOrEqual(t, fc.Now().Sub(time.Date(2026,10,21,7,27,30,0,time.UTC)), 30*time.Second, "HTTP-date Retry-After must produce ≥ 30s of fake-clock advance")`.

    `TestRetry_CtxCancel(t *testing.T)` — one subtest (RESIL-04):
    - Server returns 503 unconditionally.
    - `fc := newFakeClock(time.Unix(0, 0))`.
    - Use a custom sleep that cancels the parent ctx after being entered: `sleepFn := func(ctx context.Context, d time.Duration) error { cancel(); return ctx.Err() }` where `cancel` closes over the test ctx. OR simpler: use the real `fc.Sleep` but cancel the ctx BEFORE the second retry sleep begins via a goroutine.
    - **Recommended pattern:** Use `context.WithTimeout(context.Background(), 50*time.Millisecond)` and rely on `fc.Sleep` returning `ctx.Err()` immediately when ctx is dead. (fc.Sleep checks ctx.Err() at start.)
    - `c := newClientForTest(fc.Now, fc.Sleep, WithBaseURL(srv.URL), WithRetry(5, 100*time.Millisecond), WithMaxRetryWait(time.Second))`.
    - `ctx, cancel := context.WithTimeout(context.Background(), 50*time.Millisecond); defer cancel()`.
    - `time.Sleep(60 * time.Millisecond)` — let ctx expire BEFORE the first call so the loop-top ctx.Err() check fires on attempt 0.
    - Actually for true mid-sleep cancel: keep `c.timeout` at 0 (disable SDK timeout) and use `ctx, cancel := context.WithCancel(...)`; then cancel via `time.AfterFunc(5*time.Millisecond, cancel)` BEFORE the call.
    - `_, err := c.Countries(ctx, CountriesRequest{})`.
    - `require.Error(t, err)`.
    - `assert.True(t, errors.Is(err, context.Canceled) || errors.Is(err, context.DeadlineExceeded), "expected ctx error, got %v", err)`.

    `TestRetry_NeverRetriesCtxErrors(t *testing.T)` — one subtest (D-75 / RESIL-04):
    - The simplest direct verification of "shouldRetry never claims to retry ctx errors": `assert.False(t, shouldRetry(nil, context.Canceled))`; `assert.False(t, shouldRetry(nil, context.DeadlineExceeded))`. This is a pure-function test that DOES NOT go through the HTTP path.

    `TestRetry_DeterministicClock(t *testing.T)` — one subtest (TEST-05 explicit "fake clock"):
    - Server returns 503 three times then 200.
    - `fc := newFakeClock(time.Unix(0, 0))`.
    - `c := newClientForTest(fc.Now, fc.Sleep, WithBaseURL(srv.URL), WithRetry(4, 100*time.Millisecond), WithMaxRetryWait(10*time.Second))`.
    - `start := fc.Now()`.
    - `_, err := c.Countries(context.Background(), CountriesRequest{})`.
    - `require.NoError(t, err)`.
    - `elapsed := fc.Now().Sub(start)`.
    - With full-jitter, between attempts: attempt 0 → up to 100ms, attempt 1 → up to 200ms, attempt 2 → up to 400ms. Three sleeps total (between the 4 attempts: only first 3 sleep; final attempt is the successful one with no preceding sleep — actually wait, the loop calls sleep BETWEEN attempts; with 4 attempts there are 3 sleeps; 3 of them succeeded on 4th = sleeps happen between 1→2, 2→3, 3→4). Upper bound: 100+200+400 = 700ms.
    - `assert.LessOrEqual(t, elapsed, 700*time.Millisecond, "max possible fake-clock advance is 700ms with baseDelay=100ms across 3 retry sleeps")`.
    - `assert.GreaterOrEqual(t, elapsed, time.Duration(0), "fake clock must have advanced at least 0")`.

    `TestRetry_NotARoundTripper(t *testing.T)` — one subtest (RESIL-05 enforcement / Pattern 1 audit):
    - Walks the repo root with `filepath.Walk` looking for any file named `transport_retry.go` — `require.NoFileExists(t, filepath.Join(repoRoot, "transport_retry.go"))` (testify `require.NoFileExists` exists per stretchr/testify docs).
    - Greps the package for any `retryTransport` struct declaration: `f, err := os.Open(path); s := bufio.NewScanner(f); for s.Scan() { line := s.Text(); assert.False(t, strings.Contains(line, "type retryTransport"), ...) }` across all `*.go` files in repo root.
    - Use `findRepoRoot()` helper that already exists in `internal_test.go` lines 215-230 — call it via same-package visibility.

    All test functions follow the Gold Rule 3 etiquette: `t.Parallel()` at outer level, every subtest `t.Parallel()`, table-driven where ≥ 2 cases share setup, `require` for preconditions, `assert` for verifications.

    Reuse the existing `roundTripperFunc` from `transport_header_test.go:19` — do NOT redeclare (compile error if so; same-package visibility carries it).
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && go test -race -count=1 -run 'TestShouldRetry|TestParseRetryAfter|TestComputeBackoff|TestRetry_E2E|TestRetry_HonorsRetryAfter|TestRetry_CtxCancel|TestRetry_NeverRetriesCtxErrors|TestRetry_DeterministicClock|TestRetry_NotARoundTripper' ./...</automated>
  </verify>
  <done>
    All ten retry tests pass under `-race`; the existing Phase 1-3 suite is unaffected (`go test -race -count=1 ./...` exits 0); `TestNoInitOrGlobalState` remains green; no `transport_retry.go` file exists; no `retryTransport` struct exists; the marquee `TestRetry_E2E_429Then500Then200` proves the 429→500→200 path under deterministic clock; ctx cancellation interrupts the loop within ≤ 100 ms (verified by the ctx-cancel test); `gofmt -l request.go retry_test.go` produces no output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → upstream | retry loop fires on 408/429/5xx and transport errors; honors Retry-After; ctx cancellation propagates immediately. |
| Per-Client randomness | per-Client `*rand.Rand` (D-78) prevents fleet-wide thundering herd (Pitfall RETRY-4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of Service (self-inflicted) | hostile server returns `Retry-After: 999999999` to hold the request indefinitely | mitigate | per-attempt sleep capped at `cfg.maxWait` (default 60s) via `min(retryAfter, cfg.maxWait)` in `computeBackoff` (D-76). |
| T-04-06 | Denial of Service (self-inflicted) | hostile server returns `Retry-After: <past date>` to disable backoff (run-away retry loop) | mitigate | `parseRetryAfter` guards `d := t.Sub(now); d > 0` so past dates return `(0, false)` and the loop falls back to jitter (Pitfall 9). |
| T-04-07 | Denial of Service (against upstream) | thundering herd via correlated jitter across fleet | mitigate | per-Client `*rand.Rand` seeded by `crypto/rand.Read` via `rand.NewChaCha8` (D-78); independent across instances. |
| T-04-08 | Denial of Service (caller) | retry loop ignores ctx cancellation | mitigate | loop-top `ctx.Err()` check + `c.sleepFunc` is ctx-aware (returns `ctx.Err()` on cancel during sleep); CLIENT-09 ≤100ms cancellation contract preserved. |
| T-04-09 | Denial of Service | retry double-fire when caller `*http.Client` already retries | mitigate | retry lives in `doJSONGet` (endpoint layer), NOT a RoundTripper — RESIL-05. `TestRetry_NotARoundTripper` mechanically enforces. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib only (`math/rand/v2`, `crypto/rand`, `syscall`). Per RESEARCH.md `Package Legitimacy Audit`. |
</threat_model>

<verification>
- Per-task: `go test -race -count=1 -run TestShouldRetry ./...` exits 0.
- Per-task: `go test -race -count=1 -run TestParseRetryAfter ./...` exits 0.
- Per-task: `go test -race -count=1 -run TestComputeBackoff ./...` exits 0.
- Per-task: `go test -race -count=1 -run TestRetry_E2E ./...` exits 0 (marquee TEST-05 case).
- Per-task: `go test -race -count=1 -run TestRetry_HonorsRetryAfterSeconds ./...` exits 0.
- Per-task: `go test -race -count=1 -run TestRetry_HonorsRetryAfterDate ./...` exits 0.
- Per-task: `go test -race -count=1 -run TestRetry_CtxCancel ./...` exits 0.
- Per-task: `go test -race -count=1 -run TestRetry_NeverRetriesCtxErrors ./...` exits 0.
- Per-task: `go test -race -count=1 -run TestRetry_DeterministicClock ./...` exits 0.
- Per-task: `go test -race -count=1 -run TestRetry_NotARoundTripper ./...` exits 0 (RESIL-05 audit).
- Plan gate: `go test -race -count=1 ./...` exits 0 (full unit suite green).
- AST invariant: `TestNoInitOrGlobalState` remains green — no new package-level vars; only consts (`defaultBaseDelay`, `defaultMaxRetryWait`) which are NOT flagged by the audit (only `var` declarations are audited).
- Structural: `find /data/git/private/holidays -maxdepth 2 -name 'transport_retry.go'` produces no output.
- Structural: `grep -RIn --include='*.go' 'retryTransport' /data/git/private/holidays/` produces no matches in production code (only test mentions in `TestRetry_NotARoundTripper` itself are allowed).
- Style: `gofmt -l retry.go retry_test.go options.go options_test.go request.go config.go` produces no output.
- Vet: `go vet ./...` exits 0.
</verification>

<success_criteria>
- `retry.go` exists at repo root with `retryConfig` (3 fields), `shouldRetry`, `parseRetryAfter`, `computeBackoff`, and named constants `defaultBaseDelay`, `defaultMaxRetryWait`.
- `WithRetry(int, time.Duration)` and `WithMaxRetryWait(time.Duration)` are callable exported options.
- `doJSONGet` wraps `c.http.Do(req)` in a retry loop matching D-77 verbatim.
- All ten retry tests pass; the 429→500→200 marquee test fires exactly 3 round trips and returns the decoded body.
- ctx cancellation interrupts the retry loop within ≤ 100 ms.
- No `transport_retry.go` file and no `retryTransport` struct exist in production code (RESIL-05 enforcement).
- Retry-exhausted error wraps `lastErr` via `%w` so callers' `errors.Is`/`errors.As` still work.
</success_criteria>

<output>
Create `.planning/phases/04-resilience/04-03-SUMMARY.md` when done. Include: files added/modified, retry helper signatures, the doJSONGet edit boundary (before/after lines), all ten test names + outcomes, the RESIL-05 audit confirmation (no transport_retry.go), any deviations.
</output>
