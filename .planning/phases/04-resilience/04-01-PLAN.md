---
phase: 04-resilience
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - clock_test.go
autonomous: true
requirements:
  - TEST-05
  - TEST-06
user_setup: []

must_haves:
  truths:
    - "A deterministic `fakeClock` test helper exists at repo root and is reusable across `retry_test.go` and `cache_test.go` without any further declaration."
    - "Calling `fakeClock.Sleep(ctx, d)` advances the fake clock by `d` synchronously instead of waiting on the wall clock — but returns `ctx.Err()` immediately if `ctx` is already cancelled."
    - "`fakeClock.Now()` and `fakeClock.Advance(d)` are race-safe — `go test -race -run TestFakeClock_RaceFree` exits 0."
  artifacts:
    - path: "clock_test.go"
      provides: "fakeClock struct + Now/Advance/Sleep methods (D-95); newFakeClock(t) constructor."
      contains: "type fakeClock struct"
  key_links:
    - from: "clock_test.go::fakeClock.Sleep"
      to: "Client.sleepFunc field (added in Plan 02)"
      via: "matching signature func(ctx context.Context, d time.Duration) error"
      pattern: "fakeClock.*Sleep\\(.*context.Context.*time.Duration.*\\) error"
---

<objective>
Land the deterministic time/sleep test helper that every downstream Phase 4 test (`retry_test.go`, `cache_test.go`) depends on. No production code in this plan — `clock_test.go` is a `_test.go` file so the Go toolchain excludes it from `go build` (verified — every existing repo test file uses `package openholidays` not `_test`).

**Purpose:** Unblock TEST-05 (retry under deterministic clock) and TEST-06 (cache TTL eviction without real `time.Sleep`). Implements D-95 verbatim.

**Output:** One new file `clock_test.go` at repo root. Zero changes to production code. The composition tests `TestFakeClock_RaceFree` (the leaf race-detector smoke test) lives in this file so the helper is self-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-resilience/04-CONTEXT.md
@.planning/phases/04-resilience/04-RESEARCH.md
@.planning/phases/04-resilience/04-PATTERNS.md
@.planning/phases/04-resilience/04-VALIDATION.md

<interfaces>
<!-- The fakeClock helper must match Client.sleepFunc signature (added in Plan 02). -->
<!-- Reference signature from 04-CONTEXT.md D-94:                                   -->
<!--   sleepFunc func(context.Context, time.Duration) error                         -->

From .planning/phases/04-resilience/04-RESEARCH.md lines 815-846 (verbatim implementation pattern):
```go
type fakeClock struct {
    mu  sync.Mutex
    now time.Time
}
func newFakeClock(t time.Time) *fakeClock { return &fakeClock{now: t} }
func (f *fakeClock) Now() time.Time { /* mu.Lock; return f.now; mu.Unlock */ }
func (f *fakeClock) Advance(d time.Duration) { /* mu.Lock; f.now = f.now.Add(d); mu.Unlock */ }
func (f *fakeClock) Sleep(ctx context.Context, d time.Duration) error {
    if err := ctx.Err(); err != nil { return err }
    f.Advance(d)
    return nil
}
```

Test-helper-as-same-package-file analog (file placement convention):
- transport_header_test.go:14-24 declares `roundTripperFunc` once for same-package reuse (D-50)
- transport_logging_test.go::trackedReader follows the same convention
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create clock_test.go with fakeClock helper and self-verifying race test</name>
  <read_first>
    - .planning/phases/04-resilience/04-RESEARCH.md lines 815-846 (full fakeClock implementation pattern)
    - .planning/phases/04-resilience/04-PATTERNS.md lines 469-511 (clock_test.go shape; analog placement)
    - .planning/phases/04-resilience/04-CONTEXT.md D-94 + D-95 (locked semantics)
    - transport_header_test.go lines 14-24 (test-helper-as-same-package-file convention to mirror)
  </read_first>
  <files>clock_test.go</files>
  <action>
    Create new file `clock_test.go` at repo root declaring `package openholidays` (NOT `_test` per D-94 — downstream test files in Plan 02-05 need direct field access to `Client.nowFunc`/`Client.sleepFunc`). File-level godoc cites D-95 verbatim and names the two consumer files (`retry_test.go`, `cache_test.go`) so future readers understand why this lives at repo root.

    Declare:
    - `type fakeClock struct { mu sync.Mutex; now time.Time }` — exact field order from D-95.
    - `func newFakeClock(t time.Time) *fakeClock` — constructor that returns `&fakeClock{now: t}`. Godoc names the helper.
    - `func (f *fakeClock) Now() time.Time` — `f.mu.Lock()` / `defer f.mu.Unlock()` / `return f.now`.
    - `func (f *fakeClock) Advance(d time.Duration)` — `f.mu.Lock()` / `defer f.mu.Unlock()` / `f.now = f.now.Add(d)`.
    - `func (f *fakeClock) Sleep(ctx context.Context, d time.Duration) error` — `if err := ctx.Err(); err != nil { return err }` then `f.Advance(d)` then `return nil`. Signature MUST match `Client.sleepFunc` exactly (this is the interface contract Plan 02 will consume).

    Add one self-verifying test `TestFakeClock_RaceFree(t *testing.T)` per Gold Rule 3 (one TestXxx per exported... in this case, per test helper concern):
    - `t.Parallel()` at the top.
    - Subtest "Now and Advance are race-free under 100 goroutines": construct `newFakeClock(time.Unix(0, 0))`, launch 100 goroutines, each calls `fc.Advance(time.Millisecond)` 100 times; another 100 goroutines call `fc.Now()` 100 times concurrently. Use `sync.WaitGroup`. After wait, assert `fc.Now()` equals `time.Unix(0, 0).Add(100 * 100 * time.Millisecond)` (10 seconds) — proves Advance calls did not lose under contention.
    - Subtest "Sleep returns ctx.Err() on cancelled ctx without advancing the clock": `ctx, cancel := context.WithCancel(context.Background()); cancel()`; `fc := newFakeClock(time.Unix(0, 0))`; assert `fc.Sleep(ctx, time.Hour)` returns `context.Canceled`; assert `fc.Now() == time.Unix(0, 0)` (NOT advanced).
    - Subtest "Sleep advances the clock by d and returns nil on live ctx": `fc := newFakeClock(time.Unix(0, 0))`; `err := fc.Sleep(context.Background(), 5*time.Second)`; assert `err == nil`; assert `fc.Now() == time.Unix(5, 0)`.

    Use `testify/assert` for verifications, `testify/require` for preconditions (NewClient nil checks etc. — though there is no NewClient here so require is mostly unused). Every subtest calls `t.Parallel()` immediately (Gold Rule 3 etiquette per options_test.go pattern).

    Imports (alphabetized stdlib first, then testify): `context`, `sync`, `testing`, `time` from stdlib; `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require` (require may be unused — only add it if any precondition check needs it; otherwise omit to keep `go vet` clean — verify by checking if the test body uses `require.X`).
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && go test -race -run TestFakeClock_RaceFree -count=1 ./...</automated>
  </verify>
  <done>
    clock_test.go exists at repo root; declares `package openholidays`; `go test -race -run TestFakeClock_RaceFree ./...` exits 0; `go vet ./...` exits 0; `gofmt -l clock_test.go` produces no output; `go build ./...` exits 0 unchanged (clock_test.go is excluded from production build because it's a `_test.go` file).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan ships test-only code with no network IO and no production behavior change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | fakeClock state under concurrent mutation | mitigate | `sync.Mutex` guards both `now` reads and writes; `TestFakeClock_RaceFree` exercises 100 concurrent writers and 100 readers under `-race` (D-95 + Pitfall TEST-4). |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — only stdlib + already-pinned testify. Per RESEARCH.md `Package Legitimacy Audit`: no new packages. |
</threat_model>

<verification>
- `go test -race -run TestFakeClock_RaceFree -count=1 ./...` exits 0.
- `go test -race ./...` exits 0 (no Phase 1-3 test regressed).
- `go vet ./...` exits 0 (clock_test.go is well-formed).
- `gofmt -l clock_test.go` produces no output (gofmt-clean per PROJECT.md).
- `go build ./...` exits 0 (production build unchanged — test file is excluded by Go toolchain).
- `grep -n "func init" clock_test.go` produces no output (CLIENT-10 invariant — fast complementary check).
- `internal_test.go::TestNoInitOrGlobalState` remains green (not modified — test files are exempt by design; allowlist unchanged).
</verification>

<success_criteria>
- `clock_test.go` exists in `/data/git/private/holidays/` as a same-package test helper.
- `fakeClock` exposes exactly the three methods required by D-94/D-95: `Now() time.Time`, `Advance(d time.Duration)`, `Sleep(ctx context.Context, d time.Duration) error`.
- `Sleep` signature `func(context.Context, time.Duration) error` matches the field type `Client.sleepFunc` will receive in Plan 02 — verifiable by future plan's assignment statement compiling without a conversion.
- Race-detector smoke test `TestFakeClock_RaceFree` passes under `go test -race -run TestFakeClock_RaceFree`.
</success_criteria>

<output>
Create `.planning/phases/04-resilience/04-01-SUMMARY.md` when done. Include: files added, test cases added, the race-detector run output, any deviations encountered.
</output>
