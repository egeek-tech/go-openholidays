---
phase: 04-resilience
plan: 06
type: execute
wave: 5
depends_on:
  - 04-02
  - 04-03
  - 04-04
  - 04-05
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "PROJECT.md `Key Decisions` table records the Phase 4 cache surface decision (CL-15) and the strict-decoding immutability decision (CL-16) — both append-only edits, no prior rows modified."
    - "CL-15 documents: the Cache interface (Get/Put/Close), MemoryCache exported impl, WithCache/WithCacheBackend options, and the rationale (RESIL-06 mandates a public interface for v0.2 extensibility per CONTEXT.md D-79/D-80)."
    - "CL-16 documents: WithStrictDecoding(bool) immutability (no per-call override; no runtime toggle) and the rationale (Pitfall JSON-1 + cache+strict composition correctness per D-91/D-93)."
    - "The deviation noted in Plan 04 — that CL-14 was already taken by Phase 3's Gold-Rule-3 exception — is honored: Phase 4 uses CL-15 and CL-16, not CL-14 and CL-15 as CONTEXT.md initially indicated."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table gains two new rows (CL-15 cache surface, CL-16 strict-decoding immutability)."
  key_links:
    - from: ".planning/PROJECT.md Key Decisions table"
      to: "Future maintainers reading PROJECT.md before extending the Cache interface or strict-decoding behavior"
      via: "documented rationale + source pointer to CONTEXT.md D-79/D-80/D-91"
      pattern: "CL-15.*Cache"
---

<objective>
Append two new rows (CL-15 cache, CL-16 strict-decoding) to the PROJECT.md `Key Decisions` table to document the Phase 4 deviations from the literal REQUIREMENTS.md surface. This is a docs-only plan — zero code changes, zero tests added.

**Purpose:** Per PROJECT.md convention (rows already exist for CL-01..CL-06 from Phase 1 + CL-14 from Phase 3 gap closure), every meaningful Phase 4 surface decision that adds public API or constrains future evolution gets a row. Without these rows, future maintainers (or the v0.2 planner) would re-derive the rationale from scratch.

**Output:** Two new rows in the PROJECT.md `Key Decisions` Markdown table. Plan 02-05 SUMMARYs are unchanged (each plan committed its own SUMMARY). No new files. No source code edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-resilience/04-CONTEXT.md
@.planning/phases/04-resilience/04-02-SUMMARY.md
@.planning/phases/04-resilience/04-03-SUMMARY.md
@.planning/phases/04-resilience/04-04-SUMMARY.md
@.planning/phases/04-resilience/04-05-SUMMARY.md

<interfaces>
<!-- PROJECT.md Key Decisions table (line 98 onwards). Each row format: -->
<!-- | CL-NN: <one-line summary> | <rationale + source pointer> | <adoption phase> | -->

<!-- Existing rows: -->
<!--   CL-01..CL-06: Phase 1 deviations (5 sentinels, case-insensitive validators, etc.) -->
<!--   CL-14: Phase 3 gap-closure Gold-Rule-3 exception for school_holidays_test.go -->

<!-- The CONTEXT.md says CL-14 (cache) and CL-15 (strict). But CL-14 is taken. -->
<!-- Phase 4 uses CL-15 (cache) and CL-16 (strict) instead. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append CL-15 (Cache surface) and CL-16 (StrictDecoding immutability) rows to PROJECT.md Key Decisions table</name>
  <read_first>
    - .planning/PROJECT.md (read the Key Decisions section — locate the existing table; verify the last row is CL-14; verify the table format with column headers)
    - .planning/phases/04-resilience/04-CONTEXT.md D-79, D-80 (cache surface rationale)
    - .planning/phases/04-resilience/04-CONTEXT.md D-91, D-93 (strict-decoding immutability rationale)
    - .planning/phases/04-resilience/04-04-PLAN.md `<deviations>` section (CL-14 collision note + CL-15/CL-16 reassignment)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Read `.planning/PROJECT.md` to locate the `Key Decisions` table (around line 98 per the earlier grep). Confirm: (a) the table has three columns: Decision / Rationale / Adoption phase; (b) the last existing row is CL-14 (Phase 3); (c) no CL-15 or CL-16 already exists.

    Append two new rows AT THE BOTTOM of the table (after CL-14). Use the same single-line markdown-table-row format as existing rows. Each row ends with a `| Adopted in Phase 4 |` cell.

    **CL-15 row:** Cache public-interface surface.
    - Decision cell: `CL-15: Phase 4 ships an EXPORTED Cache interface (Get/Put/Close) plus an exported NewMemoryCache(ttl) constructor and MemoryCache type, even though only the in-memory implementation ships in v0.1.0. WithCache(ttl) and WithCacheBackend(c Cache) are the two opt-in option constructors; ttl<=0 disables; WithCacheBackend(nil) is a no-op; WithCacheBackend supersedes prior WithCache (last-wins per functional-options convention). The cache key encoding is "<method> <path>?<sorted-query>" and excludes Host — per-Client cache isolation handles Pitfall CACHE-2 without putting Host in the key.`
    - Rationale cell: `RESIL-06 mandates a public Cache interface so a v0.2 consumer can supply a Redis/BoltDB backend without forking the library. Exposing the interface AND the in-memory impl in v0.1.0 commits us to that contract before any consumer requests it; the alternative (keeping Cache internal and forcing M2 to break it open) would require a breaking change. Anchored on .planning/phases/04-resilience/04-CONTEXT.md D-79/D-80 + RESEARCH.md "Pattern 4" + Pitfall CACHE-2/CACHE-4. The exported CacheHitContextKey var is also part of this surface — added to internal_test.go::allowedVars in Plan 04 (deviation from CONTEXT.md D-97 step 6 noted in 04-04-SUMMARY.md).`
    - Adoption cell: `Adopted in Phase 4`

    **CL-16 row:** Strict-decoding immutability.
    - Decision cell: `CL-16: WithStrictDecoding(bool) is OFF by default and IMMUTABLE after NewClient. No per-call override, no runtime toggle. A strict-mode client cannot be made lenient at runtime; a lenient-mode client cannot be made strict. Cache+strict composition: cache stores raw bytes; decode runs on read; therefore strict-mode applies to every cached entry on every Get. A consumer wanting "lenient for cache + strict for fresh" must run two Clients.`
    - Rationale cell: `Pitfall JSON-1 — upstream OpenHolidays schema has been observed to drift (the "quality" field on Holiday was added without spec update). Strict-by-default would break consumers on upstream additions. Immutability prevents the trap where toggling at runtime would corrupt the cache (cached bytes decoded with one mode could surface as a strict-failure after toggle). Anchored on .planning/phases/04-resilience/04-CONTEXT.md D-91/D-92/D-93 + Pitfall JSON-1.`
    - Adoption cell: `Adopted in Phase 4`

    Use the same `|` cell-separator style and trailing-pipe convention as the existing CL-01..CL-14 rows. Preserve the trailing newline of the table.

    Do NOT modify any other content of PROJECT.md. The Constraints section, Tech Stack, Conventions, GSD Workflow Enforcement etc. are unchanged. Only the Key Decisions table grows by two rows.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && grep -cE '^\| CL-(15|16):' .planning/PROJECT.md</automated>
  </verify>
  <done>
    `grep` shows exactly 2 matches (CL-15 and CL-16) — both new rows present; the existing CL-01..CL-06 + CL-14 rows are unchanged (verify via a `grep -cE '^\| CL-' .planning/PROJECT.md` showing total = 9 rows); markdown table format is preserved (no row breaks across lines); the file passes any markdown linter (or `pre-commit` hook) the repo has configured.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Docs-only plan; no production code change; no network IO. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — markdown only. Per RESEARCH.md `Package Legitimacy Audit`. |
</threat_model>

<verification>
- `grep -cE '^\| CL-15:' .planning/PROJECT.md` returns 1.
- `grep -cE '^\| CL-16:' .planning/PROJECT.md` returns 1.
- `grep -cE '^\| CL-' .planning/PROJECT.md` returns 9 (CL-01..CL-06 + CL-14 + CL-15 + CL-16).
- The existing CL-14 row content is unchanged (`grep -A0 '^| CL-14' .planning/PROJECT.md` matches the pre-Plan-06 content).
- `go test -race -count=1 ./...` still exits 0 (no Go source touched; tests unaffected).
- `go vet ./...` exits 0.
</verification>

<success_criteria>
- PROJECT.md `Key Decisions` table contains a new CL-15 row documenting the Cache public surface (interface + MemoryCache + WithCache + WithCacheBackend + key encoding + CacheHitContextKey).
- PROJECT.md `Key Decisions` table contains a new CL-16 row documenting WithStrictDecoding immutability + cache+strict composition.
- No other PROJECT.md content is modified.
- The numbering avoids the collision with Phase 3's CL-14 (school_holidays Gold-Rule-3 exception) — Phase 4 uses CL-15 and CL-16.
- Go test suite remains green (`go test -race -count=1 ./...` exits 0).
</success_criteria>

<output>
Create `.planning/phases/04-resilience/04-06-SUMMARY.md` when done. Include: the two new CL rows added (full text), the verification grep outputs, the confirmation that no other PROJECT.md content was modified, and any deviations.
</output>
