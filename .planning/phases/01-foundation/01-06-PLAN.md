---
phase: 01-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
files_modified:
  - internal_test.go
  - .planning/PROJECT.md
autonomous: true
requirements:
  - CLIENT-10
must_haves:
  truths:
    - "`TestNoInitOrGlobalState` exists in `internal_test.go` and walks every `*.go` file in the package (excluding `*_test.go`) using `go/parser` + `go/ast` (CLIENT-10 mechanical verification per RESEARCH.md §'Validation Architecture')."
    - "`TestNoInitOrGlobalState` asserts that no `func init()` is declared in any non-test `*.go` file under the repo root (CLIENT-10, Pitfall 6)."
    - "`TestNoInitOrGlobalState` asserts that the only package-level `var` declarations are the 6 sentinels: `ErrInvalidCountry`, `ErrInvalidLanguage`, `ErrDateRangeTooLarge`, `ErrInvalidDateRange`, `ErrEmptyResponse`, AND the unexported `errEmptyDate` from date.go."
    - "`grep -rn 'func init' --include='*.go' .` from the repo root returns no matches (CLIENT-10 sanity grep)."
    - "PROJECT.md `## Key Decisions` table contains 5 new rows recording CL-01 through CL-05 with rationale, source CONTEXT.md/RESEARCH.md citation, and `Outcome: Adopted in Phase 1` (closes the Phase 1 deviation paperwork before phase verification)."
    - "STATE.md `Open Todos` checklist item `Add PROJECT.md Key Decisions entries for Phase 1 deviations` is marked complete with phase 1 reference."
  artifacts:
    - path: "internal_test.go"
      provides: "TestNoInitOrGlobalState (AST-based CLIENT-10 mechanical check)"
      contains: "TestNoInitOrGlobalState"
    - path: ".planning/PROJECT.md"
      provides: "5 new Key Decisions rows recording CL-01..CL-05"
      contains: "CL-01"
  key_links:
    - from: "internal_test.go"
      to: "go/parser, go/ast"
      via: "stdlib AST walk"
      pattern: "go/parser|go/ast"
    - from: ".planning/PROJECT.md"
      to: "CONTEXT.md scope clarifications"
      via: "Key Decisions table rows"
      pattern: "CL-0[1-5]"
---

<objective>
Close out Phase 1 by (a) shipping the mechanical CLIENT-10 verifier — an AST-based test that walks every `*.go` source file in the repo and fails if it finds any `func init()` or any package-level `var` outside the locked sentinel list — and (b) recording the five Phase 1 scope-clarification decisions (CL-01..CL-05) in PROJECT.md's `Key Decisions` table so phase verification passes.

Purpose: CLIENT-10 ("no `init()` side effects, no global mutable state") is a Phase 1 invariant that must be true from the first commit and forever. Mechanical verification via AST walk catches future regressions at CI time. Recording CL-01..CL-05 in PROJECT.md fulfills CONTEXT.md's mandate ("Phase 1's executor must add the Key Decisions entries before phase verification passes") — the STATE.md open todo item and ROADMAP "Phase 1: Foundation" success criteria depend on this.

Output: `internal_test.go` (test-only AST walk; no production code) and an updated `.planning/PROJECT.md` containing the 5 new Key Decisions table rows. Optionally tick the STATE.md open-todo checkbox.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md
@/data/git/private/holidays/errors.go
@/data/git/private/holidays/date.go

<source_artifact_pointers>
- CONTEXT.md `Scope clarifications` block — CL-01 (5 sentinels), CL-02 (case-insensitive country validator), CL-03 (fuzz in Phase 1)
- CONTEXT.md `<specifics>` — "Phase 1's executor must add the Key Decisions entries before phase verification passes"
- RESEARCH.md §"Upstream API Schema — Verified" Open Question Q1 + Assumption A6 → CL-04 (6-value HolidayType, not 4)
- RESEARCH.md §"Open Questions" Q2 + Assumption A3 → CL-05 (NameFor rename to avoid Name-method-vs-Name-field collision)
- RESEARCH.md §"CLIENT-10 — Mechanical Verification" (text in §"Validation Architecture" Wave 0 Gaps) — AST-walk approach using `go/parser` + `go/ast`
- RESEARCH.md §"Common Pitfalls" Pitfall 6 (no init / no global mutable state)
- STATE.md `Open Todos` — bullet `Add PROJECT.md Key Decisions entries for Phase 1 deviations: 5th sentinel ErrInvalidDateRange (CL-01), case-insensitive validators (CL-02), early fuzz target (CL-03)` (this plan closes that todo)
</source_artifact_pointers>

<interfaces>
<!-- This plan creates ONE test file (no production code) and edits ONE planning document (PROJECT.md). -->

internal_test.go produces a single test:

```
package openholidays

// In test file:
func TestNoInitOrGlobalState(t *testing.T)
```

PROJECT.md edit appends 5 rows to the existing `## Key Decisions` table (located between lines 83-98 of the current PROJECT.md). Table header (already present):
```
| Decision | Rationale | Outcome |
|----------|-----------|---------|
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create internal_test.go with TestNoInitOrGlobalState AST audit</name>
  <files>internal_test.go</files>
  <read_first>
    - /data/git/private/holidays/errors.go (confirm the 5 exported sentinels live in a single var block)
    - /data/git/private/holidays/date.go (confirm the unexported `errEmptyDate` is a package-level var in date.go)
    - /data/git/private/holidays/types.go (confirm there are NO package-level vars in types.go — only `const` for HolidayType and types/struct defs)
    - /data/git/private/holidays/validate.go (confirm no package-level vars)
    - /data/git/private/holidays/version.go (confirm `Version` is a `const`, NOT a `var`)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Validation Architecture" Wave 0 Gaps — TestNoInitOrGlobalState specification
    - .planning/phases/01-foundation/01-RESEARCH.md §"Common Pitfalls" Pitfall 6 (the rationale and the grep-based warning sign)
  </read_first>
  <action>
    Create /data/git/private/holidays/internal_test.go declaring `package openholidays` (in-package test). Imports: `go/ast`, `go/parser`, `go/token`, `os`, `path/filepath`, `sort`, `strings`, `testing`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`.

    Write a single test function `func TestNoInitOrGlobalState(t *testing.T)` implementing the AST-walk approach from RESEARCH.md §"Validation Architecture":

    Implementation outline (executor implements this from the description; no fenced code per planning policy):

    1. Define an `allowedVars` set/slice containing exactly: `ErrInvalidCountry`, `ErrInvalidLanguage`, `ErrDateRangeTooLarge`, `ErrInvalidDateRange`, `ErrEmptyResponse`, `errEmptyDate`. These six identifiers are the ONLY package-level `var` declarations Phase 1 permits. Add a godoc-style comment block above the slice explaining each entry's source (D-13 for the five exported, D-06 for the unexported errEmptyDate).

    2. Walk the repo root: `filepath.WalkDir(".", ...)`. For each entry:
       - Skip if it is a directory named `.planning`, `.git`, `testdata`, or `internal` (defensive — Phase 1 has none of these but later phases will).
       - Skip if `!strings.HasSuffix(name, ".go")` (only Go files).
       - Skip if `strings.HasSuffix(name, "_test.go")` (CLIENT-10 applies to production code, not tests; tests may have setup helpers via `TestMain`).
       - Skip if the file path contains `/cmd/` (Phase 5 CLI will have a main package — out of scope for Phase 1).
       - For matching files: `parser.ParseFile(fset, path, nil, parser.SkipObjectResolution)`. `require.NoError` on parse error.

    3. For each parsed AST file, walk the top-level `Decls`:
       - For `*ast.FuncDecl` declarations: if `decl.Name.Name == "init"` AND `decl.Recv == nil` (no receiver — a package-level init function), record a failure `"init function in <path>:<line>"`.
       - For `*ast.GenDecl` declarations: if `decl.Tok == token.VAR`, iterate the `Specs`. For each `*ast.ValueSpec` in the spec, iterate `spec.Names`. For each `*ast.Ident`: if the name is NOT in `allowedVars`, record a failure `"unexpected package-level var '<name>' in <path>:<line>"`.

    4. After walking all files: `sort.Strings(failures)` for deterministic output. Use `assert.Empty(t, failures, "CLIENT-10 violation(s) detected; see RESEARCH.md Pitfall 6 / Validation Architecture")` to report all violations at once (so contributors see every issue, not just the first).

    5. As a fast-feedback complement, also include a `t.Run("found_at_least_one_go_file", func(t *testing.T){ ... })` sub-test asserting the walk found at least 4 production `.go` files (sanity check: if the walk silently visits nothing, the absence of failures is meaningless). Expected count today: errors.go, date.go, types.go, validate.go, doc.go, version.go → at least 4.

    6. The test runs in CI on every push; the failure message points at PITFALLS.md / RESEARCH.md for the rationale.

    Test conventions (CONVENTIONS.md Rule 3):
    - Top-level `t.Parallel()` (the test only reads from disk).
    - Subtests in `t.Run` for the sanity check and the no-init walk if it makes sense to split them; otherwise one TestXxx body is fine.
    - `require` for parse errors (preconditions), `assert` for the violations slice.

    Run `gofmt -w internal_test.go` after writing.
  </action>
  <verify>
    <automated>go test -race -run TestNoInitOrGlobalState ./... 2>&amp;1 | tail -1 | grep -q '^ok' &amp;&amp; gofmt -l internal_test.go | grep -c '.' | grep -q '^0$' &amp;&amp; go vet ./... &amp;&amp; ! grep -rn 'func init' --include='*.go' /data/git/private/holidays/ | grep -v _test.go | grep -v '.planning/' | grep '.'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `internal_test.go` declares `package openholidays` and imports the AST stdlib (`go/parser`, `go/ast`, `go/token`).
    - Source: A single top-level `func TestNoInitOrGlobalState(t *testing.T)` is declared.
    - Source: An `allowedVars` data structure (slice, map, or set) is present listing exactly the 6 permitted package-level var names: `ErrInvalidCountry`, `ErrInvalidLanguage`, `ErrDateRangeTooLarge`, `ErrInvalidDateRange`, `ErrEmptyResponse`, `errEmptyDate`.
    - Behavior: `go test -race -run TestNoInitOrGlobalState ./...` exits 0 and reports `ok` (the current Phase 1 codebase is clean — no init, only the 6 allowed vars).
    - CLI: `grep -rn 'func init' --include='*.go' /data/git/private/holidays/ | grep -v _test.go | grep -v '.planning/'` returns no matches (sanity grep — locks CLIENT-10 mechanically).
    - CLI: `gofmt -l internal_test.go` produces no output; `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>internal_test.go ships TestNoInitOrGlobalState; AST walk passes against the current Phase 1 sources; CLIENT-10 mechanically locked for every future commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append CL-01..CL-05 rows to PROJECT.md Key Decisions table</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - /data/git/private/holidays/.planning/PROJECT.md lines 83-98 — current `## Key Decisions` table location and existing row format
    - .planning/phases/01-foundation/01-CONTEXT.md `Scope clarifications` (CL-01, CL-02, CL-03)
    - .planning/phases/01-foundation/01-RESEARCH.md Open Question Q1 → CL-04 (6-value HolidayType)
    - .planning/phases/01-foundation/01-RESEARCH.md Open Question Q2 + Assumption A3 → CL-05 (NameFor rename)
  </read_first>
  <action>
    Edit /data/git/private/holidays/.planning/PROJECT.md `## Key Decisions` table (currently at lines 83-98 of the file). Append 5 new rows AFTER the existing rows (do NOT modify existing rows; add at the end of the table body, before the closing line of the document).

    Use this exact table-row format. Each row uses single-line pipe-delimited markdown; rationale and outcome cite the source CONTEXT.md or RESEARCH.md section. Outcome column reads `Adopted in Phase 1` to mark the decision as no longer pending. Each row references the deviation source explicitly:

    Row CL-01 — Decision: `CL-01: Phase 1 ships 5 sentinel errors instead of ROADMAP-literal 4 (adds ErrInvalidDateRange)`. Rationale: `Two semantically distinct failure modes (validFrom > validTo vs window > 3 years) deserve two errors.Is-distinguishable identities; CONTEXT.md D-13 / RESEARCH.md §Pattern 2`. Outcome: `Adopted in Phase 1`.

    Row CL-02 — Decision: `CL-02: validateCountry is case-insensitive (canonicalizes input to uppercase) — deviates from VALID-01 literal "2 uppercase ASCII letters"`. Rationale: `Ergonomic input parity with validateLanguage's case-insensitive lowercase behavior; wire format remains uppercase per upstream API; CONTEXT.md D-20`. Outcome: `Adopted in Phase 1`.

    Row CL-03 — Decision: `CL-03: FuzzDateUnmarshal ships in Phase 1 instead of Phase 5 as ROADMAP places fuzz tests`. Rationale: `Pitfall JSON-3 mandates a fuzz target for every custom unmarshaler; not waiting four phases to surface regressions; CONTEXT.md D-12`. Outcome: `Adopted in Phase 1`.

    Row CL-04 — Decision: `CL-04: HolidayType ships 6 PascalCase upstream-verified values (Public, Bank, Optional, School, BackToSchool, EndOfLessons) instead of REQUIREMENTS.md TYPES-04's 4 (Public, School, Bank, Observance)`. Rationale: `Verified live OpenAPI spec on 2026-05-27 lists 6 values; Observance does not exist upstream; three real values were missing from the requirement; RESEARCH.md §Upstream API Schema — Verified, Open Question Q1`. Outcome: `Adopted in Phase 1`.

    Row CL-05 — Decision: `CL-05: Country/Language/Subdivision NameFor(lang) renamed from TYPES-05's literal "Name(lang)" to avoid Go method-vs-field name collision with the existing Name []LocalizedText field`. Rationale: `Go forbids a method and a field on the same type to share a name; renaming the method (less invasive than renaming the field, which would change JSON tag handling); RESEARCH.md §Pattern 4, Open Question Q2, Assumption A3`. Outcome: `Adopted in Phase 1`.

    Use the Edit tool with a single edit that inserts these 5 rows BEFORE the line immediately following the last existing Key Decisions row (the line containing `| Gold Rule 3 — \`testify\` ... |`). The existing table separator and header remain unchanged.

    Do not reformat or touch other parts of PROJECT.md. Keep the file gofmt-irrelevant (markdown — no formatter to run).

    After the edit, verify the new rows are present in the file via grep.
  </action>
  <verify>
    <automated>grep -c '^| CL-0[1-5]:' /data/git/private/holidays/.planning/PROJECT.md | grep -q '^5$' &amp;&amp; grep -q 'CL-01: Phase 1 ships 5 sentinel errors' /data/git/private/holidays/.planning/PROJECT.md &amp;&amp; grep -q 'CL-02: validateCountry is case-insensitive' /data/git/private/holidays/.planning/PROJECT.md &amp;&amp; grep -q 'CL-03: FuzzDateUnmarshal ships in Phase 1' /data/git/private/holidays/.planning/PROJECT.md &amp;&amp; grep -q 'CL-04: HolidayType ships 6 PascalCase' /data/git/private/holidays/.planning/PROJECT.md &amp;&amp; grep -q 'CL-05:' /data/git/private/holidays/.planning/PROJECT.md &amp;&amp; grep -c 'Adopted in Phase 1' /data/git/private/holidays/.planning/PROJECT.md | grep -q '^5$'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/PROJECT.md` `## Key Decisions` table contains exactly 5 new rows starting with `| CL-01:`, `| CL-02:`, `| CL-03:`, `| CL-04:`, `| CL-05:` (`grep -c '^| CL-0[1-5]:'` returns 5).
    - Source: Each row has the literal substring `Adopted in Phase 1` in its Outcome column (`grep -c 'Adopted in Phase 1'` returns ≥ 5).
    - Source: The existing 11 Key Decisions rows (Variant M scope, Greenfield POC, YOLO mode, Horizontal Layers, Quality model profile, Research/Plan-Check/Verifier agents, MIT license, Module path owner deferred, Gold Rule 1, Gold Rule 2, Gold Rule 3) remain untouched and in their original positions.
    - Source: No other section of PROJECT.md is modified — `git diff .planning/PROJECT.md` shows only insertions in the Key Decisions table.
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions table extended with 5 rows recording CL-01..CL-05; STATE.md open-todo will be closed by the next /gsd state command.</done>
</task>

</tasks>

<verification>
```bash
# Phase 1 full test suite green
go test -race -cover ./...                                                        # all 6 test files pass

# CLIENT-10 mechanical lock
go test -race -run TestNoInitOrGlobalState ./...                                   # CLIENT-10 audit passes
grep -rn 'func init' --include='*.go' /data/git/private/holidays/ | grep -v _test.go | grep -v '.planning/' | wc -l   # 0

# PROJECT.md Key Decisions update
grep -c '^| CL-0[1-5]:' /data/git/private/holidays/.planning/PROJECT.md            # 5
grep -c 'Adopted in Phase 1' /data/git/private/holidays/.planning/PROJECT.md       # ≥ 5

# Phase 1 overall acceptance
go build ./...                                                                     # builds clean
go vet ./...                                                                       # vets clean
gofmt -l doc.go version.go errors.go date.go types.go validate.go internal_test.go errors_test.go date_test.go types_test.go validate_test.go   # produces no output
go test -fuzz=FuzzDateUnmarshal -fuzztime=30s ./...                                 # fuzz cycle clean (phase-gate smoke)
```
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source files in repo → `go/parser` AST walk | Test-time input (own source tree). Trust boundary is theoretical — the test runs against the project's own code. |
| Developer edits → PROJECT.md `Key Decisions` table | Documentation change; no runtime impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-T | Tampering | A future PR adding `func init()` to slip side-effects past code review | mitigate | `TestNoInitOrGlobalState` (AST walk) fails CI for any added `init` function or any added package-level `var` outside the locked 6-entry allowlist. Failure message points at CLIENT-10 / Pitfall 6. |
| T-01-06-E | Tampering / scope-creep | A future PR adding a new package-level `var` (e.g., default cache, default logger, "configuration registry") | mitigate | Same AST walk catches it — the `allowedVars` slice is the explicit allowlist; adding a new var requires editing both the slice AND providing a justification in a PR review. |
| T-01-06-IL | Information Disclosure | PROJECT.md decision rationale | accept | All five rows quote CONTEXT.md and RESEARCH.md decisions, which are open-source planning documents. No secrets. |

No new package installs. No `[ASSUMED]`/`[SUS]` packages → no human-verify checkpoint needed.
</threat_model>

<success_criteria>
1. `internal_test.go` ships `TestNoInitOrGlobalState` walking the repo's `*.go` files via `go/parser`+`go/ast`, asserting zero `func init()` and only the 6 allowed package-level vars.
2. The test passes against the current Phase 1 codebase (`go test -race -run TestNoInitOrGlobalState ./...` exits 0).
3. `.planning/PROJECT.md` `## Key Decisions` table has 5 new rows (CL-01..CL-05) each marked `Adopted in Phase 1`.
4. Whole Phase 1 acceptance gate passes: `go test -race -cover ./...` green; `go test -fuzz=FuzzDateUnmarshal -fuzztime=30s ./...` green; `go build ./...` + `go vet ./...` succeed; `gofmt -l` produces no output across all Phase 1 source files.
5. ROADMAP success criteria #1, #2, #3, #4, #5 all satisfied (criteria 1-4 by Plans 02/03/04/05; criterion 5 by all plans collectively; the AST audit locks the CLIENT-10 portion of #4).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-06-SUMMARY.md` when done. Record: CLIENT-10 mechanically locked via AST walk; 5 scope-clarification decisions (CL-01..CL-05) recorded in PROJECT.md; Phase 1 ready for `/gsd:verify-phase 1`. Flag for STATE.md update: the open todo `Add PROJECT.md Key Decisions entries for Phase 1 deviations` is now closed by this plan.
</output>
