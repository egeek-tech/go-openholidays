---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - validate.go
  - validate_test.go
autonomous: true
requirements:
  - VALID-01
  - VALID-02
  - VALID-03
  - VALID-04
  - ERR-04
must_haves:
  truths:
    - "`validateCountry(code)` accepts case-insensitive ISO 3166-1 alpha-2 input, canonicalizes via `strings.ToUpper`, verifies the result is exactly 2 ASCII letters in [A-Z], returns the canonical form (D-20, VALID-01, CL-02)."
    - "`validateLanguage(code)` accepts case-insensitive ISO 639-1 alpha-2 input, canonicalizes via `strings.ToLower`, verifies 2 ASCII letters in [a-z], returns the canonical form (D-21, VALID-04)."
    - "`validateDateRange(from, to Date)` returns `%w`-wrapped `ErrInvalidDateRange` when `from.After(to)` (D-22, VALID-02)."
    - "`validateDateRange(from, to Date)` returns `%w`-wrapped `ErrDateRangeTooLarge` when the window exceeds 3 calendar years inclusive — i.e., `to >= from + 3 years + 1 day` (D-22, VALID-03)."
    - "Validator error messages wrap their sentinel with `%w` and include the offending value via `%q` or `%s`; no secrets, no full response bodies (D-23, ERR-04)."
    - "Leap-year boundary: `validateDateRange(NewDate(2024, time.February, 29), NewDate(2027, time.February, 28))` passes; `validateDateRange(NewDate(2024, time.February, 29), NewDate(2027, time.March, 1))` returns `ErrDateRangeTooLarge` (Pitfall 3 / TZ-2 mitigation, ROADMAP criterion #4)."
    - "All three validators are UNEXPORTED root-package functions (lowercase first letter) — they are called from Phase 2 endpoint methods, not from external code (ARCHITECTURE.md Pattern 5)."
    - "Exact-3-year boundary: `validateDateRange(NewDate(2025, time.January, 1), NewDate(2028, time.January, 1))` passes (3 years inclusive); same `from` with `to = 2028-01-02` returns `ErrDateRangeTooLarge`."
  artifacts:
    - path: "validate.go"
      provides: "validateCountry, validateLanguage, validateDateRange (all unexported); isTwoASCIIUppers, isTwoASCIILowers helpers"
      contains: "func validateCountry"
    - path: "validate_test.go"
      provides: "TestValidateCountry, TestValidateLanguage, TestValidateDateRange, TestValidators_NoSensitiveData"
      contains: "TestValidateDateRange"
  key_links:
    - from: "validate.go"
      to: "errors.go"
      via: "uses sentinels (ErrInvalidCountry, ErrInvalidLanguage, ErrDateRangeTooLarge, ErrInvalidDateRange)"
      pattern: "ErrInvalid(Country|Language|DateRange)|ErrDateRangeTooLarge"
    - from: "validate.go"
      to: "date.go"
      via: "uses Date type"
      pattern: "Date\\)"
---

<objective>
Ship `validate.go` with three unexported root-package validator functions — `validateCountry`, `validateLanguage`, `validateDateRange` — wrapping the sentinels from `errors.go` (Plan 02) and operating on the `Date` type from `date.go` (Plan 03). Cover them in `validate_test.go` with testify table-driven tests including the leap-year boundary and the 3-year-exact / 3-year-plus-1-day boundaries from ROADMAP success criterion #4.

Purpose: These validators are called by Phase 2 endpoint methods BEFORE any HTTP call to reject malformed input client-side (ASVS V5.1.3 input-validation control). The sentinels they wrap drive `errors.Is(err, ErrInvalidCountry)` matching in caller code. Phase 1 ships the canonical implementations; Phase 2 wires them into `PublicHolidaysRequest` and `SchoolHolidaysRequest` validation.

Output: `validate.go` + `validate_test.go`. All tests green under `-race`; `gofmt`/`vet` clean. Boundary cases locked: VALID-02 (from > to), VALID-03 (>3yr including leap-year boundary), VALID-04 (language shape), VALID-01 (country shape with case-insensitive accept).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md
@/data/git/private/holidays/errors.go
@/data/git/private/holidays/date.go

<source_artifact_pointers>
- CONTEXT.md D-20 (validateCountry canonicalizes uppercase), D-21 (validateLanguage canonicalizes lowercase), D-22 (validateDateRange — ErrInvalidDateRange for from>to, ErrDateRangeTooLarge for >3yr, uses `AddDate(3,0,1)` calendar arithmetic), D-23 (`%w` + `%q` value wrapping)
- CONTEXT.md `Scope clarifications` — CL-02 (case-insensitive country validator deviates from VALID-01 literal "2 uppercase ASCII letters"; must land in PROJECT.md Key Decisions by Plan 06)
- RESEARCH.md §"Pattern 3: Validators as unexported root-package functions (VALID-01..VALID-04)" — canonical implementation (lines ~619-693)
- RESEARCH.md §"Common Pitfalls" — Pitfall 3 (leap-year off-by-one — TZ-2)
- RESEARCH.md §"Validation Architecture" — required tests for VALID-01..VALID-04
- ARCHITECTURE.md §"Pattern 5: Validation as Unexported Package Functions" — file placement (root package, lowercase function names)
- ROADMAP §Phase 1 success criterion #4 — verbatim list of failure modes
</source_artifact_pointers>

<interfaces>
<!-- This plan creates the three validators consumed by Phase 2 endpoint methods. The validators wrap sentinels from errors.go and operate on Date from date.go. -->

Production surface created by this plan (exact identifiers — ALL UNEXPORTED):

```
// In package openholidays:

func validateCountry(code string) (string, error)
func validateLanguage(code string) (string, error)
func validateDateRange(from, to Date) error

// Unexported helpers:
func isTwoASCIIUppers(s string) bool
func isTwoASCIILowers(s string) bool
```

Dependencies (must already exist before Wave 3 begins):
- `ErrInvalidCountry`, `ErrInvalidLanguage`, `ErrDateRangeTooLarge`, `ErrInvalidDateRange` from errors.go (Plan 02)
- `Date`, `Date.After`, `Date.Before`, `Date.AddDate` (promoted from embedded `time.Time`) from date.go (Plan 03)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create validate.go with three unexported validators</name>
  <files>validate.go</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §"Pattern 3: Validators as unexported root-package functions (VALID-01..VALID-04)" — canonical implementation. Includes the explanation for `AddDate(3, 0, 1)` (calendar-correct vs DST-broken) and the byte-arithmetic helpers `isTwoASCIIUppers`/`isTwoASCIILowers`.
    - .planning/phases/01-foundation/01-CONTEXT.md D-20..D-23
    - /data/git/private/holidays/errors.go (Plan 02 output — confirms exact sentinel names)
    - /data/git/private/holidays/date.go (Plan 03 output — confirms Date.After/Before are the comparison methods and Date embeds time.Time so AddDate is promoted)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Common Pitfalls" Pitfall 3 (leap-year semantics)
  </read_first>
  <behavior>
    - `validateCountry("PL") == ("PL", nil)`; `validateCountry("pl") == ("PL", nil)`; `validateCountry("Pl") == ("PL", nil)` — canonicalization to uppercase per D-20 / CL-02.
    - `validateCountry("")`, `validateCountry("P")`, `validateCountry("POL")`, `validateCountry("P1")`, `validateCountry(" PL")` all return `("", err)` where `errors.Is(err, ErrInvalidCountry) == true`.
    - `validateCountry("ŻŻ")` (non-ASCII) returns `("", err)` wrapping `ErrInvalidCountry` — byte-arithmetic check rejects non-ASCII.
    - `validateLanguage("pl") == ("pl", nil)`; `validateLanguage("PL") == ("pl", nil)`; `validateLanguage("Pl") == ("pl", nil)` — canonicalization to lowercase per D-21.
    - `validateLanguage("")`, `validateLanguage("p")`, `validateLanguage("pol")`, `validateLanguage("p1")` all return `("", err)` wrapping `ErrInvalidLanguage`.
    - `validateDateRange(2025-01-01, 2025-01-01) == nil` (equal dates accepted per VALID-02 spec).
    - `validateDateRange(2025-12-31, 2025-01-01)` returns err wrapping `ErrInvalidDateRange` (from > to).
    - `validateDateRange(2025-01-01, 2028-01-01) == nil` (exactly 3 years inclusive — locks the canonical 3-year-window boundary).
    - `validateDateRange(2025-01-01, 2028-01-02)` returns err wrapping `ErrDateRangeTooLarge`.
    - `validateDateRange(2024-02-29, 2027-02-28) == nil`; `validateDateRange(2024-02-29, 2027-03-01)` returns `ErrDateRangeTooLarge` (Pitfall 3 leap-year correctness).
    - Error messages wrap sentinels with `%w` AND include the offending value via `%q` or `%s` (D-23).
  </behavior>
  <action>
    Create /data/git/private/holidays/validate.go declaring `package openholidays`. Imports: only `fmt` and `strings` (stdlib).

    Implement the canonical RESEARCH.md §"Pattern 3" verbatim. Required identifiers, all UNEXPORTED:

    1. `func validateCountry(code string) (string, error)`:
       - `canon := strings.ToUpper(code)` (D-20)
       - `if !isTwoASCIIUppers(canon) { return "", fmt.Errorf("%w: %q", ErrInvalidCountry, code) }` (D-23 — wrap sentinel and quote the original (non-canonicalized) input value)
       - `return canon, nil`
       Godoc starts `// validateCountry canonicalizes a country ISO 3166-1 alpha-2 code to uppercase and verifies it is exactly 2 ASCII letters.` Explicitly mention the canonical (uppercase) form is what gets sent on the wire.

    2. `func validateLanguage(code string) (string, error)`:
       - `canon := strings.ToLower(code)` (D-21)
       - `if !isTwoASCIILowers(canon) { return "", fmt.Errorf("%w: %q", ErrInvalidLanguage, code) }`
       - `return canon, nil`
       Godoc explains shape-only check (no allowlist per Deferred ideas).

    3. `func validateDateRange(from, to Date) error`:
       - `if from.After(to) { return fmt.Errorf("%w: from=%s to=%s", ErrInvalidDateRange, from, to) }` (D-22, VALID-02)
       - `limit := Date{from.AddDate(3, 0, 1)}` — calendar arithmetic (D-22, Pitfall 3, TZ-2 mitigation; `AddDate` is promoted from embedded `time.Time`)
       - `if !to.Before(limit) { return fmt.Errorf("%w: from=%s to=%s spans more than 3 years", ErrDateRangeTooLarge, from, to) }` (D-22, VALID-03)
       - `return nil`
       Godoc cites the AddDate-vs-duration-math rationale and the inclusive-3-year semantics.

    4. `func isTwoASCIIUppers(s string) bool`:
       - `if len(s) != 2 { return false }`
       - `return s[0] >= 'A' && s[0] <= 'Z' && s[1] >= 'A' && s[1] <= 'Z'`
       Godoc explains why byte arithmetic over `unicode.IsLetter` (we want ASCII only — `'Ö'` must NOT pass).

    5. `func isTwoASCIILowers(s string) bool`:
       - Symmetric to `isTwoASCIIUppers` against `[a-z]`.

    Error string convention: all sentinels already start with `"openholidays: "` (Plan 02); `%w`-wrapped messages preserve this prefix. The `%q` formatting on user-supplied codes is safe because country/language codes and ISO dates are non-secret (D-23, ERR-04).

    Run `gofmt -w validate.go` after writing.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./... &amp;&amp; gofmt -l validate.go | grep -c '.' | grep -q '^0$' &amp;&amp; grep -q '^func validateCountry(code string) (string, error)' validate.go &amp;&amp; grep -q '^func validateLanguage(code string) (string, error)' validate.go &amp;&amp; grep -q '^func validateDateRange(from, to Date) error' validate.go &amp;&amp; grep -q '^func isTwoASCIIUppers(s string) bool' validate.go &amp;&amp; grep -q '^func isTwoASCIILowers(s string) bool' validate.go &amp;&amp; ! grep -qE '^func (ValidateCountry|ValidateLanguage|ValidateDateRange)' validate.go &amp;&amp; grep -q 'AddDate(3, 0, 1)' validate.go</automated>
  </verify>
  <acceptance_criteria>
    - Source: `validate.go` declares `package openholidays` and imports only `fmt` and `strings`.
    - Source: Three validator functions exist with the EXACT signatures from the &lt;interfaces&gt; block — all UNEXPORTED (lowercase first letter).
    - Source: NO exported `ValidateCountry`/`ValidateLanguage`/`ValidateDateRange` versions exist (the validators are package-internal per ARCHITECTURE.md Pattern 5).
    - Source: `validateDateRange` uses `AddDate(3, 0, 1)` (literal, calendar-arithmetic — confirms Pitfall 3 mitigation).
    - Source: `isTwoASCIIUppers` and `isTwoASCIILowers` are present and unexported.
    - Source: Each validator wraps its sentinel with `%w` and includes the offending value via `%q`/`%s` (D-23 — grep for `fmt.Errorf` shows %w + %q or %w + %s patterns).
    - CLI: `go build ./...` and `go vet ./...` both exit 0.
    - CLI: `gofmt -l validate.go` produces no output.
  </acceptance_criteria>
  <done>validate.go ships three unexported validators + two helpers; references the existing sentinels from errors.go and the Date type from date.go; gofmt + vet clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create validate_test.go with table-driven coverage including leap-year boundaries</name>
  <files>validate_test.go</files>
  <read_first>
    - /data/git/private/holidays/validate.go (Task 1 output — confirm exact function signatures and error wrapping format)
    - .planning/codebase/CONVENTIONS.md §"Rule 3 — Test conventions"
    - .planning/phases/01-foundation/01-RESEARCH.md §"Validation Architecture" — required test cases for VALID-01..VALID-04
    - .planning/phases/01-foundation/01-RESEARCH.md §"Common Pitfalls" Pitfall 3 (leap-year boundary cases)
    - ROADMAP success criterion #4 — verbatim failure-mode list
  </read_first>
  <action>
    Create /data/git/private/holidays/validate_test.go declaring `package openholidays` (in-package, required for access to unexported `validate*` functions). Imports: `errors`, `testing`, `time`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`.

    Per Gold Rule 3 — exactly one TestXxx per exported production function. Validators are unexported but get their own tests anyway (CONVENTIONS.md: "Internal helpers may have their own tests when they merit independent coverage"). Write these top-level test functions:

    1. `func TestValidateCountry(t *testing.T)` — VALID-01 / CL-02 — table-driven. Cases:
       - `"PL"` → `("PL", nil)`
       - `"pl"` → `("PL", nil)` (case-insensitive canonicalization — CL-02)
       - `"Pl"` → `("PL", nil)` (mixed case)
       - `"DE"` → `("DE", nil)` (additional happy case)
       - `""` → `("", err)`, `errors.Is(err, ErrInvalidCountry) == true`
       - `"P"` (1 letter) → wraps ErrInvalidCountry
       - `"POL"` (3 letters) → wraps ErrInvalidCountry
       - `"P1"` (digit) → wraps ErrInvalidCountry
       - `" PL"` (leading whitespace) → wraps ErrInvalidCountry
       - `"PL "` (trailing whitespace) → wraps ErrInvalidCountry
       - `"ŻŻ"` (non-ASCII letters) → wraps ErrInvalidCountry — locks the byte-arithmetic check that rejects non-ASCII (validates "we want ASCII only" from RESEARCH.md §"Why byte arithmetic instead of `unicode.IsLetter`")
       - Each error case also asserts the message contains the ORIGINAL (non-canonicalized) input via `%q`, e.g., `assert.Contains(t, err.Error(), "\"pl\"")` for the `"pl"` input — even though `"pl"` happens to be valid, this assertion is in the success-case verification of "the canonical form is returned" not the error case.
       For success cases: use `require.NoError(t, err)` then `assert.Equal(t, "PL", canon)`. For error cases: `require.Error(t, err)` then `assert.True(t, errors.Is(err, ErrInvalidCountry))`.

    2. `func TestValidateLanguage(t *testing.T)` — VALID-04 — table-driven, symmetric to TestValidateCountry but with `ErrInvalidLanguage` and lowercase canonical form:
       - `"pl"` → `("pl", nil)`
       - `"PL"` → `("pl", nil)`
       - `"Pl"` → `("pl", nil)`
       - `"en"` → `("en", nil)`
       - `""`, `"p"`, `"pol"`, `"p1"`, `"ŻŻ"` → all wrap `ErrInvalidLanguage`

    3. `func TestValidateDateRange(t *testing.T)` — VALID-02 / VALID-03 / Pitfall 3 — table-driven. CRITICAL cases (ROADMAP criterion #4):
       - **VALID-02 happy path:** `from=2025-01-01, to=2025-01-01` (equal) → `nil`
       - **VALID-02 happy path:** `from=2025-01-01, to=2025-12-31` → `nil`
       - **VALID-02 failure:** `from=2025-12-31, to=2025-01-01` (from > to) → wraps `ErrInvalidDateRange`
       - **VALID-03 boundary (PASS):** `from=2025-01-01, to=2028-01-01` (exactly 3 years inclusive) → `nil`
       - **VALID-03 boundary (FAIL):** `from=2025-01-01, to=2028-01-02` (3 years + 1 day) → wraps `ErrDateRangeTooLarge`
       - **VALID-03 leap-year boundary (PASS):** `from=2024-02-29, to=2027-02-28` (3 calendar years; Pitfall 3) → `nil`
       - **VALID-03 leap-year boundary (FAIL):** `from=2024-02-29, to=2027-03-01` (3 years + 1 day across leap-year fold) → wraps `ErrDateRangeTooLarge`
       - **Extreme range:** `from=2025-01-01, to=2050-01-01` (25 years) → wraps `ErrDateRangeTooLarge` (smoke check well outside the boundary)
       - For ErrInvalidDateRange and ErrDateRangeTooLarge cases, the error message MUST include `from=` and `to=` substrings (verifies D-23 value-in-error convention) — e.g., `assert.Contains(t, err.Error(), "from=2025-12-31")`.

    4. `func TestValidators_NoSensitiveData(t *testing.T)` — ERR-04 — guard against future regressions. Cases:
       - `validateCountry("topsecret")` (11 chars, definitely invalid) — assert the error message contains `"topsecret"` (the offending value IS in the error per D-23) but the test serves as documentation that "secrets" passed by mistake to a validator WILL leak into logs. This test is partly documentary: it locks the invariant that validator error messages contain ONLY the offending code/date value (which the caller passed). It does NOT include `os.Setenv`, `os.Args`, or any system-level secret.
       - `validateLanguage("apikey")` — assert error contains `"apikey"`.
       - `validateDateRange(NewDate(2025, time.January, 1), NewDate(2099, time.January, 1))` — assert error contains `from=2025-01-01` and `to=2099-01-01` (date values are non-secret).
       - Assert that NONE of these error messages contain the substring `"http"`, `"://"`, `"Body:"`, or `"Authorization"` (no leaked HTTP/transport data — ERR-04 invariant; defensive).

    Test conventions (mandatory):
    - `t.Parallel()` on top-level and per-case.
    - Every assertion inside `t.Run`.
    - `require.NoError(t, err)` for happy-path preconditions; `require.Error(t, err)` for error-path preconditions; `assert.True(t, errors.Is(err, sentinel))` for sentinel verification (Phase 2 callers do exactly this).

    Run `gofmt -w validate_test.go` after writing.
  </action>
  <verify>
    <automated>go test -race -run 'TestValidateCountry|TestValidateLanguage|TestValidateDateRange|TestValidators_NoSensitiveData' ./... 2>&amp;1 | tail -1 | grep -q '^ok' &amp;&amp; gofmt -l validate_test.go | grep -c '.' | grep -q '^0$' &amp;&amp; go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - Source: `validate_test.go` declares `package openholidays` (in-package — needs access to unexported `validateCountry`/`validateLanguage`/`validateDateRange`).
    - Source: Exactly 4 top-level `func TestXxx` functions: TestValidateCountry, TestValidateLanguage, TestValidateDateRange, TestValidators_NoSensitiveData.
    - Source: Every assertion lives inside a `t.Run` block.
    - Source: TestValidateDateRange contains at least 8 t.Run cases (incl. the exact-3-year-pass, 3y+1d-fail, leap-year-pass, leap-year-fail, from>to-fail cases — covers ROADMAP criterion #4 verbatim).
    - Source: TestValidators_NoSensitiveData explicitly checks for absence of `"http"`, `"://"`, `"Body:"`, `"Authorization"` substrings in validator error messages (ERR-04 guard).
    - Source: Imports `github.com/stretchr/testify/{assert,require}`, `errors`, `time`.
    - Behavior: `go test -race -run 'TestValidate*|TestValidators_NoSensitiveData' ./...` exits 0 and reports `ok`.
    - Behavior: Leap-year boundary cases (`2024-02-29` → `2027-02-28` pass; `→ 2027-03-01` fail) both pass — Pitfall 3 / ROADMAP criterion #4 locked.
    - CLI: `gofmt -l validate_test.go` produces no output; `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>validate_test.go ships 4 TestXxx functions covering VALID-01..VALID-04 + ERR-04 guard; leap-year boundary regression tests included; `go test -race ./...` green.</done>
</task>

</tasks>

<verification>
```bash
go test -race -cover ./...                                # entire Phase 1 suite green (errors + date + types + validate)
gofmt -l validate.go validate_test.go                      # produces no output
go vet ./...                                               # vets clean

# Validator surface assertions
grep -c '^func validate' validate.go                       # 3 (all unexported)
! grep -qE '^func (ValidateCountry|ValidateLanguage|ValidateDateRange)' validate.go   # no exported versions
grep -q 'AddDate(3, 0, 1)' validate.go                     # calendar arithmetic (Pitfall 3)

# ROADMAP criterion #4 verbatim — the failure-mode case names
grep -c 't.Run' validate_test.go                           # ≥ 25 (rough headcount across the four tests)
```
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller input → validate*() | The first defence layer before any HTTP call (Phase 2). All untrusted strings/dates pass through these validators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-IV | Input Validation (ASVS V5) | `validateCountry`, `validateLanguage` | mitigate | Byte-arithmetic ASCII check rejects all non-ASCII (e.g., `'Ö'` from a non-ASCII keyboard). Length-2 fast-fail before character class check. Test TestValidateCountry/"non-ASCII" locks the regression. Wire format is canonicalized (uppercase for country, lowercase for language) BEFORE it reaches HTTP, eliminating server-side normalization ambiguity. |
| T-01-05-DOS | Denial-of-service | Adversarial date input | mitigate | `validateDateRange` runs in O(1): two `Date.After`/`Date.Before` calls plus one `AddDate`. No loops, no allocations, no string scanning. Even pathological year inputs (year 9999) are handled cleanly by `time.Time.AddDate` per RESEARCH.md §"Security Domain". |
| T-01-05-OFB | Tampering (off-by-one across leap years) | `validateDateRange` | mitigate | Calendar arithmetic via `AddDate(3, 0, 1)` (D-22, Pitfall 3, TZ-2). Test cases lock the boundary: `2024-02-29 → 2027-02-28` MUST pass, `→ 2027-03-01` MUST fail. CI regression catches any future PR replacing with duration math. |
| T-01-05-IL | Information Disclosure | Validator error messages | mitigate | ERR-04: validator error messages include ONLY the offending non-secret value (country code, language code, ISO date strings). Test TestValidators_NoSensitiveData explicitly checks for absence of `"http"`, `"://"`, `"Body:"`, `"Authorization"` substrings — defensive regression guard. |

No new packages installed by this plan. No `[ASSUMED]`/`[SUS]` packages → no human-verify checkpoint needed.
</threat_model>

<success_criteria>
1. `validate.go` ships three unexported validators (`validateCountry`, `validateLanguage`, `validateDateRange`) and two unexported helpers (`isTwoASCIIUppers`, `isTwoASCIILowers`), all per RESEARCH.md §"Pattern 3".
2. `validate_test.go` ships 4 TestXxx functions; ROADMAP success criterion #4 is verified verbatim (1-letter, lowercase, 3-letter, from>to, >3yr all rejected with the correct sentinel; leap-year boundary cases included).
3. ERR-04 invariant locked: validator error messages contain only the offending non-secret value; defensive `TestValidators_NoSensitiveData` guards against future regressions.
4. `go test -race ./...` exits 0; `gofmt -l validate.go validate_test.go` produces no output; `go vet ./...` exits 0.
5. ROADMAP success criterion #5: `go build ./...` and `go vet ./...` continue to succeed on Go 1.23.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done. Record: three unexported validators shipped; CL-02 (case-insensitive country validator deviation from VALID-01 literal text) needs to be recorded in PROJECT.md Key Decisions by Plan 06; leap-year boundary regression tests locked.
</output>
