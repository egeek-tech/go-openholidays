---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - date.go
  - date_test.go
autonomous: true
requirements:
  - TYPES-02
must_haves:
  truths:
    - "`type Date struct { time.Time }` exists as a wrapper struct embedding `time.Time` (D-05)."
    - "`(d Date) MarshalJSON()` emits `\"YYYY-MM-DD\"`; `Date{}` round-trips to `\"0001-01-01\"` (D-07, Pitfall 4)."
    - "`(*Date) UnmarshalJSON(b)` rejects `null` and empty-string inputs by returning a `%w`-wrapped `errEmptyDate` error; ROADMAP criterion #1 holds (D-06, JSON-3, JSON-4)."
    - "`json.Marshal` and `json.Unmarshal` round-trip `\"2025-12-24\"` byte-for-byte (ROADMAP criterion #1)."
    - "`NewDate(y, m, d)` produces a Date at UTC midnight (D-11, TZ-1)."
    - "`ParseDate(s)` parses `YYYY-MM-DD` returning a UTC-midnight Date; empty string returns `errEmptyDate`; malformed input wraps `time.Parse` error (D-11)."
    - "`Date.String()` returns `YYYY-MM-DD` form, shadowing the embedded `time.Time.String()` (D-08)."
    - "`Equal`, `Before`, `After`, `Compare` shadow the embedded methods and normalize both operands to UTC midnight via `toUTCMidnight` (D-09)."
    - "`DaysUntil(other)` returns inclusive day count, calendar-correct across DST (D-10, TZ-2). For `d == other`, returns 1."
    - "`FuzzDateUnmarshal` exists as a `*testing.F`-based fuzz target with at least 3 seed corpus entries (D-12, CL-03, JSON-3)."
    - "`errEmptyDate` is UNEXPORTED — it must not appear in the public sentinel list and `errors.Is(err, errEmptyDate)` from outside the package is impossible (D-06)."
  artifacts:
    - path: "date.go"
      provides: "Date type + Marshal/UnmarshalJSON + NewDate + ParseDate + String + Equal/Before/After/Compare + DaysUntil + errEmptyDate"
      contains: "type Date struct"
    - path: "date_test.go"
      provides: "TestNewDate, TestParseDate, TestDate_MarshalJSON, TestDate_UnmarshalJSON, TestDate_String, TestDate_Equal, TestDate_Before, TestDate_After, TestDate_Compare, TestDate_DaysUntil, FuzzDateUnmarshal"
      contains: "FuzzDateUnmarshal"
  key_links:
    - from: "date.go"
      to: "package openholidays"
      via: "package declaration"
      pattern: "^package openholidays$"
    - from: "date_test.go"
      to: "github.com/stretchr/testify"
      via: "test-only import"
      pattern: "stretchr/testify"
---

<objective>
Ship `date.go` (the custom `Date` wrapper struct + constructors + JSON round-trip + comparison helpers + `DaysUntil`) and `date_test.go` (full testify coverage + `FuzzDateUnmarshal` fuzz target). Lock ROADMAP success criterion #1 (`"2025-12-24"` JSON round-trip; `null` and `""` produce errors, not zero values).

Purpose: `Date` is consumed by `types.go` (Plan 04 — `Holiday.StartDate`, `Holiday.EndDate`) and `validate.go` (Plan 05 — `validateDateRange`). It is the foundational type for every other Phase 1 file beyond errors and must be stable before Wave 3 starts.

Output: `date.go` (production) + `date_test.go` (testify unit + `FuzzDateUnmarshal`). All tests green under `-race`; `gofmt`-clean; `go vet`-clean. Fuzz target seed corpus exercises the canonical failure modes (null, empty, missing quotes, invalid format, valid).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md

<source_artifact_pointers>
- CONTEXT.md D-05 (wrapper struct), D-06 (UnmarshalJSON rejects null+empty via unexported errEmptyDate), D-07 (MarshalJSON, zero round-trips to "0001-01-01"), D-08 (String override), D-09 (Equal/Before/After/Compare shadow with UTC normalization), D-10 (DaysUntil DST-correct), D-11 (NewDate + ParseDate constructors), D-12 (FuzzDateUnmarshal in Phase 1)
- CONTEXT.md `Scope clarifications` — CL-03: fuzz ships in Phase 1 (must be recorded in PROJECT.md Key Decisions by Plan 06)
- RESEARCH.md §"Pattern 1: Custom `Date` wrapper struct (TYPES-02, D-05 through D-11)" — canonical implementation (lines ~386-517). Executor implements this verbatim.
- RESEARCH.md §"Common Pitfalls" — Pitfall 1 (null/empty errors), Pitfall 2 (TZ surprise), Pitfall 3 (3-year leap-year), Pitfall 4 (zero round-trip symmetry)
- RESEARCH.md §"Validation Architecture" — required tests for TYPES-02
</source_artifact_pointers>

<interfaces>
<!-- This plan creates the canonical Date surface. Plan 04 (types.go) will embed `Date` in `Holiday.StartDate` / `Holiday.EndDate`. Plan 05 (validate.go) will call `Date.After`, `Date.AddDate`, `Date.Before` in `validateDateRange`. -->

Production surface created by this plan (exact identifiers):

```
// In package openholidays:

type Date struct {
    time.Time
}

const dateLayout = "2006-01-02"            // unexported
var errEmptyDate error                      // unexported (D-06)

func NewDate(year int, month time.Month, day int) Date
func ParseDate(s string) (Date, error)
func (d Date) MarshalJSON() ([]byte, error)
func (d *Date) UnmarshalJSON(b []byte) error
func (d Date) String() string
func (d Date) Equal(other Date) bool
func (d Date) Before(other Date) bool
func (d Date) After(other Date) bool
func (d Date) Compare(other Date) int
func (d Date) DaysUntil(other Date) int
func (d Date) toUTCMidnight() time.Time     // unexported helper
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create date.go with Date type and all behaviors</name>
  <files>date.go</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §"Pattern 1: Custom `Date` wrapper struct" — canonical implementation including the `toUTCMidnight` defensive helper. Implement verbatim.
    - .planning/phases/01-foundation/01-CONTEXT.md D-05..D-12
    - .planning/phases/01-foundation/01-RESEARCH.md §"Common Pitfalls" Pitfalls 1, 2, 3, 4
  </read_first>
  <behavior>
    - `MarshalJSON` produces `"YYYY-MM-DD"` JSON bytes. Zero `Date{}` produces `"0001-01-01"`.
    - `UnmarshalJSON` returns a `%w`-wrapped `errEmptyDate` for `null` and for empty-string `""`; returns a wrapped `time.Parse` error for malformed input; returns a non-string-shaped error for non-JSON-string input (e.g., `123`).
    - Successful `UnmarshalJSON("\"2025-12-24\"")` populates the receiver to UTC midnight 2025-12-24.
    - `NewDate(2025, time.December, 24)` returns a Date with `Year() == 2025`, `Month() == time.December`, `Day() == 24`, `Location() == time.UTC`.
    - `ParseDate("")` returns `errEmptyDate`; `ParseDate("not-a-date")` returns a wrapped `time.Parse` error; `ParseDate("2025-12-24")` returns the same UTC-midnight Date.
    - `String()` returns `"2025-12-24"` for the December 24 2025 Date (NOT the `"2025-12-24 00:00:00 +0000 UTC"` form from embedded time.Time).
    - `Equal`, `Before`, `After`, `Compare` work as expected even when one operand is `Date{time.Date(2025, time.Dec, 24, 12, 0, 0, 0, warsaw)}` because both are normalized to UTC midnight before comparison (Pitfall 2 defensive case).
    - `DaysUntil(other)` returns 1 when `d == other` (inclusive count), 14 for a 14-day Śląskie ferie zimowe span (Phase 3 use case), and a negative number when `d > other`.
  </behavior>
  <action>
    Create /data/git/private/holidays/date.go declaring `package openholidays`. Imports: only `bytes`, `fmt`, `time` (stdlib). Implement the canonical code from RESEARCH.md §"Pattern 1" verbatim, including the unexported `errEmptyDate` sentinel and the unexported `toUTCMidnight` helper.

    Exact identifiers + signatures:

    1. `const dateLayout = "2006-01-02"` (unexported).

    2. `var errEmptyDate = fmt.Errorf("openholidays: empty date string")` (unexported per D-06; godoc comment explains why it is unexported — kept off the public sentinel list to keep that surface at 5).

    3. `type Date struct { time.Time }` with godoc starting `// Date is a calendar date (no timezone) returned by the OpenHolidays API.` Mention internal UTC-midnight normalization and the embedded promoted methods.

    4. `func NewDate(year int, month time.Month, day int) Date` — body returns `Date{time.Date(year, month, day, 0, 0, 0, 0, time.UTC)}` (D-11, TZ-1).

    5. `func ParseDate(s string) (Date, error)` — empty string returns `errEmptyDate`; otherwise `time.Parse(dateLayout, s)` then wrap into `Date{t}`; on parse error return `fmt.Errorf("openholidays: invalid date %q: %w", s, err)` (D-11, D-23 value-in-error convention).

    6. `func (d Date) MarshalJSON() ([]byte, error)` — emits 12-byte buffer using `AppendFormat` (the canonical efficient form from RESEARCH.md). Zero `Date{}` round-trips to `"0001-01-01"` (D-07, Pitfall 4).

    7. `func (d *Date) UnmarshalJSON(b []byte) error` — checks (in order):
       - `bytes.Equal(b, []byte("null"))` → return `fmt.Errorf("openholidays: null is not a valid date: %w", errEmptyDate)` (D-06, JSON-3 mandatory pattern)
       - `len(b) < 2 || b[0] != '"' || b[len(b)-1] != '"'` → return `fmt.Errorf("openholidays: date must be a JSON string, got %s", b)` (non-string input like `123` or `true`)
       - `s == ""` (after stripping quotes) → return `fmt.Errorf("openholidays: %w", errEmptyDate)`
       - otherwise `time.Parse(dateLayout, s)`, on error wrap as `fmt.Errorf("openholidays: invalid date %q: %w", s, err)`, on success `*d = Date{t}`.

    8. `func (d Date) String() string` — returns `d.Format(dateLayout)` (D-08, shadows embedded `time.Time.String()`).

    9. `func (d Date) Equal(other Date) bool` — `d.toUTCMidnight().Equal(other.toUTCMidnight())` (D-09).

    10. `func (d Date) Before(other Date) bool` — analogous (D-09).

    11. `func (d Date) After(other Date) bool` — analogous (D-09).

    12. `func (d Date) Compare(other Date) int` — `d.toUTCMidnight().Compare(other.toUTCMidnight())` (D-09).

    13. `func (d Date) DaysUntil(other Date) int` — RESEARCH.md §"Pattern 1" implementation: compute `int(b.Sub(a).Hours() / 24)`; if `days >= 0` return `days + 1` (inclusive count), otherwise `days - 1` (D-10, TZ-2).

    14. `func (d Date) toUTCMidnight() time.Time` (unexported helper) — `time.Date(d.Year(), d.Month(), d.Day(), 0, 0, 0, 0, time.UTC)` — defensive normalization in case external code constructed a `Date{time.Date(..., loc)}` directly (RESEARCH.md §"Why this shape", Assumption A1).

    Every exported symbol gets a godoc comment starting with its name (PROJECT.md constraint). All error message strings start with `"openholidays: "` (CONVENTIONS.md error-string convention).

    Run `gofmt -w date.go` after writing.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./... &amp;&amp; gofmt -l date.go | grep -c '.' | grep -q '^0$' &amp;&amp; grep -q '^type Date struct {$' date.go &amp;&amp; grep -q 'time.Time$' date.go &amp;&amp; grep -q 'func NewDate(year int, month time.Month, day int) Date' date.go &amp;&amp; grep -q 'func ParseDate(s string) (Date, error)' date.go &amp;&amp; grep -q 'func (d Date) MarshalJSON()' date.go &amp;&amp; grep -q 'func (d \*Date) UnmarshalJSON(b \[\]byte) error' date.go &amp;&amp; grep -q 'func (d Date) DaysUntil(other Date) int' date.go &amp;&amp; grep -q 'var errEmptyDate' date.go &amp;&amp; ! grep -q '^var ErrEmptyDate' date.go</automated>
  </verify>
  <acceptance_criteria>
    - Source: `date.go` declares `package openholidays` and imports only `bytes`, `fmt`, `time`.
    - Source: `type Date struct { time.Time }` exists.
    - Source: All identifiers from the &lt;interfaces&gt; block are present with their exact signatures.
    - Source: `errEmptyDate` is lowercase-first (unexported); there is no `ErrEmptyDate` (capital E) in `date.go`.
    - Source: `dateLayout` const equals `"2006-01-02"`.
    - Source: `Date.String()` returns `d.Format(dateLayout)` (NOT delegating to the embedded `time.Time.String`).
    - CLI: `go build ./...` and `go vet ./...` both exit 0.
    - CLI: `gofmt -l date.go` produces no output.
  </acceptance_criteria>
  <done>date.go committed with all 14 identifiers from the interfaces block; gofmt + vet clean; ready for date_test.go.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create date_test.go with full testify coverage and FuzzDateUnmarshal</name>
  <files>date_test.go</files>
  <read_first>
    - /data/git/private/holidays/date.go (the file Task 1 just produced — confirm exact identifiers, behavior, error message phrasing)
    - .planning/codebase/CONVENTIONS.md §"Rule 3 — Test conventions" — testify + one-test-per-prod-function + t.Run
    - .planning/phases/01-foundation/01-VALIDATION.md "Wave 0 Requirements" — required tests for TYPES-02 + D-12
    - .planning/phases/01-foundation/01-RESEARCH.md §"Common Pitfalls" — Pitfalls 1 (null/empty), 2 (TZ surprise), 4 (zero round-trip)
  </read_first>
  <action>
    Create /data/git/private/holidays/date_test.go declaring `package openholidays`. Imports: `encoding/json`, `errors`, `testing`, `time`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`.

    Per Gold Rule 3 (one TestXxx per exported production function) and the production surface from Task 1, write these top-level test functions:

    1. `func TestNewDate(t *testing.T)` — table-driven, cases: typical (2025/12/24), leap-year (2024/02/29), year-1 (1/1/1), end-of-year (2025/12/31). Each case asserts `Location() == time.UTC`, `Year()/Month()/Day()` match input, `Hour()/Minute()/Second()/Nanosecond()` are all 0.

    2. `func TestParseDate(t *testing.T)` — table-driven:
       - `""` → `errors.Is(err, errEmptyDate) == true` (use package-internal access)
       - `"2025-12-24"` → succeeds, result equals `NewDate(2025, time.December, 24)`
       - `"not-a-date"` → returns an error whose message contains `"openholidays: invalid date "not-a-date""`
       - `"2025-13-01"` → error (month out of range; verifies that `time.Parse` strictness is preserved)
       - `"2025-02-29"` → error (2025 is not a leap year)
       - `"2024-02-29"` → succeeds (leap-year boundary)

    3. `func TestDate_MarshalJSON(t *testing.T)` — table-driven:
       - `NewDate(2025, time.December, 24)` → `[]byte(`"2025-12-24"`)`
       - `Date{}` → `[]byte(`"0001-01-01"`)` (D-07, Pitfall 4)
       - Round-trip lock: `b, err := json.Marshal(NewDate(2025, time.December, 24)); require.NoError(t, err); var d Date; require.NoError(t, json.Unmarshal(b, &d)); assert.True(t, d.Equal(NewDate(2025, time.December, 24)))` — this is ROADMAP success criterion #1 in literal form.

    4. `func TestDate_UnmarshalJSON(t *testing.T)` — table-driven; CRITICAL coverage per Pitfall 1 / JSON-3 / JSON-4:
       - `[]byte("null")` → `errors.Is(err, errEmptyDate) == true`
       - `[]byte("\"\"")` (JSON empty string) → `errors.Is(err, errEmptyDate) == true`
       - `[]byte("\"2025-12-24\"")` → succeeds; receiver becomes `NewDate(2025, time.December, 24)`
       - `[]byte("123")` (non-string JSON) → returns an error whose message contains `"date must be a JSON string"`
       - `[]byte("true")` → returns an error whose message contains `"date must be a JSON string"`
       - `[]byte("\"not-a-date\"")` → returns a wrapped `time.Parse` error
       - `[]byte("\"\"")` round-trip: after error, receiver must be unchanged (assert receiver `IsZero()`).
       Use `require.Error` then `require.True(t, errors.Is(err, errEmptyDate))` for the sentinel-wrapping cases.

    5. `func TestDate_String(t *testing.T)` — table-driven:
       - `NewDate(2025, time.December, 24).String() == "2025-12-24"`
       - `Date{}.String() == "0001-01-01"` (locks the symmetry with MarshalJSON zero output)

    6. `func TestDate_Equal(t *testing.T)` — cases:
       - Two dates from `NewDate` with identical y/m/d return true
       - Different days return false
       - `Date{time.Date(2025, time.Dec, 24, 12, 30, 0, 0, time.UTC)}.Equal(NewDate(2025, time.December, 24))` returns true (defensive UTC-midnight normalization — Pitfall 2)
       - Different-Location operand still normalizes: `Date{time.Date(2025, time.Dec, 24, 23, 0, 0, 0, time.FixedZone("X", -3600))}.Equal(NewDate(2025, time.December, 24))` returns true (because both normalize to UTC midnight 2025-12-24 — Pitfall 2 defensive)

    7. `func TestDate_Before(t *testing.T)` — `NewDate(2025, time.January, 1).Before(NewDate(2025, time.December, 31))` → true; reverse → false; equal → false (strict).

    8. `func TestDate_After(t *testing.T)` — symmetric to Before.

    9. `func TestDate_Compare(t *testing.T)` — `-1`/`0`/`+1` outputs verified for less-than, equal, greater-than.

    10. `func TestDate_DaysUntil(t *testing.T)` — table-driven (TZ-2 mitigation):
        - Same day → 1 (inclusive count per D-10)
        - 1 day later → 2
        - 14 days later (Śląskie ferie zimowe span) → 14. Use the exact pattern `NewDate(2025, time.February, 17).DaysUntil(NewDate(2025, time.March, 2)) == 14`.
        - Negative direction: `NewDate(2025, time.March, 2).DaysUntil(NewDate(2025, time.February, 17)) == -14`.
        - DST-crossing span: pick a US Eastern DST crossing in March 2025 and verify the day count is correct. Note: since both operands are UTC-midnight per Date design, DST is structurally eliminated; this case still serves as a regression test that the implementation does NOT accidentally rely on local time.

    11. `func FuzzDateUnmarshal(f *testing.F)` — D-12 / CL-03 / JSON-3 mandate:
        - Seed corpus (use `f.Add(...)`): `[]byte("\"2025-12-24\"")`, `[]byte("null")`, `[]byte("\"\"")`, `[]byte("\"2024-02-29\"")`, `[]byte("123")`, `[]byte("\"not-a-date\"")`, `[]byte("")`.
        - Fuzz body: `f.Fuzz(func(t *testing.T, b []byte) { var d Date; _ = d.UnmarshalJSON(b) })`. The invariant: `UnmarshalJSON` must NEVER panic, regardless of input bytes (per JSON-3 mandate). No assertions on return value — the panic-free property is the entire invariant.

    Test conventions (mandatory):
    - Every TestXxx body uses `t.Run` for each case (table-driven where ≥ 2 cases).
    - Top-level and per-case `t.Parallel()` on TestXxx (NOT inside `FuzzDateUnmarshal` — fuzz targets handle their own concurrency).
    - `require` for setup/preconditions, `assert` for verifications.
    - No `tc := tc` shadow (Go 1.22+ — RESEARCH.md "Anti-Patterns").

    Run `gofmt -w date_test.go` after writing.
  </action>
  <verify>
    <automated>go test -race -run 'TestNewDate|TestParseDate|TestDate_MarshalJSON|TestDate_UnmarshalJSON|TestDate_String|TestDate_Equal|TestDate_Before|TestDate_After|TestDate_Compare|TestDate_DaysUntil' ./... 2>&amp;1 | tail -1 | grep -q '^ok' &amp;&amp; go test -fuzz=FuzzDateUnmarshal -fuzztime=10s ./... 2>&amp;1 | grep -qE 'no new interesting|fuzz: elapsed' &amp;&amp; gofmt -l date_test.go | grep -c '.' | grep -q '^0$' &amp;&amp; go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - Source: `date_test.go` declares `package openholidays` (in-package — needs access to unexported `errEmptyDate` and `dateLayout`).
    - Source: Exactly 10 top-level `func TestXxx` functions (TestNewDate, TestParseDate, TestDate_MarshalJSON, TestDate_UnmarshalJSON, TestDate_String, TestDate_Equal, TestDate_Before, TestDate_After, TestDate_Compare, TestDate_DaysUntil) AND exactly one `func FuzzDateUnmarshal(*testing.F)`.
    - Source: Every test case (table row) is wrapped in `t.Run(name, func(t *testing.T){ ... })` — no top-level assertions in any TestXxx body.
    - Source: `FuzzDateUnmarshal` has at least 7 `f.Add(...)` seed corpus entries covering `"2025-12-24"`, `null`, `""`, `"2024-02-29"`, `123`, `"not-a-date"`, and empty bytes.
    - Behavior: `go test -race ./...` exits 0 and reports `ok`.
    - Behavior: `go test -fuzz=FuzzDateUnmarshal -fuzztime=10s ./...` exits 0 without any failing input crashes (panic-free fuzz invariant per JSON-3).
    - Behavior: ROADMAP success criterion #1 verified by TestDate_MarshalJSON round-trip case AND TestDate_UnmarshalJSON null/empty rejection cases.
    - CLI: `gofmt -l date_test.go` produces no output; `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>date_test.go ships all 10 TestXxx + FuzzDateUnmarshal; testify pattern followed; ROADMAP criterion #1 locked; `go test -race ./...` + `go test -fuzz=FuzzDateUnmarshal -fuzztime=10s ./...` both green.</done>
</task>

</tasks>

<verification>
```bash
go test -race -cover ./...                                # all date tests green
go test -fuzz=FuzzDateUnmarshal -fuzztime=10s ./...       # fuzz finds nothing in 10s (smoke check)
gofmt -l date.go date_test.go                              # produces no output
go vet ./...                                               # vets clean
grep -c 'func Test' date_test.go                           # 10
grep -c 'func FuzzDateUnmarshal' date_test.go              # 1
grep -c '^type Date struct {$' date.go                     # 1
! grep -q '^var ErrEmptyDate' date.go                      # errEmptyDate is unexported (D-06)
```
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External JSON input → `(*Date).UnmarshalJSON` | The single untrusted-input ingress in Phase 1. Phase 2 will feed actual upstream API bytes through this method. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-DOS | Denial-of-service | `(*Date).UnmarshalJSON` slow path on adversarial input | mitigate | `time.Parse` is O(len(s)) and resistant to backtracking. Empty-string and non-string-JSON branches short-circuit before `time.Parse`. `FuzzDateUnmarshal` (Phase 1 — D-12 / JSON-3) provides defense-in-depth: panics surface in fuzz runs. |
| T-01-03-JSON | Tampering | Silent acceptance of `null` or `""` producing zero `Date{}` | mitigate | D-06 mandates both branches return errors (Pitfall 1 / JSON-3 / JSON-4). Test cases `TestDate_UnmarshalJSON/null` and `/empty_string` assert `errors.Is(err, errEmptyDate)` — regression caught at CI. |
| T-01-03-TZ | Tampering | Local-TZ producing off-by-one dates | mitigate | TZ-1 mitigation: `time.Parse(dateLayout, s)` returns UTC by default; `toUTCMidnight` defensive normalization is invoked by every comparison method (D-09). Tests verify defensive normalization with explicit non-UTC operands. |
| T-01-03-IL | Information Disclosure | `(*Date).UnmarshalJSON` error message contains user input | accept | Error messages include the raw `[]byte` input (for diagnostics: `"openholidays: date must be a JSON string, got %s"`). Date strings are user-supplied non-secret data. ERR-04 is satisfied — no credentials, no full HTTP response bodies. |

No new package installs in this plan. No `[ASSUMED]`/`[SUS]` packages → no human-verify checkpoint needed.
</threat_model>

<success_criteria>
1. `date.go` ships every identifier from the &lt;interfaces&gt; block with the exact signatures and the canonical RESEARCH.md §"Pattern 1" body.
2. `date_test.go` declares exactly 10 TestXxx + 1 FuzzDateUnmarshal; Gold Rule 3 followed (testify + t.Run + one-per-prod-function).
3. `go test -race ./...` exits 0; `go test -fuzz=FuzzDateUnmarshal -fuzztime=10s ./...` exits 0 (no panics found in 10s smoke).
4. ROADMAP success criterion #1 verified: `var d Date; json.Unmarshal([]byte("\"2025-12-24\""), &d)` round-trips through `MarshalJSON` to the same bytes; `null` and `""` produce errors, not silent zero values.
5. `gofmt -l date.go date_test.go` produces no output; `go vet ./...` exits 0.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done. Record: Date type stable; ROADMAP criterion #1 locked; FuzzDateUnmarshal shipped in Phase 1 per CL-03 (must land in PROJECT.md Key Decisions by Plan 06); `errEmptyDate` kept unexported per D-06 so the public 5-sentinel surface is preserved.
</output>
