---
phase: 05-distribution
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - example_test.go
  - README.md
  - docs/design.md
  - CHANGELOG.md
  - CONTRIBUTING.md
  - doc.go
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-06
  - DOC-07
  - TEST-09
tags:
  - documentation
  - godoc
  - pkg.go.dev

must_haves:
  truths:
    - "example_test.go lives in package openholidays_test (external test package — pkg.go.dev convention) with one Example_<Symbol> per exported method on Client (PublicHolidays, SchoolHolidays, Countries, Languages, Subdivisions, IsInRegion, Close) AND on Holiday helpers (NameFor, IsInRegion, Days, Range) AND a top-level Example_quickstart that mirrors the README quickstart (TEST-09 + DOC-03)."
    - "Deterministic Example_* with // Output: blocks (Holiday.NameFor, HolidayType.IsKnown); compile-only ExampleClient_* (no // Output:) for methods that depend on the live API (Pitfall 7)."
    - "README.md ≤20-line quickstart that compiles as Example_quickstart (DOC-01); badges in order [CI] [codecov] [goreport] [godoc] [license] per RESEARCH §Open Questions Q4."
    - "doc.go extended with quickstart pointer referencing Example_quickstart; em-dashes preserved (CL-17 / CONVENTIONS.md)."
    - "docs/design.md exists with ASCII transport-chain + cache diagrams from RESEARCH §System Architecture (DOC-04)."
    - "CHANGELOG.md is a one-line pointer per CONTEXT.md D-12 + RESEARCH Open Question Q3 (goreleaser auto-generates the actual change log on GitHub Releases)."
    - "CONTRIBUTING.md documents dev loop (go test, go test -race, golangci-lint run, go test -tags=integration, go test -fuzz) + conventional-commit policy note + branch/PR flow (DOC-06)."
    - "Every exported symbol's godoc comment begins with its name (DOC-07 — verified by grep; matches pkg.go.dev best practice)."
  artifacts:
    - path: "example_test.go"
      provides: "Example_quickstart + one Example_<Symbol> per exported method"
    - path: "README.md"
      provides: "Badges + ≤20-line quickstart + public API table pointer + license"
    - path: "docs/design.md"
      provides: "ASCII transport-chain diagram + cache architecture + error model"
    - path: "CHANGELOG.md"
      provides: "One-line pointer to GitHub Releases (D-12)"
    - path: "CONTRIBUTING.md"
      provides: "Dev loop + conventional-commit note + branch+PR flow"
    - path: "doc.go"
      provides: "Extended package overview with quickstart pointer"
  key_links:
    - from: "example_test.go"
      to: "openholidays public API"
      via: "ExampleClient_PublicHolidays imports github.com/egeek-tech/go-openholidays; pkg.go.dev attaches each Example_* to its target symbol"
      pattern: "package openholidays_test"
    - from: "README.md"
      to: "example_test.go::Example_quickstart"
      via: "README quickstart copy-pastes the Example_quickstart body to keep the doc and the test in sync"
      pattern: "ExampleClient|Example_quickstart"
---

<objective>
Land the full documentation surface so v0.1.0 ships with discoverable, runnable, professional-grade docs. Every exported symbol gets godoc; every public method gets at least one Example_*; README presents badges + ≤20-line quickstart; docs/design.md captures the transport architecture; CHANGELOG points to the auto-generated release notes; CONTRIBUTING covers the dev loop.

Purpose:
- DOC-01: README with badges + quickstart.
- DOC-02: doc.go package overview + runnable example pointer.
- DOC-03: example_test.go — one Example_* per public method (TEST-09).
- DOC-04: docs/design.md — architecture doc.
- DOC-05: CHANGELOG.md — one-line pointer per D-12.
- DOC-06: CONTRIBUTING.md — dev loop.
- DOC-07: Every exported symbol has Go-style doc comment (audited via grep).

Output:
- `example_test.go` — package openholidays_test; Example_quickstart + ~12 Example_<Symbol>.
- `README.md` — badges + quickstart + API table.
- `docs/design.md` — architecture doc.
- `CHANGELOG.md` — one-line pointer.
- `CONTRIBUTING.md` — dev loop + conventions.
- `doc.go` — extended with quickstart pointer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distribution/05-CONTEXT.md
@.planning/phases/05-distribution/05-RESEARCH.md
@.planning/phases/05-distribution/05-PATTERNS.md

<interfaces>
Public surface to document (Example_* per pkg.go.dev convention "Example_<Symbol>" or "Example<Type>_<Method>"):

From client.go / options.go:
  - NewClient → ExampleNewClient
  - Client.Close → ExampleClient_Close
  - All `WithXxx` options → ExampleWithRetry, ExampleWithCache, ExampleWithRequestHook (bundle as one demo per CONTEXT.md "Claude's Discretion")

From countries.go / languages.go / subdivisions.go / public_holidays.go / school_holidays.go:
  - Client.Countries → ExampleClient_Countries
  - Client.Languages → ExampleClient_Languages
  - Client.Subdivisions → ExampleClient_Subdivisions
  - Client.PublicHolidays → ExampleClient_PublicHolidays
  - Client.SchoolHolidays → ExampleClient_SchoolHolidays

From holiday.go / client_isinregion.go:
  - Holiday.NameFor → ExampleHoliday_NameFor (// Output: deterministic — uses constructed Holiday literal)
  - Holiday.IsInRegion → ExampleHoliday_IsInRegion (// Output: deterministic)
  - Holiday.Days → ExampleHoliday_Days (// Output: deterministic)
  - Holiday.Range → ExampleHoliday_Range (// Output: deterministic)
  - Client.IsInRegion → ExampleClient_IsInRegion (compile-only)

From types.go:
  - Country.NameFor, Language.NameFor, Subdivision.NameFor (bundle into one ExampleCountry_NameFor with // Output:)
  - HolidayType.IsKnown → ExampleHolidayType_IsKnown (// Output: deterministic)

From date.go:
  - NewDate → ExampleNewDate (// Output:)
  - ParseDate → ExampleParseDate (// Output:)

Top-level:
  - Example_quickstart — README's ≤20-line quickstart, identical text; compile-only.

Conventions (per RESEARCH Pitfall 7 + CONTEXT.md Claude's Discretion):
  - Methods that hit the live API → compile-only (no `// Output:` block).
  - Pure helpers / constructors → `// Output:` block with deterministic value.

Library version (for README badge URLs and quickstart): module path is `github.com/egeek-tech/go-openholidays` (D-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create example_test.go with Example_quickstart + one Example_<Symbol> per exported method (TEST-09 + DOC-03)</name>
  <files>example_test.go</files>
  <read_first>
    - .planning/phases/05-distribution/05-RESEARCH.md (§"Code Examples → Example_*" lines 767-819 — ExampleClient_PublicHolidays compile-only + ExampleHoliday_NameFor with // Output: Boże Narodzenie; §"Common Pitfalls → Pitfall 7" — // Output: only when deterministic; §"Open Questions Q5" — single example_test.go at root, not split)
    - .planning/phases/05-distribution/05-PATTERNS.md (§"example_test.go (godoc-example, request-response)" — godoc voice mirror; package openholidays_test convention; em-dash style)
    - holiday.go (NameFor + IsInRegion + Days + Range signatures and behavior)
    - types.go (LocalizedText, SubdivisionRef shapes for in-memory literals; HolidayType IsKnown semantics)
    - date.go (NewDate, ParseDate signatures; Date.String() emits "YYYY-MM-DD")
    - public_holidays.go (PublicHolidaysRequest fields)
    - client_isinregion.go (Client.IsInRegion signature)
  </read_first>
  <action>
    Create `example_test.go` at repo root declared `package openholidays_test` (external test package — pkg.go.dev convention; exercises only the public surface per RESEARCH §Pattern + PATTERNS.md note).

    Top-of-file godoc preamble: "Example_<Symbol> tests for pkg.go.dev rendering. Tests with `// Output:` blocks are deterministic doctests; tests without // Output: are compile-only — used where output depends on the live API. Convention per RESEARCH §Pitfall 7."

    Imports: stdlib (`context`, `fmt`, `time`) + library (`github.com/egeek-tech/go-openholidays`).

    Implement the following Example_* functions. For each, the godoc comment begins with the function name (per pkg.go.dev best practice) and explains briefly what the example demonstrates.

    **Compile-only (no `// Output:`) — methods that hit the live API:**

    1. `func Example_quickstart()` — top-level example mirroring the README quickstart (DOC-01). ≤20 lines of code. Body: `c := openholidays.NewClient(); defer c.Close(); ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second); defer cancel(); hs, err := c.PublicHolidays(ctx, openholidays.PublicHolidaysRequest{CountryIsoCode: "PL", ValidFrom: openholidays.NewDate(2025, time.January, 1), ValidTo: openholidays.NewDate(2025, time.December, 31)}); if err != nil { fmt.Println("error:", err); return }; fmt.Printf("got %d Polish public holidays\n", len(hs))`.

    2. `func ExampleClient_PublicHolidays()` — same shape as Example_quickstart but named per the canonical pkg.go.dev attachment rule (RESEARCH §Code Examples lines 783-805).

    3. `func ExampleClient_SchoolHolidays()` — analogous, with SchoolHolidaysRequest (add `SubdivisionCode: "PL-SL"` to demonstrate the regional filter).

    4. `func ExampleClient_Countries()` — `c.Countries(ctx, openholidays.CountriesRequest{LanguageIsoCode: "en"})`.

    5. `func ExampleClient_Languages()` — `c.Languages(ctx, openholidays.LanguagesRequest{})`.

    6. `func ExampleClient_Subdivisions()` — `c.Subdivisions(ctx, openholidays.SubdivisionsRequest{CountryIsoCode: "PL", LanguageIsoCode: "en"})`.

    7. `func ExampleClient_IsInRegion()` — `c.IsInRegion(ctx, h, "PL-SL")` against a constructed Holiday — compile-only because c.IsInRegion may issue HTTP for hierarchical lookups (Plan 03-07 Phase 3).

    8. `func ExampleClient_Close()` — illustrates `c := NewClient(); defer c.Close()` idempotency.

    9. `func ExampleNewClient()` — illustrates options composition: `c := NewClient(WithUserAgent("myapp/1.0"), WithTimeout(15*time.Second), WithRetry(3, 250*time.Millisecond), WithCache(time.Hour))`.

    **Deterministic (`// Output:` blocks) — pure helpers + constructors:**

    10. `func ExampleHoliday_NameFor()` — mirror RESEARCH §Code Examples lines 807-818 verbatim. Body uses constructed Holiday literal with `Name: []LocalizedText{{Language: "pl", Text: "Boże Narodzenie"}, {Language: "en", Text: "Christmas Day"}}`; calls `fmt.Println(h.NameFor("xx"))`; `// Output: Boże Narodzenie` (fallback to first entry — the Polish testdata exception applies per CONVENTIONS.md Rule 1).

    11. `func ExampleHoliday_IsInRegion()` — construct `Holiday{Nationwide: false, Subdivisions: []SubdivisionRef{{Code: "PL-SL"}}}`; `fmt.Println(h.IsInRegion("PL-SL"), h.IsInRegion("PL-WP"))`; `// Output: true false`.

    12. `func ExampleHoliday_Days()` — `h := Holiday{StartDate: NewDate(2025,1,1), EndDate: NewDate(2025,1,7)}; fmt.Println(h.Days())`; `// Output: 7` (inclusive day count per HELP-03).

    13. `func ExampleHoliday_Range()` — iterate Range() over a 3-day Holiday; print each Date; `// Output:` shows the three dates.

    14. `func ExampleCountry_NameFor()` — `c := Country{IsoCode: "PL", Name: []LocalizedText{{Language: "en", Text: "Poland"}, {Language: "de", Text: "Polen"}}}; fmt.Println(c.NameFor("de"))`; `// Output: Polen`.

    15. `func ExampleHolidayType_IsKnown()` — `fmt.Println(HolidayType("Public").IsKnown(), HolidayType("Bogus").IsKnown())`; `// Output: true false`.

    16. `func ExampleNewDate()` — `d := NewDate(2025, time.December, 24); fmt.Println(d)` (Date.String produces "YYYY-MM-DD"); `// Output: 2025-12-24`.

    17. `func ExampleParseDate()` — `d, err := ParseDate("2025-12-24"); if err != nil { fmt.Println("error:", err); return }; fmt.Println(d)`; `// Output: 2025-12-24`.

    Use prefix `openholidays.` consistently on every symbol since the file is in `package openholidays_test`. Em-dash style allowed in godoc comments. Gold Rule 1 English-only — the only exception is the Polish-language text inside JSON-like literals in the deterministic Example output blocks (matches testdata-fixture rule).

    Total expected count: 17 Example_* functions covering every exported method, helper, and constructor on the public surface.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && go vet ./. 2>&1 && go test -run '^Example' -count=1 ./. 2>&1 | tee /tmp/examples.out; grep -E '^(--- FAIL|PASS|ok)' /tmp/examples.out</automated>
  </verify>
  <acceptance_criteria>
    - File `example_test.go` exists at repo root.
    - First declaration is `package openholidays_test`.
    - `grep -E '^func Example_quickstart\(\)' example_test.go` exits 0.
    - `grep -cE '^func Example' example_test.go` ≥ 15 (one per public method/helper).
    - `grep -F 'ExampleClient_PublicHolidays' example_test.go` exits 0.
    - `grep -F 'ExampleClient_SchoolHolidays' example_test.go` exits 0.
    - `grep -F 'ExampleClient_Countries' example_test.go` exits 0.
    - `grep -F 'ExampleClient_Languages' example_test.go` exits 0.
    - `grep -F 'ExampleClient_Subdivisions' example_test.go` exits 0.
    - `grep -F 'ExampleHoliday_NameFor' example_test.go` exits 0.
    - `grep -F 'ExampleHoliday_IsInRegion' example_test.go` exits 0.
    - `grep -F 'ExampleHoliday_Days' example_test.go` exits 0.
    - `grep -F 'ExampleHoliday_Range' example_test.go` exits 0.
    - `grep -F 'ExampleHolidayType_IsKnown' example_test.go` exits 0.
    - `grep -F 'ExampleNewDate' example_test.go` exits 0.
    - `grep -F '// Output:' example_test.go` finds ≥ 6 deterministic output blocks.
    - `grep -F 'github.com/stretchr/testify' example_test.go` returns NOTHING (Example_* must NOT use testify; pkg.go.dev only renders pure Examples).
    - `go vet ./.` exits 0.
    - `go test -run '^Example' -count=1 .` exits 0 — all Examples compile AND all deterministic ones match their `// Output:` blocks.
    - At least one deterministic Example (e.g., ExampleHoliday_NameFor) verified by `go test -run ExampleHoliday_NameFor .` exiting 0.
  </acceptance_criteria>
  <done>
    All 15+ Example_* exist, deterministic ones pass `go test -run Example`, compile-only ones link cleanly. pkg.go.dev will attach each to its target symbol after the v0.1.0 tag.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author README.md + extend doc.go (DOC-01 + DOC-02)</name>
  <files>README.md, doc.go</files>
  <read_first>
    - .planning/phases/05-distribution/05-RESEARCH.md (§"Open Questions Q4" — badge order [CI] [codecov] [goreport] [godoc] [license]; §"Documentation — README.md")
    - .planning/phases/05-distribution/05-CONTEXT.md (D-09 Codecov badge URL format)
    - .planning/phases/05-distribution/05-PATTERNS.md (§"doc.go" — em-dash preservation; current doc.go contents at lines 488-503)
    - doc.go (current contents — extend, don't rewrite)
    - example_test.go (Task 1 — Example_quickstart is the canonical quickstart text)
  </read_first>
  <action>
    **README.md** — author at repo root. UTF-8 plain text. Required sections in order:

    1. **Title** — `# go-openholidays`.
    2. **Badges** in the order per RESEARCH §Open Questions Q4:
       - CI: `[![CI](https://github.com/egeek-tech/go-openholidays/actions/workflows/ci.yml/badge.svg)](https://github.com/egeek-tech/go-openholidays/actions/workflows/ci.yml)`
       - Codecov: `[![codecov](https://codecov.io/gh/egeek-tech/go-openholidays/branch/main/graph/badge.svg)](https://codecov.io/gh/egeek-tech/go-openholidays)`
       - Go Report Card: `[![Go Report Card](https://goreportcard.com/badge/github.com/egeek-tech/go-openholidays)](https://goreportcard.com/report/github.com/egeek-tech/go-openholidays)`
       - Godoc: `[![Go Reference](https://pkg.go.dev/badge/github.com/egeek-tech/go-openholidays.svg)](https://pkg.go.dev/github.com/egeek-tech/go-openholidays)`
       - License: `[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)`
    3. **One-line description** — "An idiomatic, dependency-light Go client for the public OpenHolidays API. Public holidays + school holidays per administrative subdivision (e.g. Polish *ferie* per województwo)."
    4. **Install** — `go get github.com/egeek-tech/go-openholidays`.
    5. **Quickstart** — ≤20 lines fenced Go code block. Body MUST be identical to Example_quickstart in `example_test.go` (Task 1). DOC-01 requires the quickstart compiles; testing is via `go test -run Example_quickstart` (covered by Task 1's acceptance criterion).
    6. **Public API** — short table or bulleted list pointing to pkg.go.dev for the full reference. Mention: `Client.{PublicHolidays, SchoolHolidays, Countries, Languages, Subdivisions}`, `Holiday.{NameFor, IsInRegion, Days, Range}`, opt-in middleware `WithRetry`, `WithCache`, `WithRequestHook`, `WithStrictDecoding`.
    7. **CLI** — short section on `cmd/ohcli`: `go install github.com/egeek-tech/go-openholidays/cmd/ohcli@latest`; example invocations `ohcli public PL 2025`, `ohcli school PL 2025 --region PL-SL`, `ohcli countries --json`.
    8. **Contributing** — one-line pointer: "See [CONTRIBUTING.md](./CONTRIBUTING.md) for the dev loop."
    9. **License** — "MIT — see [LICENSE](./LICENSE)".

    English-only. Em-dashes acceptable.

    **doc.go** — EXTEND the existing file. Read it first; preserve all existing prose verbatim. Append after the "Design principles:" bullet list (before the closing `package openholidays` line — godoc-style trailing blank line after each paragraph):

    ```
    //
    // Quickstart:
    //
    //	c := openholidays.NewClient()
    //	defer c.Close()
    //	hs, err := c.PublicHolidays(ctx, openholidays.PublicHolidaysRequest{
    //	    CountryIsoCode: "PL",
    //	    ValidFrom:      openholidays.NewDate(2025, time.January, 1),
    //	    ValidTo:        openholidays.NewDate(2025, time.December, 31),
    //	})
    //
    // See package examples on pkg.go.dev for every exported method.
    ```

    Keep the existing em-dashes (CL-17 / CONVENTIONS.md). Do not mass-replace.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && go vet ./. 2>&1 && wc -l README.md && grep -E '^```go' README.md | head -3 && grep -F 'pkg.go.dev/github.com/egeek-tech/go-openholidays' README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `README.md` exists at repo root.
    - `head -1 README.md` is `# go-openholidays` (or a heading starting with `#` and containing "go-openholidays").
    - `grep -F 'codecov.io/gh/egeek-tech/go-openholidays' README.md` exits 0 (D-09 badge URL).
    - `grep -F 'pkg.go.dev/badge/github.com/egeek-tech/go-openholidays' README.md` exits 0 (godoc badge).
    - `grep -F 'goreportcard.com' README.md` exits 0 (Go Report Card badge).
    - `grep -F 'License-MIT' README.md` exits 0.
    - `grep -F 'actions/workflows/ci.yml' README.md` exits 0 (CI badge link).
    - The order of badge lines (verified with `grep -n` + sort): CI line number < Codecov line < goreportcard < pkg.go.dev < License (per RESEARCH §Open Questions Q4).
    - `grep -F '```go' README.md` exits 0 (a Go-fenced quickstart code block is present).
    - The quickstart code block has ≤20 lines of Go code (excluding the ```go and ``` fence lines): `awk '/^```go$/,/^```$/' README.md | grep -vE '^```' | wc -l` is ≤20.
    - `grep -F 'go get github.com/egeek-tech/go-openholidays' README.md` exits 0 (install instruction).
    - `grep -F 'CONTRIBUTING.md' README.md` exits 0 (link to contrib doc).
    - `grep -F 'LICENSE' README.md` exits 0 (link to license).
    - File `doc.go` still exists; first non-comment declaration is `package openholidays`.
    - `grep -F 'Quickstart' doc.go` exits 0 (new section added).
    - `grep -F 'c.PublicHolidays(ctx' doc.go` exits 0 (quickstart pointer code in godoc).
    - The original `doc.go` opening sentence "Package openholidays is a Go client for the OpenHolidays public-holidays and school-holidays API" preserved (no mass-replace).
    - Em-dashes preserved in doc.go: `grep -F '—' doc.go` exits 0 (CL-17).
    - `go vet ./.` exits 0.
  </acceptance_criteria>
  <done>
    README.md authored with badges + quickstart + API pointer; doc.go extended without losing prior content; em-dashes preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author docs/design.md + CHANGELOG.md + CONTRIBUTING.md + godoc audit (DOC-04 + DOC-05 + DOC-06 + DOC-07)</name>
  <files>docs/design.md, CHANGELOG.md, CONTRIBUTING.md</files>
  <read_first>
    - .planning/phases/05-distribution/05-RESEARCH.md (§"System Architecture Diagram" lines 181-243 — ASCII transport-chain reference for docs/design.md; §"Open Questions Q3" — CHANGELOG.md one-line pointer pattern; §"CHANGELOG strategy"; §"Documentation — CONTRIBUTING.md")
    - .planning/phases/05-distribution/05-CONTEXT.md (D-12 CHANGELOG via goreleaser auto-gen; "Specifics" — conventional commits already de facto; CONTRIBUTING depth minimal for v0.1.0)
    - transport.go, transport_cache.go, transport_hook.go, retry.go, cache.go (the architecture docs/design.md describes — RoundTripper chain order from STATE.md Key Decisions: retry → cache → hook → logging → header → base)
    - errors.go (error model section of docs/design.md — sentinels + APIError)
  </read_first>
  <action>
    **docs/design.md** — author at `docs/design.md` (create the `docs/` directory). DOC-04 — short architecture doc.

    Required sections (English-only; ASCII diagrams preferred per CONTEXT.md "Claude's Discretion" + RESEARCH §Open Questions):

    1. `# go-openholidays — Design`.
    2. **Overview** — one paragraph: "go-openholidays is a single-package Go SDK that exposes five OpenHolidays endpoints through a Client with a composable RoundTripper chain. This doc describes the architecture; pkg.go.dev hosts the per-symbol reference."
    3. **Client Lifecycle** — paragraph + small ASCII diagram showing NewClient → composed *http.Client (transport chain) → endpoint methods → Close (stops cache sweeper).
    4. **RoundTripper Chain** — ASCII diagram from RESEARCH §"System Architecture Diagram" lines 217-220 (`hook → cache → logging → header → base`), explained per Phase 2 + Phase 4 decisions. Quote the key invariant: "Retry lives in the endpoint layer, NOT as a RoundTripper" (RESIL-05 / STATE.md Key Decisions).
    5. **Cache Architecture** — paragraph on RESIL-06..09: public Cache interface, MemoryCache impl, sweeper goroutine started lazily on first Set, stopped by Close. Note RESIL-07: only Countries/Languages/Subdivisions cached by default; PublicHolidays/SchoolHolidays never cached.
    6. **Retry Architecture** — paragraph on RESIL-01..05: exponential backoff + full jitter (`math/rand/v2`), Retry-After header parsing (integer + HTTP-date), ctx-aware sleep, endpoint-layer placement to avoid double-retry with caller-supplied *http.Client.
    7. **Error Model** — list sentinels (`ErrInvalidCountry`, `ErrInvalidLanguage`, `ErrInvalidDateRange`, `ErrDateRangeTooLarge`, `ErrEmptyResponse`, `ErrResponseTooLarge`, `ErrMalformedResponse`) + `*APIError{StatusCode, Path, Body}`. Note ERR-04: never leak credentials or full bodies above Debug.
    8. **Strict Decoding** — one paragraph: WithStrictDecoding(bool) is OFF by default + immutable (CL-16); strict mode applies to cached entries on every Get because decode runs on read.

    No Mermaid diagrams. Em-dashes acceptable.

    **CHANGELOG.md** — one-line pointer per CONTEXT.md D-12 + RESEARCH §Open Questions Q3:

    ```
    # Changelog

    Release notes for go-openholidays are generated from conventional commits
    and published on each GitHub Release.

    See: https://github.com/egeek-tech/go-openholidays/releases
    ```

    **CONTRIBUTING.md** — author per CONTEXT.md Claude's Discretion (minimal v0.1.0):

    Required sections:
    1. `# Contributing to go-openholidays`.
    2. **Dev Loop** — bulleted commands:
       - `go test -race ./...` — unit tests with race detector.
       - `go test -race -cover ./...` — coverage report.
       - `golangci-lint run` — lint (config in `.golangci.yml`).
       - `govulncheck ./...` — vulnerability scan.
       - `OPENHOLIDAYS_LIVE=1 go test -tags=integration ./...` — integration tests against live API.
       - `go test -fuzz=FuzzUnmarshalHoliday -fuzztime=30s .` — fuzz target.
    3. **Commits** — note that this project uses Conventional Commits 1.0 (see https://www.conventionalcommits.org/en/v1.0.0/). Examples: `feat(cli): add --csv output`, `fix(retry): parseRetryAfter overflow`, `docs(05): refresh README`. Prefixes parsed by goreleaser to generate the changelog (Plan 06's `.goreleaser.yaml`).
    4. **Branch + PR Flow** — short paragraph: create a feature branch from main, open a PR, ensure CI is green (matrix + lint + vuln), squash on merge.
    5. **Code Style** — pointer to `.planning/codebase/CONVENTIONS.md` (Gold Rules 1, 2, 3); restate one-line summaries.

    No Code of Conduct, no DCO note, no issue templates (all deferred per CONTEXT.md).

    English-only. Em-dashes acceptable.

    **DOC-07 godoc audit** — for every exported symbol across the production tree, confirm the doc comment begins with the symbol name. This is a verification-only step in this task (no code change unless the audit surfaces a non-compliant comment). The grep check is in the acceptance criteria below; if the audit fails, FIX the offending godoc comments before declaring this task done.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && test -f docs/design.md && test -f CHANGELOG.md && test -f CONTRIBUTING.md && for sym in NewClient WithBaseURL WithTimeout WithUserAgent WithHTTPClient WithLogger WithStrictDecoding WithRetry WithMaxRetryWait WithCache WithCacheBackend WithRequestHook NameFor IsInRegion Days Range Holiday HolidayType Country Language Subdivision LocalizedText SubdivisionRef GroupRef Date NewDate ParseDate ErrInvalidCountry ErrInvalidLanguage ErrDateRangeTooLarge ErrInvalidDateRange ErrEmptyResponse ErrResponseTooLarge ErrMalformedResponse APIError Cache MemoryCache NewMemoryCache RequestHookFunc CacheHitContextKey Version; do grep -l -E "^// $sym " *.go > /dev/null 2>&1 || echo "MISSING: $sym"; done | tee /tmp/godoc-audit.txt; test ! -s /tmp/godoc-audit.txt && echo "DOC-07: all godoc comments start with symbol name"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/design.md` exists; first line is `# go-openholidays — Design` or `# go-openholidays - Design` (em-dash or hyphen acceptable in title).
    - `grep -F 'RoundTripper' docs/design.md` exits 0 (architecture section present).
    - `grep -F 'retry → cache → hook → logging → header → base' docs/design.md || grep -F 'retry -> cache -> hook -> logging -> header -> base' docs/design.md` exits 0 (chain order documented).
    - `grep -F 'Cache' docs/design.md` exits 0 (cache architecture section).
    - `grep -F 'WithStrictDecoding' docs/design.md` exits 0 (strict-decoding section).
    - `grep -F 'APIError' docs/design.md` exits 0 (error model section).
    - At least one ASCII diagram present: `grep -E '^\s*[│|+─\-]' docs/design.md` finds at least 3 lines (visual diagram block).
    - File `CHANGELOG.md` exists.
    - `wc -l CHANGELOG.md` shows ≤8 lines (one-line pointer per D-12).
    - `grep -F 'github.com/egeek-tech/go-openholidays/releases' CHANGELOG.md` exits 0.
    - `grep -F 'conventional commits' CHANGELOG.md` exits 0.
    - File `CONTRIBUTING.md` exists.
    - `grep -F 'go test -race' CONTRIBUTING.md` exits 0.
    - `grep -F 'golangci-lint run' CONTRIBUTING.md` exits 0.
    - `grep -F 'govulncheck' CONTRIBUTING.md` exits 0.
    - `grep -F 'OPENHOLIDAYS_LIVE=1' CONTRIBUTING.md` exits 0.
    - `grep -F 'go test -fuzz' CONTRIBUTING.md` exits 0.
    - `grep -F 'Conventional Commits' CONTRIBUTING.md` exits 0 (D-12 enforcement note).
    - `grep -F 'CONVENTIONS.md' CONTRIBUTING.md` exits 0 (Gold Rules pointer).
    - DOC-07 godoc audit: `/tmp/godoc-audit.txt` is empty — every exported symbol's godoc comment begins with the symbol name. If non-empty, this task fails until the offending comments are fixed.
    - `go vet ./.` exits 0.
  </acceptance_criteria>
  <done>
    docs/design.md captures architecture with ASCII diagrams; CHANGELOG.md is the one-line pointer; CONTRIBUTING.md covers dev loop + conventional-commit policy; DOC-07 godoc audit confirms every exported symbol's first sentence starts with its own name.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User reading README badges → external CDN | Badge images are static SVG from github.com/codecov.io/goreportcard.com/pkg.go.dev — trusted publishers. |
| Example_* tests → pkg.go.dev | Tests compiled in CI before tag; pkg.go.dev only renders, never executes user-controlled code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-26 | Tampering | misleading example output drifting from real behavior | low | mitigate | Deterministic examples use `// Output:` blocks that `go test -run Example` re-verifies on every CI run. Compile-only examples link cleanly even if output drifts — README quickstart is anchored to Example_quickstart for sync. |
| T-05-27 | Information Disclosure | example code includes credentials or PII | low | accept | OpenHolidays has no auth; examples use public country codes (PL, DE) and a constructed Holiday literal. No PII anywhere. |
| T-05-28 | Information Disclosure | docs/design.md leaks unfinished architecture | low | accept | docs/design.md describes the SHIPPED v0.1.0 architecture (Phases 1-4). No mention of M2+ items beyond brief "see ROADMAP" pointer. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package installs. Documentation files only. |
</threat_model>

<verification>
- `go test -run '^Example' -count=1 .` exits 0 (TEST-09 — all Example_* compile + deterministic ones match // Output:).
- DOC-07 godoc audit: every exported symbol's doc comment begins with its name (grep loop in verify command).
- README.md has badges in correct order, ≤20-line quickstart, links to CONTRIBUTING + LICENSE.
- docs/design.md describes RoundTripper chain order + cache + retry + error model + strict decoding.
- CHANGELOG.md is the one-line pointer (D-12).
- CONTRIBUTING.md documents the dev loop + conventional-commit policy.
- doc.go extended with quickstart code pointer; em-dashes preserved.
</verification>

<success_criteria>
1. example_test.go has ≥15 Example_* functions covering every exported method/helper.
2. `go test -run Example .` exits 0 (TEST-09).
3. README.md has badges (CI/Codecov/Goreport/Godoc/License in order), ≤20-line quickstart, install instructions, CLI section, CONTRIBUTING + LICENSE pointers.
4. doc.go extended; original prose preserved; em-dashes preserved.
5. docs/design.md authored with RoundTripper chain + cache + retry + error model + strict decoding sections; ASCII diagrams.
6. CHANGELOG.md is a one-line pointer to GitHub Releases (D-12).
7. CONTRIBUTING.md covers dev loop + Conventional Commits + branch/PR flow + Gold Rules pointer.
8. DOC-07 audit: every exported symbol's godoc starts with its name (grep verifies).
</success_criteria>

<output>
Create `.planning/phases/05-distribution/05-07-SUMMARY.md` when done.
</output>
