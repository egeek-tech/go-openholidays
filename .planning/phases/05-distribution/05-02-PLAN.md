---
phase: 05-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - cmd/ohcli/main_test.go
  - cmd/ohcli/public_test.go
  - cmd/ohcli/school_test.go
  - cmd/ohcli/countries_test.go
  - cmd/ohcli/format_test.go
autonomous: true
requirements:
  - CLI-04
tags:
  - cli
  - testing
  - testify
  - httptest

must_haves:
  truths:
    - "Each cmd/ohcli subcommand has exactly one TestXxx per exported (or per-prod) function — TestRun (dispatch), TestCmdPublic, TestCmdSchool, TestCmdCountries, plus TestRenderText/TestRenderJSON/TestRenderCSV/TestRenderCountries (Gold Rule 3)."
    - "All test scenarios live in t.Run subtests with t.Parallel(); require for preconditions, assert for verifications (Gold Rule 3, mirrors public_holidays_test.go pattern)."
    - "Unit tests use httptest.NewServer + fixtures from ../../testdata/*.json. Zero live API calls (D-08)."
    - "Exit-code coverage: 0 (success), 0 (empty result with stderr message per D-07), 1 (network error), 2 (usage error: missing arg / bad flag / unknown command)."
    - "Tests assert the D-05 stderr `ohcli: ` prefix on every error path and on the empty-result path."
    - "CSV/JSON/text renderers each have a dedicated TestRenderXxx with explicit byte assertions."
    - "OPENHOLIDAYS_BASE_URL plumbed via t.Setenv per scenario; no global env mutation between parallel subtests."
  artifacts:
    - path: "cmd/ohcli/main_test.go"
      provides: "TestRun — dispatch behavior (no args, unknown command, version, help)"
    - path: "cmd/ohcli/public_test.go"
      provides: "TestCmdPublic — text/json/csv output, empty results, exit codes 0/1/2, --lang fallback"
    - path: "cmd/ohcli/school_test.go"
      provides: "TestCmdSchool — --region PL-SL filter, all output modes, exit codes"
    - path: "cmd/ohcli/countries_test.go"
      provides: "TestCmdCountries — countries dispatch, --format, empty-result branch"
    - path: "cmd/ohcli/format_test.go"
      provides: "TestRenderText, TestRenderJSON, TestRenderCSV, TestRenderCountries — pure transform assertions"
  key_links:
    - from: "cmd/ohcli/*_test.go"
      to: "cmd/ohcli/main.go::run"
      via: "code := run(argv, &stdout, &stderr) — assert code + buffers"
      pattern: "code := run\\(\\[\\]string\\{"
    - from: "cmd/ohcli/*_test.go"
      to: "testdata/{public_holidays_pl_2025,school_holidays_pl_2025,countries}.json"
      via: "os.ReadFile(filepath.Join(\"..\", \"..\", \"testdata\", ...))"
      pattern: "filepath\\.Join\\(\"\\.\\.\", \"\\.\\.\", \"testdata\""
    - from: "cmd/ohcli/*_test.go"
      to: "OPENHOLIDAYS_BASE_URL → openholidays.WithBaseURL"
      via: "t.Setenv(\"OPENHOLIDAYS_BASE_URL\", srv.URL)"
      pattern: "t\\.Setenv\\(\"OPENHOLIDAYS_BASE_URL\""
---

<objective>
Land the CLI test suite proving every subcommand, flag, exit code, output mode, and error path works correctly against `httptest.NewServer`. Tests mirror the existing repo's testify+t.Run+t.Parallel patterns from `public_holidays_test.go` and inject `httptest` URLs into the CLI via the `OPENHOLIDAYS_BASE_URL` seam introduced in Plan 01.

Purpose: CLI-04 requires `go install ./cmd/ohcli` to build clean — this plan adds the behavioral proof that the binary works as specified (D-03 output modes, D-05 stderr prefix, D-06 exit codes, D-07 empty-result behavior, D-08 zero live API calls).

Output:
- `cmd/ohcli/main_test.go` — dispatch tests
- `cmd/ohcli/public_test.go` — TestCmdPublic with t.Run subtests
- `cmd/ohcli/school_test.go` — TestCmdSchool including --region filter
- `cmd/ohcli/countries_test.go` — TestCmdCountries
- `cmd/ohcli/format_test.go` — pure-transform tests for each renderer
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distribution/05-CONTEXT.md
@.planning/phases/05-distribution/05-RESEARCH.md
@.planning/phases/05-distribution/05-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/05-distribution/05-01-SUMMARY.md

<!-- Test patterns to mirror exactly -->
<interfaces>
From public_holidays_test.go — the analog file:
- File-level godoc preamble explaining Gold Rule 3 application
- `package openholidays` (root package; for cmd tests use `package main`)
- Imports: stdlib first, then testify (`assert`, `require`) last group
- One `TestXxx` per prod function; `t.Parallel()` at top + on every leaf `t.Run`
- Fixture loading via `os.ReadFile(filepath.Join("testdata", "X.json"))` — for cmd/ohcli tests use `filepath.Join("..", "..", "testdata", "X.json")`
- `httptest.NewServer(http.HandlerFunc(...))` + `t.Cleanup(srv.Close)`
- Error assertions: `errors.Is(err, ErrXxx)` + `assert.True(t, errors.Is(...))`

From Plan 01 (this Wave's prerequisite):
```go
func run(args []string, stdout, stderr io.Writer) int  // entrypoint exported only for tests
func newClient() *openholidays.Client                  // reads OPENHOLIDAYS_BASE_URL
func cmdPublic(ctx context.Context, args []string, stdout, stderr io.Writer) int
func cmdSchool(ctx context.Context, args []string, stdout, stderr io.Writer) int
func cmdCountries(ctx context.Context, args []string, stdout, stderr io.Writer) int
func renderText(w io.Writer, hs []openholidays.Holiday, lang string) error
func renderJSON(w io.Writer, hs []openholidays.Holiday) error
func renderCSV(w io.Writer, hs []openholidays.Holiday, lang string) error
func renderCountries(w io.Writer, cs []openholidays.Country, lang, format string) error
```

Test fixtures (existing in repo root testdata/):
- testdata/countries.json
- testdata/public_holidays_pl_2025.json — 14 holidays per Phase 3 golden fixture
- testdata/school_holidays_pl_2025.json — 7 periods including Śląskie ferie zimowe
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cmd/ohcli/format_test.go (pure-transform renderer tests)</name>
  <files>cmd/ohcli/format_test.go</files>
  <behavior>
    - TestRenderText: produces header `DATE  END  NAME  NATIONWIDE  TYPE` and one tab-aligned row per Holiday; empty slice → header only.
    - TestRenderJSON: produces valid JSON; round-trips via `json.Unmarshal` back to `[]openholidays.Holiday` losslessly; indented with 2-space.
    - TestRenderCSV: header row exactly `start_date,end_date,name,nationwide,type,subdivision_codes`; one data row per holiday; subdivision codes joined by `;`; embedded commas in `name` are correctly quoted by `encoding/csv`.
    - TestRenderCountries: header `iso_code,name,official_languages` for CSV; JSON encodes `[]Country`; text format aligns columns.
    - Each TestRenderXxx has at least two t.Run cases: one happy path with a populated slice (constructed in-memory, no fixtures) and one edge case (empty slice or quoted-field).
  </behavior>
  <read_first>
    - .planning/phases/05-distribution/05-PATTERNS.md (§"cmd/ohcli/format_test.go (unit-test, transform)" — analog holiday_test.go pattern + sample TestRenderCSV body)
    - holiday_test.go (pure-helper testify + t.Run + t.Parallel pattern — single source of truth for in-repo style)
    - cmd/ohcli/format.go (signatures from Plan 01 Task 2 — TestRender* asserts against these renderers)
    - types.go (LocalizedText, SubdivisionRef, HolidayType constants — needed to construct in-memory Holiday literals)
    - date.go (NewDate constructor)
  </read_first>
  <action>
    Create `cmd/ohcli/format_test.go` declared `package main` (same-package test so internal helpers are reachable). File-level godoc preamble mirroring `public_holidays_test.go` lines 1-15: explain "One TestXxx per renderer per Gold Rule 3; English-only; in-memory Holiday literals (no fixtures needed)".

    Imports order: stdlib first (`bytes`, `encoding/csv`, `encoding/json`, `strings`, `testing`, `time`) then testify last group (`github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`), then the library at module path.

    Four `TestRenderXxx` functions, each top-level `t.Parallel()`, each containing 2-3 `t.Run` subtests with their own `t.Parallel()`:

    1. `TestRenderText`:
       - "happy path 2 holidays" — construct `[]openholidays.Holiday{{StartDate: NewDate(2025,1,1), EndDate: NewDate(2025,1,1), Type: HolidayType("Public"), Nationwide: true, Name: []LocalizedText{{Language:"en", Text:"New Year's Day"}}}, ...}` (mirror PATTERNS.md sample). `var buf bytes.Buffer; require.NoError(t, renderText(&buf, hs, "en"))`. Assert `buf.String()` contains "DATE", "NAME", "2025-01-01", "New Year's Day", "true", "Public".
       - "empty slice prints header only" — `renderText(&buf, nil, "en")`; `strings.Split(strings.TrimRight(buf.String(),"\n"),"\n")` length is 1; line contains "DATE".

    2. `TestRenderJSON`:
       - "round-trip 1 holiday" — render then `json.Unmarshal(buf.Bytes(), &got)`; `require.Len(t, got, 1)`; `assert.Equal(t, hs[0].StartDate, got[0].StartDate)`.
       - "indent is two spaces" — `assert.Contains(t, buf.String(), "\n  ")` (verifies SetIndent("","  ")).

    3. `TestRenderCSV`:
       - "header row + one data row" (mirror PATTERNS.md body) — `require.Len(t, lines, 2); assert.Equal(t, "start_date,end_date,name,nationwide,type,subdivision_codes", lines[0]); assert.Contains(t, lines[1], "2025-01-01,2025-01-01,New Year,true,Public")`.
       - "name with embedded comma is RFC-4180 quoted" — `Name: [...]{Text:"Friday, Holiday"}` → assert `lines[1]` contains `"Friday, Holiday"` (with literal quotes).
       - "subdivisions joined by semicolon" — `Subdivisions: [SubdivisionRef{Code:"PL-SL"}, {Code:"PL-WP"}]` → `lines[1]` contains `PL-SL;PL-WP`.

    4. `TestRenderCountries`:
       - "json format" — construct `[]openholidays.Country{{IsoCode:"PL", Name:[]LocalizedText{{Language:"en", Text:"Poland"}}}}`; `require.NoError(t, renderCountries(&buf, cs, "en", "json"))`; assert `buf.String()` contains `"PL"` and `"Poland"`.
       - "csv header" — `renderCountries(&buf, cs, "en", "csv")`; first line equals `iso_code,name,official_languages`.

    Use `require` for renderer NoError preconditions; `assert` for content checks. No `t.Setenv`, no `httptest` — pure transforms.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && go test -race -count=1 ./cmd/ohcli/ -run '^TestRender' 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - File `cmd/ohcli/format_test.go` exists.
    - `grep -E '^func TestRenderText\(t \*testing\.T\)' cmd/ohcli/format_test.go` exits 0.
    - `grep -E '^func TestRenderJSON\(t \*testing\.T\)' cmd/ohcli/format_test.go` exits 0.
    - `grep -E '^func TestRenderCSV\(t \*testing\.T\)' cmd/ohcli/format_test.go` exits 0.
    - `grep -E '^func TestRenderCountries\(t \*testing\.T\)' cmd/ohcli/format_test.go` exits 0.
    - `grep -c 't.Parallel()' cmd/ohcli/format_test.go` ≥ 8 (top-level + each leaf t.Run).
    - `grep -c 't.Run(' cmd/ohcli/format_test.go` ≥ 8 (2-3 cases per renderer × 4 renderers).
    - `grep -F 'github.com/stretchr/testify/require' cmd/ohcli/format_test.go` exits 0.
    - `grep -F 'github.com/stretchr/testify/assert' cmd/ohcli/format_test.go` exits 0.
    - `go test -race -count=1 ./cmd/ohcli/ -run '^TestRender'` exits 0 (all 4 renderers green under -race).
    - `go vet ./cmd/ohcli/` exits 0.
  </acceptance_criteria>
  <done>
    All four renderer tests green under `-race`; tests use only in-memory Holiday/Country literals (no fixtures).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create cmd/ohcli/{main,public,school,countries}_test.go (httptest-driven subcommand tests)</name>
  <files>cmd/ohcli/main_test.go, cmd/ohcli/public_test.go, cmd/ohcli/school_test.go, cmd/ohcli/countries_test.go</files>
  <behavior>
    - TestRun: no-args → exit 2 + stderr usage; unknown command → exit 2 + `ohcli: unknown command`; version → exit 0 + stdout "0.1.0"; help / -h / --help → exit 0 + stdout usage.
    - TestCmdPublic: text-mode happy path (PL 2025 → exit 0, stdout contains "2025-01-01" and "DATE" header, stderr empty); --json mode (stdout parses as []Holiday with 14 items); --csv mode (stdout starts with `start_date,end_date,...`); empty result (`[]` body → exit 0 + stderr `ohcli: no public holidays found for PL 2025` + empty stdout per D-07); usage error: missing year → exit 2 + stderr `ohcli: ` prefix; usage error: bad year → exit 2; usage error: invalid format → exit 2; runtime error: server 500 → exit 1 + stderr `ohcli: `.
    - TestCmdSchool: happy path with --region PL-SL filters request (server asserts `subdivisionCode=PL-SL` on the request URL); empty result with region message format `(region PL-XX)` per D-07 "Specifics" wording.
    - TestCmdCountries: happy path against countries.json fixture; --format csv produces correct header; positional arg passed → exit 2 + `ohcli: countries takes no positional args`.
  </behavior>
  <read_first>
    - .planning/phases/05-distribution/05-PATTERNS.md (§"cmd/ohcli/*_test.go (cli-test, request-response)" — full pattern including the cmdPublic mirror sample lines 148-199; fixture-path note about `filepath.Join("..","..","testdata",...)`)
    - public_holidays_test.go (canonical analog — testify + t.Run + t.Parallel + httptest.NewServer + t.Cleanup pattern, error-path assertions via errors.Is)
    - school_holidays_test.go (Śląskie ferie zimowe assertion pattern + subdivisionCode query-string assertion)
    - countries_test.go (countries fixture loading pattern)
    - cmd/ohcli/main.go (run() signature, dispatch arms, usage const) — to confirm exact argv shapes tests will invoke
    - cmd/ohcli/public.go, school.go, countries.go (handler signatures + empty-result message exact strings — tests assert these literally)
    - .planning/phases/05-distribution/05-CONTEXT.md (D-07 "Specifics" — `ohcli: no school holidays found for PL 2025 (region PL-XX)` exact wording)
  </read_first>
  <action>
    Create four sibling test files in `cmd/ohcli/`, all `package main`. Each file's preamble matches the `public_holidays_test.go` voice: explain Gold Rule 3 application ("one TestRun per dispatch, one TestCmdXxx per subcommand handler") and the testdata path note (tests live two levels deep; load fixtures via `filepath.Join("..","..","testdata", name)`).

    **cmd/ohcli/main_test.go — `func TestRun(t *testing.T)`**:
    - `t.Parallel()` at top.
    - t.Run "no args returns exit 2 with usage on stderr":
        `var stdout, stderr bytes.Buffer; code := run([]string{"ohcli"}, &stdout, &stderr); assert.Equal(t, 2, code); assert.Contains(t, stderr.String(), "usage:")`.
    - t.Run "unknown command returns exit 2 with ohcli: prefix":
        `code := run([]string{"ohcli", "frobnicate"}, &stdout, &stderr); assert.Equal(t, 2, code); assert.Contains(t, stderr.String(), "ohcli: unknown command")`.
    - t.Run "version prints library Version":
        `code := run([]string{"ohcli", "version"}, &stdout, &stderr); assert.Equal(t, 0, code); assert.Equal(t, openholidays.Version+"\n", stdout.String())`.
    - t.Run "help prints usage to stdout":
        `code := run([]string{"ohcli", "help"}, &stdout, &stderr); assert.Equal(t, 0, code); assert.Contains(t, stdout.String(), "usage:")`.
    - t.Run "-h prints usage to stdout": same as help, argv `--help` and `-h` cases (table-driven inner subtests fine).

    **cmd/ohcli/public_test.go — `func TestCmdPublic(t *testing.T)`**:
    - Top `t.Parallel()`.
    - For each scenario: build `httptest.NewServer`, `t.Cleanup(srv.Close)`, `t.Setenv("OPENHOLIDAYS_BASE_URL", srv.URL)` (t.Setenv inside a t.Run that is NOT parallel — Go etiquette per internal_test.go).
    - **Note on `t.Setenv` + parallel:** Per Go's testing documentation, `t.Setenv` calls `t.Parallel()` is a runtime error. For these scenarios DO NOT call `t.Parallel()` inside the leaf t.Run. The top-level `t.Parallel()` still allows TestCmdPublic to run in parallel with other TestXxx files; only the inner subtests run sequentially within this test.
    - t.Run "text output for PL 2025 prints 14-row table":
        Load fixture `body, err := os.ReadFile(filepath.Join("..","..","testdata","public_holidays_pl_2025.json")); require.NoError(t, err)`.
        Server handler: `assert.Equal(t, "/PublicHolidays", r.URL.Path); assert.Equal(t, "PL", r.URL.Query().Get("countryIsoCode")); assert.Equal(t, "2025-01-01", r.URL.Query().Get("validFrom")); assert.Equal(t, "2025-12-31", r.URL.Query().Get("validTo")); w.Header().Set("Content-Type","application/json"); _, _ = w.Write(body)`.
        `code := run([]string{"ohcli","public","PL","2025"}, &stdout, &stderr); require.Equal(t, 0, code, "stderr: %s", stderr.String()); assert.Contains(t, stdout.String(), "DATE"); assert.Contains(t, stdout.String(), "2025-01-01"); assert.Empty(t, stderr.String())`.
    - t.Run "--json output is valid JSON []Holiday":
        Same handler; `code := run([]string{"ohcli","public","PL","2025","--json"}, ...)`; `require.Equal(t, 0, code)`; `var got []openholidays.Holiday; require.NoError(t, json.Unmarshal(stdout.Bytes(), &got)); require.Len(t, got, 14)` (Phase 3 fixture has 14 entries).
    - t.Run "--csv output starts with snake_case header":
        Same handler; argv `[..., "--csv"]`; first line of stdout equals `start_date,end_date,name,nationwide,type,subdivision_codes`.
    - t.Run "empty result writes stderr + exit 0 + empty stdout (D-07)":
        Server writes `[]`; `code := run([]string{"ohcli","public","PL","2025"}, ...); assert.Equal(t, 0, code); assert.Empty(t, stdout.String(), "stdout must be empty so pipes don't break"); assert.Contains(t, stderr.String(), "ohcli: no public holidays found for PL 2025")`.
    - t.Run "usage error: missing year returns exit 2":
        `code := run([]string{"ohcli","public","PL"}, &stdout, &stderr); assert.Equal(t, 2, code); assert.Contains(t, stderr.String(), "ohcli:")`.
    - t.Run "usage error: invalid year value returns exit 2":
        argv `["ohcli","public","PL","9999"]` (year > 2100 bounds) or `["ohcli","public","PL","abc"]` (Atoi fails); `assert.Equal(t, 2, code); assert.Contains(t, stderr.String(), "ohcli: invalid year")`.
    - t.Run "usage error: invalid --format returns exit 2":
        argv `[..., "--format=ical"]`; `assert.Equal(t, 2, code); assert.Contains(t, stderr.String(), "ohcli: invalid format")`.
    - t.Run "runtime error: server 500 returns exit 1":
        Server writes `w.WriteHeader(500); w.Write([]byte("boom"))`; `code := run([]string{"ohcli","public","PL","2025"}, ...); assert.Equal(t, 1, code); assert.Contains(t, stderr.String(), "ohcli: ")`.

    **cmd/ohcli/school_test.go — `func TestCmdSchool(t *testing.T)`**:
    - Same shape as TestCmdPublic.
    - t.Run "--region PL-SL filters request and renders text output":
        Load `school_holidays_pl_2025.json`. Server asserts `assert.Equal(t, "PL-SL", r.URL.Query().Get("subdivisionCode"))`. Argv `["ohcli","school","PL","2025","--region","PL-SL"]`. exit 0; stdout contains "2025" and the Polish school-holiday name (e.g. "ferie zimowe" appears in fixture per Phase 3 SC#2).
    - t.Run "empty result with --region prints region in stderr message (D-07 specifics)":
        Server writes `[]`. Argv `[..., "--region","PL-XX"]`. `stderr.String()` contains `ohcli: no school holidays found for PL 2025 (region PL-XX)`.
    - t.Run "no --region works (subdivisionCode is empty)":
        Server checks `assert.Equal(t, "", r.URL.Query().Get("subdivisionCode"))`.
    - t.Run "usage error: missing year returns exit 2" and "runtime error: 500 returns exit 1" — same shape as public.

    **cmd/ohcli/countries_test.go — `func TestCmdCountries(t *testing.T)`**:
    - t.Run "text output happy path":
        Load `countries.json`. Server returns body; argv `["ohcli","countries"]`; exit 0; stdout non-empty.
    - t.Run "--format=csv has correct header":
        First line of stdout is `iso_code,name,official_languages`.
    - t.Run "positional arg rejected returns exit 2":
        Argv `["ohcli","countries","extra"]`; `assert.Equal(t, 2, code); assert.Contains(t, stderr.String(), "ohcli: countries takes no positional args")`.
    - t.Run "empty result writes stderr + exit 0":
        Server writes `[]`; argv `["ohcli","countries"]`; exit 0; stderr contains `ohcli: no countries found`; stdout empty.

    Em-dash style and English-only throughout. Reuse the `os.ReadFile + filepath.Join("..","..","testdata",...)` pattern from PATTERNS.md.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && go test -race -count=1 ./cmd/ohcli/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Files `cmd/ohcli/main_test.go`, `public_test.go`, `school_test.go`, `countries_test.go` all exist; all `package main`.
    - `grep -E '^func TestRun\(t \*testing\.T\)' cmd/ohcli/main_test.go` exits 0.
    - `grep -E '^func TestCmdPublic\(t \*testing\.T\)' cmd/ohcli/public_test.go` exits 0.
    - `grep -E '^func TestCmdSchool\(t \*testing\.T\)' cmd/ohcli/school_test.go` exits 0.
    - `grep -E '^func TestCmdCountries\(t \*testing\.T\)' cmd/ohcli/countries_test.go` exits 0.
    - `grep -F 't.Setenv("OPENHOLIDAYS_BASE_URL"' cmd/ohcli/public_test.go cmd/ohcli/school_test.go cmd/ohcli/countries_test.go` finds at least one match per file.
    - `grep -F 'filepath.Join("..", "..", "testdata"' cmd/ohcli/public_test.go cmd/ohcli/school_test.go cmd/ohcli/countries_test.go` finds the fixture-path pattern in each.
    - `grep -F 'httptest.NewServer' cmd/ohcli/public_test.go cmd/ohcli/school_test.go cmd/ohcli/countries_test.go` finds the server pattern in each.
    - `grep -F 't.Cleanup(srv.Close)' cmd/ohcli/public_test.go cmd/ohcli/school_test.go cmd/ohcli/countries_test.go` finds the cleanup pattern in each.
    - `grep -F 'ohcli: no public holidays found' cmd/ohcli/public_test.go` exits 0 (D-07 message assertion).
    - `grep -F 'ohcli: no school holidays found' cmd/ohcli/school_test.go` exits 0.
    - `grep -F '(region PL-XX)' cmd/ohcli/school_test.go` exits 0 (D-07 "Specifics" exact wording).
    - `grep -F 'subdivisionCode' cmd/ohcli/school_test.go` exits 0 (CLI-02 server-side query assertion).
    - `grep -F 'ohcli: countries takes no positional args' cmd/ohcli/countries_test.go` exits 0.
    - At least one assertion of exit code 0, one of 1, one of 2 across the four files: `grep -hE 'assert\.Equal\(t, [012], code\)' cmd/ohcli/*_test.go | sort -u | wc -l` ≥ 3.
    - `go test -race -count=1 ./cmd/ohcli/` exits 0 (all tests green under race detector).
    - `go vet ./cmd/ohcli/` exits 0.
    - Zero live API calls: `grep -F 'openholidaysapi.org' cmd/ohcli/*_test.go` returns no matches (D-08 enforced).
  </acceptance_criteria>
  <done>
    `go test -race ./cmd/ohcli/` green. CLI behaviorally verified: all subcommands, all output modes, all exit codes (0/1/2), empty-result branches with D-07 messages, --region filter assertion, zero live API calls.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test argv → `run()` | Trusted (test-controlled); validates run's defensive paths. |
| httptest.NewServer → CLI | Trusted (local in-memory server in the same test process). |
| Fixture files → tests | Trusted (committed to repo at testdata/*.json). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | parallel t.Setenv collisions | low | mitigate | Inner t.Run subtests do NOT call t.Parallel when they use t.Setenv (Go runtime forbids the combo). Top-level TestCmdXxx still runs in parallel with other top-level tests. |
| T-05-06 | Information Disclosure | live API calls in unit tests | low | mitigate | D-08: all unit tests use httptest.NewServer; `grep -F openholidaysapi.org cmd/ohcli/*_test.go` returns nothing (acceptance criterion). |
| T-05-07 | DoS | flaky tests due to network races | low | mitigate | t.Cleanup(srv.Close) deterministically tears down servers; no goroutines escape between t.Run iterations. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package-manager installs; only test-only testify already pinned in go.mod v1.11.1. |
</threat_model>

<verification>
- `go test -race -count=1 ./cmd/ohcli/` exits 0.
- `go vet ./cmd/ohcli/` exits 0.
- All exit-code paths covered (0 success, 0 empty-result, 1 runtime error, 2 usage error).
- All output formats asserted (text header row + tabular content, json round-trip, csv snake_case header).
- D-07 empty-result behavior asserted in stderr (with and without region).
- D-05 `ohcli: ` prefix asserted on every error path.
- D-08 zero live API calls — proven by grep returning no `openholidaysapi.org` hits in test files.
- CLI-02 `--region PL-SL` subdivisionCode propagation asserted at the http handler level.
</verification>

<success_criteria>
1. `go test -race -count=1 ./cmd/ohcli/` green.
2. All four TestRender* renderer tests exist and pass.
3. TestRun covers no-args, unknown-command, version, help.
4. TestCmdPublic, TestCmdSchool, TestCmdCountries each cover happy + empty + usage-error + runtime-error scenarios.
5. Tests follow Gold Rule 3: one TestXxx per prod function (TestRun, TestCmdPublic, TestCmdSchool, TestCmdCountries, TestRenderText, TestRenderJSON, TestRenderCSV, TestRenderCountries), every case in t.Run.
6. No live API calls (D-08).
</success_criteria>

<output>
Create `.planning/phases/05-distribution/05-02-SUMMARY.md` when done.
</output>
