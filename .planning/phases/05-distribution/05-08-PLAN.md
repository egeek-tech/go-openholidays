---
phase: 05-distribution
plan: 08
type: execute
wave: 4
depends_on:
  - 05-05
  - 05-06
  - 05-07
files_modified:
  - docs/release-runbook.md
  - .planning/PROJECT.md
autonomous: false
requirements:
  - CI-04
  - REL-04
  - DOC-07
tags:
  - release
  - module-path
  - runbook
  - pkg-go-dev

must_haves:
  truths:
    - "D-01: go.mod first line equals `module github.com/egeek-tech/go-openholidays` (REL-04 already satisfied — verified 2026-05-28 per RESEARCH §Validation Architecture row REL-04). No vanity import path for v0.1.0."
    - "docs/release-runbook.md captures the end-to-end release flow: pre-tag checks → tag-push → release.yml run → post-tag verification (pkg.go.dev, Go Report Card, attestation verification with gh CLI)."
    - "PROJECT.md Key Decisions appended with CL-18: 'Phase 5 Source-current action version pin overrides + cached perf budget reinterpretation' covering attest-build-provenance@v4, codecov-action@v5, and CL-18 the Pitfall 5 reinterpretation (cached = Countries, not PublicHolidays)."
    - "REL-01 + REL-02 + REL-03 listed in the runbook as manual-only post-tag verifications per VALIDATION.md `Manual-Only Verifications` table (external services with index lag)."
    - "A dry-run release rehearsal step is documented (push `v0.1.0-rc.1` to a side branch, observe release.yml end-to-end, then push `v0.1.0` to main)."
    - "D-02: Repo is already public — pkg.go.dev auto-indexes after the v0.1.0 tag is pushed from main; runbook makes pre-tag visibility check explicit and post-tag verification a user checkpoint."
    - "User checkpoint at the end of the runbook confirms pkg.go.dev visibility + Go Report Card grade A + `gh attestation verify` works."
  artifacts:
    - path: "docs/release-runbook.md"
      provides: "Pre-tag checklist + tag push procedure + post-tag verification matrix + rollback procedure"
    - path: ".planning/PROJECT.md (Key Decisions section, appended)"
      provides: "CL-18 entry: action-version pins (codecov@v5, attest@v4, goreleaser@v6) + cached perf budget = Countries (CL-18 covers both per RESEARCH §Assumptions A2/A3/A4)"
  key_links:
    - from: "docs/release-runbook.md"
      to: ".github/workflows/release.yml"
      via: "Runbook step 'Push the tag' triggers release.yml on push: tags: ['v*']"
      pattern: "git push origin v0\\.1\\.0"
    - from: "docs/release-runbook.md"
      to: "pkg.go.dev/github.com/egeek-tech/go-openholidays"
      via: "Post-tag step polls https://pkg.go.dev/.../v0.1.0 with up to 30 min lag"
      pattern: "pkg.go.dev/github\\.com/egeek-tech/go-openholidays"
---

<objective>
Land the final pre-release readiness deliverables: confirm `go.mod` module path (REL-04 — already satisfied; this plan documents the confirmation), capture the release runbook, append PROJECT.md Key Decisions CL-18 with the action-version + cached-perf-budget clarifications surfaced during research, and define the manual-only post-tag verification checkpoint. The plan's checkpoint task pauses for a human to push the actual `v0.1.0` tag and confirm post-tag external services (pkg.go.dev, Go Report Card) render correctly.

Purpose:
- REL-04: Module path owner committed (egeek-tech/go-openholidays — locked in 01-CONTEXT.md D-01; this plan documents the verification).
- DOC-07: Final-pass godoc audit (Plan 07 ran the audit; this plan re-runs as a release-gate check).
- CI-04 + REL-01..03: Manual verification of external-service rendering after tag push (per VALIDATION.md `Manual-Only Verifications` table).
- Captures CL-18 in PROJECT.md to keep the historical decision log truthful.

Output:
- `docs/release-runbook.md` — single source of truth for shipping v0.1.0 and future tags.
- `.planning/PROJECT.md` — extended Key Decisions with CL-18.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distribution/05-CONTEXT.md
@.planning/phases/05-distribution/05-RESEARCH.md
@.planning/phases/05-distribution/05-PATTERNS.md
@.planning/phases/05-distribution/05-VALIDATION.md
@.planning/phases/05-distribution/05-05-SUMMARY.md
@.planning/phases/05-distribution/05-06-SUMMARY.md
@.planning/phases/05-distribution/05-07-SUMMARY.md

<interfaces>
From go.mod (already verified 2026-05-28 — RESEARCH §Validation Architecture REL-04 row marks it satisfied):
  module github.com/egeek-tech/go-openholidays
  go 1.23

From RESEARCH §Common Pitfalls Pitfall 6:
  pkg.go.dev index lag after first tag — up to 30 min. Mitigation: curl the URL once to trigger proxy fetch.

From RESEARCH §Assumptions A2, A3, A4:
  CL-18 candidate covers:
    A2 — cached perf budget applies to Countries (not PublicHolidays per RESIL-07).
    A3 — actions/attest-build-provenance@v4 (not @v1 per CONTEXT.md D-11).
    A4 — codecov/codecov-action@v5 (not @v4 per CONTEXT.md D-09) with use_oidc: true.

From VALIDATION.md Manual-Only Verifications table:
  - pkg.go.dev index lag → REL-04 / DOC-07 manual
  - goreleaser binaries on tag → REL-01 / REL-02 manual
  - gh attestation verify → REL-03 manual
  - Go Report Card grade A → DOC-07 manual

From STATE.md Key Decisions (existing CL-01..CL-17): the format for CL-18 entry — | Decision | Rationale | Outcome | with bold decision name, multi-sentence rationale, and "Adopted in Phase 5" outcome.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append CL-18 to PROJECT.md Key Decisions (action-version pins + cached perf budget reinterpretation)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (existing Key Decisions table — append CL-18 in same row format)
    - .planning/phases/05-distribution/05-RESEARCH.md (§"Assumptions Log" entries A2, A3, A4; §"User Constraints → Source-current notes" lines 38-45; §"Common Pitfalls → Pitfall 5" for the cached-budget reinterpretation)
    - .planning/phases/05-distribution/05-CONTEXT.md (D-09 codecov@v4, D-11 attest@v1 — the original values CL-18 overrides)
  </read_first>
  <action>
    Read the existing PROJECT.md Key Decisions table (Markdown table format with columns: Decision | Rationale | Outcome). Locate the last decision row (CL-17, marked "Adopted in Phase 1 (gap closure)"). Append a new row CL-18 BELOW it with these contents:

    **Decision column** (bold):
    `CL-18: Three Phase 5 mechanical clarifications recorded as a single Key Decision: (a) action-version pins overriding CONTEXT.md values — codecov/codecov-action@v5 (NOT @v4) with use_oidc: true for token-free OIDC on public repos, actions/attest-build-provenance@v4 (NOT @v1; v1/v2/v3 are deprecated per RESEARCH Pitfall 2), goreleaser/goreleaser-action@v6, actions/setup-go@v5, actions/checkout@v4, golangci/golangci-lint-action@v7, golang/govulncheck-action@v1; (b) the ROADMAP Phase 5 SC#3 "< 5 ms cached" perf budget is reinterpreted to apply to Countries/Languages/Subdivisions, NOT to PublicHolidays, because RESIL-07 / Phase 4 D-83 explicitly excludes holiday endpoints from caching — BenchmarkClient_Countries is the canonical cached-path measurement, NOT a "cached" sub-benchmark of PublicHolidays; (c) Phase 5 tests use context.Background() + context.WithTimeout, NOT t.Context() — Pitfall 1: t.Context() is Go 1.24+ and would break the Go 1.23 CI matrix leg.`

    **Rationale column**:
    `(a) CONTEXT.md was authored before the action versions drifted; pinning to the stale values would silently break (attest @v1 deprecated, codecov @v4 missing OIDC). Source-current pinning verified via RESEARCH WebFetch of each action's README 2026-05-28. (b) ROADMAP SC#3's literal "< 5 ms cached PublicHolidays" cannot be measured because the cache layer never accelerates PublicHolidays by design. Forcing the budget against PublicHolidays would either produce a meaningless "cached" measurement (cache no-op, equal to cold) or violate RESIL-07's stale-data policy. (c) The CI matrix lower bound is Go 1.23; using a Go 1.24+ test helper makes the matrix red despite identical functional behavior. Anchored on .planning/phases/05-distribution/05-RESEARCH.md §"Assumptions Log" A2/A3/A4 + §"Common Pitfalls → Pitfalls 1, 2, 5" + §"User Constraints → Source-current notes".`

    **Outcome column**: `Adopted in Phase 5`

    Verify after the edit that the table renders correctly (no broken pipe characters, no truncated rows). DO NOT modify any other PROJECT.md content.

    English-only. Em-dashes acceptable per CL-17.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && grep -c '| CL-1[0-9]:' .planning/PROJECT.md && grep -F 'CL-18:' .planning/PROJECT.md && grep -F 'codecov/codecov-action@v5' .planning/PROJECT.md && grep -F 'actions/attest-build-provenance@v4' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` updated.
    - `grep -F 'CL-18' .planning/PROJECT.md` exits 0.
    - `grep -F 'codecov/codecov-action@v5' .planning/PROJECT.md` exits 0 (Source-current pin recorded).
    - `grep -F 'actions/attest-build-provenance@v4' .planning/PROJECT.md` exits 0.
    - `grep -F 'goreleaser/goreleaser-action@v6' .planning/PROJECT.md` exits 0.
    - `grep -F 'BenchmarkClient_Countries' .planning/PROJECT.md` exits 0 (cached perf budget reinterpretation).
    - `grep -F 'RESIL-07' .planning/PROJECT.md` exits 0 (rationale references the locked Phase 4 decision).
    - `grep -F 't.Context()' .planning/PROJECT.md` exits 0 (Pitfall 1 reference).
    - `grep -F 'Adopted in Phase 5' .planning/PROJECT.md` exits 0 (Outcome column populated).
    - Existing CL-17 entry preserved verbatim: `grep -F 'CL-17:' .planning/PROJECT.md` still exits 0; that line's content not edited.
    - The markdown table is still well-formed: `awk '/^\| Decision \|/,/^$/' .planning/PROJECT.md | grep -c '^| CL-' | sort -n` ≥ 18 (CL-01 through CL-18 all present as table rows).
  </acceptance_criteria>
  <done>
    CL-18 row appended to PROJECT.md Key Decisions; prior decisions preserved; action versions + cached-budget reinterpretation + Pitfall 1 t.Context() all captured in one entry.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author docs/release-runbook.md (pre-tag → tag-push → post-tag verification)</name>
  <files>docs/release-runbook.md</files>
  <read_first>
    - .planning/phases/05-distribution/05-RESEARCH.md (§"Common Pitfalls → Pitfall 6" — pkg.go.dev 30-min lag mitigation; §"Validation Architecture → Sampling Rate" — phase gate guidance including "tag-shipping dry-run via v0.1.0-rc.1")
    - .planning/phases/05-distribution/05-VALIDATION.md (§"Manual-Only Verifications" — exact post-tag checks for REL-01..04 and DOC-07)
    - .github/workflows/release.yml (Plan 06 — the workflow this runbook triggers)
    - .goreleaser.yaml (Plan 06 — defines the 6 binaries + checksums.txt the runbook asserts on)
    - go.mod (first line proves REL-04 — `module github.com/egeek-tech/go-openholidays`)
  </read_first>
  <action>
    Create `docs/release-runbook.md`. English-only. Em-dashes acceptable.

    Required sections:

    **1. `# Release Runbook — go-openholidays`** (heading).

    **2. Pre-tag checklist** — bulleted list:
    - `[ ] On main branch, fully synced with origin (`git status` clean, `git pull` no-op)`.
    - `[ ] CI is green on the most recent main commit (visit Actions tab; the `CI` workflow matrix legs all pass).`
    - `[ ] `version.go` `Version` value matches the intended tag minus the `v` prefix (e.g., for tag `v0.1.0` the source has `var Version = "0.1.0"`).`
    - `[ ] `go.mod` first line is `module github.com/egeek-tech/go-openholidays` (REL-04 — confirmed 2026-05-28).`
    - `[ ] `go test -race -coverprofile=coverage.out ./...` exits 0 locally; coverage ≥ 85%.`
    - `[ ] `golangci-lint run` exits 0 locally.`
    - `[ ] `govulncheck ./...` exits 0 locally.`
    - `[ ] `go test -run Example .` exits 0 (TEST-09).`
    - `[ ] (Optional) `goreleaser check` exits 0 if goreleaser is installed locally.`

    **3. Dry-run rehearsal (recommended for v0.1.0; optional for subsequent tags)**:
    - Create a side branch, push tag `v0.1.0-rc.1` to it: `git tag v0.1.0-rc.1 && git push origin v0.1.0-rc.1`.
    - Observe `release.yml` running in the Actions tab.
    - When green: confirm the GitHub Release for `v0.1.0-rc.1` exists with 6 binary archives + `checksums.txt`.
    - `gh attestation verify <one_binary> --repo egeek-tech/go-openholidays` exits 0.
    - Delete the rc tag locally + on the remote when done: `git tag -d v0.1.0-rc.1 && git push origin :refs/tags/v0.1.0-rc.1`.
    - Delete the rc release on GitHub (if not auto-marked prerelease).

    **4. Tag push (the actual release)**:
    - `git tag -a v0.1.0 -m "v0.1.0 — initial release"` (or non-annotated `git tag v0.1.0` if preferred — goreleaser handles both).
    - `git push origin v0.1.0`.
    - Watch `release.yml` in Actions tab; ETA ~3-5 min (build cross-compile + attestation).

    **5. Post-tag verification matrix** (per VALIDATION.md §"Manual-Only Verifications"):

    | Check | Command / URL | Expected | When |
    |-------|---------------|----------|------|
    | REL-01: pkg.go.dev renders | `curl -fsS https://pkg.go.dev/github.com/egeek-tech/go-openholidays@v0.1.0` | HTTP 200; no "no documentation" banner | Within 30 min of tag (proxy lag — see Pitfall 6) |
    | REL-02: Go Report Card grade A | `curl -fsS https://goreportcard.com/badge/github.com/egeek-tech/go-openholidays` then `curl https://goreportcard.com/report/github.com/egeek-tech/go-openholidays` | Green grade-A badge | Within 24 hours of tag |
    | REL-03: GitHub Release artifacts | `gh release view v0.1.0 --json assets` | Lists 6 binary archives + `checksums.txt` | Immediately after release.yml green |
    | REL-03: Attestation verifiable | Download one binary, run `gh attestation verify ohcli_v0.1.0_linux_amd64.tar.gz --repo egeek-tech/go-openholidays` | Exit 0; reports "verified" | Immediately after release.yml green |
    | DOC-07: pkg.go.dev godoc audit | Visit https://pkg.go.dev/github.com/egeek-tech/go-openholidays — every exported symbol has a doc comment beginning with its name | All sections render with no "Documentation: no" warnings | Within 30 min of tag |

    **6. Pkg.go.dev index trigger (mitigation for Pitfall 6)**:
    - If `pkg.go.dev` returns "no module found" 5 min after tag, manually hit the URL: `curl -fsS https://pkg.go.dev/github.com/egeek-tech/go-openholidays@v0.1.0 -o /dev/null`. This kicks the proxy to fetch.
    - Repeat with `@latest` if needed.

    **7. Rollback (if release.yml fails or binaries are wrong)**:
    - Goreleaser is idempotent on rerun: re-run via "Re-run workflow" in the Actions tab if a transient failure.
    - If the tagged release contains incorrect binaries: `gh release delete v0.1.0 --yes`, then `git push origin :refs/tags/v0.1.0` (delete the tag remotely), `git tag -d v0.1.0` (delete locally), fix the issue, re-tag with the same name or bump to v0.1.1.

    **8. Post-release housekeeping**:
    - Update `version.go` to the next pre-release / development version (e.g., `var Version = "0.2.0-dev"`) and commit on main.
    - Update STATE.md `milestone` status to `completed`.
    - File a post-release retrospective per `.planning/RETROSPECTIVE.md` cadence (if such a file exists; otherwise note in STATE.md Session Continuity).

    Anchor every step to the workflow / config file it depends on (release.yml, .goreleaser.yaml, version.go, etc.) with explicit pointers. English-only.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && test -f docs/release-runbook.md && grep -F 'Release Runbook' docs/release-runbook.md && grep -F 'Pre-tag checklist' docs/release-runbook.md && grep -F 'v0.1.0-rc.1' docs/release-runbook.md && grep -F 'gh attestation verify' docs/release-runbook.md && grep -F 'pkg.go.dev/github.com/egeek-tech/go-openholidays' docs/release-runbook.md && grep -F 'goreportcard.com' docs/release-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/release-runbook.md` exists.
    - `head -1 docs/release-runbook.md` contains "Release Runbook" and "go-openholidays".
    - `grep -F 'Pre-tag checklist' docs/release-runbook.md` exits 0 (Section 2).
    - `grep -F 'v0.1.0-rc.1' docs/release-runbook.md` exits 0 (dry-run rehearsal).
    - `grep -F 'git push origin v0.1.0' docs/release-runbook.md` exits 0 (Section 4 — the actual tag push).
    - `grep -F 'gh attestation verify' docs/release-runbook.md` exits 0 (REL-03 verification).
    - `grep -F 'pkg.go.dev/github.com/egeek-tech/go-openholidays' docs/release-runbook.md` exits 0 (REL-01).
    - `grep -F 'goreportcard.com' docs/release-runbook.md` exits 0 (REL-02).
    - `grep -F 'gh release view v0.1.0' docs/release-runbook.md` exits 0 (binary asset verification).
    - `grep -F 'Pitfall 6' docs/release-runbook.md || grep -F 'pkg.go.dev' docs/release-runbook.md | grep -F '30 min'` exits 0 (proxy lag mitigation documented).
    - `grep -F 'Rollback' docs/release-runbook.md` exits 0 (Section 7).
    - `grep -F 'version.go' docs/release-runbook.md` exits 0 (post-release housekeeping references the source-of-truth file).
    - The markdown is structurally valid: `python3 -c "import sys; lines = open('docs/release-runbook.md').readlines(); assert any(l.startswith('# ') for l in lines); print('runbook ok')"` exits 0.
  </acceptance_criteria>
  <done>
    docs/release-runbook.md captures the full lifecycle: pre-tag checks, dry-run rehearsal, tag push, post-tag verification matrix (REL-01..04 + DOC-07), pkg.go.dev index trigger, rollback procedure, post-release housekeeping.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — push v0.1.0 tag and confirm external services (REL-01..04 + DOC-07 manual checks)</name>
  <files>(none — human-only verification of external services; no files modified by this task)</files>
  <action>Pause execution. Hand off to the human verifier per the <how-to-verify> steps below: confirm the pre-tag checklist, optionally run the v0.1.0-rc.1 dry-run rehearsal, push the real v0.1.0 tag, then verify REL-01 (pkg.go.dev render), REL-02 (Go Report Card grade A), REL-03 (binary count + attestation via `gh attestation verify`), and DOC-07 (godoc completeness on pkg.go.dev). Resume on the resume-signal.</action>
  <verify><human-check>See &lt;how-to-verify&gt; — human reads pkg.go.dev, Go Report Card, and `gh release view v0.1.0 --json assets` output; confirms each REL/DOC row in the runbook verification matrix.</human-check></verify>
  <done>Human types `approved` per &lt;resume-signal&gt;; v0.1.0 tag is published; release.yml workflow is green; pkg.go.dev renders the package; Go Report Card shows grade A; `gh attestation verify` exits 0 on at least one binary.</done>
  <what-built>
    Plans 01-07 have shipped: CLI core + tests, fuzz + benchmarks, integration tests, lint + CI workflow, integration + release workflows + dependabot, documentation + Example_*, finalized PROJECT.md decision log, and the release runbook. The release pipeline is complete; what remains is the human-driven post-tag verification of external services that no automation can poll reliably from CI (pkg.go.dev index lag, Go Report Card scan, gh CLI attestation verification).
  </what-built>
  <how-to-verify>
    1. Confirm the pre-tag checklist (Section 2 of `docs/release-runbook.md`) is fully checked off locally:
       - `git status` shows main branch clean.
       - `git pull` shows no incoming commits.
       - The CI workflow (most recent main commit) is green on the GitHub Actions tab.
       - `version.go` `Version` value equals `0.1.0` (matches the intended tag).
       - `go.mod` first line: `module github.com/egeek-tech/go-openholidays`.
       - `go test -race -coverprofile=coverage.out ./...` exits 0 locally; coverage ≥ 85%.
       - `golangci-lint run` exits 0.
       - `govulncheck ./...` exits 0.
       - `go test -run Example .` exits 0.

    2. (Strongly recommended) Run the dry-run rehearsal per Section 3 of the runbook with tag `v0.1.0-rc.1` on a side branch. Confirm:
       - `release.yml` workflow goes green.
       - GitHub Release `v0.1.0-rc.1` has 6 binary archives + `checksums.txt`.
       - `gh attestation verify <any-binary> --repo egeek-tech/go-openholidays` exits 0.
       - Then clean up: `git tag -d v0.1.0-rc.1 && git push origin :refs/tags/v0.1.0-rc.1` and delete the rc Release on GitHub.

    3. Push the real tag:
       - `git tag v0.1.0` (or annotated: `git tag -a v0.1.0 -m "v0.1.0 — initial release"`).
       - `git push origin v0.1.0`.

    4. Watch `release.yml` in the Actions tab. ETA 3-5 min. When green:

    5. Run the post-tag verification matrix per Section 5 of the runbook:
       - REL-03: `gh release view v0.1.0 --json assets | jq '.assets | length'` returns 7 (6 binaries + 1 checksums.txt). All asset names match `ohcli_v0.1.0_*` pattern.
       - REL-03 (attestation): Pick one binary, run `gh attestation verify <binary> --repo egeek-tech/go-openholidays`. Exit 0.
       - REL-01 (per D-02 — pkg.go.dev auto-indexes after the v0.1.0 tag push from main; this step verifies the index landed): Visit `https://pkg.go.dev/github.com/egeek-tech/go-openholidays@v0.1.0`. If "no module found" appears, hit `curl https://pkg.go.dev/github.com/egeek-tech/go-openholidays@v0.1.0 -o /dev/null` to trigger the proxy. Wait up to 30 min. Confirm: every exported symbol renders; Example_* attached to symbols; godoc opening lines start with the symbol name.
       - REL-02: Visit `https://goreportcard.com/report/github.com/egeek-tech/go-openholidays`. Confirm grade A. May take up to 24h for first scan.
       - DOC-07 (final pass): Re-check on pkg.go.dev that no exported symbol shows "Documentation: no". Spot-check 3 random exported symbols (e.g., NewClient, Holiday.NameFor, HolidayType.IsKnown).

    6. Post-release housekeeping per Section 8:
       - Bump `version.go` to `var Version = "0.2.0-dev"` on a new branch + PR.
       - Update `STATE.md` milestone status to `completed`.
  </how-to-verify>
  <resume-signal>
    Type `approved` (with notes on which external services rendered correctly, including approximate index lag for pkg.go.dev), or describe the failure mode (e.g., "release.yml step 3 failed with attest error", "Go Report Card grade B due to godoc gap on X") so a follow-up plan can address it.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human tag push → release.yml | Trusted (only repo owner can push protected tags to main; GitHub branch protection enforced outside YAML). |
| Sigstore/Fulcio / GitHub OIDC → attestation transparency log | Trusted; first-party. |
| pkg.go.dev / Go Report Card → human verifier | Trusted external services; verifier reads but does not write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-29 | Tampering | tag pushed to wrong repo or with corrupted git history | high | mitigate | Runbook Step 4 explicitly requires `git push origin v0.1.0` from main; pre-tag checklist confirms branch + clean status. Repo settings (configured outside this plan) restrict tag protection to repo owner. |
| T-05-30 | Tampering | release.yml runs goreleaser with incorrect config | medium | mitigate | Dry-run rehearsal (Section 3) catches misconfiguration before the real v0.1.0 push. goreleaser check (pre-tag step, optional) catches local syntax errors. |
| T-05-31 | Information Disclosure | pkg.go.dev publishes internal-only types | low | mitigate | Plan 07's DOC-07 audit + golangci-lint v2 + `internal/` package convention (PROJECT.md) confirms only the intended public surface is exported. pkg.go.dev only renders exported symbols. |
| T-05-32 | EoP | attacker tag-spoofing the repo | high | mitigate | GitHub Artifact Attestations bind the binary to (workflow + repo + commit SHA + Sigstore-signed metadata) — `gh attestation verify --repo egeek-tech/go-openholidays` fails if the binary was produced by ANY other repo, including a hostile fork. This is the primary defense and is exactly the property D-11 buys. |
| T-05-33 | Repudiation | release artifacts lack provenance | low | mitigate | Same as T-05-32 — attestations + transparency log + `gh attestation verify` produces a non-repudiable audit trail. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package installs. |
</threat_model>

<verification>
- PROJECT.md has CL-18 appended; all prior CL-NN entries preserved.
- docs/release-runbook.md exists with all 8 sections; references release.yml, .goreleaser.yaml, version.go, go.mod.
- REL-04 satisfied (go.mod confirmed — verified at planning time; runbook documents the assertion).
- Manual checkpoint pauses execution for the human to:
  1. Run pre-tag checklist (automated commands; human reads CI green).
  2. Optionally execute dry-run rehearsal with v0.1.0-rc.1.
  3. Push v0.1.0 tag.
  4. Verify REL-01 (pkg.go.dev render), REL-02 (Go Report Card grade A), REL-03 (binary count + attestation), DOC-07 (godoc audit on pkg.go.dev).
- Human resumes with `approved` or a failure description.
</verification>

<success_criteria>
1. PROJECT.md Key Decisions has CL-18 entry covering action-version pins, cached-budget reinterpretation, and Pitfall 1 t.Context() avoidance.
2. docs/release-runbook.md captures pre-tag → dry-run → tag-push → post-tag verification → rollback → housekeeping.
3. Human checkpoint: v0.1.0 tag pushed; release.yml goes green; pkg.go.dev renders; Go Report Card grade A; `gh attestation verify` exits 0.
4. REL-04 documented; all 27 Phase 5 requirements satisfied across Plans 01-08.
</success_criteria>

<output>
Create `.planning/phases/05-distribution/05-08-SUMMARY.md` when done.
</output>
