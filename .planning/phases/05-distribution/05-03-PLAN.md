---
phase: 05-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - fuzz_test.go
  - bench_test.go
  - testdata/fuzz/FuzzParseLocalizedText/seed_0
  - testdata/fuzz/FuzzParseLocalizedText/seed_1
  - testdata/fuzz/FuzzParseLocalizedText/seed_2
  - testdata/fuzz/FuzzUnmarshalHoliday/seed_0
  - testdata/fuzz/FuzzUnmarshalHoliday/seed_1
  - testdata/fuzz/FuzzUnmarshalHoliday/seed_2
autonomous: true
requirements:
  - TEST-07
  - TEST-11
tags:
  - go
  - fuzz
  - benchmark

must_haves:
  truths:
    - "FuzzParseLocalizedText exists in package openholidays and seeds with both repo-existing testdata fixtures and adversarial inputs (LocalizedText slice perturbations) — runs without panic for the seed corpus."
    - "FuzzUnmarshalHoliday exists and exercises encoding/json.Unmarshal([]byte, &[]Holiday{}) — runs without panic for the seed corpus."
    - "BenchmarkClient_PublicHolidays measures cold path against httptest.NewServer serving testdata/public_holidays_pl_2025.json (< 500 ms cold target per ROADMAP SC#3 / TEST-11)."
    - "BenchmarkClient_Countries measures cached path against httptest.NewServer serving testdata/countries.json (< 5 ms cached target per CL-18 / RESEARCH Pitfall 5 reinterpretation)."
    - "Hand-curated seed corpus files exist under testdata/fuzz/<Name>/ so go test -fuzz discovers them on first run (per RESEARCH §1.4 / Pattern 3)."
  artifacts:
    - path: "fuzz_test.go"
      provides: "FuzzParseLocalizedText (exercises pickLocalized via JSON LocalizedText) + FuzzUnmarshalHoliday (exercises []Holiday decoder)"
    - path: "bench_test.go"
      provides: "BenchmarkClient_PublicHolidays (cold sub-bench), BenchmarkClient_Countries (cached sub-bench per CL-18)"
    - path: "testdata/fuzz/FuzzParseLocalizedText/"
      provides: "3 hand-curated seed files exercising LocalizedText edge cases"
    - path: "testdata/fuzz/FuzzUnmarshalHoliday/"
      provides: "3 hand-curated seed files exercising []Holiday edge cases"
  key_links:
    - from: "fuzz_test.go::FuzzUnmarshalHoliday"
      to: "encoding/json.Unmarshal into []Holiday"
      via: "json.Unmarshal(data, &hs) — panic-free is the invariant"
      pattern: "json\\.Unmarshal\\(data, &hs\\)"
    - from: "bench_test.go::BenchmarkClient_Countries"
      to: "openholidays.NewClient(WithBaseURL, WithCache)"
      via: "WithCache enables the cache layer; second+ Countries call hits the cache (RESIL-07)"
      pattern: "WithCache\\("
---

<objective>
Land fuzz tests for two JSON decode paths (TEST-07) and benchmarks proving the perf budget (TEST-11). Reinterpret ROADMAP SC#3's "< 5 ms cached" budget per RESEARCH §"Common Pitfalls → Pitfall 5" and CL-18: measure `Countries` cached (RESIL-07: Countries IS cacheable; PublicHolidays is NOT). Add hand-curated seed corpus per RESEARCH §"Pattern 3" (load existing fixtures via `F.Add` + 3 adversarial seeds per target).

Purpose:
- TEST-07: Surface panic-class bugs in `pickLocalized` (called by Holiday.NameFor, Country.NameFor, etc.) and in the `[]Holiday` JSON decoder.
- TEST-11: Prove cold-path < 500 ms and cached-path < 5 ms with a deterministic in-memory httptest backend.

Output:
- `fuzz_test.go` — `FuzzParseLocalizedText`, `FuzzUnmarshalHoliday` in `package openholidays` (same-package; reaches unexported `pickLocalized`).
- `bench_test.go` — `BenchmarkClient_PublicHolidays`, `BenchmarkClient_Countries` in `package openholidays`.
- `testdata/fuzz/FuzzParseLocalizedText/seed_0..2` — 3 hand-curated seed files.
- `testdata/fuzz/FuzzUnmarshalHoliday/seed_0..2` — 3 hand-curated seed files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distribution/05-CONTEXT.md
@.planning/phases/05-distribution/05-RESEARCH.md
@.planning/phases/05-distribution/05-PATTERNS.md

<interfaces>
From types.go (unexported helper — same-package access from fuzz_test.go):
- func pickLocalized(entries []LocalizedText, lang string) string — case-insensitive match + fallback to entries[0].Text + empty if nil
- type LocalizedText struct { Language string; Text string }
- type Holiday struct { ... full shape in types.go ... }

From client.go / options.go (consumed by benchmarks):
- func NewClient(opts ...Option) *Client
- func WithBaseURL(u string) Option
- func WithCache(ttl time.Duration) Option
- func (c *Client) Close() error

From date_test.go (existing fuzz target for reference voice):
- FuzzDateUnmarshal at line ~414 — follow its godoc preamble shape

Existing fixtures (load via os.ReadFile in F.Add):
- testdata/public_holidays_pl_2025.json
- testdata/school_holidays_pl_2025.json
- testdata/countries.json
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create testdata/fuzz/{FuzzParseLocalizedText,FuzzUnmarshalHoliday}/ seed corpus files</name>
  <files>testdata/fuzz/FuzzParseLocalizedText/seed_0, testdata/fuzz/FuzzParseLocalizedText/seed_1, testdata/fuzz/FuzzParseLocalizedText/seed_2, testdata/fuzz/FuzzUnmarshalHoliday/seed_0, testdata/fuzz/FuzzUnmarshalHoliday/seed_1, testdata/fuzz/FuzzUnmarshalHoliday/seed_2</files>
  <read_first>
    - .planning/phases/05-distribution/05-RESEARCH.md (§"Pattern 3" lines 402-435 — F.Add + adversarial seeds; §1.4 "fuzz seed corpus" — hand-curated + auto-from-fixtures)
    - testdata/public_holidays_pl_2025.json (the format real adversarial seeds must vary from)
    - https://go.dev/doc/security/fuzz/ (read-only reference for the `go test fuzz v1` file format)
  </read_first>
  <action>
    Create the two fuzz directories with 3 seed files each. Each file uses Go fuzzer's text-format header `go test fuzz v1` (single line) followed by one `[]byte("...")` value per seed input.

    testdata/fuzz/FuzzParseLocalizedText/ (3 seeds — JSON LocalizedText slice payloads):
    - seed_0: empty array. Two lines: `go test fuzz v1` then `[]byte("[]")`.
    - seed_1: missing-language entry exercises pickLocalized fallback. Two lines: `go test fuzz v1` then `[]byte("[{\"language\":\"pl\",\"text\":\"Wigilia\"},{\"language\":\"\",\"text\":\"\"}]")`.
    - seed_2: null text field. Two lines: `go test fuzz v1` then `[]byte("[{\"language\":\"xx\",\"text\":null}]")`.

    testdata/fuzz/FuzzUnmarshalHoliday/ (3 seeds — JSON Holiday/[]Holiday payloads):
    - seed_0: empty object array element. Two lines: `go test fuzz v1` then `[]byte("[{}]")`.
    - seed_1: EndDate before StartDate. Two lines: `go test fuzz v1` then `[]byte("[{\"id\":\"x\",\"startDate\":\"2025-01-01\",\"endDate\":\"2024-12-31\",\"type\":\"Public\",\"name\":[]}]")`.
    - seed_2: nullable fields with unexpected types. Two lines: `go test fuzz v1` then `[]byte("[{\"id\":\"x\",\"startDate\":null,\"endDate\":\"2025-01-01\",\"type\":\"\",\"name\":null}]")`.

    These complement the fixture-loaded seeds added via F.Add in Task 2. Files must be plain text (no extra trailing whitespace) — Go's fuzz tooling is strict about format.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && ls testdata/fuzz/FuzzParseLocalizedText/ testdata/fuzz/FuzzUnmarshalHoliday/ && for f in testdata/fuzz/FuzzParseLocalizedText/seed_* testdata/fuzz/FuzzUnmarshalHoliday/seed_*; do head -1 "$f"; done | sort -u</automated>
  </verify>
  <acceptance_criteria>
    - Directory `testdata/fuzz/FuzzParseLocalizedText/` exists with files seed_0, seed_1, seed_2.
    - Directory `testdata/fuzz/FuzzUnmarshalHoliday/` exists with files seed_0, seed_1, seed_2.
    - Each seed file's first line is exactly `go test fuzz v1`.
    - Each seed file's second line begins with `[]byte("` and ends with `")`.
    - `find testdata/fuzz -type f | wc -l` equals 6.
    - No seeds contain Polish-language identifiers in Go-code form (only inside JSON string literals as upstream-style fixture content — Gold Rule 1 testdata exception).
  </acceptance_criteria>
  <done>
    Seed corpus directories exist with valid `go test fuzz v1` headers; `go test -fuzz=Fuzz...` discovers them on first run.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create fuzz_test.go with FuzzParseLocalizedText + FuzzUnmarshalHoliday</name>
  <files>fuzz_test.go</files>
  <read_first>
    - .planning/phases/05-distribution/05-RESEARCH.md (§"Pattern 3" lines 402-435 — full FuzzUnmarshalHoliday reference body with F.Add fixture loading + adversarial seeds + f.Fuzz harness)
    - .planning/phases/05-distribution/05-PATTERNS.md (§"fuzz_test.go (fuzz-target, transform)" — same-package convention; imports pattern from existing test files)
    - date_test.go (find FuzzDateUnmarshal at line ~414 for the existing same-package fuzz target's voice + import style)
    - types.go (pickLocalized signature at line ~275 — what FuzzParseLocalizedText exercises; LocalizedText struct shape)
    - public_holidays_test.go (testify-imports order to mirror; same-package `package openholidays`)
  </read_first>
  <action>
    Create `fuzz_test.go` declared `package openholidays` (same-package — gives direct access to unexported `pickLocalized` per PATTERNS.md "Same-package test helpers stay in same-package files").

    Top of file: English-only godoc preamble matching `date_test.go::FuzzDateUnmarshal` voice. Explain: "Two fuzz targets per TEST-07. FuzzParseLocalizedText decodes a JSON LocalizedText slice and calls pickLocalized (the shared NameFor backend). FuzzUnmarshalHoliday exercises encoding/json.Unmarshal into []Holiday. Both invariants: decoder + helper must never panic, regardless of input. Errors are acceptable; panics fail the fuzz run."

    Imports: stdlib only — `encoding/json`, `os`, `path/filepath`, `testing`. No testify (fuzz targets don't need assertion libraries; the harness catches panics directly).

    Implement two functions per RESEARCH §"Pattern 3" (lines 411-435):

    1. `func FuzzParseLocalizedText(f *testing.F)`:
       - Seed list — load `testdata/public_holidays_pl_2025.json` and `testdata/school_holidays_pl_2025.json` via `os.ReadFile`; `f.Fatal(err)` on read failure. For each loaded body, call `f.Add(body)`.
       - Adversarial seeds (3): `f.Add([]byte("[]"))`, `f.Add([]byte("[{\"language\":\"pl\",\"text\":\"Wigilia\"},{\"language\":\"\",\"text\":\"\"}]"))`, `f.Add([]byte("[{\"language\":\"xx\",\"text\":null}]"))`.
       - Body: `f.Fuzz(func(t *testing.T, data []byte) { var entries []LocalizedText; _ = json.Unmarshal(data, &entries); _ = pickLocalized(entries, "pl"); _ = pickLocalized(entries, "") })`.

    2. `func FuzzUnmarshalHoliday(f *testing.F)`:
       - Same seed-load pattern, both fixtures.
       - Adversarial seeds (3): `f.Add([]byte("{}"))`, `f.Add([]byte("{\"id\":\"\",\"startDate\":\"2025-01-01\",\"endDate\":\"2024-12-31\",\"type\":\"Public\",\"name\":[]}"))`, `f.Add([]byte("[{\"id\":\"x\",\"startDate\":null,\"endDate\":\"2025-01-01\",\"type\":\"\",\"name\":null}]"))`.
       - Body: `f.Fuzz(func(t *testing.T, data []byte) { var hs []Holiday; _ = json.Unmarshal(data, &hs) })`.

    No assertions in f.Fuzz bodies — the harness reports panics directly. Use `filepath.Join("testdata", "X.json")` for portability.

    Each function has a Go-style doc comment beginning with its name (DOC-07 hygiene applies to test files too).
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && go test -run '^FuzzParseLocalizedText$|^FuzzUnmarshalHoliday$' -count=1 . 2>&1 && go test -fuzz=FuzzParseLocalizedText -fuzztime=5s . 2>&1 | tail -5 && go test -fuzz=FuzzUnmarshalHoliday -fuzztime=5s . 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `fuzz_test.go` exists; first declaration `package openholidays`.
    - `grep -E '^func FuzzParseLocalizedText\(f \*testing\.F\)' fuzz_test.go` exits 0.
    - `grep -E '^func FuzzUnmarshalHoliday\(f \*testing\.F\)' fuzz_test.go` exits 0.
    - `grep -F 'pickLocalized(entries' fuzz_test.go` exits 0 (FuzzParseLocalizedText exercises the unexported helper — same-package access).
    - `grep -F 'json.Unmarshal(data, &hs)' fuzz_test.go` exits 0.
    - `grep -c 'f.Add' fuzz_test.go` ≥ 8 (≥ 4 per function: 2 fixture-loaded + 3 adversarial — at minimum 4 `f.Add` calls per function counting through F.Add inside a loop OR explicit calls).
    - `go vet ./.` exits 0.
    - `go test -run '^Fuzz' -count=1 .` exits 0 (seed corpus passes — `go test` mode runs F.Add seeds without entering the fuzzing engine).
    - `go test -fuzz=FuzzParseLocalizedText -fuzztime=5s .` exits 0 (5-second fuzz run with no panics; output line `fuzz: elapsed:` appears).
    - `go test -fuzz=FuzzUnmarshalHoliday -fuzztime=5s .` exits 0.
    - `grep -F 'github.com/stretchr/testify' fuzz_test.go` returns nothing (no testify; harness reports panics directly).
    - Imports are stdlib-only.
  </acceptance_criteria>
  <done>
    Both fuzz targets exist, seeds populate from fixtures + adversarial inputs, 5-second fuzz runs surface zero panics. CI (Plan 05) time-boxes fuzz runs longer.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create bench_test.go with cold + cached benchmarks (TEST-11 + CL-18 reinterpretation)</name>
  <files>bench_test.go</files>
  <read_first>
    - .planning/phases/05-distribution/05-RESEARCH.md (§"Pattern 5" lines 485-535 — full BenchmarkClient_PublicHolidays reference with httptest backend + cold + cached sub-benchmarks; §"Common Pitfalls → Pitfall 5" — the CL-18 reinterpretation that "cached" measures Countries, not PublicHolidays)
    - .planning/phases/05-distribution/05-PATTERNS.md (§"bench_test.go (bench-test, request-response)" — pattern + Pitfall 5 reference)
    - public_holidays_test.go (httptest.NewServer pattern + t.Cleanup analog in benchmark form b.Cleanup)
    - cache.go (MemoryCache + NewMemoryCache signatures; verify cache is enabled via WithCache only for Countries/Languages/Subdivisions per RESIL-07)
    - countries.go (CountriesRequest field — just LanguageIsoCode)
  </read_first>
  <action>
    Create `bench_test.go` declared `package openholidays` (same-package — needed to use the library's internal types directly, matching `public_holidays_test.go` precedent).

    Top of file: English-only godoc preamble explaining: "Two benchmarks per TEST-11. BenchmarkClient_PublicHolidays measures the cold path (no cache, in-memory httptest backend) against PL 2025 public holidays; the ROADMAP SC#3 target is < 500 ms cold. BenchmarkClient_Countries measures the cached path against the Countries endpoint, which is the only endpoint family the cache covers per RESIL-07 (PublicHolidays is intentionally NOT cached). This is the CL-18 reinterpretation of SC#3's '< 5 ms cached' — the target applies to whichever endpoint the cache accelerates, which is Countries / Languages / Subdivisions."

    Imports: stdlib (`context`, `net/http`, `net/http/httptest`, `os`, `path/filepath`, `testing`, `time`) plus `github.com/stretchr/testify/require` (for fixture-load preconditions, mirrors RESEARCH §Pattern 5).

    Implement two functions:

    1. `func BenchmarkClient_PublicHolidays(b *testing.B)`:
       - Load `body, err := os.ReadFile(filepath.Join("testdata", "public_holidays_pl_2025.json")); require.NoError(b, err)`.
       - Create `srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, _ *http.Request) { w.Header().Set("Content-Type", "application/json"); _, _ = w.Write(body) }))`. `b.Cleanup(srv.Close)`.
       - Build request: `req := PublicHolidaysRequest{CountryIsoCode: "PL", ValidFrom: NewDate(2025, time.January, 1), ValidTo: NewDate(2025, time.December, 31)}`.
       - `b.Run("cold", func(b *testing.B) { ... })`:
         - `c := NewClient(WithBaseURL(srv.URL))` (no WithCache — cold path).
         - `b.Cleanup(func() { _ = c.Close() })`.
         - `ctx := context.Background()`.
         - `b.ResetTimer()`.
         - Loop: `for i := 0; i < b.N; i++ { _, err := c.PublicHolidays(ctx, req); if err != nil { b.Fatal(err) } }`.

    2. `func BenchmarkClient_Countries(b *testing.B)`:
       - Load `body, err := os.ReadFile(filepath.Join("testdata", "countries.json")); require.NoError(b, err)`.
       - httptest server pattern same as above.
       - `b.Run("cached", func(b *testing.B) { ... })`:
         - `c := NewClient(WithBaseURL(srv.URL), WithCache(time.Hour))`.
         - `b.Cleanup(func() { _ = c.Close() })`.
         - `ctx := context.Background()`.
         - Warm: `_, _ = c.Countries(ctx, CountriesRequest{})` (populates cache).
         - `b.ResetTimer()`.
         - Loop: `for i := 0; i < b.N; i++ { _, err := c.Countries(ctx, CountriesRequest{}); if err != nil { b.Fatal(err) } }`.

    DO NOT add a "cached" sub-benchmark to BenchmarkClient_PublicHolidays — RESIL-07 explicitly excludes PublicHolidays from caching, so a "cached" measurement there would be misleading. This is exactly the Pitfall 5 anti-pattern.

    Each function has a Go-style doc comment beginning with its name; em-dash style preserved.
  </action>
  <verify>
    <automated>cd /data/git/private/holidays && go test -run=^$ -bench='^BenchmarkClient_(PublicHolidays|Countries)$' -benchtime=5x -benchmem . 2>&1 | tee /tmp/bench.out && grep -E 'BenchmarkClient_(PublicHolidays|Countries)' /tmp/bench.out</automated>
  </verify>
  <acceptance_criteria>
    - File `bench_test.go` exists; first declaration `package openholidays`.
    - `grep -E '^func BenchmarkClient_PublicHolidays\(b \*testing\.B\)' bench_test.go` exits 0.
    - `grep -E '^func BenchmarkClient_Countries\(b \*testing\.B\)' bench_test.go` exits 0.
    - `grep -F 'b.Run("cold"' bench_test.go` exits 0 (cold sub-bench).
    - `grep -F 'b.Run("cached"' bench_test.go` exits 0 (cached sub-bench under Countries).
    - `grep -F 'WithCache(' bench_test.go` exits 0 — WithCache appears only in BenchmarkClient_Countries (verifiable with awk between function boundaries; minimal check: a single WithCache call total).
    - `grep -c 'WithCache(' bench_test.go` equals 1 (only Countries cached — Pitfall 5 enforcement).
    - `grep -F 'httptest.NewServer' bench_test.go` exits 0.
    - `grep -F 'b.Cleanup' bench_test.go` exits 0 (deterministic teardown).
    - `grep -F 'b.ResetTimer()' bench_test.go` exits 0.
    - `go vet ./.` exits 0.
    - `go test -run=^$ -bench='^BenchmarkClient_(PublicHolidays|Countries)$' -benchtime=5x .` exits 0 — both benchmarks complete; output lines `BenchmarkClient_PublicHolidays/cold-N` and `BenchmarkClient_Countries/cached-N` appear.
    - Note: actual ns/op verification against < 500 ms (cold) and < 5 ms (cached) targets is qualitative at this stage — CI may add a Go-side assertion in a later phase. For now we verify the benchmarks run cleanly; absolute time targets are observed manually in the bench output.
  </acceptance_criteria>
  <done>
    Both benchmarks execute; cold path measures PublicHolidays; cached path measures Countries per CL-18; only one WithCache call in the file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuzz input → JSON decoder + pickLocalized | Untrusted (Go fuzzer generates adversarial bytes). |
| httptest server → benchmark client | Trusted (local in-memory). |
| Fixture files → fuzz seed F.Add | Trusted (committed fixtures). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | DoS | fuzzer surfaces a panic in pickLocalized or decoder | medium | mitigate | Fuzz harness reports panics as failures; CI runs `-fuzztime=10s` per target (Plan 05). Any surfaced panic blocks merge until fixed. |
| T-05-09 | Tampering | adversarial fuzz seed in testdata/fuzz/ commits | low | accept | Seed files are reviewed in PRs; they trigger zero side effects beyond JSON decode of small (< 200-byte) payloads. |
| T-05-10 | Information Disclosure | benchmark exposes sensitive data | low | accept | Benchmarks use the committed PL 2025 fixture; no real PII or secrets in any path. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package installs. testify is pre-approved (go.mod v1.11.1). |
</threat_model>

<verification>
- `go test -run '^Fuzz' -count=1 .` green (seed corpus passes both fuzz targets).
- `go test -fuzz=FuzzParseLocalizedText -fuzztime=5s .` and `go test -fuzz=FuzzUnmarshalHoliday -fuzztime=5s .` exit 0.
- `go test -run=^$ -bench='^BenchmarkClient_' -benchtime=5x .` runs both benchmarks cleanly.
- Files in `testdata/fuzz/FuzzParseLocalizedText/` and `testdata/fuzz/FuzzUnmarshalHoliday/` each have the `go test fuzz v1` header.
- Only ONE `WithCache(` call in bench_test.go (Pitfall 5 enforcement: Countries cached, NOT PublicHolidays).
</verification>

<success_criteria>
1. `fuzz_test.go` exists with both FuzzParseLocalizedText and FuzzUnmarshalHoliday.
2. `bench_test.go` exists with BenchmarkClient_PublicHolidays (cold sub-bench) and BenchmarkClient_Countries (cached sub-bench).
3. testdata/fuzz/ directories populated with 3 seed files each, valid format.
4. 5-second fuzz runs surface zero panics on both targets.
5. Benchmarks run cleanly; output lines for both sub-benchmarks appear.
6. CL-18 reinterpretation respected: PublicHolidays measured cold only; Countries measured cached only.
</success_criteria>

<output>
Create `.planning/phases/05-distribution/05-03-SUMMARY.md` when done.
</output>
