---
phase: 02-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - transport.go
  - transport_header_test.go
  - transport_logging_test.go
autonomous: true
requirements: [TRANS-01, TRANS-04, OBS-01, OBS-02]

must_haves:
  truths:
    - "A unit test of headerTransport in isolation verifies Accept: application/json and User-Agent: go-openholidays/<Version> are set when caller did not supply them."
    - "A unit test of headerTransport in isolation verifies caller-supplied Accept and User-Agent values are preserved (no overwrite)."
    - "A unit test of headerTransport in isolation verifies the inbound *http.Request is not mutated by RoundTrip (caller's req.Header.Get(\"Accept\") remains empty after the call)."
    - "A unit test of loggingTransport in isolation verifies a single slog.LevelDebug record is emitted with all six OBS-02 fields (method, path, status, duration_ms, attempt, bytes_in)."
    - "A unit test of loggingTransport in isolation verifies the response body is NEVER read inside RoundTrip (the next.RoundTrip's resp.Body stays at its original position after the call)."
    - "loggingTransport forwards resp.ContentLength=-1 as bytes_in=-1 (does not coerce to 0)."
    - "go vet ./... and go test -race -run \"TestHeaderTransport|TestLoggingTransport\" ./... exit 0 with both subtests green."
  artifacts:
    - path: "transport.go"
      provides: "headerTransport struct + RoundTrip; loggingTransport struct + RoundTrip; statusOf/bytesIn nil-safe helpers"
      contains: "type headerTransport struct"
    - path: "transport_header_test.go"
      provides: "TestHeaderTransport_RoundTrip + roundTripperFunc test adapter"
      contains: "func TestHeaderTransport_RoundTrip"
    - path: "transport_logging_test.go"
      provides: "TestLoggingTransport_RoundTrip"
      contains: "func TestLoggingTransport_RoundTrip"
  key_links:
    - from: "headerTransport.RoundTrip"
      to: "http.Request.Clone(req.Context())"
      via: "deep-copy before header mutation"
      pattern: "req\\.Clone\\(req\\.Context\\(\\)\\)"
    - from: "loggingTransport.RoundTrip"
      to: "slog.Logger.LogAttrs"
      via: "single Debug record per round trip"
      pattern: "LogAttrs\\(req\\.Context\\(\\), slog\\.LevelDebug"
    - from: "transport_header_test.go"
      to: "transport_logging_test.go"
      via: "shared unexported roundTripperFunc adapter declared once in transport_header_test.go (same package)"
      pattern: "type roundTripperFunc func"
---

<objective>
Ship the two RoundTripper decorators that compose Phase 2's HTTP middleware chain: `headerTransport` (injects `Accept` and `User-Agent` defaults via `req.Clone`) and `loggingTransport` (emits a single `slog.LevelDebug` record with the six OBS-02 fields per round trip). Both transports are unit-isolated via a test-only `roundTripperFunc` adapter — no `httptest.Server` is involved in this plan; the chain integration with `*http.Client` lands in plan 02.

Purpose: TRANS-01 / TRANS-04 / OBS-01 / OBS-02 land as small, independently testable middleware. The file is structurally similar to Phase 1's `validate.go` — two small parallel structs each with focused godoc and a mechanical contract (clone-and-set / wrap-and-log).

Output:
  - `transport.go` — production code: `headerTransport`, `loggingTransport`, `statusOf`, `bytesIn`.
  - `transport_header_test.go` — `TestHeaderTransport_RoundTrip` with the documented branches.
  - `transport_logging_test.go` — `TestLoggingTransport_RoundTrip` capturing slog records to a buffer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transport/02-CONTEXT.md
@.planning/phases/02-transport/02-RESEARCH.md
@.planning/phases/02-transport/02-PATTERNS.md
@.planning/phases/02-transport/02-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@validate.go
@errors.go
@version.go
@errors_test.go
@validate_test.go

<interfaces>
<!-- Existing exported symbols Plan 01 reads but does not modify. -->
<!-- Source: errors.go (Phase 1), version.go (Phase 1). -->

From version.go:
  const Version = "0.1.0"  // used as default User-Agent suffix "go-openholidays/<Version>"

From errors.go:
  (no symbol used by transport.go; transports do not construct errors)

<!-- New exported symbols Plan 01 ships (no exported symbols — transports are unexported package internals). -->
<!-- Downstream plans (02, 03) MAY import these via the same package. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement headerTransport and loggingTransport in transport.go</name>
  <files>transport.go</files>
  <read_first>
    - validate.go (file-header godoc shape + two-parallel-structs pattern with shared rationale — copy verbatim style for transport.go)
    - errors.go (file-header godoc shape + sentinel-message convention "openholidays: ...")
    - version.go (read the Version const that defaultConfig will use; transport.go itself does NOT read Version — userAgent is injected via the struct field)
    - .planning/phases/02-transport/02-RESEARCH.md §"Pattern 3: headerTransport with req.Clone" and §"Pattern 4: loggingTransport with slog.LogAttrs" (verbatim implementation skeletons)
    - .planning/phases/02-transport/02-PATTERNS.md §"transport.go [NEW]" (file-header style + struct godoc + import grouping)
    - .planning/phases/02-transport/02-CONTEXT.md decisions D-29, D-30, D-31 (chain order, clone-before-mutate, OBS-02 field set, attempt hardcoded to 1, bytes_in semantics)
  </read_first>
  <behavior>
    - headerTransport.RoundTrip: clones req via req.Clone(req.Context()) BEFORE any header mutation; sets Accept="application/json" only when not already present; sets User-Agent=h.userAgent only when not already present; calls h.next.RoundTrip(reqCopy); returns its result verbatim.
    - loggingTransport.RoundTrip: captures start := time.Now(); calls l.next.RoundTrip(req); emits exactly one slog.LogAttrs call at slog.LevelDebug with message "openholidays http" and six attrs in order: method (string), path (string), status (int, statusOf(resp)), duration_ms (int64, time.Since(start).Milliseconds()), attempt (int, hardcoded 1), bytes_in (int64, bytesIn(resp)); returns (resp, err) verbatim.
    - statusOf(resp *http.Response) int: returns -1 if resp == nil, else resp.StatusCode.
    - bytesIn(resp *http.Response) int64: returns -1 if resp == nil, else resp.ContentLength (forwards -1 unchanged for HTTP/2 chunked responses).
    - loggingTransport.RoundTrip MUST NOT call resp.Body.Read / io.ReadAll / io.Copy on resp.Body (OBS-01 contract — Pitfall OBS-1).
  </behavior>
  <action>
    Create `transport.go` at repo root. Package declaration `package openholidays`. File-header godoc paragraph mirroring `errors.go` lines 1-7 — single `// Package openholidays — HTTP RoundTripper decorators.` line followed by a one-paragraph context block listing both transports and their contracts (D-30 clone-before-mutate, D-31 no body read, OBS-01 Pitfall OBS-1 reference). Import group: stdlib only — `log/slog`, `net/http`, `time` (alphabetical; no third-party). Declare in this order: (1) `type headerTransport struct { userAgent string; next http.RoundTripper }` with godoc `// headerTransport injects standard request headers ...` covering Accept default, User-Agent default with `go-openholidays/<Version>` reference, "caller-supplied values preserved" contract, and the req.Clone(req.Context()) implementation note per D-30 / Pitfall HTTP-2. (2) `func (h *headerTransport) RoundTrip(req *http.Request) (*http.Response, error)` — body per D-30: assign `reqCopy := req.Clone(req.Context())`, then guarded `if reqCopy.Header.Get("Accept") == ""` Set, then guarded `if reqCopy.Header.Get("User-Agent") == ""` Set with `h.userAgent`, then `return h.next.RoundTrip(reqCopy)`. (3) `type loggingTransport struct { logger *slog.Logger; next http.RoundTripper }` with godoc covering OBS-02 field list, "attempt hardcoded to 1 in Phase 2; Phase 3 retry transport will inject via ctx value", "bytes_in is resp.ContentLength which is -1 for HTTP/2 chunked responses (the live OpenHolidays API uses HTTP/2)", and "Response body is NEVER read here (OBS-01 + Pitfall OBS-1)". (4) `func (l *loggingTransport) RoundTrip(req *http.Request) (*http.Response, error)` — body per D-31: `start := time.Now()`, `resp, err := l.next.RoundTrip(req)`, single `l.logger.LogAttrs(req.Context(), slog.LevelDebug, "openholidays http", slog.String("method", req.Method), slog.String("path", req.URL.Path), slog.Int("status", statusOf(resp)), slog.Int64("duration_ms", time.Since(start).Milliseconds()), slog.Int("attempt", 1), slog.Int64("bytes_in", bytesIn(resp)))`, then `return resp, err`. (5) `func statusOf(resp *http.Response) int` — nil-safe: `if resp == nil { return -1 }; return resp.StatusCode`. (6) `func bytesIn(resp *http.Response) int64` — nil-safe: `if resp == nil { return -1 }; return resp.ContentLength`. Both helpers get godoc per `validate.go` `isTwoASCIIUppers` style (lines 98-101): focused single-purpose comment that explicitly calls out the nil-safety rationale and the "HTTP/2 chunked → -1" semantic for `bytesIn`. The slog message string `"openholidays http"` (no colon) is the only intentional deviation from the `"openholidays: "` error-string convention — log messages are not errors. Do NOT import `"log"` or `"fmt"` — `slog` is the only logger and no error wrapping happens in this file.
  </action>
  <verify>
    <automated>go vet ./... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - File `transport.go` exists at repo root with `package openholidays` declaration.
    - `grep -c '^type headerTransport struct' transport.go` returns 1.
    - `grep -c '^type loggingTransport struct' transport.go` returns 1.
    - `grep -c 'req.Clone(req.Context())' transport.go` returns 1 (D-30 mandatory clone).
    - `grep -c 'LogAttrs(req.Context(), slog.LevelDebug, "openholidays http"' transport.go` returns 1 (single Debug record per round trip).
    - `grep -c 'resp.Body' transport.go` returns 0 (OBS-01: body never read inside transports).
    - `grep -E 'slog\.(String|Int|Int64)\("(method|path|status|duration_ms|attempt|bytes_in)"' transport.go | wc -l` returns 6 (all six OBS-02 fields present).
    - `grep -c 'slog.Int("attempt", 1)' transport.go` returns 1 (D-31 hardcoded 1 in Phase 2).
    - `grep -c 'func statusOf' transport.go` returns 1; `grep -c 'func bytesIn' transport.go` returns 1.
    - `grep -E 'return -1$' transport.go | wc -l` is at least 2 (statusOf nil-safe, bytesIn nil-safe).
    - `go vet ./...` exits 0.
    - `go build ./...` exits 0 (no test code yet — production-only build green).
    - File has no `func init()` declarations (CLIENT-10 invariant maintained).
    - File has no package-level `var (...)` declarations (CLIENT-10 invariant maintained).
  </acceptance_criteria>
  <done>transport.go compiles cleanly and contains both RoundTrippers with the exact contracts above; no production code outside this file is touched by this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write transport_header_test.go with roundTripperFunc adapter and TestHeaderTransport_RoundTrip</name>
  <files>transport_header_test.go</files>
  <read_first>
    - transport.go (just written in task 1 — read the headerTransport struct and method signatures to assert against)
    - errors_test.go (test-file-header style — minimal package declaration + grouped imports; TestAPIError_Is t.Run-per-case shape with require/assert split)
    - validate_test.go (testify imports + t.Parallel etiquette + table-driven case shape)
    - .planning/phases/02-transport/02-RESEARCH.md §"Test pattern: header transport sets defaults (TRANS-01)" (verbatim skeleton)
    - .planning/phases/02-transport/02-PATTERNS.md §"transport_header_test.go [NEW]" (case-table shape + roundTripperFunc adapter rationale)
    - .planning/codebase/CONVENTIONS.md (Gold Rule 3: testify + one TestXxx per exported prod func + every case in t.Run; rule applies to unexported prod symbols in same-package tests too)
  </read_first>
  <behavior>
    - Test case "sets defaults when caller did not supply them": construct `*headerTransport` with userAgent="go-openholidays/0.1.0" (literal const matches version.Version) and `next` being a roundTripperFunc that captures the request it sees; build req via http.NewRequest GET "https://example.test/Countries"; call h.RoundTrip(req); assert captured.Header.Get("Accept") == "application/json"; assert captured.Header.Get("User-Agent") == "go-openholidays/0.1.0"; assert req.Header.Get("Accept") == "" (caller's original request was not mutated — Pitfall HTTP-2).
    - Test case "preserves caller-supplied Accept and User-Agent": same setup but pre-set req.Header.Set("Accept", "application/vnd.custom+json") and req.Header.Set("User-Agent", "my-app/2.0"); after RoundTrip, captured.Header.Get("Accept") == "application/vnd.custom+json" and captured.Header.Get("User-Agent") == "my-app/2.0".
    - Test case "next RoundTripper error is propagated unchanged": next returns (nil, errors.New("boom")); assert RoundTrip returns matching error and nil response.
    - Top-level t.Parallel(); every leaf t.Run calls t.Parallel(); use require for setup preconditions (NoError on NewRequest) and assert for verifications.
  </behavior>
  <action>
    Create `transport_header_test.go` at repo root. Package `openholidays`. Header godoc per `errors_test.go` lines 1-10 — minimal package declaration, no extra comment block above. Imports in two groups: stdlib (`errors`, `io`, `net/http`, `strings`, `testing`) alphabetical, blank line, then third-party (`github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`) alphabetical. Declare unexported test helper `type roundTripperFunc func(*http.Request) (*http.Response, error)` and `func (f roundTripperFunc) RoundTrip(r *http.Request) (*http.Response, error) { return f(r) }` — this adapter is shared with `transport_logging_test.go` (Plan 01 task 3) via same-package visibility. Add a one-paragraph godoc above `roundTripperFunc` explaining its purpose ("test-only adapter that lets a plain func satisfy http.RoundTripper; used as the `next` slot to unit-isolate a single transport in the chain"). Implement `func TestHeaderTransport_RoundTrip(t *testing.T)` with `t.Parallel()` immediately; then three subtests each via `t.Run(name, func(t *testing.T) { t.Parallel(); ... })`. Subtest 1 "sets defaults when caller did not supply them" — declare `var captured *http.Request` outside the inner func; construct `h := &headerTransport{ userAgent: "go-openholidays/" + Version, next: roundTripperFunc(func(r *http.Request) (*http.Response, error) { captured = r; return &http.Response{StatusCode: http.StatusOK, Body: io.NopCloser(strings.NewReader(""))}, nil }) }`; `req, err := http.NewRequest(http.MethodGet, "https://example.test/Countries", nil)`; `require.NoError(t, err)`; `_, err = h.RoundTrip(req)`; `require.NoError(t, err)`; `require.NotNil(t, captured, "next.RoundTrip was not invoked")`; `assert.Equal(t, "application/json", captured.Header.Get("Accept"))`; `assert.Equal(t, "go-openholidays/"+Version, captured.Header.Get("User-Agent"))`; `assert.Empty(t, req.Header.Get("Accept"), "caller's original req.Header must be untouched (Pitfall HTTP-2 / D-30)")`. Subtest 2 "preserves caller-supplied Accept and User-Agent" — same setup but pre-set both headers; assert they survive verbatim. Subtest 3 "next RoundTripper error is propagated" — next returns `(nil, errors.New("boom"))`; assert `resp == nil` and `err.Error() == "boom"`. Read the `Version` const directly (same package, no import needed). Reference: Pitfall HTTP-2 in PITFALLS.md (`req.Header` mutation race) and Pattern 3 in RESEARCH.md.
  </action>
  <verify>
    <automated>go test -race -run TestHeaderTransport_RoundTrip ./...</automated>
  </verify>
  <acceptance_criteria>
    - File `transport_header_test.go` exists at repo root with `package openholidays`.
    - `grep -c 'type roundTripperFunc func' transport_header_test.go` returns 1.
    - `grep -c 'func (f roundTripperFunc) RoundTrip' transport_header_test.go` returns 1.
    - `grep -c 'func TestHeaderTransport_RoundTrip' transport_header_test.go` returns 1 (single test function per Gold Rule 3).
    - `grep -c 't.Parallel()' transport_header_test.go` is at least 4 (top-level + 3 subtests).
    - `grep -c 't.Run(' transport_header_test.go` returns 3 (three documented branches).
    - `grep -c 'captured.Header.Get("Accept")' transport_header_test.go` is at least 1; same for `"User-Agent"`.
    - `grep -c 'req.Header.Get("Accept")' transport_header_test.go` is at least 1 (caller-isolation assertion).
    - `go test -race -run TestHeaderTransport_RoundTrip ./...` exits 0 and reports three passing subtests.
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>TestHeaderTransport_RoundTrip green under -race with three subtests covering set-when-absent, preserve-when-present, and error-propagation branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write transport_logging_test.go capturing slog records and asserting OBS-02 fields</name>
  <files>transport_logging_test.go</files>
  <read_first>
    - transport.go (loggingTransport struct + RoundTrip signature + statusOf/bytesIn helpers)
    - transport_header_test.go (the roundTripperFunc adapter declared in task 2 — same package, reuse without redeclaring)
    - errors_test.go TestAPIError_Error (lines 107-146) — string/struct format-contract verified via table; analog for "record contract" verified via JSON unmarshal into map[string]any
    - .planning/phases/02-transport/02-RESEARCH.md §"Test pattern: logging transport captures slog records (OBS-01, OBS-02)" (verbatim skeleton including the chunked ContentLength=-1 case)
    - .planning/phases/02-transport/02-PATTERNS.md §"transport_logging_test.go [NEW]" (slog.NewJSONHandler capture pattern + duration_ms float64 type-assertion via require.True)
    - .planning/phases/02-transport/02-CONTEXT.md D-31 (OBS-02 field list, attempt=1 hardcoded, bytes_in=-1 for chunked)
  </read_first>
  <behavior>
    - Test subtest "emits Debug record with all OBS-02 fields": capture slog output to bytes.Buffer via slog.NewJSONHandler at slog.LevelDebug; construct loggingTransport with that logger and a roundTripperFunc returning {StatusCode: 200, ContentLength: 6055, Body: io.NopCloser(strings.NewReader(""))}; call RoundTrip with GET https://example.test/Countries; unmarshal buf into map[string]any; assert "level" == "DEBUG", "msg" == "openholidays http", "method" == "GET", "path" == "/Countries", "status" == 200, "attempt" == 1, "bytes_in" == 6055; assert duration_ms is a non-negative float64.
    - Test subtest "forwards ContentLength=-1 (HTTP/2 chunked) as bytes_in=-1": same setup but the response has ContentLength=-1; assert rec["bytes_in"] == -1 (NOT coerced to 0).
    - Test subtest "logs network error with status=-1 and bytes_in=-1": next.RoundTrip returns (nil, errors.New("dial failed")); assert rec["status"] == -1 and rec["bytes_in"] == -1 (statusOf/bytesIn nil-safe paths).
    - Test subtest "does not read resp.Body": construct a response whose Body is a *trackedReader (counting reads); after RoundTrip, assert read count == 0 (OBS-01 / Pitfall OBS-1).
    - Top-level t.Parallel(); every leaf t.Run calls t.Parallel(); use require for setup preconditions and assert for verifications.
  </behavior>
  <action>
    Create `transport_logging_test.go` at repo root. Package `openholidays`. Imports in two groups: stdlib (`bytes`, `encoding/json`, `errors`, `io`, `log/slog`, `net/http`, `strings`, `sync/atomic`, `testing`) alphabetical, blank line, then third-party (`github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`). Do NOT redeclare `roundTripperFunc` — it lives in transport_header_test.go in the same package. Declare an unexported helper struct `type trackedReader struct { reads atomic.Int64 }` with method `func (t *trackedReader) Read(p []byte) (int, error) { t.reads.Add(1); return 0, io.EOF }` to detect any body-read attempt. Implement `func TestLoggingTransport_RoundTrip(t *testing.T)` with `t.Parallel()` immediately; then four subtests via `t.Run(name, func(t *testing.T) { t.Parallel(); ... })`. Each subtest declares its own `bytes.Buffer` and `slog.New(slog.NewJSONHandler(&buf, &slog.HandlerOptions{Level: slog.LevelDebug}))` — do NOT share the buffer across subtests (parallel tests would race). Subtest 1 "emits Debug record with all OBS-02 fields" — construct `l := &loggingTransport{logger: logger, next: roundTripperFunc(func(r *http.Request) (*http.Response, error) { return &http.Response{StatusCode: 200, ContentLength: 6055, Body: io.NopCloser(strings.NewReader(""))}, nil })}`; `req, err := http.NewRequest(http.MethodGet, "https://example.test/Countries", nil)`; `require.NoError(t, err)`; `_, err = l.RoundTrip(req)`; `require.NoError(t, err)`; declare `var rec map[string]any`; `require.NoError(t, json.Unmarshal(buf.Bytes(), &rec))`; assert each field: `assert.Equal(t, "DEBUG", rec["level"])`; `assert.Equal(t, "openholidays http", rec["msg"])`; `assert.Equal(t, "GET", rec["method"])`; `assert.Equal(t, "/Countries", rec["path"])`; `assert.EqualValues(t, 200, rec["status"])`; `assert.EqualValues(t, 1, rec["attempt"])`; `assert.EqualValues(t, 6055, rec["bytes_in"])`; `dur, ok := rec["duration_ms"].(float64); require.True(t, ok, "duration_ms missing or wrong type: %v", rec["duration_ms"]); assert.GreaterOrEqual(t, dur, 0.0)`. Subtest 2 "forwards ContentLength=-1 as bytes_in=-1 (HTTP/2 chunked)" — same shape but `ContentLength: -1`; assert `rec["bytes_in"]` equals -1 via `assert.EqualValues(t, -1, rec["bytes_in"])`. Subtest 3 "logs network error with status=-1 and bytes_in=-1" — `next: roundTripperFunc(func(r *http.Request) (*http.Response, error) { return nil, errors.New("dial failed") })`; call RoundTrip, expect error returned; unmarshal record; `assert.EqualValues(t, -1, rec["status"])`; `assert.EqualValues(t, -1, rec["bytes_in"])`. Subtest 4 "does not read resp.Body (OBS-01)" — body is `tr := &trackedReader{}; resp := &http.Response{StatusCode: 200, ContentLength: 100, Body: io.NopCloser(tr)}`; call RoundTrip; assert `tr.reads.Load() == 0` via `assert.Zero(t, tr.reads.Load(), "loggingTransport must not read resp.Body (OBS-01 / Pitfall OBS-1)")`. Note: `assert.EqualValues` is used for status/attempt/bytes_in because slog/JSON renders ints as float64; `EqualValues` does the numeric coercion without forcing the test to assert against `float64(200)` literally.
  </action>
  <verify>
    <automated>go test -race -run TestLoggingTransport_RoundTrip ./...</automated>
  </verify>
  <acceptance_criteria>
    - File `transport_logging_test.go` exists at repo root with `package openholidays`.
    - `grep -c 'func TestLoggingTransport_RoundTrip' transport_logging_test.go` returns 1.
    - `grep -c 'roundTripperFunc' transport_logging_test.go` is at least 1 (reused from transport_header_test.go without redeclaration).
    - `grep -c 'type roundTripperFunc' transport_logging_test.go` returns 0 (NOT redeclared — same package).
    - `grep -c 'slog.NewJSONHandler' transport_logging_test.go` is at least 1.
    - `grep -c 't.Run(' transport_logging_test.go` returns 4 (four documented branches).
    - All six OBS-02 fields are asserted: `grep -cE 'rec\["(method|path|status|duration_ms|attempt|bytes_in)"\]' transport_logging_test.go` returns at least 6.
    - `grep -c 'type trackedReader' transport_logging_test.go` returns 1 (no-body-read assertion helper).
    - `go test -race -run TestLoggingTransport_RoundTrip ./...` exits 0 with four passing subtests.
    - `go test -race -run "TestHeaderTransport_RoundTrip|TestLoggingTransport_RoundTrip" ./...` exits 0 with seven passing subtests total (3 from header + 4 from logging).
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>TestLoggingTransport_RoundTrip green under -race with all six OBS-02 fields asserted, ContentLength=-1 forwarded unchanged, network-error path covered, and the no-body-read invariant (OBS-01) mechanically verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RoundTripper chain (test-internal) | The unit tests substitute `roundTripperFunc` (test-only) for the production `http.DefaultTransport` next slot. No actual network crosses any boundary in this plan. |
| Logging sink (slog handler) | Test captures slog output to an in-process `bytes.Buffer`. Production behavior delegates to whatever `slog.Logger` the application supplies — out of scope for Plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `headerTransport.RoundTrip` mutating caller's `*http.Request` | mitigate | Use `req.Clone(req.Context())` BEFORE any header mutation (D-30, Pitfall HTTP-2, RESEARCH §Security Domain "req.Header race"). Asserted by `transport_header_test.go` subtest 1: `req.Header.Get("Accept")` remains empty after RoundTrip. |
| T-02-02 | Information Disclosure | `loggingTransport` reads response body and emits bytes at Debug or higher | mitigate | `loggingTransport.RoundTrip` does NOT call `resp.Body.Read`/`io.ReadAll`/`io.Copy` (D-31, OBS-01, Pitfall OBS-1, RESEARCH §Security Domain "Response body in operator logs"). Mechanically asserted by `transport_logging_test.go` subtest 4 via `trackedReader` whose `reads` counter must remain 0. |
| T-02-03 | Information Disclosure | `loggingTransport` emits log records at Info or above | mitigate | Single `LogAttrs(ctx, slog.LevelDebug, ...)` call hard-codes `slog.LevelDebug`; no other levels appear in the file. Asserted by `transport_logging_test.go` subtest 1: `rec["level"] == "DEBUG"`. |
| T-02-04 | Repudiation | Server omits `User-Agent` → CDN classifies as bot → upstream cascade | mitigate | `headerTransport` always sets `User-Agent` to a non-empty value matching `^go-openholidays/` when caller did not supply one (TRANS-01, Pitfall HTTP-5, RESEARCH §Security Domain "CDN UA rejection"). |
| T-02-05 | Denial of Service | Logging hot path allocates per-attr via variadic-key-value parsing | accept | RESEARCH §Pattern 4 calls out that `LogAttrs` (used here) avoids the variadic overhead vs. `Debug` — this is the documented optimization, not a residual threat. |
| T-02-SC | Tampering | Supply-chain via package install | not-applicable | Plan 01 adds zero new dependencies. RESEARCH.md §"Package Legitimacy Audit" records this explicitly: all imports are stdlib; testify v1.11.1 is pinned from Phase 1 and already verified. No `go get` or `go mod tidy` lockfile churn expected for this plan. |
</threat_model>

<verification>
Run all of:
- `go vet ./...` — zero warnings.
- `go build ./...` — zero errors (production build before tests are required, since transport.go is the first new production file).
- `go test -race -run "TestHeaderTransport_RoundTrip|TestLoggingTransport_RoundTrip" ./...` — both functions green; 3+4=7 subtests total.
- `go test -race ./...` — full Phase 1 + Phase 2 suite green (Phase 1's `TestNoInitOrGlobalState` must still pass; transport.go declares no `init()` and no package-level vars, so the audit passes without allowlist changes).
- `grep -n 'func init' transport.go` — empty (CLIENT-10 invariant).
- `grep -n '^var ' transport.go` — empty (CLIENT-10 invariant).
</verification>

<success_criteria>
1. `transport.go` exists at repo root with `headerTransport`, `loggingTransport`, `statusOf`, `bytesIn` declarations; no exported symbols (transports stay internal).
2. `transport_header_test.go` exists with `TestHeaderTransport_RoundTrip` and the shared `roundTripperFunc` adapter; three subtests green under `-race`.
3. `transport_logging_test.go` exists with `TestLoggingTransport_RoundTrip`; four subtests green under `-race`; all six OBS-02 fields mechanically asserted; the no-body-read invariant verified via `trackedReader`.
4. `go vet ./... && go test -race ./...` are green for the whole module.
5. `TestNoInitOrGlobalState` (Phase 1 CLIENT-10 audit) still passes — `transport.go` contains no `init()` and no package-level `var (...)` declarations.
</success_criteria>

<output>
Create `.planning/phases/02-transport/02-01-SUMMARY.md` when done summarizing: files written, line counts, contracts asserted by each subtest, and explicit confirmation that no Phase 1 file was touched and `TestNoInitOrGlobalState` still passes.
</output>
