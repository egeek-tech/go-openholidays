---
phase: 02-transport
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - errors.go
  - errors_test.go
  - internal_test.go
  - countries.go
  - countries_test.go
  - client_test.go
  - testdata/countries.json
autonomous: false
requirements: [ENDPT-01, TRANS-02, TRANS-03, CLIENT-07, CLIENT-09, TEST-04]

must_haves:
  truths:
    - "A caller writing c := NewClient(WithBaseURL(httptest.URL)); countries, err := c.Countries(context.Background()) against the httptest fixture server receives exactly 2 typed Country entries (PL first, then DE) and a nil error."
    - "An httptest server returning 404 + JSON body {\"detail\": \"Country not supported\"} produces an *APIError that errors.As populates with StatusCode=404, Path=\"/Countries\", Message=\"Country not supported\", and Body containing the upstream bytes (capped at 4 KiB)."
    - "An httptest server hanging 10s on the response: context cancellation 50ms in returns from Countries within < 200ms with errors.Is(err, context.Canceled) true (CLIENT-09)."
    - "50 parallel Countries() goroutines against a synthetic 5-20ms-delay httptest server all return identical typed payloads with no errors under -race (CLIENT-07 / TEST-04)."
    - "An httptest server streaming 11 MiB of valid JSON returns ErrResponseTooLarge via errors.Is; runtime.NumGoroutine() after a 100ms settle pause is within +2 of the baseline (no goroutine leak from drain failure; TRANS-02 + TRANS-03)."
    - "ErrResponseTooLarge is an exported sentinel; errors.Is(err, openholidays.ErrResponseTooLarge) works through fmt.Errorf wrappers."
    - "internal_test.go's allowedVars allowlist contains ErrResponseTooLarge so TestNoInitOrGlobalState (CLIENT-10) stays green after the errors.go extension."
    - "testdata/countries.json contains exactly two Country entries (PL + DE) with the multi-language Name arrays verified live on 2026-05-27."
    - "go test -race -cover ./... exits 0 with the whole Phase 2 suite green."
  artifacts:
    - path: "errors.go"
      provides: "append ErrResponseTooLarge to the existing var (...) block (D-24, CL-07)"
      contains: "ErrResponseTooLarge"
    - path: "errors_test.go"
      provides: "extend TestSentinelErrors / equivalent to cover ErrResponseTooLarge (sentinel-identity + 'openholidays: ' prefix)"
      contains: "ErrResponseTooLarge"
    - path: "internal_test.go"
      provides: "extend allowedVars map with ErrResponseTooLarge entry"
      contains: "\"ErrResponseTooLarge\":"
    - path: "countries.go"
      provides: "Client.Countries endpoint method + buildAPIError + parseAPIMessage helpers + maxResponseBytes const"
      contains: "func (c *Client) Countries"
    - path: "countries_test.go"
      provides: "TestClient_Countries (happy + 4xx + 5xx + ctx-cancel + oversize + empty-body subtests) and countriesFixtureCapturedAt const"
      contains: "func TestClient_Countries"
    - path: "client_test.go"
      provides: "APPEND TestClient_ConcurrentAccess (CLIENT-07 / TEST-04) and TestClient_ContextCancel (CLIENT-09) — both exercise the Countries endpoint end-to-end"
      contains: "func TestClient_ConcurrentAccess"
    - path: "testdata/countries.json"
      provides: "2-country PL + DE fixture captured from live https://openholidaysapi.org/Countries on 2026-05-27"
      contains: "\"isoCode\": \"PL\""
  key_links:
    - from: "Countries"
      to: "ErrResponseTooLarge sentinel"
      via: "post-Decode sentinel-byte read + fmt.Errorf %w wrap"
      pattern: "ErrResponseTooLarge"
    - from: "Countries"
      to: "buildAPIError → *APIError"
      via: "construct on resp.StatusCode >= 400"
      pattern: "buildAPIError\\(resp, \"/Countries\"\\)"
    - from: "Countries"
      to: "defer drain-then-close"
      via: "io.Copy(io.Discard, io.LimitReader(resp.Body, maxResponseBytes+1)); resp.Body.Close()"
      pattern: "io\\.Copy\\(io\\.Discard"
    - from: "internal_test.go allowedVars"
      to: "ErrResponseTooLarge"
      via: "CLIENT-10 AST audit allowlist"
      pattern: "ErrResponseTooLarge"
    - from: "countries_test.go"
      to: "testdata/countries.json"
      via: "os.ReadFile(filepath.Join(\"testdata\", \"countries.json\"))"
      pattern: "countries.json"
---

<objective>
Land the first end-to-end endpoint method (`Client.Countries`) plus the supporting `ErrResponseTooLarge` sentinel (D-24 / CL-07), the `internal_test.go` allowlist update that keeps the CLIENT-10 AST audit green, the live-captured `testdata/countries.json` fixture, and the umbrella tests `TestClient_ConcurrentAccess` (CLIENT-07 / TEST-04) and `TestClient_ContextCancel` (CLIENT-09) that prove goroutine safety and cancellation latency against an `httptest.Server`.

Purpose: ENDPT-01 + TRANS-02 + TRANS-03 + CLIENT-07 + CLIENT-09 + TEST-04 — the final Phase 2 contract that subsequent endpoint phases will mirror. The endpoint method is the canonical shape (`func (c *Client) X(ctx context.Context) ([]T, error)`) every Phase 3 endpoint will follow; the drain-then-close defer, the 10 MiB cap with sentinel-byte detection, the 4 KiB APIError body truncation, and the RFC 7807 ProblemDetails parsing all land here.

Output:
  - `errors.go` [MOD] — append `ErrResponseTooLarge` to the existing `var (...)` block.
  - `errors_test.go` [MOD] — extend sentinel coverage to include `ErrResponseTooLarge`.
  - `internal_test.go` [MOD] — add `"ErrResponseTooLarge": {},` to `allowedVars`.
  - `countries.go` [NEW] — `Client.Countries` + `buildAPIError` + `parseAPIMessage` + `const maxResponseBytes = 10 << 20`.
  - `countries_test.go` [NEW] — `TestClient_Countries` with happy/4xx/5xx/ctx-cancel/oversize/empty-body subtests + `countriesFixtureCapturedAt` const.
  - `client_test.go` [MOD] — append `TestClient_ConcurrentAccess` and `TestClient_ContextCancel`.
  - `testdata/countries.json` [NEW] — live-captured 2-country fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transport/02-CONTEXT.md
@.planning/phases/02-transport/02-RESEARCH.md
@.planning/phases/02-transport/02-PATTERNS.md
@.planning/phases/02-transport/02-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@errors.go
@errors_test.go
@internal_test.go
@client.go
@options.go
@config.go
@transport.go
@types.go
@types_test.go
@version.go
@date.go

<interfaces>
<!-- Symbols Plan 03 consumes from Plan 01 + Plan 02 (same package). -->

From client.go (Plan 02):
  type Client struct {
      http      *http.Client
      baseURL   string
      userAgent string
      logger    *slog.Logger
      timeout   time.Duration
      closed    atomic.Bool
  }
  func NewClient(opts ...Option) *Client
  func (c *Client) Close() error

From options.go (Plan 02):
  func WithBaseURL(u string) Option   // used by tests to point Client at httptest.Server URL
  func WithHTTPClient(c *http.Client) Option

From config.go (Plan 02):
  // composeHTTPClient and buildTransport already wire the chain; Countries just calls c.http.Do

From transport.go (Plan 01):
  // The chain is wired by Plan 02's NewClient; Countries does not reference the transports directly

From errors.go (Phase 1):
  var (
      ErrInvalidCountry = errors.New("openholidays: invalid country code")
      ErrInvalidLanguage = errors.New("openholidays: invalid language code")
      ErrDateRangeTooLarge = errors.New("openholidays: date range too large")
      ErrInvalidDateRange = errors.New("openholidays: invalid date range")
      ErrEmptyResponse = errors.New("openholidays: empty response body")
  )
  type APIError struct {
      StatusCode int
      Path       string
      Body       []byte    // Phase 2 caps populated length at 4 KiB (Phase 1 D-17)
      Message    string
  }
  func (e *APIError) Error() string
  func (e *APIError) Is(target error) bool

From types.go (Phase 1):
  type Country struct {
      IsoCode           string          `json:"isoCode"`
      Name              []LocalizedText `json:"name"`
      OfficialLanguages []string        `json:"officialLanguages"`
  }
  func (c *Country) NameFor(lang string) string   // used by happy-path test assertions

From version.go (Phase 1):
  const Version = "0.1.0"

<!-- New exported surface Plan 03 ships -->
  var ErrResponseTooLarge   // sixth exported sentinel
  func (c *Client) Countries(ctx context.Context) ([]Country, error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend errors.go with ErrResponseTooLarge, extend internal_test.go allowedVars, and extend errors_test.go with regression coverage</name>
  <files>errors.go, errors_test.go, internal_test.go</files>
  <read_first>
    - errors.go (current `var (...)` block lines 17-36 — copy the godoc style verbatim; the new sentinel goes inside the same block before the closing paren)
    - errors_test.go (the TestSentinelErrors / TestAPIError_Is shape — read to confirm what assertion pattern the new sentinel must satisfy; specifically the `"openholidays: "` prefix check at lines 30-55)
    - internal_test.go (current `allowedVars` map lines 50-57 — the new entry appends one row alphabetized-by-introduction-order; the audit walks every production .go file at the repo root and rejects unlisted package-level vars)
    - .planning/phases/02-transport/02-CONTEXT.md decisions D-24 (sentinel definition + wrap-with-%w semantics), D-25 (maxResponseBytes lives in countries.go, NOT errors.go), §Specifics callout (extending allowedVars is mandatory or CLIENT-10 audit fails)
    - .planning/phases/02-transport/02-PATTERNS.md §"errors.go [MOD]" (verbatim godoc shape for ErrResponseTooLarge + Pitfall 5 note about mid-truncation case) and §"internal_test.go [MOD]" (one-line append)
    - .planning/phases/02-transport/02-RESEARCH.md §"Pitfall 5: errors.Is(err, ErrResponseTooLarge) fails because Decode masks the error" (godoc must document the mid-truncation edge case)
  </read_first>
  <behavior>
    - errors.go: append a sixth `ErrResponseTooLarge = errors.New("openholidays: response too large")` inside the existing `var (...)` block, with multi-line godoc citing Pitfall 5 (mid-truncation case returns *json.SyntaxError, not this sentinel).
    - internal_test.go: add `"ErrResponseTooLarge": {},` to the `allowedVars` map. Order: append immediately after `"ErrEmptyResponse": {},` per the existing convention (exported first, alphabetized-loosely-by-introduction-order).
    - errors_test.go: extend the existing sentinel coverage (TestSentinelErrors or equivalent) so ErrResponseTooLarge passes the same checks as the other 5 sentinels: non-nil, error message starts with `"openholidays: "`, errors.Is against a wrap returns true.
    - TestNoInitOrGlobalState (the CLIENT-10 AST audit) must still pass after these edits — that is the entire point of the allowedVars update.
  </behavior>
  <action>
    Edit THREE files atomically (this is one task because the three edits must land together — if any single one is missing, either CLIENT-10 audit fails or sentinel test coverage drops).

    **File 1: `errors.go`** — Read existing file. Inside the existing `var (...)` block (lines 17-36), insert a sixth entry IMMEDIATELY BEFORE the closing `)` on line 36. Match the spacing and godoc convention of the existing five entries (one blank line between each, godoc opens with the symbol name + "is returned" + when). Exact insertion:
    ```
    	// ErrResponseTooLarge is returned when an upstream response exceeds the
    	// 10 MiB cap and the truncation is detected after JSON decode completes.
    	// A response truncated mid-JSON-value returns a decode error wrapping
    	// *json.SyntaxError instead (see RESEARCH.md Pitfall 5).
    	ErrResponseTooLarge = errors.New("openholidays: response too large")
    ```
    Insert this with a leading blank line separating it from `ErrEmptyResponse` per the existing one-blank-line-between-vars convention. No other lines in `errors.go` change.

    **File 2: `internal_test.go`** — Read existing file. Add `"ErrResponseTooLarge":  {},` (note: TWO spaces between the colon-closing-quote and `{},` to align with the existing visual column at lines 51-55) to the `allowedVars` map at line 50-57. Append it as the sixth entry IMMEDIATELY AFTER `"ErrEmptyResponse":     {},` and BEFORE `"errEmptyDate":         {},`. The unexported `errEmptyDate` stays last per the existing comment "exported first, then unexported". No other lines change.

    **File 3: `errors_test.go`** — Read existing file to discover the exact test function name(s) that cover the existing five sentinels. There are two viable patterns: either (a) a table-driven `TestSentinelErrors` with a slice of `namedSentinel{name, err}` records that is iterated, OR (b) one `TestXxx` per sentinel. Phase 1's PATTERNS.md mentions "TestSentinelErrors lines 18-55" — read to confirm the actual shape. If it is a table, append `{name: "ErrResponseTooLarge", err: ErrResponseTooLarge}` (or equivalent literal) to the slice and confirm the existing assertions (non-nil + `"openholidays: "` prefix + errors.Is through wrap) now cover six entries. If it is one TestXxx per sentinel, add a new top-level `func TestErrResponseTooLarge(t *testing.T)` that mirrors the shape of an existing one (e.g., the test for ErrInvalidCountry): `t.Parallel()`; subtest "non-nil sentinel": `assert.NotNil(t, ErrResponseTooLarge)`; subtest "prefix": `assert.True(t, strings.HasPrefix(ErrResponseTooLarge.Error(), "openholidays: "))`; subtest "errors.Is through fmt.Errorf wrap": `wrap := fmt.Errorf("test: %w", ErrResponseTooLarge); assert.True(t, errors.Is(wrap, ErrResponseTooLarge))`. Make the new test consistent in style with the existing sentinel coverage; do not introduce a different testify pattern.
  </action>
  <verify>
    <automated>go test -race -run "TestSentinel|TestErrResponseTooLarge|TestNoInitOrGlobalState" ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ErrResponseTooLarge = errors.New("openholidays: response too large")' errors.go` returns 1.
    - `grep -c 'ErrResponseTooLarge is returned' errors.go` returns 1 (godoc opens with symbol name).
    - `grep -c '"ErrResponseTooLarge":' internal_test.go` returns 1 (allowlist updated).
    - `grep -E 'ErrResponseTooLarge' errors_test.go | wc -l` is at least 1 (test coverage extended).
    - `go test -race -run TestNoInitOrGlobalState ./...` exits 0 — CLIENT-10 AST audit accepts the new sentinel because it is on the allowlist.
    - `go test -race ./...` exits 0 — full module green, the new sentinel passes whatever existing sentinel-shape assertions are in errors_test.go.
    - The total exported sentinel count is now SIX (per Phase 2 D-24 / CL-07): `grep -cE '^\s*Err[A-Z][a-zA-Z]+ = errors\.New' errors.go` returns 6.
    - `grep -c 'openholidays: response too large' errors.go` returns 1 (exact message string lock — must satisfy the "openholidays: " prefix convention).
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>ErrResponseTooLarge is exported, allowlisted, and covered by errors_test.go with the same shape as the other five sentinels; CLIENT-10 audit passes; the new sentinel is callable as `errors.Is(err, openholidays.ErrResponseTooLarge)` through fmt.Errorf wraps.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture testdata/countries.json from live upstream API</name>
  <files>testdata/countries.json</files>
  <read_first>
    - .planning/phases/02-transport/02-RESEARCH.md §"Test pattern: testdata fixture (PL + DE)" lines 1029-1061 (verbatim canonical content if live capture fails; capture-command shape; const documentation)
    - .planning/phases/02-transport/02-CONTEXT.md §Specifics callout (fixture capture date documented via `const countriesFixtureCapturedAt = "2026-05-27"` in countries_test.go)
    - .planning/codebase/CONVENTIONS.md Rule 1 testdata-fixture exception (non-English upstream strings admitted when they mirror real responses)
  </read_first>
  <action>
    This is the ONE piece of Phase 2 work that requires live network from outside `go test`. Capture the PL + DE slice of the live OpenHolidays `/Countries` response into `testdata/countries.json`.

    From the repo root, run:

    ```
    mkdir -p testdata
    curl -sSf -H "Accept: application/json" -H "User-Agent: go-openholidays-test/0.1.0" https://openholidaysapi.org/Countries | jq '[.[] | select(.isoCode == "PL" or .isoCode == "DE")]' > testdata/countries.json
    ```

    If `curl` is unavailable, substitute `wget` with `-q -O -`. If the live API is unreachable on the day this plan runs, paste the canonical content from RESEARCH.md §"Test pattern: testdata fixture" lines 1042-1061 verbatim into `testdata/countries.json` and proceed — the test asserts the SHAPE (length=2, isoCode in {PL, DE}, non-empty multi-language Name array), not the exact upstream bytes; the fixture is canonical for the test, not the authoritative live data.

    Do NOT capture a `quality` field — Phase 1 JSON-1 decision tolerates upstream drift via lenient decoding but the fixture should stay minimal. The `jq` filter above naturally omits any field not present in PL/DE entries on capture day.

    Do NOT commit any other testdata files. This is the only fixture Phase 2 ships.
  </action>
  <what-built>
    A two-entry JSON file at `testdata/countries.json` captured from the live OpenHolidays `/Countries` endpoint (or the documented canonical fallback) — Poland (`PL`) and Germany (`DE`) records with their multi-language `name` arrays and `officialLanguages` field.
  </what-built>
  <how-to-verify>
    Run all of these checks; each MUST pass:

    1. `cat testdata/countries.json | jq 'length'` prints exactly `2`.
    2. `cat testdata/countries.json | jq -r '.[] | .isoCode' | sort | tr '\n' ' '` prints `DE PL ` (set equality regardless of upstream ordering).
    3. `cat testdata/countries.json | jq '.[0].name | length' >= 1` — both entries have at least one localized Name.
    4. `jq -e '.[] | select(.isoCode == "PL") | .name | map(.language) | index("PL")' testdata/countries.json` exits 0 (PL entry has a Polish localization).
    5. `jq empty testdata/countries.json` exits 0 (valid JSON).
  </how-to-verify>
  <verify>
    <automated>jq -e 'length == 2 and (map(.isoCode) | sort) == ["DE","PL"]' testdata/countries.json</automated>
  </verify>
  <done>
    `testdata/countries.json` exists at repo root, contains exactly two Country entries with isoCode set membership `{PL, DE}`, every entry has a non-empty `name` array, and `jq empty` confirms valid JSON. The capture-date const `countriesFixtureCapturedAt` in `countries_test.go` (task 4 of this plan) MUST be set to the actual capture date — typically `"2026-05-27"` per CONTEXT.md §Specifics, but adjust if capture happens on a different day.
  </done>
  <resume-signal>Type "captured" once `testdata/countries.json` is present and the `jq -e` automated check exits 0.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement countries.go (Countries endpoint + buildAPIError + parseAPIMessage + maxResponseBytes const)</name>
  <files>countries.go</files>
  <read_first>
    - client.go (Plan 02 - read the Client struct field names: c.http, c.baseURL, c.timeout — Countries reads all three)
    - errors.go (read the updated file — ErrResponseTooLarge sentinel + APIError struct + Body 4 KiB cap convention)
    - types.go (Country struct + NameFor accessor — Countries returns []Country)
    - date.go ParseDate at lines 54-63 (analog: input → wrapped sentinel error on failure; ParseDate's `fmt.Errorf("openholidays: invalid date %q: %w", s, err)` form is the wrap-prefix-with-context pattern Countries follows)
    - .planning/phases/02-transport/02-CONTEXT.md decisions D-25 (const placement: countries.go file-top per PATTERNS.md; CONTEXT.md says transport.go but PATTERNS.md authoritatively places it in countries.go since only Countries uses it), D-41 (signature), D-42 (error handling order), D-43 (APIError construction), D-44 (bounded decode), D-45 (drain-then-close)
    - .planning/phases/02-transport/02-RESEARCH.md §"Pattern 5: Endpoint method — Countries(ctx)" lines 513-630 (verbatim implementation skeleton including const placement, sentinel-byte read, parseAPIMessage priority detail → title → error) and §"Pitfall 5" + §"Open Question 1" (recommendation: empty body = ErrEmptyResponse via io.EOF branch)
    - .planning/phases/02-transport/02-PATTERNS.md §"countries.go [NEW]" (endpoint godoc + const placement + error-wrap shape table)
    - .planning/research/PITFALLS.md §"HTTP-3: response body drain + close" (drain-before-close required for keep-alive reuse + Go 1.24 auto-drain mask)
  </read_first>
  <behavior>
    - Signature: `func (c *Client) Countries(ctx context.Context) ([]Country, error)`.
    - Error handling order (D-42): nil-ctx guard → context.WithTimeout (only if c.timeout > 0) → http.NewRequestWithContext → c.http.Do → defer drain-then-close → status-code check → JSON decode → sentinel-byte read.
    - nil context returns `errors.New("openholidays: nil context")` (not a sentinel — defensive guard).
    - Per-request timeout via `context.WithTimeout(ctx, c.timeout)` IFF `c.timeout > 0` (D-28: 0 = no SDK timeout).
    - Request URL: `c.baseURL + "/Countries"` via `http.NewRequestWithContext(ctx, http.MethodGet, ..., nil)`.
    - `c.http.Do(req)` failure: `return nil, fmt.Errorf("openholidays: GET /Countries: %w", err)` — wrap must preserve `errors.Is(err, context.Canceled)`.
    - Drain-then-close defer (D-45): `defer func() { _, _ = io.Copy(io.Discard, io.LimitReader(resp.Body, maxResponseBytes+1)); _ = resp.Body.Close() }()` placed IMMEDIATELY after `c.http.Do` returns no error. LimitReader cap on the drain is belt-and-braces against a hostile infinite stream.
    - 4xx/5xx: `return nil, buildAPIError(resp, "/Countries")`. buildAPIError reads up to 4 KiB of the upstream body via io.LimitReader, calls parseAPIMessage for best-effort Message extraction.
    - 2xx: `json.NewDecoder(io.LimitReader(resp.Body, maxResponseBytes)).Decode(&countries)`. If Decode returns io.EOF (literal empty body), wrap ErrEmptyResponse per RESEARCH OQ-1: `fmt.Errorf("openholidays: empty /Countries response: %w", ErrEmptyResponse)`. Other decode errors wrap with `fmt.Errorf("openholidays: decode /Countries: %w", err)`.
    - Post-Decode sentinel-byte read (D-24, RESEARCH §"Pitfall 5"): `var one [1]byte; if n, _ := resp.Body.Read(one[:]); n > 0 { return nil, fmt.Errorf("openholidays: response exceeded %d bytes: %w", maxResponseBytes, ErrResponseTooLarge) }`.
    - parseAPIMessage priority order: detail → title → error (D-43; RFC 7807 ProblemDetails confirmed live on 2026-05-27).
    - APIError.Body capped at 4 KiB (Phase 1 D-17): `body, _ := io.ReadAll(io.LimitReader(resp.Body, 4 << 10))`.
  </behavior>
  <action>
    Create `countries.go` at repo root. Package `openholidays`. File-header godoc: `// Package openholidays — Countries endpoint method and supporting buildAPIError / parseAPIMessage helpers.` followed by a paragraph naming the contract (ENDPT-01 + TRANS-02 + TRANS-03 + the 4 KiB APIError body cap from Phase 1 D-17). Import group: stdlib only (`context`, `encoding/json`, `errors`, `fmt`, `io`, `net/http`), alphabetical, single group.

    (1) Constant at file top (after imports), per PATTERNS.md placement decision:
    ```
    // maxResponseBytes is the hard ceiling on any decoded response body (D-25).
    // 10 MiB. Not configurable in v0.1.0 — PROJECT.md fixes the cap.
    const maxResponseBytes = 10 << 20
    ```

    (2) Per the PATTERNS.md endpoint godoc convention, declare `func (c *Client) Countries(ctx context.Context) ([]Country, error)` with multi-paragraph godoc per `types.go` Country.NameFor and PATTERNS.md §countries.go lines 550-565. Mandatory content: "Countries fetches the list of supported countries from the upstream API." paragraph 1 + multi-language Name array note + Country.NameFor reference. Paragraph 2: per-request timeout via WithTimeout, ≤ 100 ms ctx-cancellation guarantee. Paragraph 3: 4xx/5xx behavior — `*APIError` with parsed Message + 4 KiB Body cap, RFC 7807 ProblemDetails priority detail → title → error. Paragraph 4: errors.Is(err, context.Canceled) / errors.Is(err, ErrResponseTooLarge) / errors.Is(err, ErrEmptyResponse) all survive wrap.

    Method body in this exact order per D-42:
    ```
    if ctx == nil {
        return nil, errors.New("openholidays: nil context")
    }
    if c.timeout > 0 {
        var cancel context.CancelFunc
        ctx, cancel = context.WithTimeout(ctx, c.timeout)
        defer cancel()
    }
    req, err := http.NewRequestWithContext(ctx, http.MethodGet, c.baseURL+"/Countries", nil)
    if err != nil {
        return nil, fmt.Errorf("openholidays: build /Countries request: %w", err)
    }
    resp, err := c.http.Do(req)
    if err != nil {
        return nil, fmt.Errorf("openholidays: GET /Countries: %w", err)
    }
    defer func() {
        _, _ = io.Copy(io.Discard, io.LimitReader(resp.Body, maxResponseBytes+1))
        _ = resp.Body.Close()
    }()
    if resp.StatusCode >= 400 {
        return nil, buildAPIError(resp, "/Countries")
    }
    var countries []Country
    decoder := json.NewDecoder(io.LimitReader(resp.Body, maxResponseBytes))
    if err := decoder.Decode(&countries); err != nil {
        if errors.Is(err, io.EOF) {
            return nil, fmt.Errorf("openholidays: empty /Countries response: %w", ErrEmptyResponse)
        }
        return nil, fmt.Errorf("openholidays: decode /Countries: %w", err)
    }
    var one [1]byte
    if n, _ := resp.Body.Read(one[:]); n > 0 {
        return nil, fmt.Errorf("openholidays: response exceeded %d bytes: %w", maxResponseBytes, ErrResponseTooLarge)
    }
    return countries, nil
    ```

    (3) Helper `func buildAPIError(resp *http.Response, path string) *APIError` per RESEARCH §Pattern 5 lines 596-605. Godoc cites Phase 1 D-17 (4 KiB Body cap):
    ```
    const apiErrorBodyCap = 4 << 10
    body, _ := io.ReadAll(io.LimitReader(resp.Body, apiErrorBodyCap))
    msg := parseAPIMessage(body)
    return &APIError{
        StatusCode: resp.StatusCode,
        Path:       path,
        Body:       body,
        Message:    msg,
    }
    ```

    (4) Helper `func parseAPIMessage(body []byte) string` per RESEARCH §Pattern 5 lines 610-629. Godoc cites the priority order detail → title → error (RFC 7807 ProblemDetails verified live 2026-05-27) and the empty-string fallback:
    ```
    var env struct {
        Detail string `json:"detail"`
        Title  string `json:"title"`
        Error  string `json:"error"`
    }
    if err := json.Unmarshal(body, &env); err != nil {
        return ""
    }
    switch {
    case env.Detail != "":
        return env.Detail
    case env.Title != "":
        return env.Title
    case env.Error != "":
        return env.Error
    default:
        return ""
    }
    ```

    NO package-level `var (...)` declarations (CLIENT-10 invariant) — only the `const maxResponseBytes` and `const apiErrorBodyCap` (consts are not vars and are not on the CLIENT-10 watchlist).
  </action>
  <verify>
    <automated>go vet ./... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - File `countries.go` exists at repo root with `package openholidays`.
    - `grep -c '^const maxResponseBytes = 10 << 20' countries.go` returns 1 (D-25 / PATTERNS.md placement).
    - `grep -c '^func (c \*Client) Countries(ctx context.Context) (\[\]Country, error)' countries.go` returns 1 (signature locked per D-41).
    - `grep -c 'http.NewRequestWithContext' countries.go` returns 1 (not the deprecated http.NewRequest).
    - `grep -c 'io.Copy(io.Discard' countries.go` returns 1 (D-45 drain-then-close).
    - `grep -c 'io.LimitReader(resp.Body, maxResponseBytes+1)' countries.go` returns 1 (drain bound is maxResponseBytes+1 per RESEARCH § Pitfall 3).
    - `grep -c 'json.NewDecoder(io.LimitReader(resp.Body, maxResponseBytes))' countries.go` returns 1 (D-44 bounded decode).
    - `grep -c 'errors.Is(err, io.EOF)' countries.go` returns 1 (RESEARCH OQ-1: empty body → ErrEmptyResponse).
    - `grep -c 'ErrResponseTooLarge' countries.go` returns 1 (sentinel-byte truncation gate).
    - `grep -c 'buildAPIError(resp, "/Countries")' countries.go` returns 1 (D-43).
    - `grep -c '^func buildAPIError' countries.go` returns 1.
    - `grep -c '^func parseAPIMessage' countries.go` returns 1.
    - `grep -c 'env.Detail' countries.go` returns 1; `grep -c 'env.Title' countries.go` returns 1; `grep -c 'env.Error' countries.go` returns 1 (parseAPIMessage tries all three fields per D-43 + RESEARCH live verification).
    - `grep -c 'openholidays: nil context' countries.go` returns 1 (D-42 nil-ctx defensive guard).
    - All error strings in countries.go that originate in the SDK start with `"openholidays: "` (run: `grep -nE '(errors\.New|fmt\.Errorf)\(' countries.go | grep -v 'openholidays:' | wc -l` returns 0 — every locally constructed error string carries the prefix).
    - File has no `func init()` declarations and no package-level `var (...)` blocks.
    - `go vet ./...` exits 0.
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>countries.go compiles cleanly with the documented signature; buildAPIError and parseAPIMessage support the RFC 7807 ProblemDetails priority detail → title → error; the 4 KiB Body cap and 10 MiB decode cap are mechanically enforced; CLIENT-10 audit still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write countries_test.go (TestClient_Countries with 6 subtests + countriesFixtureCapturedAt const)</name>
  <files>countries_test.go</files>
  <read_first>
    - countries.go (just written - assert against the exact error-wrap shapes: "openholidays: GET /Countries", "openholidays: response exceeded ... bytes", "openholidays: empty /Countries response", buildAPIError("/Countries"))
    - testdata/countries.json (just captured in task 2 - tests will os.ReadFile this fixture and assert against it; confirm 2 entries PL+DE)
    - types_test.go TestHoliday_JSON lines 120+ (analog for large-payload table-driven happy-path with nested t.Run subtests + fixture-mirroring tests)
    - errors_test.go TestAPIError_Is lines 151-222 (struct-driven case table with t.Run-per-case + require/assert split — analog for the 4xx/5xx subtests)
    - .planning/phases/02-transport/02-RESEARCH.md §"Test pattern: oversize response with goroutine-leak audit (TRANS-02, D-49)" lines 936-982 (verbatim 11 MiB streaming server skeleton + runtime.NumGoroutine delta assertion) and §"Test pattern: testdata fixture (PL + DE)" lines 1029-1071 (countriesFixtureCapturedAt const + fixture content)
    - .planning/phases/02-transport/02-PATTERNS.md §"countries_test.go [NEW]" (subtest layout table: happy/4xx-detail/5xx-title-fallback/4xx-body-truncation/ctx-cancel/oversize/empty-body) and §"Pattern: Test cleanup with t.Cleanup" (t.Cleanup(srv.Close) — first Phase 2 introduction)
    - .planning/phases/02-transport/02-CONTEXT.md decisions D-46 (httptest), D-47/D-48 (concurrent + ctx-cancel deferred to client_test.go in task 5 of this plan), D-49 (oversize + goroutine-leak), D-50 (per-RT tests live in transport_*_test.go — those are already in Plan 01)
  </read_first>
  <behavior>
    - Single top-level `TestClient_Countries(t *testing.T)` function per Gold Rule 3 (one TestXxx per exported prod function).
    - Subtest "happy path returns PL+DE from fixture": httptest.Server returns the contents of testdata/countries.json; c := NewClient(WithBaseURL(srv.URL)); countries, err := c.Countries(context.Background()); require.NoError(t, err); require.Len(t, countries, 2); sort by IsoCode if upstream order ambiguous; assert at least one entry has IsoCode in {"PL", "DE"}; assert NameFor("en") returns "Poland" or "Germany" for those entries.
    - Subtest "4xx returns *APIError with detail Message": server returns 404 + JSON body `{"detail": "Country not supported"}`; assert errors.As(err, &apiErr); apiErr.StatusCode == 404; apiErr.Path == "/Countries"; apiErr.Message == "Country not supported".
    - Subtest "5xx with title fallback": server returns 500 + `{"title": "Internal Server Error"}`; assert apiErr.Message == "Internal Server Error" (detail empty → title wins per parseAPIMessage priority).
    - Subtest "error fallback when no detail/title": server returns 503 + `{"error": "Service Unavailable"}`; assert apiErr.Message == "Service Unavailable" (error is the third-priority field).
    - Subtest "4xx body truncated at 4 KiB": server returns 400 + an 8 KiB payload; assert `len(apiErr.Body) == 4096` (Phase 1 D-17 cap).
    - Subtest "oversize triggers ErrResponseTooLarge": NOT t.Parallel — reads runtime.NumGoroutine(); server streams 11 MiB of valid JSON Country array; assert errors.Is(err, ErrResponseTooLarge); after 100ms settle, assert runtime.NumGoroutine() delta ≤ +2 (D-49 leak guard).
    - Subtest "empty body wraps ErrEmptyResponse": server returns 200 with `""` body; assert errors.Is(err, ErrEmptyResponse) per RESEARCH OQ-1.
    - Subtest "nil context returns defensive error (not sentinel)": call c.Countries(nil); assert err != nil and err.Error() contains "openholidays: nil context"; NOT errors.Is against any sentinel (defensive guard per D-42).
    - Package-level test const: `const countriesFixtureCapturedAt = "2026-05-27"` documenting the fixture capture date.
  </behavior>
  <action>
    Create `countries_test.go` at repo root. Package `openholidays`. File-header godoc per `types_test.go` lines 1-7: `// Package openholidays — tests for the Countries endpoint method.` followed by a paragraph noting "One TestXxx per exported production function per Gold Rule 3. Every scenario lives in a t.Run subtest. Non-English strings in the fixture (e.g. "Polska", "Deutschland") mirror real upstream OpenHolidays responses and are admitted per CONVENTIONS.md Rule 1 testdata-fixture exception."

    Imports two groups: stdlib (`bytes`, `context`, `encoding/json`, `errors`, `io`, `net/http`, `net/http/httptest`, `os`, `path/filepath`, `runtime`, `sort`, `strings`, `testing`, `time`) blank line then `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`.

    Declare the package-level const at the top (after imports, before the test function):
    ```
    // countriesFixtureCapturedAt records the date testdata/countries.json was
    // captured from the live API. Re-capture when the upstream schema is
    // suspected to have drifted. The fixture is not the authoritative shape —
    // the live API is.
    const countriesFixtureCapturedAt = "2026-05-27"
    ```

    Declare ONE top-level test function `func TestClient_Countries(t *testing.T)` per Gold Rule 3. The function starts with `t.Parallel()` immediately (with one exception noted below). Implement each subtest as a separate `t.Run(name, func(t *testing.T) { ... })` block; each subtest constructs its OWN httptest.Server (parallel subtests must not share servers because each subtest's handler is unique to its scenario).

    Subtest 1 "happy path returns PL+DE from fixture": call `t.Parallel()` first. Load fixture bytes: `body, err := os.ReadFile(filepath.Join("testdata", "countries.json")); require.NoError(t, err)`. `srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) { w.Header().Set("Content-Type", "application/json"); w.Write(body) }))`. `t.Cleanup(srv.Close)`. `c := NewClient(WithBaseURL(srv.URL))`. `countries, err := c.Countries(context.Background())`. `require.NoError(t, err)`. `require.Len(t, countries, 2)`. Sort by IsoCode for deterministic assertions: `sort.Slice(countries, func(i, j int) bool { return countries[i].IsoCode < countries[j].IsoCode })`. `assert.Equal(t, "DE", countries[0].IsoCode)`. `assert.Equal(t, "PL", countries[1].IsoCode)`. `assert.Equal(t, "Polska", countries[1].NameFor("PL"))` (Country.NameFor was specified in Phase 1 D-23/CL-05; confirm exact case sensitivity by reading types.go's NameFor implementation).

    Subtest 2 "4xx returns *APIError with detail Message": `t.Parallel()`. `srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) { w.WriteHeader(http.StatusNotFound); w.Write([]byte(`{"detail": "Country not supported"}`)) }))`. `t.Cleanup(srv.Close)`. `c := NewClient(WithBaseURL(srv.URL))`. `countries, err := c.Countries(context.Background())`. `require.Error(t, err)`. `assert.Nil(t, countries)`. `var apiErr *APIError`. `require.True(t, errors.As(err, &apiErr), "expected *APIError, got %T: %v", err, err)`. `assert.Equal(t, 404, apiErr.StatusCode)`. `assert.Equal(t, "/Countries", apiErr.Path)`. `assert.Equal(t, "Country not supported", apiErr.Message)`. `assert.Contains(t, string(apiErr.Body), "Country not supported")`.

    Subtest 3 "5xx with title fallback": same shape, status 500, body `{"title": "Internal Server Error"}`, assert apiErr.Message == "Internal Server Error".

    Subtest 4 "error field fallback when detail and title absent": status 503, body `{"error": "Service Unavailable"}`, assert apiErr.Message == "Service Unavailable".

    Subtest 5 "4xx body truncated at 4 KiB (Phase 1 D-17 cap)": server returns 400 with 8 KiB body (`big := bytes.Repeat([]byte("X"), 8192); w.WriteHeader(400); w.Write(big)`). Assert `len(apiErr.Body) == 4096`.

    Subtest 6 "empty body wraps ErrEmptyResponse" (RESEARCH OQ-1): `srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) { w.Header().Set("Content-Type", "application/json"); w.WriteHeader(200) }))` — sends 200 with zero-byte body. `_, err := c.Countries(context.Background())`. `assert.True(t, errors.Is(err, ErrEmptyResponse), "expected ErrEmptyResponse, got %v", err)`.

    Subtest 7 "nil context returns defensive error": `_, err := c.Countries(nil)`. `require.Error(t, err)`. `assert.Contains(t, err.Error(), "openholidays: nil context")`. Confirm it does NOT match any sentinel: `assert.False(t, errors.Is(err, ErrEmptyResponse))`.

    Subtest 8 "oversize triggers ErrResponseTooLarge with no goroutine leak (D-49)": DO NOT call t.Parallel — this subtest reads runtime.NumGoroutine() and must not race with other goroutine churn. The streaming server skeleton is verbatim from RESEARCH lines 942-961: writes `[` then loops `{"isoCode":"PL","name":[{"language":"EN","text":"Poland"}],"officialLanguages":["PL"]}` separated by commas until written count exceeds 11 << 20, then writes `]`. `t.Cleanup(srv.Close)`. `c := NewClient(WithBaseURL(srv.URL))`. `baseGoroutines := runtime.NumGoroutine()`. `_, err := c.Countries(context.Background())`. `require.Error(t, err)`. `assert.True(t, errors.Is(err, ErrResponseTooLarge), "expected ErrResponseTooLarge, got %v", err)`. `time.Sleep(100 * time.Millisecond)` (settle). `afterGoroutines := runtime.NumGoroutine()`. `assert.LessOrEqual(t, afterGoroutines, baseGoroutines+2, "goroutine leak suspected: baseline=%d after=%d (drain failed?)", baseGoroutines, afterGoroutines)`.

    Note 1: The top-level TestClient_Countries DOES call t.Parallel() — only subtest 8 internally skips its own t.Parallel(). This is valid: t.Parallel at the top-level runs the umbrella test in parallel with OTHER top-level tests, while skipping t.Parallel inside subtest 8 keeps it serial with respect to its sibling subtests of the same TestXxx (Go runs subtests serially unless they call t.Parallel themselves; sibling parallelism is opt-in).

    Note 2: The use of `countriesFixtureCapturedAt` const must be visible to the test framework — reference it once in the happy-path subtest as `_ = countriesFixtureCapturedAt` or via a `t.Logf("fixture captured %s", countriesFixtureCapturedAt)` line so the linter does not flag it as unused.
  </action>
  <verify>
    <automated>go test -race -run TestClient_Countries ./...</automated>
  </verify>
  <acceptance_criteria>
    - File `countries_test.go` exists at repo root with `package openholidays`.
    - `grep -c '^const countriesFixtureCapturedAt = "2026-05-27"' countries_test.go` returns 1.
    - `grep -c '^func TestClient_Countries' countries_test.go` returns 1 (exactly one TestXxx per Gold Rule 3).
    - `grep -c 't.Run(' countries_test.go` returns 8 (8 documented subtests).
    - `grep -c 'httptest.NewServer' countries_test.go` is at least 7 (seven subtests use a server; the nil-context subtest does not).
    - `grep -c 't.Cleanup(srv.Close)' countries_test.go` is at least 7 (one per httptest server; first Phase 2 use of t.Cleanup per PATTERNS.md).
    - `grep -c 'errors.As(err, &apiErr)' countries_test.go` is at least 3 (4xx-detail, 5xx-title, error-fallback subtests).
    - `grep -c 'errors.Is(err, ErrResponseTooLarge)' countries_test.go` returns 1.
    - `grep -c 'errors.Is(err, ErrEmptyResponse)' countries_test.go` is at least 1.
    - `grep -c 'runtime.NumGoroutine' countries_test.go` is at least 2 (baseline + after-settle).
    - `grep -c 'time.Sleep(100 \* time.Millisecond)' countries_test.go` returns 1 (D-49 settle pause).
    - `grep -c 'len(apiErr.Body)' countries_test.go` is at least 1 (4 KiB cap assertion).
    - `go test -race -run TestClient_Countries ./...` exits 0 with all 8 subtests green.
    - `go test -race ./...` exits 0 — the full module suite stays green.
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>TestClient_Countries green under -race with eight subtests covering happy path, RFC 7807 detail/title/error priority, 4 KiB body cap, empty body → ErrEmptyResponse, nil-context defensive guard, and oversize → ErrResponseTooLarge with goroutine-leak audit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Append TestClient_ConcurrentAccess (CLIENT-07 / TEST-04) and TestClient_ContextCancel (CLIENT-09) to client_test.go</name>
  <files>client_test.go</files>
  <read_first>
    - client_test.go (read the existing file from Plan 02 - TestNewClient + TestClient_Close already there; new tests append, do not replace)
    - countries.go (the endpoint these tests exercise end-to-end - confirm the ctx-cancellation path returns errors.Is(err, context.Canceled))
    - testdata/countries.json (used by the concurrent-access test handler to serve a stable payload)
    - .planning/phases/02-transport/02-RESEARCH.md §"Test pattern: context cancellation under 100 ms (CLIENT-09)" lines 898-931 (verbatim skeleton — server hangs 10s, cancel at 50ms, assert < 200ms + errors.Is context.Canceled) and §"Test pattern: concurrent access (TEST-04)" lines 985-1024 (verbatim 50-goroutine skeleton with synthetic 5-20ms delay)
    - .planning/phases/02-transport/02-CONTEXT.md decisions D-47 (50 parallel calls, 5-20ms delay), D-48 (50ms cancel, 200ms elapsed ceiling)
    - .planning/phases/02-transport/02-PATTERNS.md §"client_test.go [NEW]" (test layout: TestNewClient + TestClient_Close + TestClient_ConcurrentAccess + TestClient_ContextCancel)
  </read_first>
  <behavior>
    - TestClient_ConcurrentAccess: 50 goroutines call c.Countries(context.Background()) in parallel; each call passes through the same Client (immutability under concurrent reads must hold). Server applies a 5-20ms synthetic delay before serving the testdata/countries.json fixture. Race detector active. All 50 calls return identical payloads, no errors.
    - TestClient_ContextCancel: server hangs 10s on the response (uses time.After + ctx.Done branch); caller cancels ctx after 50ms via time.AfterFunc; assert elapsed < 200ms (D-48: 100ms target + 100ms CI slack); assert errors.Is(err, context.Canceled) — the fmt.Errorf wrap in countries.go must preserve the sentinel.
  </behavior>
  <action>
    Read existing `client_test.go` from Plan 02. APPEND two new top-level test functions to the end of the file, preserving every existing line above. If the imports section already declares some of the packages needed, do not redeclare them; add only the missing imports (likely: `context`, `errors`, `math/rand`, `net/http/httptest`, `os`, `path/filepath`, `sync`).

    **TestClient_ConcurrentAccess** (RESEARCH lines 988-1024 verbatim with minor adaptations):
    ```
    // TestClient_ConcurrentAccess verifies CLIENT-07 + TEST-04: 50 parallel
    // Countries calls under -race must complete with identical payloads and
    // no data-race reports. Client is immutable after NewClient (only c.closed
    // is mutable, and Close is not exercised here), so concurrent reads of
    // every field are race-safe by definition.
    func TestClient_ConcurrentAccess(t *testing.T) {
        t.Parallel()

        body, err := os.ReadFile(filepath.Join("testdata", "countries.json"))
        require.NoError(t, err)

        // Synthetic delay simulates real network latency without flake risk.
        srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
            time.Sleep(time.Duration(5+rand.Intn(15)) * time.Millisecond) // 5-20ms (D-47)
            w.Header().Set("Content-Type", "application/json")
            w.Write(body)
        }))
        t.Cleanup(srv.Close)

        c := NewClient(WithBaseURL(srv.URL))
        const N = 50
        var wg sync.WaitGroup
        errs := make([]error, N)
        results := make([][]Country, N)

        for i := 0; i < N; i++ {
            wg.Add(1)
            go func(idx int) {
                defer wg.Done()
                results[idx], errs[idx] = c.Countries(context.Background())
            }(i)
        }
        wg.Wait()

        t.Run("all 50 calls succeed with identical payloads", func(t *testing.T) {
            for i := 0; i < N; i++ {
                require.NoError(t, errs[i], "call %d failed: %v", i, errs[i])
                require.NotEmpty(t, results[i], "call %d returned empty", i)
                if i > 0 {
                    assert.Equal(t, results[0], results[i], "call %d payload differs from call 0", i)
                }
            }
        })
    }
    ```

    **TestClient_ContextCancel** (RESEARCH lines 900-931 verbatim with minor adaptations):
    ```
    // TestClient_ContextCancel verifies CLIENT-09 + D-48: ctx cancellation
    // interrupts in-flight HTTP within ≤ 100ms (asserted at the 200ms ceiling
    // for 2x CI slack); errors.Is(err, context.Canceled) holds through
    // countries.go's fmt.Errorf("openholidays: GET /Countries: %w", err) wrap.
    func TestClient_ContextCancel(t *testing.T) {
        t.Parallel()

        // Server hangs forever (10s) — caller cancellation MUST interrupt.
        srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
            select {
            case <-time.After(10 * time.Second):
                w.WriteHeader(http.StatusOK)
            case <-r.Context().Done():
                // Server observes the client cancellation. No write needed.
            }
        }))
        t.Cleanup(srv.Close)

        // SDK timeout intentionally large so the ctx cancel is the cause.
        c := NewClient(WithBaseURL(srv.URL), WithTimeout(30*time.Second))

        t.Run("ctx cancel interrupts in-flight HTTP within 200ms", func(t *testing.T) {
            t.Parallel()
            ctx, cancel := context.WithCancel(context.Background())
            time.AfterFunc(50*time.Millisecond, cancel)
            start := time.Now()
            _, err := c.Countries(ctx)
            elapsed := time.Since(start)

            require.Error(t, err)
            // 100ms target + 100ms CI slack = 200ms ceiling (D-48).
            assert.Less(t, elapsed, 200*time.Millisecond,
                "ctx cancel must interrupt in-flight HTTP within 200ms; took %v", elapsed)
            assert.True(t, errors.Is(err, context.Canceled),
                "expected errors.Is(err, context.Canceled) to hold; got %v", err)
        })
    }
    ```

    Adjust the existing import block in client_test.go to include any new packages (`context`, `errors`, `math/rand`, `net/http/httptest`, `os`, `path/filepath`, `sync` in stdlib; existing testify already present). Confirm the merged import block is alphabetically sorted within each group.
  </action>
  <verify>
    <automated>go test -race -run "TestClient_ConcurrentAccess|TestClient_ContextCancel" ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^func TestClient_ConcurrentAccess' client_test.go` returns 1.
    - `grep -c '^func TestClient_ContextCancel' client_test.go` returns 1.
    - `grep -c 'sync.WaitGroup' client_test.go` is at least 1 (TestClient_ConcurrentAccess).
    - `grep -c 'time.AfterFunc(50\*time.Millisecond, cancel)' client_test.go` returns 1.
    - `grep -c 'errors.Is(err, context.Canceled)' client_test.go` returns 1.
    - `grep -c 'assert.Less(t, elapsed, 200\*time.Millisecond' client_test.go` returns 1 (D-48 ceiling).
    - `grep -c 'const N = 50' client_test.go` returns 1 (D-47 goroutine count).
    - `go test -race -run TestClient_ConcurrentAccess ./...` exits 0 (CLIENT-07 + TEST-04 green under -race).
    - `go test -race -run TestClient_ContextCancel ./...` exits 0 (CLIENT-09 green; ctx-cancel observed under 200ms).
    - `go test -race ./...` exits 0 — full module suite stays green.
    - `go vet ./...` exits 0.
    - The existing TestNewClient and TestClient_Close functions in client_test.go remain untouched (Plan 02 outputs preserved).
  </acceptance_criteria>
  <done>TestClient_ConcurrentAccess (50 parallel calls under -race, identical payloads) and TestClient_ContextCancel (50ms cancel, 200ms ceiling, errors.Is context.Canceled survives wrap) are both green; CLIENT-07, CLIENT-09, and TEST-04 are now mechanically verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream HTTP server (real or httptest) → SDK decoder | Untrusted bytes cross this boundary; SDK applies 10 MiB cap (TRANS-02), 4 KiB APIError body cap (Phase 1 D-17), and sentinel-byte truncation detection (D-24). |
| Caller `context.Context` → SDK in-flight HTTP | Cancellation must propagate through the entire RoundTripper chain and into `http.Transport` (CLIENT-09 ≤ 100 ms). |
| testdata/countries.json fixture file → test process | Live-captured upstream payload becomes test input; admitted because Phase 1 Rule 1 exception explicitly allows non-English testdata strings that mirror real upstream. |
| Multiple goroutines → same `*Client` | TestClient_ConcurrentAccess verifies the immutable Client field reads are race-safe under `-race`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Denial of Service | Hostile upstream OOMs client with huge response | mitigate | `json.NewDecoder(io.LimitReader(resp.Body, maxResponseBytes)).Decode(&v)` caps decode at 10 MiB; post-Decode sentinel-byte read surfaces truncation as `ErrResponseTooLarge` (RESEARCH §Security Domain "huge response OOM"; D-24/D-25/D-44). Asserted by `TestClient_Countries/oversize triggers ErrResponseTooLarge`. |
| T-02-12 | Denial of Service | Hostile upstream streams forever, blocking the deferred drain indefinitely | mitigate | Drain uses `io.LimitReader(resp.Body, maxResponseBytes+1)` so the drain itself is bounded (RESEARCH §Security Domain "infinite drain block"; D-45 + Pitfall HTTP-3). Asserted indirectly by the goroutine-leak audit in `TestClient_Countries/oversize` — if the drain blocked, the goroutine count would not return to baseline within 100 ms. |
| T-02-13 | Information Disclosure | Hostile upstream returns 4xx with a multi-MB error envelope; full body lands in operator logs or error messages | mitigate | `buildAPIError` reads via `io.LimitReader(resp.Body, 4 << 10)` (Phase 1 D-17). `APIError.Error()` (Phase 1 lines 76-81) intentionally omits `Body` from the string output — raw upstream bytes never leak into stringified errors. Asserted by `TestClient_Countries/4xx body truncated at 4 KiB`. |
| T-02-14 | Repudiation | Server omits `User-Agent` → upstream CDN serves 403/429 to legitimate traffic | mitigate (transitively) | Plan 01's `headerTransport` always populates `User-Agent` (TRANS-01). Plan 03 inherits this — every Countries call goes through the chain assembled in `composeHTTPClient`. Verified by `transport_header_test.go` in Plan 01. |
| T-02-15 | Denial of Service (self-inflicted) | Caller `ctx.Cancel()` is observed > 100 ms late → wasted upstream work + possible self-rate-limiting | mitigate | `context.WithTimeout` per call (D-27) + http.NewRequestWithContext propagates ctx through chain + http.Transport honors `req.Context()` cancellation. Asserted by `TestClient_ContextCancel` at the 200 ms ceiling (100 ms target + CI slack). |
| T-02-16 | Tampering | Concurrent reads of Client fields from N goroutines race when Client is mistakenly mutated | mitigate | Client struct has UNEXPORTED fields, immutable after `NewClient` (only `closed atomic.Bool` is mutable, race-safe). Asserted by `TestClient_ConcurrentAccess` (50 goroutines under -race; identical payload assertions). |
| T-02-17 | Information Disclosure | RFC 7807 error envelope contains caller-quoted PII; SDK echoes verbatim into `APIError.Message` | accept | OpenHolidays is keyless / unauthenticated; the upstream sees only the URL and standard HTTP headers (no PII traverses the SDK → upstream boundary). The 4 KiB Body cap bounds any echoed content. The risk is residual and the mitigation (re-redact arbitrary upstream text) would interfere with the legitimate Message display. Documented in Phase 1 D-17. |
| T-02-SC | Tampering | Supply-chain via package install | not-applicable | Plan 03 adds zero new dependencies. RESEARCH.md §"Package Legitimacy Audit" confirms. No `go get` invocation; the fixture file under `testdata/` is data, not code. |
</threat_model>

<verification>
Run all of:
- `go vet ./...` — zero warnings.
- `go build ./...` — zero errors.
- `go test -race -cover ./...` — entire Phase 1 + Phase 2 suite green; coverage gates are Phase 5 territory (TEST-10) but the percentage should be ≥ 85 % organically for Phase 2 code given the test density.
- `go test -race -run TestNoInitOrGlobalState ./...` — CLIENT-10 AST audit green; `ErrResponseTooLarge` is on the allowlist.
- `cat testdata/countries.json | jq 'length'` — prints `2`.
- `grep -c 'openholidays:' countries.go` — every error string in countries.go has the prefix.
- `grep -rEn 'func init\(\)' --include='*.go' .` excluding `.planning/`, `.git/`, `testdata/` — empty.
</verification>

<success_criteria>
1. `errors.go` exports six sentinels (the five Phase 1 sentinels plus `ErrResponseTooLarge`); each carries the `"openholidays: "` prefix.
2. `internal_test.go`'s `allowedVars` map contains the new `ErrResponseTooLarge` entry; `TestNoInitOrGlobalState` passes.
3. `countries.go` declares `Client.Countries(ctx)` with the locked signature; the method honors every D-42..D-45 contract (nil-ctx guard, optional context.WithTimeout, http.NewRequestWithContext, defer drain-then-close, 4xx → buildAPIError, 2xx → bounded decode + sentinel-byte truncation gate, empty body → ErrEmptyResponse).
4. `testdata/countries.json` exists with exactly 2 entries (PL + DE) captured live from `https://openholidaysapi.org/Countries` on the day the plan runs (documented via `countriesFixtureCapturedAt`).
5. `TestClient_Countries` covers happy path, RFC 7807 detail/title/error priority, 4 KiB body cap, oversize → ErrResponseTooLarge with goroutine-leak audit, empty body → ErrEmptyResponse, and nil-context defensive guard — all green under `-race`.
6. `TestClient_ConcurrentAccess` (50 goroutines, identical payloads) and `TestClient_ContextCancel` (50 ms cancel, 200 ms ceiling, errors.Is context.Canceled survives wrap) are green under `-race`.
7. `go vet ./... && go test -race ./...` are green for the whole module.
8. All 17 Phase 2 requirements that route through Plan 03 (ENDPT-01, TRANS-02, TRANS-03, CLIENT-07, CLIENT-09, TEST-04) have at least one mechanical test assertion in the test files this plan produces or modifies.
</success_criteria>

<output>
Create `.planning/phases/02-transport/02-03-SUMMARY.md` when done summarizing: files added/modified, test counts (subtests passing per test function), explicit confirmation of the goroutine-leak audit baseline + post-settle deltas observed during the oversize test run, and the date `testdata/countries.json` was captured.
</output>
