# go-openholidays code ownership.
#
# GitHub auto-requests review from the listed owner(s) for every PR that
# touches files matching the pattern. Patterns follow .gitignore syntax.
#
# The repository sits under the `egeek-tech` organization, but a bare org
# handle is rejected by GitHub's CODEOWNERS validator ("Unknown owner"). The
# entry below names the maintainer user account directly. If maintainership
# expands, create a team (e.g. `@egeek-tech/maintainers`) and replace this
# line with `* @egeek-tech/<team-slug>`.

# Catch-all — owns every path in the repo.
* @richie-tt
